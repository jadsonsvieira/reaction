=== REACTION SAAS SNAPSHOT RELEASE v0.2.0 ===
Gerado em: 17/08/2026 07:42:39
Empresa: MJSV TECNOLOGIA E SOLUCOES DIGITAIS INOVA SIMPLES (I.S.) • CNPJ: 68.614.850/0001-13


================================================================================
FILE: .env
================================================================================
DB_HOST=srv1722.hstgr.io
DB_USER=u716503964_reaction
DB_PASS=@@Miguel130510
DB_NAME=u716503964_reaction
FLASK_SECRET_KEY=chave_secreta_reacao_2026
GEMINI_API_KEY="AIzaSyAfWWIF82FJrvwOgfujvgW3jMC3hkfttqg"

# Autenticação Social OAuth (Compartilhada com o Cash)
GOOGLE_CLIENT_ID="71269651978-gp165jo1i5r6mgmb22u8s82g0jsdh5v0.apps.googleusercontent.com"
MICROSOFT_CLIENT_ID="138269ce-38e6-4c1e-bc6a-b5292e877a24"
FACEBOOK_APP_ID="2263040147842797"
================================================================================
FILE: .gitignore
================================================================================
__pycache__/
*.log
.env
venv/
.venv/
*.pyc
snapshots/
*_snapshot_*.txt
snapshot_*.txt
*.txt.bak
================================================================================
FILE: agente_reputacao.py
================================================================================
import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }
================================================================================
FILE: gerar_icones_pwa.py
================================================================================
import os
from PIL import Image

STATIC_DIR = r"C:\Users\jadso\Projetos\reaction\static"
MASTER_ICON = os.path.join(STATIC_DIR, "app_icon.png")

def generate_all_icons():
    print("Gerando todos os ícones PWA e Favicons a partir de static/app_icon.png...")
    
    if not os.path.exists(MASTER_ICON):
        print(f"Erro: {MASTER_ICON} não encontrado!")
        return

    master_img = Image.open(MASTER_ICON).convert("RGBA")

    # 512x512 PNG (PWA / Windows Desktop / Android)
    img_512 = master_img.resize((512, 512), Image.Resampling.LANCZOS)
    img_512.save(os.path.join(STATIC_DIR, "icon-512.png"), format="PNG")
    print(" -> Gerado: static/icon-512.png (512x512)")

    # 192x192 PNG (PWA / Android Launcher)
    img_192 = master_img.resize((192, 192), Image.Resampling.LANCZOS)
    img_192.save(os.path.join(STATIC_DIR, "icon-192.png"), format="PNG")
    print(" -> Gerado: static/icon-192.png (192x192)")

    # 180x180 PNG (Apple Touch Icon)
    img_180 = master_img.resize((180, 180), Image.Resampling.LANCZOS)
    img_180.save(os.path.join(STATIC_DIR, "apple-touch-icon.png"), format="PNG")
    print(" -> Gerado: static/apple-touch-icon.png (180x180)")

    # 64x64 PNG (Favicon PNG)
    img_64 = master_img.resize((64, 64), Image.Resampling.LANCZOS)
    img_64.save(os.path.join(STATIC_DIR, "favicon.png"), format="PNG")
    print(" -> Gerado: static/favicon.png (64x64)")

    # Favicon.ico multi-resolução
    ico_sizes = [(256, 256), (128, 128), (64, 64), (48, 48), (32, 32), (16, 16)]
    master_img.save(
        os.path.join(STATIC_DIR, "favicon.ico"),
        format="ICO",
        sizes=ico_sizes
    )
    print(" -> Gerado: static/favicon.ico")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA e atalhos de aplicativo foram atualizados!")

if __name__ == '__main__':
    generate_all_icons()

================================================================================
FILE: gerar_massa_dados.py
================================================================================
import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")

================================================================================
FILE: main.py
================================================================================
import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = os.environ.get("GOOGLE_CLIENT_SECRET")
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")

        # Migração das colunas de Termos, Privacidade e WhatsApp na tabela usuarios
        colunas_usuarios = {
            'telefone': 'VARCHAR(50) DEFAULT NULL',
            'termos_aceitos_em': 'DATETIME DEFAULT NULL',
            'termos_versao': 'VARCHAR(20) DEFAULT NULL',
            'termos_ip': 'VARCHAR(45) DEFAULT NULL'
        }
        for col_nome, col_def in colunas_usuarios.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM usuarios LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE usuarios ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna '{col_nome}' adicionada com sucesso na tabela usuarios.")
            except Exception as col_err:
                print(f"Erro ao adicionar coluna {col_nome} em usuarios: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas com termos e telefone"""
    usuario_id = session.get('usuario_id')
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    telefone = session.get('telefone', None)
    email = session.get('email', '')
    termos_pendentes = False
    
    if usuario_id:
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor(dictionary=True)
            try:
                cursor.execute("SELECT id, nome, email, role, foto_perfil, telefone, termos_aceitos_em FROM usuarios WHERE id = %s", (usuario_id,))
                user = cursor.fetchone()
                if user:
                    nome = user.get('nome') or nome
                    email = user.get('email') or email
                    iniciais = ''.join([part[0].upper() for part in nome.split()[:2]]) if nome else 'U'
                    foto_perfil = user.get('foto_perfil')
                    telefone = user.get('telefone')
                    termos_pendentes = (user.get('termos_aceitos_em') is None)
                    session['foto_perfil'] = foto_perfil
                    session['telefone'] = telefone
                    session['email'] = email
                    session['nome'] = nome
            except Exception as e:
                print(f"Erro ao buscar usuário logado: {e}")
            finally:
                cursor.close()
                conn.close()

    return {
        'id': usuario_id,
        'nome': nome,
        'email': email,
        'iniciais': iniciais,
        'foto_perfil': foto_perfil,
        'telefone': telefone,
        'termos_pendentes': termos_pendentes
    }

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        ip_cliente = request.headers.get('X-Forwarded-For', request.remote_addr)
        cursor.execute("""
            INSERT INTO usuarios (empresa_id, nome, email, senha, role, termos_aceitos_em, termos_versao, termos_ip) 
            VALUES (%s, %s, %s, %s, 'dono', NOW(), 'v0.2.0', %s)
        """, (empresa_id, nome_usuario, email, senha_hash, ip_cliente))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com Google (Google SSO) - Validação Criptográfica Estrita"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('id_token') or dados.get('g_token') or dados.get('access_token')
    
    # SEGURANÇA: Token é estritamente obrigatório. Nunca aceitar e-mail avulso sem token!
    if not google_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Google obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token do Google ausente. Por favor, utilize o botão oficial do Google.")

    google_email = None
    google_nome = None
    google_sub = None
    google_picture = None

    try:
        req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
        resp = requests.get(req_url, timeout=5)
        if resp.status_code == 200:
            token_data = resp.json()
            # Validar que o token pertence a uma conta com e-mail verificado pelo Google
            if str(token_data.get('email_verified', '')).lower() in ['true', '1']:
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture')
    except Exception as err_tkn:
        print(f"Erro na validação do token Google via tokeninfo API: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Google inválido, expirado ou não verificado.'}), 401
        return render_template('login.html', erro="Falha na validação do token com o Google. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com Facebook (Facebook SSO) - Validação Estrita de Graph API"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')

    # SEGURANÇA: Access token do Facebook é estritamente obrigatório!
    if not access_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Facebook obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token do Facebook ausente. Por favor, utilize o botão oficial do Facebook.")

    fb_email = None
    fb_nome = None
    fb_sub = None
    fb_picture = None

    try:
        res = requests.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
        if res.status_code == 200:
            info = res.json()
            fb_id = info.get('id')
            if info.get('email'):
                fb_email = info.get('email')
            elif fb_id:
                fb_email = f"fb_{fb_id}@facebook.user"
            fb_nome = info.get('name')
            fb_sub = fb_id
            if isinstance(info.get('picture'), dict):
                fb_picture = info.get('picture', {}).get('data', {}).get('url')
    except Exception as e:
        print(f"Erro ao verificar token Facebook com Graph API: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Facebook inválido ou expirado.'}), 401
        return render_template('login.html', erro="Falha na validação das credenciais com o Facebook.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com Microsoft (Microsoft SSO) - Validação Estrita de Graph API & JWT"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token') or dados.get('id_token')

    # SEGURANÇA: Token da Microsoft é estritamente obrigatório!
    if not access_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Microsoft obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token da Microsoft ausente. Por favor, utilize o botão oficial da Microsoft.")

    ms_email = None
    ms_nome = None
    ms_sub = None
    ms_picture = None

    # Tentar validar via Microsoft Graph API com Bearer token
    try:
        headers = {'Authorization': f'Bearer {access_token}'}
        res = requests.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
        if res.status_code == 200:
            info = res.json()
            ms_email = info.get('mail') or info.get('userPrincipalName')
            ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
            ms_sub = info.get('id')
            if ms_email:
                ms_picture = f'https://unavatar.io/{ms_email}'
    except Exception as e:
        print(f"Erro no Graph API Microsoft: {e}")

    # Se for um ID Token JWT puro do MSAL, decodificar e validar payload JWT oficial
    if not ms_email and access_token:
        try:
            parts = access_token.split('.')
            if len(parts) == 3:
                import base64, json
                padding = '=' * (4 - len(parts[1]) % 4)
                jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
                iss = jwt_payload.get('iss', '')
                if 'login.microsoftonline.com' in iss or 'sts.windows.net' in iss:
                    ms_email = jwt_payload.get('preferred_username') or jwt_payload.get('email') or jwt_payload.get('upn') or jwt_payload.get('unique_name')
                    ms_nome = jwt_payload.get('name') or jwt_payload.get('given_name') or (ms_email.split('@')[0] if ms_email else None)
                    ms_sub = jwt_payload.get('sub') or jwt_payload.get('oid')
                    if ms_email:
                        ms_picture = f'https://unavatar.io/{ms_email}'
        except Exception as jwt_err:
            print(f"Erro ao analisar ID Token Microsoft: {jwt_err}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Microsoft inválido, expirado ou não autorizado.'}), 401
        return render_template('login.html', erro="Falha na validação do token com a Microsoft.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
@app.route('/api/auth/apple', methods=['POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com Apple (Apple SSO) - Validação Estrita de ID Token"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    apple_token = dados.get('id_token') or dados.get('credential') or dados.get('token') or dados.get('access_token')

    # SEGURANÇA: Token da Apple é estritamente obrigatório! Acesso avulso por e-mail é bloqueado.
    if not apple_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Apple obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token da Apple ausente. Por favor, utilize o fluxo oficial da Apple.")

    apple_email = None
    apple_nome = None
    apple_sub = None

    try:
        parts = apple_token.split('.')
        if len(parts) == 3:
            import base64, json
            padding = '=' * (4 - len(parts[1]) % 4)
            jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
            if jwt_payload.get('iss') == 'https://appleid.apple.com':
                apple_email = jwt_payload.get('email')
                apple_sub = jwt_payload.get('sub')
                apple_nome = dados.get('name') or (apple_email.split('@')[0] if apple_email else 'Usuário Apple')
    except Exception as e:
        print(f"Erro ao validar token Apple: {e}")

    if not apple_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Apple inválido ou não verificado.'}), 401
        return render_template('login.html', erro="Falha na validação do token com a Apple.")

    apple_email = apple_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome or apple_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome or apple_email.split('@')[0], apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome or apple_email.split('@')[0], apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome or apple_email.split('@')[0],
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome or apple_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome or apple_email.split('@')[0],
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Apple SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    """Solicitação de redefinição de senha segura: gera token e envia EXCLUSIVAMENTE por e-mail"""
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            try:
                enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            except Exception as mail_err:
                print(f"Erro ao enviar email de redefinição de senha: {mail_err}")
                
        # SEGURANÇA: Mensagem genérica sem expor o token e prevenindo enumeração de usuários
        msg_sucesso = "Se o e-mail informado estiver cadastrado em nossa plataforma, as instruções e o link seguro de redefinição foram enviados para a sua caixa de entrada. Por favor, verifique o seu e-mail (inclusive a pasta de spam)."
        return render_template('login.html', sucesso=msg_sucesso)
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint restrito para gerar massa de dados demonstrativa no app"""
    admin_secret = os.environ.get("ADMIN_SECRET_KEY", "frameia_reaction_master_key_2026")
    header_secret = request.headers.get("X-Admin-Secret") or request.args.get("secret")
    
    is_admin_secret = header_secret and header_secret == admin_secret
    is_logged_user = 'usuario_id' in session and session.get('empresa_id')

    # SEGURANÇA: Bloqueia qualquer chamada anônima externa não autorizada
    if not is_admin_secret and not is_logged_user:
        return jsonify({"error": "Acesso não autorizado. Este endpoint requer autenticação ou chave mestra administrativa."}), 403

    try:
        from gerar_massa_dados import popular_banco_dados
        empresa_id = session.get('empresa_id', 1) if is_logged_user else int(request.args.get('empresa_id', 1))
        res = popular_banco_dados(empresa_id)
        return jsonify({"success": True, "message": f"Massa de dados gerada com sucesso para a empresa #{empresa_id}!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500




# ==========================================
# HELPERS E INTEGRAÇÃO WHATSAPP (META CLOUD API)
# ==========================================

def buscar_usuario_por_whatsapp(telefone, cursor):
    """Localiza o usuário e sua respectiva empresa pelo número de WhatsApp formatado"""
    if not telefone:
        return None
    # Limpa caracteres não numéricos
    tel_digits = re.sub(r'\D', '', str(telefone))
    # Testa variações (com e sem código de país 55 e nono dígito)
    variacoes = [tel_digits]
    if tel_digits.startswith('55') and len(tel_digits) > 10:
        variacoes.append(tel_digits[2:]) # sem 55
    else:
        variacoes.append('55' + tel_digits) # com 55

    for tel in variacoes:
        cursor.execute("""
            SELECT u.id as usuario_id, u.empresa_id, u.nome, u.email, u.role, u.telefone,
                   e.nome_empresa, e.plano_assinatura,
                   c.telefone_whatsapp, c.tom_voz
            FROM usuarios u
            INNER JOIN empresas e ON u.empresa_id = e.id
            LEFT JOIN configuracoes_ia c ON e.id = c.empresa_id
            WHERE u.telefone LIKE %s OR c.telefone_whatsapp LIKE %s
            LIMIT 1
        """, (f"%{tel[-8:]}%", f"%{tel[-8:]}%"))
        res = cursor.fetchone()
        if res:
            return res
    return None

def enviar_mensagem_whatsapp(telefone, texto):
    """Dispara mensagem via API oficial do WhatsApp da Meta"""
    raw_token = os.getenv('META_ACCESS_TOKEN', 'EAATkzbdHnr0BSNtiVbbWDySAcmjQX71oo6CXDu3QzbOx0rVLpmyCx4prsd3apKY6xKjh4LVdIBhJK5kOZCFSJexqCwchFwSWXB3euiU85GZCxanhH1lnxJVoVNpx72k8XGBr53ypaqDVUArZAk0MxZAThW54Q6jvyIyYSvO5Yxj88hDALOrpUbx9uCfckLZBqLgZDZD')
    token = raw_token.replace('META_ACCESS_TOKEN=', '').replace('"', '').strip() if raw_token else ''
    phone_id = os.getenv('META_PHONE_ID', '1313361515185262').replace('META_PHONE_ID=', '').replace('"', '').strip()

    if not token or not phone_id:
        print(f"[WHATSAPP ENVIO SIMULADO] Para: {telefone} | Texto: {texto[:100]}...")
        return True

    url = f"https://graph.facebook.com/v19.0/{phone_id}/messages"
    headers = {
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json"
    }
    payload = {
        "messaging_product": "whatsapp",
        "to": re.sub(r'\D', '', str(telefone)),
        "type": "text",
        "text": {"body": texto}
    }

    try:
        req = requests.post(url, headers=headers, json=payload, timeout=12)
        print(f"[WHATSAPP ENVIO] Status HTTP {req.status_code} | Resposta: {req.text[:120]}")
        return req.status_code in [200, 201]
    except Exception as e:
        print(f"[WHATSAPP ENVIO ERRO] {e}")
        return False

# ==========================================
# ROTAS PÚBLICAS: TERMOS E PRIVACIDADE (LGPD)
# ==========================================

@app.route('/termos')
def pagina_termos():
    return render_template('termos.html')

@app.route('/privacidade')
def pagina_privacidade():
    return render_template('privacidade.html')

@app.route('/api/aceitar_termos', methods=['POST'])
def api_aceitar_termos():
    if 'usuario_id' not in session:
        return jsonify({'error': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    ip_cliente = request.headers.get('X-Forwarded-For', request.remote_addr)
    
    conn = get_db_connection()
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            UPDATE usuarios 
            SET termos_aceitos_em = NOW(), termos_versao = 'v0.2.0', termos_ip = %s 
            WHERE id = %s
        """, (ip_cliente, usuario_id))
        conn.commit()
        return jsonify({'success': True, 'message': 'Termos e Política de Privacidade aceitos com sucesso!'})
    except Exception as e:
        print(f"Erro ao salvar aceite de termos: {e}")
        return jsonify({'error': 'Erro ao processar aceite.'}), 500
    finally:
        cursor.close()
        conn.close()

# ==========================================
# WEBHOOK WHATSAPP BOT (META CLOUD API)
# ==========================================

@app.route('/webhook', methods=['GET', 'POST'])
def webhook_whatsapp():
    # 1. Validação de Token da Meta (Handshake GET)
    if request.method == 'GET':
        mode = request.args.get("hub.mode")
        token = request.args.get("hub.verify_token")
        challenge = request.args.get("hub.challenge")
        
        verify_token_env = os.getenv('META_VERIFY_TOKEN', 'reaction_token_secret_022026')
        
        if mode == "subscribe" and token == verify_token_env:
            print("[WHATSAPP WEBHOOK GET] Webhook verificado com sucesso pela Meta!")
            return str(challenge), 200
        
        print("[WHATSAPP WEBHOOK GET] Falha na verificação do token Meta.")
        return "Falha na verificação. Token inválido.", 403

    # 2. Recebimento de Mensagens dos Gestores (POST)
    if request.method == 'POST':
        dados = request.get_json(silent=True) or {}
        print(f"[WHATSAPP RAW POST] {dados}")

        try:
            entries = dados.get('entry', [])
            if not entries:
                if 'value' in dados:
                    entries = [{'changes': [{'value': dados['value']}]}]
                elif 'messages' in dados:
                    entries = [{'changes': [{'value': dados}]}]

            for entry in entries:
                for change in entry.get('changes', []):
                    value = change.get('value', {})
                    for mensagem in value.get('messages', []):
                        telefone_cliente = re.sub(r'\D', '', str(mensagem.get('from', '')))
                        tipo_msg = mensagem.get('type', 'text')
                        texto_recebido = ''
                        if tipo_msg == 'text' and 'text' in mensagem:
                            texto_recebido = mensagem['text'].get('body', '').strip()
                        elif 'text' in mensagem:
                            texto_recebido = str(mensagem['text'].get('body', '')).strip()

                        if not texto_recebido:
                            continue

                        print(f"[WHATSAPP MENSAGEM] De: {telefone_cliente} | Texto: {texto_recebido}")

                        conn = get_db_connection()
                        cursor = conn.cursor(dictionary=True)

                        try:
                            usuario = buscar_usuario_por_whatsapp(telefone_cliente, cursor)

                            if not usuario:
                                msg_nao_cadastrado = (
                                    "🛡️ *Assistente ReAction [IA]*\n\n"
                                    "⚠️ *WhatsApp não vinculado ao ReAction*\n\n"
                                    "Não encontramos uma conta associada a este número de telefone.\n\n"
                                    "👉 Acesse a aba *Ajustes/Perfil* no seu painel ReAction e informe seu WhatsApp para liberar os comandos e alertas!"
                                )
                                enviar_mensagem_whatsapp(telefone_cliente, msg_nao_cadastrado)
                                continue

                            empresa_id = usuario['empresa_id']
                            nome_gestor = usuario['nome'].split()[0]
                            texto_lc = texto_recebido.lower().strip()

                            # COMANDO 1: RESUMO / REPUTAÇÃO / SAÚDE DA MARCA
                            if texto_lc in ['resumo', 'reputacao', 'reputação', 'status', 'saude', 'saúde', 'cockpit']:
                                cursor.execute("""
                                    SELECT 
                                        COUNT(*) as total_reviews,
                                        COALESCE(AVG(nota), 5.0) as media_nota,
                                        COUNT(CASE WHEN status = 'alerta_crise' OR nota <= 2 THEN 1 END) as alertas_crise,
                                        COUNT(CASE WHEN status = 'pendente' THEN 1 END) as pendentes_ia
                                    FROM avaliacoes_feed
                                    WHERE empresa_id = %s
                                """, (empresa_id,))
                                stats = cursor.fetchone() or {}

                                media = float(stats.get('media_nota', 5.0))
                                total = stats.get('total_reviews', 0)
                                crises = stats.get('alertas_crise', 0)
                                pendentes = stats.get('pendentes_ia', 0)

                                saude_pct = round((media / 5.0) * 100, 1)
                                emoji_saude = "🟢" if saude_pct >= 90 else ("🟡" if saude_pct >= 70 else "🔴")

                                resposta_resumo = (
                                    f"🛡️ *Cockpit ReAction • {usuario['nome_empresa']}*\n\n"
                                    f"Olá, *{nome_gestor}*! Aqui está o raio-x da sua reputação:\n\n"
                                    f"{emoji_saude} *Saúde da Marca:* {saude_pct}% ({media:.1f} ⭐)\n"
                                    f"📊 *Total de Avaliações:* {total}\n"
                                    f"🚨 *Alertas de Crise:* {crises}\n"
                                    f"⏳ *Pendências de IA:* {pendentes}\n\n"
                                    f"🔗 Acesse o painel: https://reaction.frameia.com.br/app"
                                )
                                enviar_mensagem_whatsapp(telefone_cliente, resposta_resumo)

                            # COMANDO 2: LISTAR AÇÕES / TAREFAS
                            elif texto_lc in ['acoes', 'ações', 'tarefas', 'pendencias', 'pendências']:
                                cursor.execute("""
                                    SELECT id, titulo, prioridade, prazo, status
                                    FROM acoes
                                    WHERE empresa_id = %s AND status = 'pendente'
                                    ORDER BY (prioridade = 'critical') DESC, prazo ASC
                                    LIMIT 5
                                """, (empresa_id,))
                                lista_acoes = cursor.fetchall()

                                if not lista_acoes:
                                    msg_acoes = f"✅ *Tudo em dia, {nome_gestor}!*\n\nNão há nenhuma ação pendente no seu Cockpit no momento."
                                else:
                                    msg_acoes = f"📋 *Ações Pendentes no Cockpit ({len(lista_acoes)}):*\n\n"
                                    for a in lista_acoes:
                                        prio_emoji = "🚨" if a['prioridade'] == 'critical' else "📌"
                                        prazo_txt = f" (Prazo: {a['prazo']})" if a.get('prazo') else ""
                                        msg_acoes += f"{prio_emoji} *#{a['id']}* - {a['titulo']}{prazo_txt}\n"
                                    msg_acoes += "\n💡 Para criar uma nova ação, envie: *criar acao [descrição]*"

                                enviar_mensagem_whatsapp(telefone_cliente, msg_acoes)

                            # COMANDO 3: CRIAR NOVA AÇÃO NO COCKPIT
                            elif texto_lc.startswith('criar acao') or texto_lc.startswith('criar ação') or texto_lc.startswith('acao ') or texto_lc.startswith('ação '):
                                titulo_acao = re.sub(r'^(?:criar\s+ac(?:a|ã)o|ac(?:a|ã)o)\s+', '', texto_recebido, flags=re.IGNORECASE).strip()
                                if titulo_acao:
                                    cursor.execute("""
                                        INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, status)
                                        VALUES (%s, %s, %s, 'normal', 'pendente')
                                    """, (empresa_id, usuario['usuario_id'], titulo_acao))
                                    conn.commit()
                                    nova_id = cursor.lastrowid

                                    msg_criada = (
                                        f"✅ *Ação Registrada com Sucesso!*\n\n"
                                        f"📌 *#{nova_id}* - {titulo_acao}\n"
                                        f"👤 Criada por: {nome_gestor}\n"
                                        f"📱 Já visível no seu Cockpit ReAction!"
                                    )
                                    enviar_mensagem_whatsapp(telefone_cliente, msg_criada)

                            # COMANDO 4: ALERTAS VERMELHOS / CRISES
                            elif texto_lc in ['alertas', 'alerta', 'crises', 'crise']:
                                cursor.execute("""
                                    SELECT id, plataforma_origem, nome_cliente, nota, comentario
                                    FROM avaliacoes_feed
                                    WHERE empresa_id = %s AND (status = 'alerta_crise' OR nota <= 2)
                                    ORDER BY id DESC
                                    LIMIT 3
                                """, (empresa_id,))
                                alertas = cursor.fetchall()

                                if not alertas:
                                    msg_alertas = f"🛡️ *Nenhum alerta de crise ativo no momento!* Sua reputação está segura, {nome_gestor}."
                                else:
                                    msg_alertas = f"🚨 *Alertas de Crise Recentes ({len(alertas)}):*\n\n"
                                    for al in alertas:
                                        msg_alertas += f"🔴 *[{al['plataforma_origem'].upper()}] {al['nome_cliente']}* ({al['nota']} ⭐)\n"
                                        msg_alertas += f"💬 \"{al['comentario'] or 'Sem comentário'}\"\n\n"
                                    msg_alertas += "👉 Acesse a Central de Reputação para responder: https://reaction.frameia.com.br/reputacao"

                                enviar_mensagem_whatsapp(telefone_cliente, msg_alertas)

                            # COMANDO PADRÃO / AJUDA
                            else:
                                msg_ajuda = (
                                    f"🤖 *Assistente ReAction [IA] • Olá, {nome_gestor}!*\n\n"
                                    f"Comandos rápidos disponíveis no seu WhatsApp:\n\n"
                                    f"🔹 *resumo* - Raio-X da reputação e saúde da marca\n"
                                    f"🔹 *acoes* - Ver ações e tarefas pendentes no Cockpit\n"
                                    f"🔹 *criar acao [texto]* - Criar nova ação diretamente no painel\n"
                                    f"🔹 *alertas* - Monitorar crises e notas baixas recentes\n"
                                    f"🔹 *ajuda* - Exibir este menu de comandos\n\n"
                                    f"🌐 Painel Web: https://reaction.frameia.com.br"
                                )
                                enviar_mensagem_whatsapp(telefone_cliente, msg_ajuda)

                        finally:
                            cursor.close()
                            conn.close()

            return jsonify({"status": "EVENT_RECEIVED"}), 200

        except Exception as err:
            print(f"[WHATSAPP WEBHOOK ERRO GERAL] {err}")
            return jsonify({"status": "ERROR", "message": str(err)}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)

================================================================================
FILE: massa_dados_producao.sql
================================================================================
-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');

================================================================================
FILE: reaction_snapshot_v0.0.4.txt
================================================================================
==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
.venv/
*.pyc
snapshots/
*_snapshot_*.txt
snapshot_*.txt
*.txt.bak


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
from PIL import Image

STATIC_DIR = r"C:\Users\jadso\Projetos\reaction\static"
MASTER_ICON = os.path.join(STATIC_DIR, "app_icon.png")

def generate_all_icons():
    print("Gerando todos os ícones PWA e Favicons a partir de static/app_icon.png...")
    
    if not os.path.exists(MASTER_ICON):
        print(f"Erro: {MASTER_ICON} não encontrado!")
        return

    master_img = Image.open(MASTER_ICON).convert("RGBA")

    # 512x512 PNG (PWA / Windows Desktop / Android)
    img_512 = master_img.resize((512, 512), Image.Resampling.LANCZOS)
    img_512.save(os.path.join(STATIC_DIR, "icon-512.png"), format="PNG")
    print(" -> Gerado: static/icon-512.png (512x512)")

    # 192x192 PNG (PWA / Android Launcher)
    img_192 = master_img.resize((192, 192), Image.Resampling.LANCZOS)
    img_192.save(os.path.join(STATIC_DIR, "icon-192.png"), format="PNG")
    print(" -> Gerado: static/icon-192.png (192x192)")

    # 180x180 PNG (Apple Touch Icon)
    img_180 = master_img.resize((180, 180), Image.Resampling.LANCZOS)
    img_180.save(os.path.join(STATIC_DIR, "apple-touch-icon.png"), format="PNG")
    print(" -> Gerado: static/apple-touch-icon.png (180x180)")

    # 64x64 PNG (Favicon PNG)
    img_64 = master_img.resize((64, 64), Image.Resampling.LANCZOS)
    img_64.save(os.path.join(STATIC_DIR, "favicon.png"), format="PNG")
    print(" -> Gerado: static/favicon.png (64x64)")

    # Favicon.ico multi-resolução
    ico_sizes = [(256, 256), (128, 128), (64, 64), (48, 48), (32, 32), (16, 16)]
    master_img.save(
        os.path.join(STATIC_DIR, "favicon.ico"),
        format="ICO",
        sizes=ico_sizes
    )
    print(" -> Gerado: static/favicon.ico")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA e atalhos de aplicativo foram atualizados!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com Google (Google SSO) - Validação Criptográfica Estrita"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('id_token') or dados.get('g_token') or dados.get('access_token')
    
    # SEGURANÇA: Token é estritamente obrigatório. Nunca aceitar e-mail avulso sem token!
    if not google_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Google obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token do Google ausente. Por favor, utilize o botão oficial do Google.")

    google_email = None
    google_nome = None
    google_sub = None
    google_picture = None

    try:
        req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
        resp = requests.get(req_url, timeout=5)
        if resp.status_code == 200:
            token_data = resp.json()
            # Validar que o token pertence a uma conta com e-mail verificado pelo Google
            if str(token_data.get('email_verified', '')).lower() in ['true', '1']:
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture')
    except Exception as err_tkn:
        print(f"Erro na validação do token Google via tokeninfo API: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Google inválido, expirado ou não verificado.'}), 401
        return render_template('login.html', erro="Falha na validação do token com o Google. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com Facebook (Facebook SSO) - Validação Estrita de Graph API"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')

    # SEGURANÇA: Access token do Facebook é estritamente obrigatório!
    if not access_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Facebook obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token do Facebook ausente. Por favor, utilize o botão oficial do Facebook.")

    fb_email = None
    fb_nome = None
    fb_sub = None
    fb_picture = None

    try:
        res = requests.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
        if res.status_code == 200:
            info = res.json()
            fb_id = info.get('id')
            if info.get('email'):
                fb_email = info.get('email')
            elif fb_id:
                fb_email = f"fb_{fb_id}@facebook.user"
            fb_nome = info.get('name')
            fb_sub = fb_id
            if isinstance(info.get('picture'), dict):
                fb_picture = info.get('picture', {}).get('data', {}).get('url')
    except Exception as e:
        print(f"Erro ao verificar token Facebook com Graph API: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Facebook inválido ou expirado.'}), 401
        return render_template('login.html', erro="Falha na validação das credenciais com o Facebook.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com Microsoft (Microsoft SSO) - Validação Estrita de Graph API & JWT"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token') or dados.get('id_token')

    # SEGURANÇA: Token da Microsoft é estritamente obrigatório!
    if not access_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Microsoft obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token da Microsoft ausente. Por favor, utilize o botão oficial da Microsoft.")

    ms_email = None
    ms_nome = None
    ms_sub = None
    ms_picture = None

    # Tentar validar via Microsoft Graph API com Bearer token
    try:
        headers = {'Authorization': f'Bearer {access_token}'}
        res = requests.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
        if res.status_code == 200:
            info = res.json()
            ms_email = info.get('mail') or info.get('userPrincipalName')
            ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
            ms_sub = info.get('id')
            if ms_email:
                ms_picture = f'https://unavatar.io/{ms_email}'
    except Exception as e:
        print(f"Erro no Graph API Microsoft: {e}")

    # Se for um ID Token JWT puro do MSAL, decodificar e validar payload JWT oficial
    if not ms_email and access_token:
        try:
            parts = access_token.split('.')
            if len(parts) == 3:
                import base64, json
                padding = '=' * (4 - len(parts[1]) % 4)
                jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
                iss = jwt_payload.get('iss', '')
                if 'login.microsoftonline.com' in iss or 'sts.windows.net' in iss:
                    ms_email = jwt_payload.get('preferred_username') or jwt_payload.get('email') or jwt_payload.get('upn') or jwt_payload.get('unique_name')
                    ms_nome = jwt_payload.get('name') or jwt_payload.get('given_name') or (ms_email.split('@')[0] if ms_email else None)
                    ms_sub = jwt_payload.get('sub') or jwt_payload.get('oid')
                    if ms_email:
                        ms_picture = f'https://unavatar.io/{ms_email}'
        except Exception as jwt_err:
            print(f"Erro ao analisar ID Token Microsoft: {jwt_err}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Microsoft inválido, expirado ou não autorizado.'}), 401
        return render_template('login.html', erro="Falha na validação do token com a Microsoft.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
@app.route('/api/auth/apple', methods=['POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com Apple (Apple SSO) - Validação Estrita de ID Token"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    apple_token = dados.get('id_token') or dados.get('credential') or dados.get('token') or dados.get('access_token')

    # SEGURANÇA: Token da Apple é estritamente obrigatório! Acesso avulso por e-mail é bloqueado.
    if not apple_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Apple obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token da Apple ausente. Por favor, utilize o fluxo oficial da Apple.")

    apple_email = None
    apple_nome = None
    apple_sub = None

    try:
        parts = apple_token.split('.')
        if len(parts) == 3:
            import base64, json
            padding = '=' * (4 - len(parts[1]) % 4)
            jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
            if jwt_payload.get('iss') == 'https://appleid.apple.com':
                apple_email = jwt_payload.get('email')
                apple_sub = jwt_payload.get('sub')
                apple_nome = dados.get('name') or (apple_email.split('@')[0] if apple_email else 'Usuário Apple')
    except Exception as e:
        print(f"Erro ao validar token Apple: {e}")

    if not apple_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Apple inválido ou não verificado.'}), 401
        return render_template('login.html', erro="Falha na validação do token com a Apple.")

    apple_email = apple_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome or apple_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome or apple_email.split('@')[0], apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome or apple_email.split('@')[0], apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome or apple_email.split('@')[0],
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome or apple_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome or apple_email.split('@')[0],
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Apple SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    """Solicitação de redefinição de senha segura: gera token e envia EXCLUSIVAMENTE por e-mail"""
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            try:
                enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            except Exception as mail_err:
                print(f"Erro ao enviar email de redefinição de senha: {mail_err}")
                
        # SEGURANÇA: Mensagem genérica sem expor o token e prevenindo enumeração de usuários
        msg_sucesso = "Se o e-mail informado estiver cadastrado em nossa plataforma, as instruções e o link seguro de redefinição foram enviados para a sua caixa de entrada. Por favor, verifique o seu e-mail (inclusive a pasta de spam)."
        return render_template('login.html', sucesso=msg_sucesso)
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint restrito para gerar massa de dados demonstrativa no app"""
    admin_secret = os.environ.get("ADMIN_SECRET_KEY", "frameia_reaction_master_key_2026")
    header_secret = request.headers.get("X-Admin-Secret") or request.args.get("secret")
    
    is_admin_secret = header_secret and header_secret == admin_secret
    is_logged_user = 'usuario_id' in session and session.get('empresa_id')

    # SEGURANÇA: Bloqueia qualquer chamada anônima externa não autorizada
    if not is_admin_secret and not is_logged_user:
        return jsonify({"error": "Acesso não autorizado. Este endpoint requer autenticação ou chave mestra administrativa."}), 403

    try:
        from gerar_massa_dados import popular_banco_dados
        empresa_id = session.get('empresa_id', 1) if is_logged_user else int(request.args.get('empresa_id', 1))
        res = popular_banco_dados(empresa_id)
        return jsonify({"success": True, "message": f"Massa de dados gerada com sucesso para a empresa #{empresa_id}!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
.venv/
*.pyc
snapshots/
*_snapshot_*.txt
snapshot_*.txt
*.txt.bak


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
from PIL import Image

STATIC_DIR = r"C:\Users\jadso\Projetos\reaction\static"
MASTER_ICON = os.path.join(STATIC_DIR, "app_icon.png")

def generate_all_icons():
    print("Gerando todos os ícones PWA e Favicons a partir de static/app_icon.png...")
    
    if not os.path.exists(MASTER_ICON):
        print(f"Erro: {MASTER_ICON} não encontrado!")
        return

    master_img = Image.open(MASTER_ICON).convert("RGBA")

    # 512x512 PNG (PWA / Windows Desktop / Android)
    img_512 = master_img.resize((512, 512), Image.Resampling.LANCZOS)
    img_512.save(os.path.join(STATIC_DIR, "icon-512.png"), format="PNG")
    print(" -> Gerado: static/icon-512.png (512x512)")

    # 192x192 PNG (PWA / Android Launcher)
    img_192 = master_img.resize((192, 192), Image.Resampling.LANCZOS)
    img_192.save(os.path.join(STATIC_DIR, "icon-192.png"), format="PNG")
    print(" -> Gerado: static/icon-192.png (192x192)")

    # 180x180 PNG (Apple Touch Icon)
    img_180 = master_img.resize((180, 180), Image.Resampling.LANCZOS)
    img_180.save(os.path.join(STATIC_DIR, "apple-touch-icon.png"), format="PNG")
    print(" -> Gerado: static/apple-touch-icon.png (180x180)")

    # 64x64 PNG (Favicon PNG)
    img_64 = master_img.resize((64, 64), Image.Resampling.LANCZOS)
    img_64.save(os.path.join(STATIC_DIR, "favicon.png"), format="PNG")
    print(" -> Gerado: static/favicon.png (64x64)")

    # Favicon.ico multi-resolução
    ico_sizes = [(256, 256), (128, 128), (64, 64), (48, 48), (32, 32), (16, 16)]
    master_img.save(
        os.path.join(STATIC_DIR, "favicon.ico"),
        format="ICO",
        sizes=ico_sizes
    )
    print(" -> Gerado: static/favicon.ico")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA e atalhos de aplicativo foram atualizados!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com Google (Google SSO) - Validação Criptográfica Estrita"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('id_token') or dados.get('g_token') or dados.get('access_token')
    
    # SEGURANÇA: Token é estritamente obrigatório. Nunca aceitar e-mail avulso sem token!
    if not google_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Google obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token do Google ausente. Por favor, utilize o botão oficial do Google.")

    google_email = None
    google_nome = None
    google_sub = None
    google_picture = None

    try:
        req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
        resp = requests.get(req_url, timeout=5)
        if resp.status_code == 200:
            token_data = resp.json()
            # Validar que o token pertence a uma conta com e-mail verificado pelo Google
            if str(token_data.get('email_verified', '')).lower() in ['true', '1']:
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture')
    except Exception as err_tkn:
        print(f"Erro na validação do token Google via tokeninfo API: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Google inválido, expirado ou não verificado.'}), 401
        return render_template('login.html', erro="Falha na validação do token com o Google. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com Facebook (Facebook SSO) - Validação Estrita de Graph API"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')

    # SEGURANÇA: Access token do Facebook é estritamente obrigatório!
    if not access_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Facebook obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token do Facebook ausente. Por favor, utilize o botão oficial do Facebook.")

    fb_email = None
    fb_nome = None
    fb_sub = None
    fb_picture = None

    try:
        res = requests.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
        if res.status_code == 200:
            info = res.json()
            fb_id = info.get('id')
            if info.get('email'):
                fb_email = info.get('email')
            elif fb_id:
                fb_email = f"fb_{fb_id}@facebook.user"
            fb_nome = info.get('name')
            fb_sub = fb_id
            if isinstance(info.get('picture'), dict):
                fb_picture = info.get('picture', {}).get('data', {}).get('url')
    except Exception as e:
        print(f"Erro ao verificar token Facebook com Graph API: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Facebook inválido ou expirado.'}), 401
        return render_template('login.html', erro="Falha na validação das credenciais com o Facebook.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com Microsoft (Microsoft SSO) - Validação Estrita de Graph API & JWT"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token') or dados.get('id_token')

    # SEGURANÇA: Token da Microsoft é estritamente obrigatório!
    if not access_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Microsoft obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token da Microsoft ausente. Por favor, utilize o botão oficial da Microsoft.")

    ms_email = None
    ms_nome = None
    ms_sub = None
    ms_picture = None

    # Tentar validar via Microsoft Graph API com Bearer token
    try:
        headers = {'Authorization': f'Bearer {access_token}'}
        res = requests.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
        if res.status_code == 200:
            info = res.json()
            ms_email = info.get('mail') or info.get('userPrincipalName')
            ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
            ms_sub = info.get('id')
            if ms_email:
                ms_picture = f'https://unavatar.io/{ms_email}'
    except Exception as e:
        print(f"Erro no Graph API Microsoft: {e}")

    # Se for um ID Token JWT puro do MSAL, decodificar e validar payload JWT oficial
    if not ms_email and access_token:
        try:
            parts = access_token.split('.')
            if len(parts) == 3:
                import base64, json
                padding = '=' * (4 - len(parts[1]) % 4)
                jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
                iss = jwt_payload.get('iss', '')
                if 'login.microsoftonline.com' in iss or 'sts.windows.net' in iss:
                    ms_email = jwt_payload.get('preferred_username') or jwt_payload.get('email') or jwt_payload.get('upn') or jwt_payload.get('unique_name')
                    ms_nome = jwt_payload.get('name') or jwt_payload.get('given_name') or (ms_email.split('@')[0] if ms_email else None)
                    ms_sub = jwt_payload.get('sub') or jwt_payload.get('oid')
                    if ms_email:
                        ms_picture = f'https://unavatar.io/{ms_email}'
        except Exception as jwt_err:
            print(f"Erro ao analisar ID Token Microsoft: {jwt_err}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Microsoft inválido, expirado ou não autorizado.'}), 401
        return render_template('login.html', erro="Falha na validação do token com a Microsoft.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
@app.route('/api/auth/apple', methods=['POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com Apple (Apple SSO) - Validação Estrita de ID Token"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    apple_token = dados.get('id_token') or dados.get('credential') or dados.get('token') or dados.get('access_token')

    # SEGURANÇA: Token da Apple é estritamente obrigatório! Acesso avulso por e-mail é bloqueado.
    if not apple_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Apple obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token da Apple ausente. Por favor, utilize o fluxo oficial da Apple.")

    apple_email = None
    apple_nome = None
    apple_sub = None

    try:
        parts = apple_token.split('.')
        if len(parts) == 3:
            import base64, json
            padding = '=' * (4 - len(parts[1]) % 4)
            jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
            if jwt_payload.get('iss') == 'https://appleid.apple.com':
                apple_email = jwt_payload.get('email')
                apple_sub = jwt_payload.get('sub')
                apple_nome = dados.get('name') or (apple_email.split('@')[0] if apple_email else 'Usuário Apple')
    except Exception as e:
        print(f"Erro ao validar token Apple: {e}")

    if not apple_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Apple inválido ou não verificado.'}), 401
        return render_template('login.html', erro="Falha na validação do token com a Apple.")

    apple_email = apple_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome or apple_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome or apple_email.split('@')[0], apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome or apple_email.split('@')[0], apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome or apple_email.split('@')[0],
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome or apple_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome or apple_email.split('@')[0],
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Apple SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    """Solicitação de redefinição de senha segura: gera token e envia EXCLUSIVAMENTE por e-mail"""
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            try:
                enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            except Exception as mail_err:
                print(f"Erro ao enviar email de redefinição de senha: {mail_err}")
                
        # SEGURANÇA: Mensagem genérica sem expor o token e prevenindo enumeração de usuários
        msg_sucesso = "Se o e-mail informado estiver cadastrado em nossa plataforma, as instruções e o link seguro de redefinição foram enviados para a sua caixa de entrada. Por favor, verifique o seu e-mail (inclusive a pasta de spam)."
        return render_template('login.html', sucesso=msg_sucesso)
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint restrito para gerar massa de dados demonstrativa no app"""
    admin_secret = os.environ.get("ADMIN_SECRET_KEY", "frameia_reaction_master_key_2026")
    header_secret = request.headers.get("X-Admin-Secret") or request.args.get("secret")
    
    is_admin_secret = header_secret and header_secret == admin_secret
    is_logged_user = 'usuario_id' in session and session.get('empresa_id')

    # SEGURANÇA: Bloqueia qualquer chamada anônima externa não autorizada
    if not is_admin_secret and not is_logged_user:
        return jsonify({"error": "Acesso não autorizado. Este endpoint requer autenticação ou chave mestra administrativa."}), 403

    try:
        from gerar_massa_dados import popular_banco_dados
        empresa_id = session.get('empresa_id', 1) if is_logged_user else int(request.args.get('empresa_id', 1))
        res = popular_banco_dados(empresa_id)
        return jsonify({"success": True, "message": f"Massa de dados gerada com sucesso para a empresa #{empresa_id}!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
.venv/
*.pyc
snapshots/
*_snapshot_*.txt
snapshot_*.txt
*.txt.bak


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
from PIL import Image

STATIC_DIR = r"C:\Users\jadso\Projetos\reaction\static"
MASTER_ICON = os.path.join(STATIC_DIR, "app_icon.png")

def generate_all_icons():
    print("Gerando todos os ícones PWA e Favicons a partir de static/app_icon.png...")
    
    if not os.path.exists(MASTER_ICON):
        print(f"Erro: {MASTER_ICON} não encontrado!")
        return

    master_img = Image.open(MASTER_ICON).convert("RGBA")

    # 512x512 PNG (PWA / Windows Desktop / Android)
    img_512 = master_img.resize((512, 512), Image.Resampling.LANCZOS)
    img_512.save(os.path.join(STATIC_DIR, "icon-512.png"), format="PNG")
    print(" -> Gerado: static/icon-512.png (512x512)")

    # 192x192 PNG (PWA / Android Launcher)
    img_192 = master_img.resize((192, 192), Image.Resampling.LANCZOS)
    img_192.save(os.path.join(STATIC_DIR, "icon-192.png"), format="PNG")
    print(" -> Gerado: static/icon-192.png (192x192)")

    # 180x180 PNG (Apple Touch Icon)
    img_180 = master_img.resize((180, 180), Image.Resampling.LANCZOS)
    img_180.save(os.path.join(STATIC_DIR, "apple-touch-icon.png"), format="PNG")
    print(" -> Gerado: static/apple-touch-icon.png (180x180)")

    # 64x64 PNG (Favicon PNG)
    img_64 = master_img.resize((64, 64), Image.Resampling.LANCZOS)
    img_64.save(os.path.join(STATIC_DIR, "favicon.png"), format="PNG")
    print(" -> Gerado: static/favicon.png (64x64)")

    # Favicon.ico multi-resolução
    ico_sizes = [(256, 256), (128, 128), (64, 64), (48, 48), (32, 32), (16, 16)]
    master_img.save(
        os.path.join(STATIC_DIR, "favicon.ico"),
        format="ICO",
        sizes=ico_sizes
    )
    print(" -> Gerado: static/favicon.ico")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA e atalhos de aplicativo foram atualizados!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com Google (Google SSO) - Validação Criptográfica Estrita"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('id_token') or dados.get('g_token') or dados.get('access_token')
    
    # SEGURANÇA: Token é estritamente obrigatório. Nunca aceitar e-mail avulso sem token!
    if not google_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Google obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token do Google ausente. Por favor, utilize o botão oficial do Google.")

    google_email = None
    google_nome = None
    google_sub = None
    google_picture = None

    try:
        req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
        resp = requests.get(req_url, timeout=5)
        if resp.status_code == 200:
            token_data = resp.json()
            # Validar que o token pertence a uma conta com e-mail verificado pelo Google
            if str(token_data.get('email_verified', '')).lower() in ['true', '1']:
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture')
    except Exception as err_tkn:
        print(f"Erro na validação do token Google via tokeninfo API: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Google inválido, expirado ou não verificado.'}), 401
        return render_template('login.html', erro="Falha na validação do token com o Google. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com Facebook (Facebook SSO) - Validação Estrita de Graph API"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')

    # SEGURANÇA: Access token do Facebook é estritamente obrigatório!
    if not access_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Facebook obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token do Facebook ausente. Por favor, utilize o botão oficial do Facebook.")

    fb_email = None
    fb_nome = None
    fb_sub = None
    fb_picture = None

    try:
        res = requests.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
        if res.status_code == 200:
            info = res.json()
            fb_id = info.get('id')
            if info.get('email'):
                fb_email = info.get('email')
            elif fb_id:
                fb_email = f"fb_{fb_id}@facebook.user"
            fb_nome = info.get('name')
            fb_sub = fb_id
            if isinstance(info.get('picture'), dict):
                fb_picture = info.get('picture', {}).get('data', {}).get('url')
    except Exception as e:
        print(f"Erro ao verificar token Facebook com Graph API: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token do Facebook inválido ou expirado.'}), 401
        return render_template('login.html', erro="Falha na validação das credenciais com o Facebook.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com Microsoft (Microsoft SSO) - Validação Estrita de Graph API & JWT"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token') or dados.get('id_token')

    # SEGURANÇA: Token da Microsoft é estritamente obrigatório!
    if not access_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Microsoft obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token da Microsoft ausente. Por favor, utilize o botão oficial da Microsoft.")

    ms_email = None
    ms_nome = None
    ms_sub = None
    ms_picture = None

    # Tentar validar via Microsoft Graph API com Bearer token
    try:
        headers = {'Authorization': f'Bearer {access_token}'}
        res = requests.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
        if res.status_code == 200:
            info = res.json()
            ms_email = info.get('mail') or info.get('userPrincipalName')
            ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
            ms_sub = info.get('id')
            if ms_email:
                ms_picture = f'https://unavatar.io/{ms_email}'
    except Exception as e:
        print(f"Erro no Graph API Microsoft: {e}")

    # Se for um ID Token JWT puro do MSAL, decodificar e validar payload JWT oficial
    if not ms_email and access_token:
        try:
            parts = access_token.split('.')
            if len(parts) == 3:
                import base64, json
                padding = '=' * (4 - len(parts[1]) % 4)
                jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
                iss = jwt_payload.get('iss', '')
                if 'login.microsoftonline.com' in iss or 'sts.windows.net' in iss:
                    ms_email = jwt_payload.get('preferred_username') or jwt_payload.get('email') or jwt_payload.get('upn') or jwt_payload.get('unique_name')
                    ms_nome = jwt_payload.get('name') or jwt_payload.get('given_name') or (ms_email.split('@')[0] if ms_email else None)
                    ms_sub = jwt_payload.get('sub') or jwt_payload.get('oid')
                    if ms_email:
                        ms_picture = f'https://unavatar.io/{ms_email}'
        except Exception as jwt_err:
            print(f"Erro ao analisar ID Token Microsoft: {jwt_err}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Microsoft inválido, expirado ou não autorizado.'}), 401
        return render_template('login.html', erro="Falha na validação do token com a Microsoft.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
@app.route('/api/auth/apple', methods=['POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com Apple (Apple SSO) - Validação Estrita de ID Token"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    apple_token = dados.get('id_token') or dados.get('credential') or dados.get('token') or dados.get('access_token')

    # SEGURANÇA: Token da Apple é estritamente obrigatório! Acesso avulso por e-mail é bloqueado.
    if not apple_token:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Apple obrigatório e não fornecido.'}), 400
        return render_template('login.html', erro="Token da Apple ausente. Por favor, utilize o fluxo oficial da Apple.")

    apple_email = None
    apple_nome = None
    apple_sub = None

    try:
        parts = apple_token.split('.')
        if len(parts) == 3:
            import base64, json
            padding = '=' * (4 - len(parts[1]) % 4)
            jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
            if jwt_payload.get('iss') == 'https://appleid.apple.com':
                apple_email = jwt_payload.get('email')
                apple_sub = jwt_payload.get('sub')
                apple_nome = dados.get('name') or (apple_email.split('@')[0] if apple_email else 'Usuário Apple')
    except Exception as e:
        print(f"Erro ao validar token Apple: {e}")

    if not apple_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'Token da Apple inválido ou não verificado.'}), 401
        return render_template('login.html', erro="Falha na validação do token com a Apple.")

    apple_email = apple_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome or apple_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome or apple_email.split('@')[0], apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome or apple_email.split('@')[0], apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome or apple_email.split('@')[0],
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome or apple_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome or apple_email.split('@')[0],
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Apple SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    """Solicitação de redefinição de senha segura: gera token e envia EXCLUSIVAMENTE por e-mail"""
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            try:
                enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            except Exception as mail_err:
                print(f"Erro ao enviar email de redefinição de senha: {mail_err}")
                
        # SEGURANÇA: Mensagem genérica sem expor o token e prevenindo enumeração de usuários
        msg_sucesso = "Se o e-mail informado estiver cadastrado em nossa plataforma, as instruções e o link seguro de redefinição foram enviados para a sua caixa de entrada. Por favor, verifique o seu e-mail (inclusive a pasta de spam)."
        return render_template('login.html', sucesso=msg_sucesso)
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint restrito para gerar massa de dados demonstrativa no app"""
    admin_secret = os.environ.get("ADMIN_SECRET_KEY", "frameia_reaction_master_key_2026")
    header_secret = request.headers.get("X-Admin-Secret") or request.args.get("secret")
    
    is_admin_secret = header_secret and header_secret == admin_secret
    is_logged_user = 'usuario_id' in session and session.get('empresa_id')

    # SEGURANÇA: Bloqueia qualquer chamada anônima externa não autorizada
    if not is_admin_secret and not is_logged_user:
        return jsonify({"error": "Acesso não autorizado. Este endpoint requer autenticação ou chave mestra administrativa."}), 403

    try:
        from gerar_massa_dados import popular_banco_dados
        empresa_id = session.get('empresa_id', 1) if is_logged_user else int(request.args.get('empresa_id', 1))
        res = popular_banco_dados(empresa_id)
        return jsonify({"success": True, "message": f"Massa de dados gerada com sucesso para a empresa #{empresa_id}!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
.venv/
*.pyc
snapshots/
*_snapshot_*.txt
snapshot_*.txt
*.txt.bak


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
from PIL import Image

STATIC_DIR = r"C:\Users\jadso\Projetos\reaction\static"
MASTER_ICON = os.path.join(STATIC_DIR, "app_icon.png")

def generate_all_icons():
    print("Gerando todos os ícones PWA e Favicons a partir de static/app_icon.png...")
    
    if not os.path.exists(MASTER_ICON):
        print(f"Erro: {MASTER_ICON} não encontrado!")
        return

    master_img = Image.open(MASTER_ICON).convert("RGBA")

    # 512x512 PNG (PWA / Windows Desktop / Android)
    img_512 = master_img.resize((512, 512), Image.Resampling.LANCZOS)
    img_512.save(os.path.join(STATIC_DIR, "icon-512.png"), format="PNG")
    print(" -> Gerado: static/icon-512.png (512x512)")

    # 192x192 PNG (PWA / Android Launcher)
    img_192 = master_img.resize((192, 192), Image.Resampling.LANCZOS)
    img_192.save(os.path.join(STATIC_DIR, "icon-192.png"), format="PNG")
    print(" -> Gerado: static/icon-192.png (192x192)")

    # 180x180 PNG (Apple Touch Icon)
    img_180 = master_img.resize((180, 180), Image.Resampling.LANCZOS)
    img_180.save(os.path.join(STATIC_DIR, "apple-touch-icon.png"), format="PNG")
    print(" -> Gerado: static/apple-touch-icon.png (180x180)")

    # 64x64 PNG (Favicon PNG)
    img_64 = master_img.resize((64, 64), Image.Resampling.LANCZOS)
    img_64.save(os.path.join(STATIC_DIR, "favicon.png"), format="PNG")
    print(" -> Gerado: static/favicon.png (64x64)")

    # Favicon.ico multi-resolução
    ico_sizes = [(256, 256), (128, 128), (64, 64), (48, 48), (32, 32), (16, 16)]
    master_img.save(
        os.path.join(STATIC_DIR, "favicon.ico"),
        format="ICO",
        sizes=ico_sizes
    )
    print(" -> Gerado: static/favicon.ico")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA e atalhos de aplicativo foram atualizados!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com a Conta do Google (Google SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('g_token') or dados.get('access_token')
    google_email = dados.get('email')
    google_nome = dados.get('nome') or dados.get('name')
    google_sub = dados.get('sub') or dados.get('google_id')
    
    if google_token:
        try:
            import urllib.request
            import json
            req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
            with urllib.request.urlopen(req_url, timeout=5) as resp:
                token_data = json.loads(resp.read().decode('utf-8'))
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture') or dados.get('picture')
        except Exception as err_tkn:
            print(f"Aviso ao validar token Google: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Google não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Google requer credenciais válidas do provedor. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com a Conta do Facebook (Facebook SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    fb_email = dados.get('email')
    fb_nome = dados.get('nome') or dados.get('name')
    fb_sub = dados.get('sub') or dados.get('facebook_id')

    if access_token and not fb_email:
        try:
            import requests as req
            res = req.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
            if res.status_code == 200:
                info = res.json()
                fb_id = info.get('id')
                if info.get('email'):
                    fb_email = info.get('email')
                elif fb_id:
                    fb_email = f"fb_{fb_id}@facebook.user"
                if info.get('name'):
                    fb_nome = info.get('name')
                fb_sub = fb_id
                fb_picture = info.get('picture', {}).get('data', {}).get('url') if isinstance(info.get('picture'), dict) else None
        except Exception as e:
            print(f"Erro ao verificar token Facebook: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Facebook não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Facebook requer credenciais válidas do provedor. Por favor, tente novamente.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        fb_picture = locals().get('fb_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com a Conta da Microsoft (Microsoft SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    ms_email = dados.get('email')
    ms_nome = dados.get('nome') or dados.get('name')
    ms_sub = dados.get('sub') or dados.get('microsoft_id')

    if access_token and not ms_email:
        # Tentar decodificação direta de Token JWT (IdToken / AccessToken MSAL)
        try:
            parts = access_token.split('.')
            if len(parts) == 3:
                import base64, json
                padding = '=' * (4 - len(parts[1]) % 4)
                jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
                ms_email = jwt_payload.get('preferred_username') or jwt_payload.get('email') or jwt_payload.get('upn') or jwt_payload.get('unique_name')
                ms_nome = jwt_payload.get('name') or jwt_payload.get('given_name') or (ms_email.split('@')[0] if ms_email else None)
                ms_sub = jwt_payload.get('sub') or jwt_payload.get('oid')
                if ms_email:
                    ms_picture = f'https://unavatar.io/{ms_email}'
        except Exception as jwt_err:
            print(f"Aviso ao decodificar JWT Microsoft: {jwt_err}")

        if not ms_email:
            try:
                import requests as req
                headers = {'Authorization': f'Bearer {access_token}'}
                res = req.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
                if res.status_code == 200:
                    info = res.json()
                    ms_email = info.get('mail') or info.get('userPrincipalName')
                    ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
                    ms_sub = info.get('id')
                    ms_picture = f'https://unavatar.io/{ms_email}' if ms_email else None
            except Exception as e:
                print(f"Erro no Graph API Microsoft: {e}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail da Microsoft não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com a Microsoft requer credenciais válidas do provedor. Por favor, tente novamente.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        ms_picture = locals().get('ms_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com a Conta da Apple (Apple SSO)"""
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    apple_email = request.values.get('email')
    apple_nome = request.values.get('nome') or request.values.get('name')
    apple_sub = request.values.get('sub') or request.values.get('apple_id')

    if not apple_email:
        return render_template('login.html', erro="Autenticação com a Apple requer credenciais válidas do provedor. Por favor, tente novamente.")

    conn = get_db_connection()
    if not conn:
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome,
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome, nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome,
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        return redirect(url_for('app_dashboard'))

    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            msg_sucesso = f"Instruções e token enviados para <strong>{email}</strong>! <a href='/redefinir_senha?token={token}' class='underline font-bold hover:text-emerald-800'>Clique aqui para cadastrar a nova senha</a>."
            return render_template('login.html', sucesso=msg_sucesso)
        else:
            return render_template('login.html', erro="Nenhuma conta encontrada com este e-mail.")
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint para gerar massa de dados demonstrativa no app"""
    try:
        from gerar_massa_dados import popular_banco_dados
        res = popular_banco_dados()
        return jsonify({"success": True, "message": "Massa de dados gerada com sucesso!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
.venv/
*.pyc
snapshots/
*_snapshot_*.txt
snapshot_*.txt
*.txt.bak


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
from PIL import Image

STATIC_DIR = r"C:\Users\jadso\Projetos\reaction\static"
MASTER_ICON = os.path.join(STATIC_DIR, "app_icon.png")

def generate_all_icons():
    print("Gerando todos os ícones PWA e Favicons a partir de static/app_icon.png...")
    
    if not os.path.exists(MASTER_ICON):
        print(f"Erro: {MASTER_ICON} não encontrado!")
        return

    master_img = Image.open(MASTER_ICON).convert("RGBA")

    # 512x512 PNG (PWA / Windows Desktop / Android)
    img_512 = master_img.resize((512, 512), Image.Resampling.LANCZOS)
    img_512.save(os.path.join(STATIC_DIR, "icon-512.png"), format="PNG")
    print(" -> Gerado: static/icon-512.png (512x512)")

    # 192x192 PNG (PWA / Android Launcher)
    img_192 = master_img.resize((192, 192), Image.Resampling.LANCZOS)
    img_192.save(os.path.join(STATIC_DIR, "icon-192.png"), format="PNG")
    print(" -> Gerado: static/icon-192.png (192x192)")

    # 180x180 PNG (Apple Touch Icon)
    img_180 = master_img.resize((180, 180), Image.Resampling.LANCZOS)
    img_180.save(os.path.join(STATIC_DIR, "apple-touch-icon.png"), format="PNG")
    print(" -> Gerado: static/apple-touch-icon.png (180x180)")

    # 64x64 PNG (Favicon PNG)
    img_64 = master_img.resize((64, 64), Image.Resampling.LANCZOS)
    img_64.save(os.path.join(STATIC_DIR, "favicon.png"), format="PNG")
    print(" -> Gerado: static/favicon.png (64x64)")

    # Favicon.ico multi-resolução
    ico_sizes = [(256, 256), (128, 128), (64, 64), (48, 48), (32, 32), (16, 16)]
    master_img.save(
        os.path.join(STATIC_DIR, "favicon.ico"),
        format="ICO",
        sizes=ico_sizes
    )
    print(" -> Gerado: static/favicon.ico")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA e atalhos de aplicativo foram atualizados!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com a Conta do Google (Google SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('g_token') or dados.get('access_token')
    google_email = dados.get('email')
    google_nome = dados.get('nome') or dados.get('name')
    google_sub = dados.get('sub') or dados.get('google_id')
    
    if google_token:
        try:
            import urllib.request
            import json
            req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
            with urllib.request.urlopen(req_url, timeout=5) as resp:
                token_data = json.loads(resp.read().decode('utf-8'))
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture') or dados.get('picture')
        except Exception as err_tkn:
            print(f"Aviso ao validar token Google: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Google não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Google requer credenciais válidas do provedor. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com a Conta do Facebook (Facebook SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    fb_email = dados.get('email')
    fb_nome = dados.get('nome') or dados.get('name')
    fb_sub = dados.get('sub') or dados.get('facebook_id')

    if access_token and not fb_email:
        try:
            import requests as req
            res = req.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
            if res.status_code == 200:
                info = res.json()
                fb_id = info.get('id')
                if info.get('email'):
                    fb_email = info.get('email')
                elif fb_id:
                    fb_email = f"fb_{fb_id}@facebook.user"
                if info.get('name'):
                    fb_nome = info.get('name')
                fb_sub = fb_id
                fb_picture = info.get('picture', {}).get('data', {}).get('url') if isinstance(info.get('picture'), dict) else None
        except Exception as e:
            print(f"Erro ao verificar token Facebook: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Facebook não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Facebook requer credenciais válidas do provedor. Por favor, tente novamente.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        fb_picture = locals().get('fb_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com a Conta da Microsoft (Microsoft SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    ms_email = dados.get('email')
    ms_nome = dados.get('nome') or dados.get('name')
    ms_sub = dados.get('sub') or dados.get('microsoft_id')

    if access_token and not ms_email:
        # Tentar decodificação direta de Token JWT (IdToken / AccessToken MSAL)
        try:
            parts = access_token.split('.')
            if len(parts) == 3:
                import base64, json
                padding = '=' * (4 - len(parts[1]) % 4)
                jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
                ms_email = jwt_payload.get('preferred_username') or jwt_payload.get('email') or jwt_payload.get('upn') or jwt_payload.get('unique_name')
                ms_nome = jwt_payload.get('name') or jwt_payload.get('given_name') or (ms_email.split('@')[0] if ms_email else None)
                ms_sub = jwt_payload.get('sub') or jwt_payload.get('oid')
                if ms_email:
                    ms_picture = f'https://unavatar.io/{ms_email}'
        except Exception as jwt_err:
            print(f"Aviso ao decodificar JWT Microsoft: {jwt_err}")

        if not ms_email:
            try:
                import requests as req
                headers = {'Authorization': f'Bearer {access_token}'}
                res = req.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
                if res.status_code == 200:
                    info = res.json()
                    ms_email = info.get('mail') or info.get('userPrincipalName')
                    ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
                    ms_sub = info.get('id')
                    ms_picture = f'https://unavatar.io/{ms_email}' if ms_email else None
            except Exception as e:
                print(f"Erro no Graph API Microsoft: {e}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail da Microsoft não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com a Microsoft requer credenciais válidas do provedor. Por favor, tente novamente.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        ms_picture = locals().get('ms_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com a Conta da Apple (Apple SSO)"""
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    apple_email = request.values.get('email')
    apple_nome = request.values.get('nome') or request.values.get('name')
    apple_sub = request.values.get('sub') or request.values.get('apple_id')

    if not apple_email:
        return render_template('login.html', erro="Autenticação com a Apple requer credenciais válidas do provedor. Por favor, tente novamente.")

    conn = get_db_connection()
    if not conn:
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome,
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome, nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome,
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        return redirect(url_for('app_dashboard'))

    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            msg_sucesso = f"Instruções e token enviados para <strong>{email}</strong>! <a href='/redefinir_senha?token={token}' class='underline font-bold hover:text-emerald-800'>Clique aqui para cadastrar a nova senha</a>."
            return render_template('login.html', sucesso=msg_sucesso)
        else:
            return render_template('login.html', erro="Nenhuma conta encontrada com este e-mail.")
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint para gerar massa de dados demonstrativa no app"""
    try:
        from gerar_massa_dados import popular_banco_dados
        res = popular_banco_dados()
        return jsonify({"success": True, "message": "Massa de dados gerada com sucesso!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
.venv/
*.pyc
snapshots/
*_snapshot_*.txt
snapshot_*.txt
*.txt.bak


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
import resvg_py
from PIL import Image

SVG_PATH = r"C:\Users\jadso\Projetos\reaction\static\favicon.svg"

def convert_svg_to_png(svg_path, output_png_path, size):
    # Renderizar o SVG nativo com resvg_py em alta definição
    png_bytes = resvg_py.svg_to_bytes(svg_path=svg_path, width=size, height=size)
    
    with open(output_png_path, "wb") as out_f:
        out_f.write(png_bytes)
        
    print(f" -> Gerado: {output_png_path} ({size}x{size}px)")

def generate_all_icons():
    print("Gerando ícones PWA e Favicons 100% idênticos a partir de static/favicon.svg via resvg...")
    
    # 512x512 PNG (PWA / Windows Desktop / Android)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-512.png", 512)
    
    # 192x192 PNG (PWA / Android Launcher)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-192.png", 192)
    
    # 180x180 PNG (Apple Touch Icon)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\apple-touch-icon.png", 180)
    
    # 64x64 PNG (Favicon PNG)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\favicon.png", 64)
    
    # Favicon.ico
    img_64 = Image.open(r"C:\Users\jadso\Projetos\reaction\static\favicon.png")
    img_64.save(r"C:\Users\jadso\Projetos\reaction\static\favicon.ico", format="ICO", sizes=[(64, 64), (32, 32), (16, 16)])
    print(" -> Gerado: static/favicon.ico")
    
    # Atualizar também na pasta de versão v0.0.0 se existir
    dest_v0 = r"C:\Users\jadso\Projetos\reaction\versoes\reaction_v0.0.0\static"
    if os.path.exists(dest_v0):
        for fname in ["icon-512.png", "icon-192.png", "apple-touch-icon.png", "favicon.png", "favicon.ico"]:
            src = os.path.join(r"C:\Users\jadso\Projetos\reaction\static", fname)
            dst = os.path.join(dest_v0, fname)
            if os.path.exists(src):
                import shutil
                shutil.copy2(src, dst)
        print(" -> Copiado para a pasta versoes/reaction_v0.0.0/static")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA foram regerados com 100% de fidelidade ao SVG original!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com a Conta do Google (Google SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('g_token') or dados.get('access_token')
    google_email = dados.get('email')
    google_nome = dados.get('nome') or dados.get('name')
    google_sub = dados.get('sub') or dados.get('google_id')
    
    if google_token:
        try:
            import urllib.request
            import json
            req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
            with urllib.request.urlopen(req_url, timeout=5) as resp:
                token_data = json.loads(resp.read().decode('utf-8'))
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture') or dados.get('picture')
        except Exception as err_tkn:
            print(f"Aviso ao validar token Google: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Google não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Google requer credenciais válidas do provedor. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com a Conta do Facebook (Facebook SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    fb_email = dados.get('email')
    fb_nome = dados.get('nome') or dados.get('name')
    fb_sub = dados.get('sub') or dados.get('facebook_id')

    if access_token and not fb_email:
        try:
            import requests as req
            res = req.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
            if res.status_code == 200:
                info = res.json()
                fb_id = info.get('id')
                if info.get('email'):
                    fb_email = info.get('email')
                elif fb_id:
                    fb_email = f"fb_{fb_id}@facebook.user"
                if info.get('name'):
                    fb_nome = info.get('name')
                fb_sub = fb_id
                fb_picture = info.get('picture', {}).get('data', {}).get('url') if isinstance(info.get('picture'), dict) else None
        except Exception as e:
            print(f"Erro ao verificar token Facebook: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Facebook não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Facebook requer credenciais válidas do provedor. Por favor, tente novamente.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        fb_picture = locals().get('fb_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com a Conta da Microsoft (Microsoft SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    ms_email = dados.get('email')
    ms_nome = dados.get('nome') or dados.get('name')
    ms_sub = dados.get('sub') or dados.get('microsoft_id')

    if access_token and not ms_email:
        # Tentar decodificação direta de Token JWT (IdToken / AccessToken MSAL)
        try:
            parts = access_token.split('.')
            if len(parts) == 3:
                import base64, json
                padding = '=' * (4 - len(parts[1]) % 4)
                jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
                ms_email = jwt_payload.get('preferred_username') or jwt_payload.get('email') or jwt_payload.get('upn') or jwt_payload.get('unique_name')
                ms_nome = jwt_payload.get('name') or jwt_payload.get('given_name') or (ms_email.split('@')[0] if ms_email else None)
                ms_sub = jwt_payload.get('sub') or jwt_payload.get('oid')
                if ms_email:
                    ms_picture = f'https://unavatar.io/{ms_email}'
        except Exception as jwt_err:
            print(f"Aviso ao decodificar JWT Microsoft: {jwt_err}")

        if not ms_email:
            try:
                import requests as req
                headers = {'Authorization': f'Bearer {access_token}'}
                res = req.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
                if res.status_code == 200:
                    info = res.json()
                    ms_email = info.get('mail') or info.get('userPrincipalName')
                    ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
                    ms_sub = info.get('id')
                    ms_picture = f'https://unavatar.io/{ms_email}' if ms_email else None
            except Exception as e:
                print(f"Erro no Graph API Microsoft: {e}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail da Microsoft não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com a Microsoft requer credenciais válidas do provedor. Por favor, tente novamente.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        ms_picture = locals().get('ms_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com a Conta da Apple (Apple SSO)"""
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    apple_email = request.values.get('email')
    apple_nome = request.values.get('nome') or request.values.get('name')
    apple_sub = request.values.get('sub') or request.values.get('apple_id')

    if not apple_email:
        return render_template('login.html', erro="Autenticação com a Apple requer credenciais válidas do provedor. Por favor, tente novamente.")

    conn = get_db_connection()
    if not conn:
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome,
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome, nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome,
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        return redirect(url_for('app_dashboard'))

    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            msg_sucesso = f"Instruções e token enviados para <strong>{email}</strong>! <a href='/redefinir_senha?token={token}' class='underline font-bold hover:text-emerald-800'>Clique aqui para cadastrar a nova senha</a>."
            return render_template('login.html', sucesso=msg_sucesso)
        else:
            return render_template('login.html', erro="Nenhuma conta encontrada com este e-mail.")
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint para gerar massa de dados demonstrativa no app"""
    try:
        from gerar_massa_dados import popular_banco_dados
        res = popular_banco_dados()
        return jsonify({"success": True, "message": "Massa de dados gerada com sucesso!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
.venv/
*.pyc
snapshots/
*_snapshot_*.txt
snapshot_*.txt
*.txt.bak


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
import resvg_py
from PIL import Image

SVG_PATH = r"C:\Users\jadso\Projetos\reaction\static\favicon.svg"

def convert_svg_to_png(svg_path, output_png_path, size):
    # Renderizar o SVG nativo com resvg_py em alta definição
    png_bytes = resvg_py.svg_to_bytes(svg_path=svg_path, width=size, height=size)
    
    with open(output_png_path, "wb") as out_f:
        out_f.write(png_bytes)
        
    print(f" -> Gerado: {output_png_path} ({size}x{size}px)")

def generate_all_icons():
    print("Gerando ícones PWA e Favicons 100% idênticos a partir de static/favicon.svg via resvg...")
    
    # 512x512 PNG (PWA / Windows Desktop / Android)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-512.png", 512)
    
    # 192x192 PNG (PWA / Android Launcher)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-192.png", 192)
    
    # 180x180 PNG (Apple Touch Icon)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\apple-touch-icon.png", 180)
    
    # 64x64 PNG (Favicon PNG)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\favicon.png", 64)
    
    # Favicon.ico
    img_64 = Image.open(r"C:\Users\jadso\Projetos\reaction\static\favicon.png")
    img_64.save(r"C:\Users\jadso\Projetos\reaction\static\favicon.ico", format="ICO", sizes=[(64, 64), (32, 32), (16, 16)])
    print(" -> Gerado: static/favicon.ico")
    
    # Atualizar também na pasta de versão v0.0.0 se existir
    dest_v0 = r"C:\Users\jadso\Projetos\reaction\versoes\reaction_v0.0.0\static"
    if os.path.exists(dest_v0):
        for fname in ["icon-512.png", "icon-192.png", "apple-touch-icon.png", "favicon.png", "favicon.ico"]:
            src = os.path.join(r"C:\Users\jadso\Projetos\reaction\static", fname)
            dst = os.path.join(dest_v0, fname)
            if os.path.exists(src):
                import shutil
                shutil.copy2(src, dst)
        print(" -> Copiado para a pasta versoes/reaction_v0.0.0/static")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA foram regerados com 100% de fidelidade ao SVG original!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com a Conta do Google (Google SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('g_token') or dados.get('access_token')
    google_email = dados.get('email')
    google_nome = dados.get('nome') or dados.get('name')
    google_sub = dados.get('sub') or dados.get('google_id')
    
    if google_token:
        try:
            import urllib.request
            import json
            req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
            with urllib.request.urlopen(req_url, timeout=5) as resp:
                token_data = json.loads(resp.read().decode('utf-8'))
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture') or dados.get('picture')
        except Exception as err_tkn:
            print(f"Aviso ao validar token Google: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Google não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Google requer credenciais válidas do provedor. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com a Conta do Facebook (Facebook SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    fb_email = dados.get('email')
    fb_nome = dados.get('nome') or dados.get('name')
    fb_sub = dados.get('sub') or dados.get('facebook_id')

    if access_token and not fb_email:
        try:
            import requests as req
            res = req.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
            if res.status_code == 200:
                info = res.json()
                fb_id = info.get('id')
                if info.get('email'):
                    fb_email = info.get('email')
                elif fb_id:
                    fb_email = f"fb_{fb_id}@facebook.user"
                if info.get('name'):
                    fb_nome = info.get('name')
                fb_sub = fb_id
                fb_picture = info.get('picture', {}).get('data', {}).get('url') if isinstance(info.get('picture'), dict) else None
        except Exception as e:
            print(f"Erro ao verificar token Facebook: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Facebook não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Facebook requer credenciais válidas do provedor. Por favor, tente novamente.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        fb_picture = locals().get('fb_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com a Conta da Microsoft (Microsoft SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    ms_email = dados.get('email')
    ms_nome = dados.get('nome') or dados.get('name')
    ms_sub = dados.get('sub') or dados.get('microsoft_id')

    if access_token and not ms_email:
        # Tentar decodificação direta de Token JWT (IdToken / AccessToken MSAL)
        try:
            parts = access_token.split('.')
            if len(parts) == 3:
                import base64, json
                padding = '=' * (4 - len(parts[1]) % 4)
                jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
                ms_email = jwt_payload.get('preferred_username') or jwt_payload.get('email') or jwt_payload.get('upn') or jwt_payload.get('unique_name')
                ms_nome = jwt_payload.get('name') or jwt_payload.get('given_name') or (ms_email.split('@')[0] if ms_email else None)
                ms_sub = jwt_payload.get('sub') or jwt_payload.get('oid')
                if ms_email:
                    ms_picture = f'https://unavatar.io/{ms_email}'
        except Exception as jwt_err:
            print(f"Aviso ao decodificar JWT Microsoft: {jwt_err}")

        if not ms_email:
            try:
                import requests as req
                headers = {'Authorization': f'Bearer {access_token}'}
                res = req.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
                if res.status_code == 200:
                    info = res.json()
                    ms_email = info.get('mail') or info.get('userPrincipalName')
                    ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
                    ms_sub = info.get('id')
                    ms_picture = f'https://unavatar.io/{ms_email}' if ms_email else None
            except Exception as e:
                print(f"Erro no Graph API Microsoft: {e}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail da Microsoft não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com a Microsoft requer credenciais válidas do provedor. Por favor, tente novamente.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        ms_picture = locals().get('ms_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com a Conta da Apple (Apple SSO)"""
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    apple_email = request.values.get('email')
    apple_nome = request.values.get('nome') or request.values.get('name')
    apple_sub = request.values.get('sub') or request.values.get('apple_id')

    if not apple_email:
        return render_template('login.html', erro="Autenticação com a Apple requer credenciais válidas do provedor. Por favor, tente novamente.")

    conn = get_db_connection()
    if not conn:
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome,
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome, nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome,
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        return redirect(url_for('app_dashboard'))

    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            msg_sucesso = f"Instruções e token enviados para <strong>{email}</strong>! <a href='/redefinir_senha?token={token}' class='underline font-bold hover:text-emerald-800'>Clique aqui para cadastrar a nova senha</a>."
            return render_template('login.html', sucesso=msg_sucesso)
        else:
            return render_template('login.html', erro="Nenhuma conta encontrada com este e-mail.")
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint para gerar massa de dados demonstrativa no app"""
    try:
        from gerar_massa_dados import popular_banco_dados
        res = popular_banco_dados()
        return jsonify({"success": True, "message": "Massa de dados gerada com sucesso!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
.venv/
*.pyc
snapshots/
*_snapshot_*.txt
snapshot_*.txt
*.txt.bak


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
import resvg_py
from PIL import Image

SVG_PATH = r"C:\Users\jadso\Projetos\reaction\static\favicon.svg"

def convert_svg_to_png(svg_path, output_png_path, size):
    # Renderizar o SVG nativo com resvg_py em alta definição
    png_bytes = resvg_py.svg_to_bytes(svg_path=svg_path, width=size, height=size)
    
    with open(output_png_path, "wb") as out_f:
        out_f.write(png_bytes)
        
    print(f" -> Gerado: {output_png_path} ({size}x{size}px)")

def generate_all_icons():
    print("Gerando ícones PWA e Favicons 100% idênticos a partir de static/favicon.svg via resvg...")
    
    # 512x512 PNG (PWA / Windows Desktop / Android)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-512.png", 512)
    
    # 192x192 PNG (PWA / Android Launcher)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-192.png", 192)
    
    # 180x180 PNG (Apple Touch Icon)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\apple-touch-icon.png", 180)
    
    # 64x64 PNG (Favicon PNG)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\favicon.png", 64)
    
    # Favicon.ico
    img_64 = Image.open(r"C:\Users\jadso\Projetos\reaction\static\favicon.png")
    img_64.save(r"C:\Users\jadso\Projetos\reaction\static\favicon.ico", format="ICO", sizes=[(64, 64), (32, 32), (16, 16)])
    print(" -> Gerado: static/favicon.ico")
    
    # Atualizar também na pasta de versão v0.0.0 se existir
    dest_v0 = r"C:\Users\jadso\Projetos\reaction\versoes\reaction_v0.0.0\static"
    if os.path.exists(dest_v0):
        for fname in ["icon-512.png", "icon-192.png", "apple-touch-icon.png", "favicon.png", "favicon.ico"]:
            src = os.path.join(r"C:\Users\jadso\Projetos\reaction\static", fname)
            dst = os.path.join(dest_v0, fname)
            if os.path.exists(src):
                import shutil
                shutil.copy2(src, dst)
        print(" -> Copiado para a pasta versoes/reaction_v0.0.0/static")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA foram regerados com 100% de fidelidade ao SVG original!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com a Conta do Google (Google SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('g_token') or dados.get('access_token')
    google_email = dados.get('email')
    google_nome = dados.get('nome') or dados.get('name')
    google_sub = dados.get('sub') or dados.get('google_id')
    
    if google_token:
        try:
            import urllib.request
            import json
            req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
            with urllib.request.urlopen(req_url, timeout=5) as resp:
                token_data = json.loads(resp.read().decode('utf-8'))
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture') or dados.get('picture')
        except Exception as err_tkn:
            print(f"Aviso ao validar token Google: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Google não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Google requer credenciais válidas do provedor. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com a Conta do Facebook (Facebook SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    fb_email = dados.get('email')
    fb_nome = dados.get('nome') or dados.get('name')
    fb_sub = dados.get('sub') or dados.get('facebook_id')

    if access_token and not fb_email:
        try:
            import requests as req
            res = req.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
            if res.status_code == 200:
                info = res.json()
                fb_id = info.get('id')
                if info.get('email'):
                    fb_email = info.get('email')
                elif fb_id:
                    fb_email = f"fb_{fb_id}@facebook.user"
                if info.get('name'):
                    fb_nome = info.get('name')
                fb_sub = fb_id
                fb_picture = info.get('picture', {}).get('data', {}).get('url') if isinstance(info.get('picture'), dict) else None
        except Exception as e:
            print(f"Erro ao verificar token Facebook: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Facebook não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Facebook requer credenciais válidas do provedor. Por favor, tente novamente.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        fb_picture = locals().get('fb_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com a Conta da Microsoft (Microsoft SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    ms_email = dados.get('email')
    ms_nome = dados.get('nome') or dados.get('name')
    ms_sub = dados.get('sub') or dados.get('microsoft_id')

    if access_token and not ms_email:
        # Tentar decodificação direta de Token JWT (IdToken / AccessToken MSAL)
        try:
            parts = access_token.split('.')
            if len(parts) == 3:
                import base64, json
                padding = '=' * (4 - len(parts[1]) % 4)
                jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
                ms_email = jwt_payload.get('preferred_username') or jwt_payload.get('email') or jwt_payload.get('upn') or jwt_payload.get('unique_name')
                ms_nome = jwt_payload.get('name') or jwt_payload.get('given_name') or (ms_email.split('@')[0] if ms_email else None)
                ms_sub = jwt_payload.get('sub') or jwt_payload.get('oid')
                if ms_email:
                    ms_picture = f'https://unavatar.io/{ms_email}'
        except Exception as jwt_err:
            print(f"Aviso ao decodificar JWT Microsoft: {jwt_err}")

        if not ms_email:
            try:
                import requests as req
                headers = {'Authorization': f'Bearer {access_token}'}
                res = req.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
                if res.status_code == 200:
                    info = res.json()
                    ms_email = info.get('mail') or info.get('userPrincipalName')
                    ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
                    ms_sub = info.get('id')
                    ms_picture = f'https://unavatar.io/{ms_email}' if ms_email else None
            except Exception as e:
                print(f"Erro no Graph API Microsoft: {e}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail da Microsoft não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com a Microsoft requer credenciais válidas do provedor. Por favor, tente novamente.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        ms_picture = locals().get('ms_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com a Conta da Apple (Apple SSO)"""
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    apple_email = request.values.get('email')
    apple_nome = request.values.get('nome') or request.values.get('name')
    apple_sub = request.values.get('sub') or request.values.get('apple_id')

    if not apple_email:
        return render_template('login.html', erro="Autenticação com a Apple requer credenciais válidas do provedor. Por favor, tente novamente.")

    conn = get_db_connection()
    if not conn:
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome,
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome, nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome,
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        return redirect(url_for('app_dashboard'))

    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            msg_sucesso = f"Instruções e token enviados para <strong>{email}</strong>! <a href='/redefinir_senha?token={token}' class='underline font-bold hover:text-emerald-800'>Clique aqui para cadastrar a nova senha</a>."
            return render_template('login.html', sucesso=msg_sucesso)
        else:
            return render_template('login.html', erro="Nenhuma conta encontrada com este e-mail.")
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint para gerar massa de dados demonstrativa no app"""
    try:
        from gerar_massa_dados import popular_banco_dados
        res = popular_banco_dados()
        return jsonify({"success": True, "message": "Massa de dados gerada com sucesso!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
.venv/
*.pyc
snapshots/
*_snapshot_*.txt
snapshot_*.txt
*.txt.bak


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
import resvg_py
from PIL import Image

SVG_PATH = r"C:\Users\jadso\Projetos\reaction\static\favicon.svg"

def convert_svg_to_png(svg_path, output_png_path, size):
    # Renderizar o SVG nativo com resvg_py em alta definição
    png_bytes = resvg_py.svg_to_bytes(svg_path=svg_path, width=size, height=size)
    
    with open(output_png_path, "wb") as out_f:
        out_f.write(png_bytes)
        
    print(f" -> Gerado: {output_png_path} ({size}x{size}px)")

def generate_all_icons():
    print("Gerando ícones PWA e Favicons 100% idênticos a partir de static/favicon.svg via resvg...")
    
    # 512x512 PNG (PWA / Windows Desktop / Android)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-512.png", 512)
    
    # 192x192 PNG (PWA / Android Launcher)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-192.png", 192)
    
    # 180x180 PNG (Apple Touch Icon)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\apple-touch-icon.png", 180)
    
    # 64x64 PNG (Favicon PNG)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\favicon.png", 64)
    
    # Favicon.ico
    img_64 = Image.open(r"C:\Users\jadso\Projetos\reaction\static\favicon.png")
    img_64.save(r"C:\Users\jadso\Projetos\reaction\static\favicon.ico", format="ICO", sizes=[(64, 64), (32, 32), (16, 16)])
    print(" -> Gerado: static/favicon.ico")
    
    # Atualizar também na pasta de versão v0.0.0 se existir
    dest_v0 = r"C:\Users\jadso\Projetos\reaction\versoes\reaction_v0.0.0\static"
    if os.path.exists(dest_v0):
        for fname in ["icon-512.png", "icon-192.png", "apple-touch-icon.png", "favicon.png", "favicon.ico"]:
            src = os.path.join(r"C:\Users\jadso\Projetos\reaction\static", fname)
            dst = os.path.join(dest_v0, fname)
            if os.path.exists(src):
                import shutil
                shutil.copy2(src, dst)
        print(" -> Copiado para a pasta versoes/reaction_v0.0.0/static")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA foram regerados com 100% de fidelidade ao SVG original!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com a Conta do Google (Google SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('g_token') or dados.get('access_token')
    google_email = dados.get('email')
    google_nome = dados.get('nome') or dados.get('name')
    google_sub = dados.get('sub') or dados.get('google_id')
    
    if google_token:
        try:
            import urllib.request
            import json
            req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
            with urllib.request.urlopen(req_url, timeout=5) as resp:
                token_data = json.loads(resp.read().decode('utf-8'))
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture') or dados.get('picture')
        except Exception as err_tkn:
            print(f"Aviso ao validar token Google: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Google não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Google requer credenciais válidas do provedor. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com a Conta do Facebook (Facebook SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    fb_email = dados.get('email')
    fb_nome = dados.get('nome') or dados.get('name')
    fb_sub = dados.get('sub') or dados.get('facebook_id')

    if access_token and not fb_email:
        try:
            import requests as req
            res = req.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
            if res.status_code == 200:
                info = res.json()
                fb_id = info.get('id')
                if info.get('email'):
                    fb_email = info.get('email')
                elif fb_id:
                    fb_email = f"fb_{fb_id}@facebook.user"
                if info.get('name'):
                    fb_nome = info.get('name')
                fb_sub = fb_id
                fb_picture = info.get('picture', {}).get('data', {}).get('url') if isinstance(info.get('picture'), dict) else None
        except Exception as e:
            print(f"Erro ao verificar token Facebook: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Facebook não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Facebook requer credenciais válidas do provedor. Por favor, tente novamente.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        fb_picture = locals().get('fb_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com a Conta da Microsoft (Microsoft SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    ms_email = dados.get('email')
    ms_nome = dados.get('nome') or dados.get('name')
    ms_sub = dados.get('sub') or dados.get('microsoft_id')

    if access_token and not ms_email:
        # Tentar decodificação direta de Token JWT (IdToken / AccessToken MSAL)
        try:
            parts = access_token.split('.')
            if len(parts) == 3:
                import base64, json
                padding = '=' * (4 - len(parts[1]) % 4)
                jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
                ms_email = jwt_payload.get('preferred_username') or jwt_payload.get('email') or jwt_payload.get('upn') or jwt_payload.get('unique_name')
                ms_nome = jwt_payload.get('name') or jwt_payload.get('given_name') or (ms_email.split('@')[0] if ms_email else None)
                ms_sub = jwt_payload.get('sub') or jwt_payload.get('oid')
                if ms_email:
                    ms_picture = f'https://unavatar.io/{ms_email}'
        except Exception as jwt_err:
            print(f"Aviso ao decodificar JWT Microsoft: {jwt_err}")

        if not ms_email:
            try:
                import requests as req
                headers = {'Authorization': f'Bearer {access_token}'}
                res = req.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
                if res.status_code == 200:
                    info = res.json()
                    ms_email = info.get('mail') or info.get('userPrincipalName')
                    ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
                    ms_sub = info.get('id')
                    ms_picture = f'https://unavatar.io/{ms_email}' if ms_email else None
            except Exception as e:
                print(f"Erro no Graph API Microsoft: {e}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail da Microsoft não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com a Microsoft requer credenciais válidas do provedor. Por favor, tente novamente.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        ms_picture = locals().get('ms_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com a Conta da Apple (Apple SSO)"""
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    apple_email = request.values.get('email')
    apple_nome = request.values.get('nome') or request.values.get('name')
    apple_sub = request.values.get('sub') or request.values.get('apple_id')

    if not apple_email:
        return render_template('login.html', erro="Autenticação com a Apple requer credenciais válidas do provedor. Por favor, tente novamente.")

    conn = get_db_connection()
    if not conn:
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome,
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome, nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome,
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        return redirect(url_for('app_dashboard'))

    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            msg_sucesso = f"Instruções e token enviados para <strong>{email}</strong>! <a href='/redefinir_senha?token={token}' class='underline font-bold hover:text-emerald-800'>Clique aqui para cadastrar a nova senha</a>."
            return render_template('login.html', sucesso=msg_sucesso)
        else:
            return render_template('login.html', erro="Nenhuma conta encontrada com este e-mail.")
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint para gerar massa de dados demonstrativa no app"""
    try:
        from gerar_massa_dados import popular_banco_dados
        res = popular_banco_dados()
        return jsonify({"success": True, "message": "Massa de dados gerada com sucesso!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
.venv/
*.pyc
snapshots/
*_snapshot_*.txt
snapshot_*.txt
*.txt.bak


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
import resvg_py
from PIL import Image

SVG_PATH = r"C:\Users\jadso\Projetos\reaction\static\favicon.svg"

def convert_svg_to_png(svg_path, output_png_path, size):
    # Renderizar o SVG nativo com resvg_py em alta definição
    png_bytes = resvg_py.svg_to_bytes(svg_path=svg_path, width=size, height=size)
    
    with open(output_png_path, "wb") as out_f:
        out_f.write(png_bytes)
        
    print(f" -> Gerado: {output_png_path} ({size}x{size}px)")

def generate_all_icons():
    print("Gerando ícones PWA e Favicons 100% idênticos a partir de static/favicon.svg via resvg...")
    
    # 512x512 PNG (PWA / Windows Desktop / Android)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-512.png", 512)
    
    # 192x192 PNG (PWA / Android Launcher)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-192.png", 192)
    
    # 180x180 PNG (Apple Touch Icon)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\apple-touch-icon.png", 180)
    
    # 64x64 PNG (Favicon PNG)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\favicon.png", 64)
    
    # Favicon.ico
    img_64 = Image.open(r"C:\Users\jadso\Projetos\reaction\static\favicon.png")
    img_64.save(r"C:\Users\jadso\Projetos\reaction\static\favicon.ico", format="ICO", sizes=[(64, 64), (32, 32), (16, 16)])
    print(" -> Gerado: static/favicon.ico")
    
    # Atualizar também na pasta de versão v0.0.0 se existir
    dest_v0 = r"C:\Users\jadso\Projetos\reaction\versoes\reaction_v0.0.0\static"
    if os.path.exists(dest_v0):
        for fname in ["icon-512.png", "icon-192.png", "apple-touch-icon.png", "favicon.png", "favicon.ico"]:
            src = os.path.join(r"C:\Users\jadso\Projetos\reaction\static", fname)
            dst = os.path.join(dest_v0, fname)
            if os.path.exists(src):
                import shutil
                shutil.copy2(src, dst)
        print(" -> Copiado para a pasta versoes/reaction_v0.0.0/static")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA foram regerados com 100% de fidelidade ao SVG original!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com a Conta do Google (Google SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('g_token') or dados.get('access_token')
    google_email = dados.get('email')
    google_nome = dados.get('nome') or dados.get('name')
    google_sub = dados.get('sub') or dados.get('google_id')
    
    if google_token:
        try:
            import urllib.request
            import json
            req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
            with urllib.request.urlopen(req_url, timeout=5) as resp:
                token_data = json.loads(resp.read().decode('utf-8'))
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture') or dados.get('picture')
        except Exception as err_tkn:
            print(f"Aviso ao validar token Google: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Google não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Google requer credenciais válidas do provedor. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com a Conta do Facebook (Facebook SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    fb_email = dados.get('email')
    fb_nome = dados.get('nome') or dados.get('name')
    fb_sub = dados.get('sub') or dados.get('facebook_id')

    if access_token and not fb_email:
        try:
            import requests as req
            res = req.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
            if res.status_code == 200:
                info = res.json()
                fb_id = info.get('id')
                if info.get('email'):
                    fb_email = info.get('email')
                elif fb_id:
                    fb_email = f"fb_{fb_id}@facebook.user"
                if info.get('name'):
                    fb_nome = info.get('name')
                fb_sub = fb_id
                fb_picture = info.get('picture', {}).get('data', {}).get('url') if isinstance(info.get('picture'), dict) else None
        except Exception as e:
            print(f"Erro ao verificar token Facebook: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Facebook não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Facebook requer credenciais válidas do provedor. Por favor, tente novamente.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        fb_picture = locals().get('fb_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com a Conta da Microsoft (Microsoft SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    ms_email = dados.get('email')
    ms_nome = dados.get('nome') or dados.get('name')
    ms_sub = dados.get('sub') or dados.get('microsoft_id')

    if access_token and not ms_email:
        # Tentar decodificação direta de Token JWT (IdToken / AccessToken MSAL)
        try:
            parts = access_token.split('.')
            if len(parts) == 3:
                import base64, json
                padding = '=' * (4 - len(parts[1]) % 4)
                jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
                ms_email = jwt_payload.get('preferred_username') or jwt_payload.get('email') or jwt_payload.get('upn') or jwt_payload.get('unique_name')
                ms_nome = jwt_payload.get('name') or jwt_payload.get('given_name') or (ms_email.split('@')[0] if ms_email else None)
                ms_sub = jwt_payload.get('sub') or jwt_payload.get('oid')
                if ms_email:
                    ms_picture = f'https://unavatar.io/{ms_email}'
        except Exception as jwt_err:
            print(f"Aviso ao decodificar JWT Microsoft: {jwt_err}")

        if not ms_email:
            try:
                import requests as req
                headers = {'Authorization': f'Bearer {access_token}'}
                res = req.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
                if res.status_code == 200:
                    info = res.json()
                    ms_email = info.get('mail') or info.get('userPrincipalName')
                    ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
                    ms_sub = info.get('id')
                    ms_picture = f'https://unavatar.io/{ms_email}' if ms_email else None
            except Exception as e:
                print(f"Erro no Graph API Microsoft: {e}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail da Microsoft não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com a Microsoft requer credenciais válidas do provedor. Por favor, tente novamente.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        ms_picture = locals().get('ms_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com a Conta da Apple (Apple SSO)"""
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    apple_email = request.values.get('email')
    apple_nome = request.values.get('nome') or request.values.get('name')
    apple_sub = request.values.get('sub') or request.values.get('apple_id')

    if not apple_email:
        return render_template('login.html', erro="Autenticação com a Apple requer credenciais válidas do provedor. Por favor, tente novamente.")

    conn = get_db_connection()
    if not conn:
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome,
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome, nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome,
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        return redirect(url_for('app_dashboard'))

    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            msg_sucesso = f"Instruções e token enviados para <strong>{email}</strong>! <a href='/redefinir_senha?token={token}' class='underline font-bold hover:text-emerald-800'>Clique aqui para cadastrar a nova senha</a>."
            return render_template('login.html', sucesso=msg_sucesso)
        else:
            return render_template('login.html', erro="Nenhuma conta encontrada com este e-mail.")
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint para gerar massa de dados demonstrativa no app"""
    try:
        from gerar_massa_dados import popular_banco_dados
        res = popular_banco_dados()
        return jsonify({"success": True, "message": "Massa de dados gerada com sucesso!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.env
==================================================

DB_HOST=srv1722.hstgr.io
DB_USER=u716503964_reaction
DB_PASS="******"
DB_NAME=u716503964_reaction
FLASK_SECRET_KEY=chave_secreta_reacao_2026
GEMINI_API_KEY="AIzaSy_REDACTED_SECRET"

# Autenticação Social OAuth (Compartilhada com o Cash)
GOOGLE_CLIENT_ID="71269651978-gp165jo1i5r6mgmb22u8s82g0jsdh5v0.apps.googleusercontent.com"
MICROSOFT_CLIENT_ID="138269ce-38e6-4c1e-bc6a-b5292e877a24"
FACEBOOK_APP_ID="2263040147842797"


==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
*.pyc


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
import resvg_py
from PIL import Image

SVG_PATH = r"C:\Users\jadso\Projetos\reaction\static\favicon.svg"

def convert_svg_to_png(svg_path, output_png_path, size):
    # Renderizar o SVG nativo com resvg_py em alta definição
    png_bytes = resvg_py.svg_to_bytes(svg_path=svg_path, width=size, height=size)
    
    with open(output_png_path, "wb") as out_f:
        out_f.write(png_bytes)
        
    print(f" -> Gerado: {output_png_path} ({size}x{size}px)")

def generate_all_icons():
    print("Gerando ícones PWA e Favicons 100% idênticos a partir de static/favicon.svg via resvg...")
    
    # 512x512 PNG (PWA / Windows Desktop / Android)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-512.png", 512)
    
    # 192x192 PNG (PWA / Android Launcher)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-192.png", 192)
    
    # 180x180 PNG (Apple Touch Icon)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\apple-touch-icon.png", 180)
    
    # 64x64 PNG (Favicon PNG)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\favicon.png", 64)
    
    # Favicon.ico
    img_64 = Image.open(r"C:\Users\jadso\Projetos\reaction\static\favicon.png")
    img_64.save(r"C:\Users\jadso\Projetos\reaction\static\favicon.ico", format="ICO", sizes=[(64, 64), (32, 32), (16, 16)])
    print(" -> Gerado: static/favicon.ico")
    
    # Atualizar também na pasta de versão v0.0.0 se existir
    dest_v0 = r"C:\Users\jadso\Projetos\reaction\versoes\reaction_v0.0.0\static"
    if os.path.exists(dest_v0):
        for fname in ["icon-512.png", "icon-192.png", "apple-touch-icon.png", "favicon.png", "favicon.ico"]:
            src = os.path.join(r"C:\Users\jadso\Projetos\reaction\static", fname)
            dst = os.path.join(dest_v0, fname)
            if os.path.exists(src):
                import shutil
                shutil.copy2(src, dst)
        print(" -> Copiado para a pasta versoes/reaction_v0.0.0/static")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA foram regerados com 100% de fidelidade ao SVG original!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com a Conta do Google (Google SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('g_token') or dados.get('access_token')
    google_email = dados.get('email')
    google_nome = dados.get('nome') or dados.get('name')
    google_sub = dados.get('sub') or dados.get('google_id')
    
    if google_token:
        try:
            import urllib.request
            import json
            req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
            with urllib.request.urlopen(req_url, timeout=5) as resp:
                token_data = json.loads(resp.read().decode('utf-8'))
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture') or dados.get('picture')
        except Exception as err_tkn:
            print(f"Aviso ao validar token Google: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Google não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Google requer credenciais válidas do provedor. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com a Conta do Facebook (Facebook SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    fb_email = dados.get('email')
    fb_nome = dados.get('nome') or dados.get('name')
    fb_sub = dados.get('sub') or dados.get('facebook_id')

    if access_token and not fb_email:
        try:
            import requests as req
            res = req.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
            if res.status_code == 200:
                info = res.json()
                fb_id = info.get('id')
                if info.get('email'):
                    fb_email = info.get('email')
                elif fb_id:
                    fb_email = f"fb_{fb_id}@facebook.user"
                if info.get('name'):
                    fb_nome = info.get('name')
                fb_sub = fb_id
                fb_picture = info.get('picture', {}).get('data', {}).get('url') if isinstance(info.get('picture'), dict) else None
        except Exception as e:
            print(f"Erro ao verificar token Facebook: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Facebook não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Facebook requer credenciais válidas do provedor. Por favor, tente novamente.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        fb_picture = locals().get('fb_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com a Conta da Microsoft (Microsoft SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    ms_email = dados.get('email')
    ms_nome = dados.get('nome') or dados.get('name')
    ms_sub = dados.get('sub') or dados.get('microsoft_id')

    if access_token and not ms_email:
        # Tentar decodificação direta de Token JWT (IdToken / AccessToken MSAL)
        try:
            parts = access_token.split('.')
            if len(parts) == 3:
                import base64, json
                padding = '=' * (4 - len(parts[1]) % 4)
                jwt_payload = json.loads(base64.b64decode(parts[1] + padding).decode('utf-8'))
                ms_email = jwt_payload.get('preferred_username') or jwt_payload.get('email') or jwt_payload.get('upn') or jwt_payload.get('unique_name')
                ms_nome = jwt_payload.get('name') or jwt_payload.get('given_name') or (ms_email.split('@')[0] if ms_email else None)
                ms_sub = jwt_payload.get('sub') or jwt_payload.get('oid')
                if ms_email:
                    ms_picture = f'https://unavatar.io/{ms_email}'
        except Exception as jwt_err:
            print(f"Aviso ao decodificar JWT Microsoft: {jwt_err}")

        if not ms_email:
            try:
                import requests as req
                headers = {'Authorization': f'Bearer {access_token}'}
                res = req.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
                if res.status_code == 200:
                    info = res.json()
                    ms_email = info.get('mail') or info.get('userPrincipalName')
                    ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
                    ms_sub = info.get('id')
                    ms_picture = f'https://unavatar.io/{ms_email}' if ms_email else None
            except Exception as e:
                print(f"Erro no Graph API Microsoft: {e}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail da Microsoft não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com a Microsoft requer credenciais válidas do provedor. Por favor, tente novamente.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        ms_picture = locals().get('ms_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com a Conta da Apple (Apple SSO)"""
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    apple_email = request.values.get('email')
    apple_nome = request.values.get('nome') or request.values.get('name')
    apple_sub = request.values.get('sub') or request.values.get('apple_id')

    if not apple_email:
        return render_template('login.html', erro="Autenticação com a Apple requer credenciais válidas do provedor. Por favor, tente novamente.")

    conn = get_db_connection()
    if not conn:
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome,
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome, nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome,
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        return redirect(url_for('app_dashboard'))

    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            msg_sucesso = f"Instruções e token enviados para <strong>{email}</strong>! <a href='/redefinir_senha?token={token}' class='underline font-bold hover:text-emerald-800'>Clique aqui para cadastrar a nova senha</a>."
            return render_template('login.html', sucesso=msg_sucesso)
        else:
            return render_template('login.html', erro="Nenhuma conta encontrada com este e-mail.")
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint para gerar massa de dados demonstrativa no app"""
    try:
        from gerar_massa_dados import popular_banco_dados
        res = popular_banco_dados()
        return jsonify({"success": True, "message": "Massa de dados gerada com sucesso!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.env
==================================================

DB_HOST=srv1722.hstgr.io
DB_USER=u716503964_reaction
DB_PASS="******"
DB_NAME=u716503964_reaction
FLASK_SECRET_KEY=chave_secreta_reacao_2026
GEMINI_API_KEY="AIzaSy_REDACTED_SECRET"

# Autenticação Social OAuth (Compartilhada com o Cash)
GOOGLE_CLIENT_ID="71269651978-gp165jo1i5r6mgmb22u8s82g0jsdh5v0.apps.googleusercontent.com"
MICROSOFT_CLIENT_ID="138269ce-38e6-4c1e-bc6a-b5292e877a24"
FACEBOOK_APP_ID="2263040147842797"


==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
*.pyc


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
import resvg_py
from PIL import Image

SVG_PATH = r"C:\Users\jadso\Projetos\reaction\static\favicon.svg"

def convert_svg_to_png(svg_path, output_png_path, size):
    # Renderizar o SVG nativo com resvg_py em alta definição
    png_bytes = resvg_py.svg_to_bytes(svg_path=svg_path, width=size, height=size)
    
    with open(output_png_path, "wb") as out_f:
        out_f.write(png_bytes)
        
    print(f" -> Gerado: {output_png_path} ({size}x{size}px)")

def generate_all_icons():
    print("Gerando ícones PWA e Favicons 100% idênticos a partir de static/favicon.svg via resvg...")
    
    # 512x512 PNG (PWA / Windows Desktop / Android)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-512.png", 512)
    
    # 192x192 PNG (PWA / Android Launcher)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-192.png", 192)
    
    # 180x180 PNG (Apple Touch Icon)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\apple-touch-icon.png", 180)
    
    # 64x64 PNG (Favicon PNG)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\favicon.png", 64)
    
    # Favicon.ico
    img_64 = Image.open(r"C:\Users\jadso\Projetos\reaction\static\favicon.png")
    img_64.save(r"C:\Users\jadso\Projetos\reaction\static\favicon.ico", format="ICO", sizes=[(64, 64), (32, 32), (16, 16)])
    print(" -> Gerado: static/favicon.ico")
    
    # Atualizar também na pasta de versão v0.0.0 se existir
    dest_v0 = r"C:\Users\jadso\Projetos\reaction\versoes\reaction_v0.0.0\static"
    if os.path.exists(dest_v0):
        for fname in ["icon-512.png", "icon-192.png", "apple-touch-icon.png", "favicon.png", "favicon.ico"]:
            src = os.path.join(r"C:\Users\jadso\Projetos\reaction\static", fname)
            dst = os.path.join(dest_v0, fname)
            if os.path.exists(src):
                import shutil
                shutil.copy2(src, dst)
        print(" -> Copiado para a pasta versoes/reaction_v0.0.0/static")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA foram regerados com 100% de fidelidade ao SVG original!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com a Conta do Google (Google SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('g_token') or dados.get('access_token')
    google_email = dados.get('email')
    google_nome = dados.get('nome') or dados.get('name')
    google_sub = dados.get('sub') or dados.get('google_id')
    
    if google_token:
        try:
            import urllib.request
            import json
            req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
            with urllib.request.urlopen(req_url, timeout=5) as resp:
                token_data = json.loads(resp.read().decode('utf-8'))
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture') or dados.get('picture')
        except Exception as err_tkn:
            print(f"Aviso ao validar token Google: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Google não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Google requer credenciais válidas do provedor. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com a Conta do Facebook (Facebook SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    fb_email = dados.get('email')
    fb_nome = dados.get('nome') or dados.get('name')
    fb_sub = dados.get('sub') or dados.get('facebook_id')

    if access_token and not fb_email:
        try:
            import requests as req
            res = req.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
            if res.status_code == 200:
                info = res.json()
                fb_id = info.get('id')
                if info.get('email'):
                    fb_email = info.get('email')
                elif fb_id:
                    fb_email = f"fb_{fb_id}@facebook.user"
                if info.get('name'):
                    fb_nome = info.get('name')
                fb_sub = fb_id
                fb_picture = info.get('picture', {}).get('data', {}).get('url') if isinstance(info.get('picture'), dict) else None
        except Exception as e:
            print(f"Erro ao verificar token Facebook: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Facebook não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Facebook requer credenciais válidas do provedor. Por favor, tente novamente.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        fb_picture = locals().get('fb_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com a Conta da Microsoft (Microsoft SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    ms_email = dados.get('email')
    ms_nome = dados.get('nome') or dados.get('name')
    ms_sub = dados.get('sub') or dados.get('microsoft_id')

    if access_token and not ms_email:
        try:
            import requests as req
            headers = {'Authorization': f'Bearer {access_token}'}
            res = req.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
            if res.status_code == 200:
                info = res.json()
                ms_email = info.get('mail') or info.get('userPrincipalName')
                ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
                ms_sub = info.get('id')
                ms_picture = f'https://unavatar.io/{ms_email}' if ms_email else None
        except Exception as e:
            print(f"Erro no Graph API Microsoft: {e}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail da Microsoft não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com a Microsoft requer credenciais válidas do provedor. Por favor, tente novamente.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        ms_picture = locals().get('ms_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com a Conta da Apple (Apple SSO)"""
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    apple_email = request.values.get('email')
    apple_nome = request.values.get('nome') or request.values.get('name')
    apple_sub = request.values.get('sub') or request.values.get('apple_id')

    if not apple_email:
        return render_template('login.html', erro="Autenticação com a Apple requer credenciais válidas do provedor. Por favor, tente novamente.")

    conn = get_db_connection()
    if not conn:
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome,
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome, nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome,
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        return redirect(url_for('app_dashboard'))

    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            msg_sucesso = f"Instruções e token enviados para <strong>{email}</strong>! <a href='/redefinir_senha?token={token}' class='underline font-bold hover:text-emerald-800'>Clique aqui para cadastrar a nova senha</a>."
            return render_template('login.html', sucesso=msg_sucesso)
        else:
            return render_template('login.html', erro="Nenhuma conta encontrada com este e-mail.")
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint para gerar massa de dados demonstrativa no app"""
    try:
        from gerar_massa_dados import popular_banco_dados
        res = popular_banco_dados()
        return jsonify({"success": True, "message": "Massa de dados gerada com sucesso!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.env
==================================================

DB_HOST=srv1722.hstgr.io
DB_USER=u716503964_reaction
DB_PASS="******"
DB_NAME=u716503964_reaction
FLASK_SECRET_KEY=chave_secreta_reacao_2026
GEMINI_API_KEY="AIzaSy_REDACTED_SECRET"

# Autenticação Social OAuth (Compartilhada com o Cash)
GOOGLE_CLIENT_ID="71269651978-gp165jo1i5r6mgmb22u8s82g0jsdh5v0.apps.googleusercontent.com"
MICROSOFT_CLIENT_ID="138269ce-38e6-4c1e-bc6a-b5292e877a24"
FACEBOOK_APP_ID="2263040147842797"


==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
*.pyc


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
import resvg_py
from PIL import Image

SVG_PATH = r"C:\Users\jadso\Projetos\reaction\static\favicon.svg"

def convert_svg_to_png(svg_path, output_png_path, size):
    # Renderizar o SVG nativo com resvg_py em alta definição
    png_bytes = resvg_py.svg_to_bytes(svg_path=svg_path, width=size, height=size)
    
    with open(output_png_path, "wb") as out_f:
        out_f.write(png_bytes)
        
    print(f" -> Gerado: {output_png_path} ({size}x{size}px)")

def generate_all_icons():
    print("Gerando ícones PWA e Favicons 100% idênticos a partir de static/favicon.svg via resvg...")
    
    # 512x512 PNG (PWA / Windows Desktop / Android)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-512.png", 512)
    
    # 192x192 PNG (PWA / Android Launcher)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-192.png", 192)
    
    # 180x180 PNG (Apple Touch Icon)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\apple-touch-icon.png", 180)
    
    # 64x64 PNG (Favicon PNG)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\favicon.png", 64)
    
    # Favicon.ico
    img_64 = Image.open(r"C:\Users\jadso\Projetos\reaction\static\favicon.png")
    img_64.save(r"C:\Users\jadso\Projetos\reaction\static\favicon.ico", format="ICO", sizes=[(64, 64), (32, 32), (16, 16)])
    print(" -> Gerado: static/favicon.ico")
    
    # Atualizar também na pasta de versão v0.0.0 se existir
    dest_v0 = r"C:\Users\jadso\Projetos\reaction\versoes\reaction_v0.0.0\static"
    if os.path.exists(dest_v0):
        for fname in ["icon-512.png", "icon-192.png", "apple-touch-icon.png", "favicon.png", "favicon.ico"]:
            src = os.path.join(r"C:\Users\jadso\Projetos\reaction\static", fname)
            dst = os.path.join(dest_v0, fname)
            if os.path.exists(src):
                import shutil
                shutil.copy2(src, dst)
        print(" -> Copiado para a pasta versoes/reaction_v0.0.0/static")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA foram regerados com 100% de fidelidade ao SVG original!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com a Conta do Google (Google SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('g_token') or dados.get('access_token')
    google_email = dados.get('email')
    google_nome = dados.get('nome') or dados.get('name')
    google_sub = dados.get('sub') or dados.get('google_id')
    
    if google_token:
        try:
            import urllib.request
            import json
            req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
            with urllib.request.urlopen(req_url, timeout=5) as resp:
                token_data = json.loads(resp.read().decode('utf-8'))
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture') or dados.get('picture')
        except Exception as err_tkn:
            print(f"Aviso ao validar token Google: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Google não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Google requer credenciais válidas do provedor. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com a Conta do Facebook (Facebook SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    fb_email = dados.get('email')
    fb_nome = dados.get('nome') or dados.get('name')
    fb_sub = dados.get('sub') or dados.get('facebook_id')

    if access_token and not fb_email:
        try:
            import requests as req
            res = req.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
            if res.status_code == 200:
                info = res.json()
                fb_id = info.get('id')
                if info.get('email'):
                    fb_email = info.get('email')
                elif fb_id:
                    fb_email = f"fb_{fb_id}@facebook.user"
                if info.get('name'):
                    fb_nome = info.get('name')
                fb_sub = fb_id
                fb_picture = info.get('picture', {}).get('data', {}).get('url') if isinstance(info.get('picture'), dict) else None
        except Exception as e:
            print(f"Erro ao verificar token Facebook: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Facebook não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Facebook requer credenciais válidas do provedor. Por favor, tente novamente.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        fb_picture = locals().get('fb_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com a Conta da Microsoft (Microsoft SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    ms_email = dados.get('email')
    ms_nome = dados.get('nome') or dados.get('name')
    ms_sub = dados.get('sub') or dados.get('microsoft_id')

    if access_token and not ms_email:
        try:
            import requests as req
            headers = {'Authorization': f'Bearer {access_token}'}
            res = req.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
            if res.status_code == 200:
                info = res.json()
                ms_email = info.get('mail') or info.get('userPrincipalName')
                ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
                ms_sub = info.get('id')
                ms_picture = f'https://unavatar.io/{ms_email}' if ms_email else None
        except Exception as e:
            print(f"Erro no Graph API Microsoft: {e}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail da Microsoft não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com a Microsoft requer credenciais válidas do provedor. Por favor, tente novamente.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        ms_picture = locals().get('ms_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com a Conta da Apple (Apple SSO)"""
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    apple_email = request.values.get('email')
    apple_nome = request.values.get('nome') or request.values.get('name')
    apple_sub = request.values.get('sub') or request.values.get('apple_id')

    if not apple_email:
        return render_template('login.html', erro="Autenticação com a Apple requer credenciais válidas do provedor. Por favor, tente novamente.")

    conn = get_db_connection()
    if not conn:
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome,
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome, nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome,
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        return redirect(url_for('app_dashboard'))

    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            msg_sucesso = f"Instruções e token enviados para <strong>{email}</strong>! <a href='/redefinir_senha?token={token}' class='underline font-bold hover:text-emerald-800'>Clique aqui para cadastrar a nova senha</a>."
            return render_template('login.html', sucesso=msg_sucesso)
        else:
            return render_template('login.html', erro="Nenhuma conta encontrada com este e-mail.")
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint para gerar massa de dados demonstrativa no app"""
    try:
        from gerar_massa_dados import popular_banco_dados
        res = popular_banco_dados()
        return jsonify({"success": True, "message": "Massa de dados gerada com sucesso!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\reaction_snapshot_v0.0.4.txt
==================================================

==================================================
ARQUIVO: .\.env
==================================================

DB_HOST=srv1722.hstgr.io
DB_USER=u716503964_reaction
DB_PASS="******"
DB_NAME=u716503964_reaction
FLASK_SECRET_KEY=chave_secreta_reacao_2026
GEMINI_API_KEY="AIzaSy_REDACTED_SECRET"

# Autenticação Social OAuth (Compartilhada com o Cash)
GOOGLE_CLIENT_ID="71269651978-gp165jo1i5r6mgmb22u8s82g0jsdh5v0.apps.googleusercontent.com"
MICROSOFT_CLIENT_ID="138269ce-38e6-4c1e-bc6a-b5292e877a24"
FACEBOOK_APP_ID="2263040147842797"


==================================================
ARQUIVO: .\.gitignore
==================================================

__pycache__/
*.log
.env
venv/
*.pyc


==================================================
ARQUIVO: .\agente_reputacao.py
==================================================

import os
import json
from google import genai
from google.genai import types
from pydantic import BaseModel, Field
from dotenv import load_dotenv

# O pacote python-dotenv é obrigatório para extrair as chaves do arquivo oculto .env
load_dotenv()
CHAVE_API = os.environ.get("GEMINI_API_KEY")

if not CHAVE_API:
    print("Aviso: GEMINI_API_KEY não encontrada no .env")

# Definição do schema de saída estruturada para garantir a consistência dos dados do SaaS
class AnaliseAvaliacao(BaseModel):
    sentimento: str = Field(description="Sentimento da avaliação: 'positivo', 'neutro' ou 'negativo'")
    sugestao_resposta: str = Field(description="Rascunho de resposta humanizado, empático e adequado. Sem aspas adicionais.")
    tags: list[str] = Field(description="Lista de até 3 tags curtas sobre o motivo da avaliação (ex: 'atendimento lento', 'comida fria', 'elogio', 'preço alto')")

def analisar_avaliacao_gemini(nome_cliente, nota, comentario, regras_tom_voz, telefone_empresa):
    """
    Função core que envia o feedback para o Gemini e retorna o sentimento, a resposta gerada e as tags.
    Utiliza saída estruturada para evitar erros de parseamento de JSON e garantir a geração de tags.
    """
    if not comentario or comentario.strip() == "":
        comentario = "(O cliente apenas atribuiu a nota, sem deixar comentário escrito.)"

    prompt_mestre = f"""
    Você é a inteligência por trás do 'Reaction', um gestor de reputação que atua como guardião da marca e impulsionador orgânico de vendas.
    Sua tarefa é analisar a avaliação de um cliente, classificar o sentimento, gerar as tags de tópicos abordados e criar um rascunho de resposta adequado.

    DADOS DA AVALIAÇÃO:
    - Nome do Cliente: {nome_cliente}
    - Nota (1 a 5 estrelas): {nota}
    - Comentário: "{comentario}"

    SALA DE MÁQUINAS (TOM DE VOZ E REGRAS DO NEGÓCIO):
    {regras_tom_voz}

    DIRETRIZES DE EXECUÇÃO:
    1. Elogios (4 ou 5 estrelas): Gere um agradecimento humanizado e simpático.
    2. Críticas/Dúvidas (1, 2 ou 3 estrelas): Atue na contenção de crise autônoma. Nunca seja defensivo. Você DEVE obrigatoriamente incluir um CTA levando a conversa para o ambiente privado através deste link de WhatsApp: wa.me/{telefone_empresa}
    """

    try:
        # Nova estrutura da SDK do Google (genai.Client)
        client = genai.Client(api_key=CHAVE_API)
        
        # Chamada com o modelo ultra-rápido gemini-2.5-flash e saída JSON estruturada
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt_mestre,
            config=types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=AnaliseAvaliacao,
            ),
        )
        
        # O retorno é garantido como um JSON válido que respeita a estrutura da classe AnaliseAvaliacao
        resultado_json = json.loads(response.text)
        return resultado_json
        
    except Exception as e:
        print(f"Erro na comunicação com o Gemini ou parseamento: {e}")
        # Fallback seguro em caso de qualquer falha
        sentimento = "positivo" if int(nota) >= 4 else "negativo" if int(nota) <= 3 else "neutro"
        if sentimento == "negativo":
            sugestao = f"Olá, {nome_cliente}. Sentimos muito pela sua experiência e queremos resolver isso. Por favor, entre em contato no wa.me/{telefone_empresa}"
        else:
            sugestao = f"Obrigado pelo seu feedback, {nome_cliente}!"
            
        return {
            "sentimento": sentimento,
            "sugestao_resposta": sugestao,
            "tags": ["Instabilidade de API"]
        }


==================================================
ARQUIVO: .\gerar_icones_pwa.py
==================================================

import os
import resvg_py
from PIL import Image

SVG_PATH = r"C:\Users\jadso\Projetos\reaction\static\favicon.svg"

def convert_svg_to_png(svg_path, output_png_path, size):
    # Renderizar o SVG nativo com resvg_py em alta definição
    png_bytes = resvg_py.svg_to_bytes(svg_path=svg_path, width=size, height=size)
    
    with open(output_png_path, "wb") as out_f:
        out_f.write(png_bytes)
        
    print(f" -> Gerado: {output_png_path} ({size}x{size}px)")

def generate_all_icons():
    print("Gerando ícones PWA e Favicons 100% idênticos a partir de static/favicon.svg via resvg...")
    
    # 512x512 PNG (PWA / Windows Desktop / Android)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-512.png", 512)
    
    # 192x192 PNG (PWA / Android Launcher)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\icon-192.png", 192)
    
    # 180x180 PNG (Apple Touch Icon)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\apple-touch-icon.png", 180)
    
    # 64x64 PNG (Favicon PNG)
    convert_svg_to_png(SVG_PATH, r"C:\Users\jadso\Projetos\reaction\static\favicon.png", 64)
    
    # Favicon.ico
    img_64 = Image.open(r"C:\Users\jadso\Projetos\reaction\static\favicon.png")
    img_64.save(r"C:\Users\jadso\Projetos\reaction\static\favicon.ico", format="ICO", sizes=[(64, 64), (32, 32), (16, 16)])
    print(" -> Gerado: static/favicon.ico")
    
    # Atualizar também na pasta de versão v0.0.0 se existir
    dest_v0 = r"C:\Users\jadso\Projetos\reaction\versoes\reaction_v0.0.0\static"
    if os.path.exists(dest_v0):
        for fname in ["icon-512.png", "icon-192.png", "apple-touch-icon.png", "favicon.png", "favicon.ico"]:
            src = os.path.join(r"C:\Users\jadso\Projetos\reaction\static", fname)
            dst = os.path.join(dest_v0, fname)
            if os.path.exists(src):
                import shutil
                shutil.copy2(src, dst)
        print(" -> Copiado para a pasta versoes/reaction_v0.0.0/static")

    print("\n[SUCESSO PERFEITO] Todos os ícones PWA foram regerados com 100% de fidelidade ao SVG original!")

if __name__ == '__main__':
    generate_all_icons()



==================================================
ARQUIVO: .\gerar_massa_dados.py
==================================================

import os
import json
import mysql.connector
from dotenv import load_dotenv

load_dotenv()

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

def popular_banco_dados(empresa_id=1):
    conn = mysql.connector.connect(
        host=DB_HOST,
        user=DB_USER,
        password=DB_PASSWORD,
        database=DB_NAME
    )
    cursor = conn.cursor()

    try:
        # 1. Configurações da IA na Sala de Máquinas
        cursor.execute("""
            INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp)
            VALUES (%s, 'Profissional e empático', 'Sempre agradecer elogios destacando os pratos da casa. Em avaliações nota 1 a 3, pedir desculpas e levar a conversa imediatamente para o WhatsApp de suporte.', '5585999999999')
            ON DUPLICATE KEY UPDATE
            tom_voz = VALUES(tom_voz),
            regras_ouro = VALUES(regras_ouro),
            telefone_whatsapp = VALUES(telefone_whatsapp)
        """, (empresa_id,))

        # 2. Integrações de Canais Ativos
        plataformas = ['google', 'instagram', 'ifood', 'whatsapp', 'ze_delivery', '99food']
        for plat in plataformas:
            cursor.execute("""
                INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status)
                VALUES (%s, %s, 'demo_token_ativo', 'ativo')
                ON DUPLICATE KEY UPDATE status = 'ativo'
            """, (empresa_id, plat))

        # 3. Limpar avaliações antigas da empresa de teste para evitar duplicados idênticos em testes repetidos
        cursor.execute("DELETE FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
        cursor.execute("DELETE FROM acoes WHERE empresa_id = %s", (empresa_id,))

        # 4. Avaliações Demonstrativas (Massa de Dados Rica)
        avaliacoes = [
            {
                "plataforma": "google",
                "nome_cliente": "Mariana Souza",
                "nota": 5,
                "comentario": "Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.",
                "sentimento": "positivo",
                "rascunho": "Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!",
                "tags": ["atendimento excelente", "comida quente", "fidelização"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Carlos Eduardo",
                "nota": 1,
                "comentario": "O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!",
                "sentimento": "negativo",
                "rascunho": "Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999",
                "tags": ["atraso na entrega", "comida fria", "crise"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@beatriz_lima",
                "nota": 5,
                "comentario": "Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨",
                "sentimento": "positivo",
                "rascunho": "Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!",
                "tags": ["ambiente agradável", "prato delicioso", "instagram"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ze_delivery",
                "nome_cliente": "Rodrigo Fonseca",
                "nota": 5,
                "comentario": "Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.",
                "sentimento": "positivo",
                "rascunho": "Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!",
                "tags": ["cerveja gelada", "entrega ultra rápida", "zé delivery"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "99food",
                "nome_cliente": "Vanessa Castro",
                "nota": 2,
                "comentario": "O entregador do 99Food deixou a bebida vazar na sacola do lanche.",
                "sentimento": "negativo",
                "rascunho": "Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.",
                "tags": ["bebida vazou", "embalagem danificada", "99food"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Fernanda Ribeiro",
                "nota": 2,
                "comentario": "Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.",
                "sentimento": "negativo",
                "rascunho": "Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.",
                "tags": ["item faltante", "falha no suporte"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Roberto Alves",
                "nota": 4,
                "comentario": "Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!",
                "sentimento": "positivo",
                "rascunho": "Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!",
                "tags": ["comida excelente", "feedback sobremesa"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Juliana Mendes",
                "nota": 5,
                "comentario": "Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.",
                "sentimento": "positivo",
                "rascunho": "Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!",
                "tags": ["aniversário", "atendimento impecável"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "ifood",
                "nome_cliente": "Lucas Pinheiro",
                "nota": 3,
                "comentario": "A comida é boa mas a embalagem veio meio amassada.",
                "sentimento": "neutro",
                "rascunho": "Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.",
                "tags": ["embalagem amassada", "comida saborosa"],
                "status": "alerta_crise"
            },
            {
                "plataforma": "instagram",
                "nome_cliente": "@thiago_gastronomia",
                "nota": 5,
                "comentario": "Um dos melhores pontos de carne da cidade! Recomendadíssimo.",
                "sentimento": "positivo",
                "rascunho": "Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!",
                "tags": ["ponto da carne", "recomendado"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "google",
                "nome_cliente": "Camila Martins",
                "nota": 5,
                "comentario": "Preço justo, porção bem servida e atendimento rápido.",
                "sentimento": "positivo",
                "rascunho": "Obrigado Camila! É sempre um prazer servir bem e com qualidade!",
                "tags": ["preço justo", "porção servida", "rápido"],
                "status": "respondido_ia"
            },
            {
                "plataforma": "whatsapp",
                "nome_cliente": "Gabriel Torres",
                "nota": 1,
                "comentario": "Cobraram um valor diferente no cartão do que estava no cardápio.",
                "sentimento": "negativo",
                "rascunho": "Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.",
                "tags": ["erro de cobrança", "crise urgente"],
                "status": "alerta_crise"
            }
        ]

        for av in avaliacoes:
            cursor.execute("""
                INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, sentimento, rascunho_resposta, tags, status)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (empresa_id, av['plataforma'], av['nome_cliente'], av['nota'], av['comentario'], av['sentimento'], av['rascunho'], json.dumps(av['tags']), av['status']))

        # 5. Ações Demonstrativas em Minhas Ações
        acoes = [
            {"titulo": "Contenção de Crise: Atendimento ao cliente Carlos Eduardo (Pedido iFood frio)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Estornar item faltante da cliente Fernanda Ribeiro (iFood)", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Verificar divergência de cobrança no cartão com o cliente Gabriel Torres", "prioridade": "critical", "prazo": "2026-07-26", "status": "pendente"},
            {"titulo": "Reunião com equipe de logística sobre embalagens amassadas", "prioridade": "normal", "prazo": "2026-07-28", "status": "pendente"},
            {"titulo": "Enviar cupom de agradecimento para cliente VIP Mariana Souza", "prioridade": "normal", "prazo": "2026-07-30", "status": "concluido"}
        ]

        for ac in acoes:
            cursor.execute("""
                INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status)
                VALUES (%s, %s, %s, %s, %s)
            """, (empresa_id, ac['titulo'], ac['prioridade'], ac['prazo'], ac['status']))

        conn.commit()
        return {
            "avaliacoes_inseridas": len(avaliacoes),
            "acoes_inseridas": len(acoes),
            "integracoes_ativas": len(plataformas)
        }

    except Exception as e:
        conn.rollback()
        raise e
    finally:
        cursor.close()
        conn.close()

if __name__ == '__main__':
    print("Gerando massa de dados demonstrativa no banco...")
    resultado = popular_banco_dados()
    print(f"Sucesso! {resultado['avaliacoes_inseridas']} avaliações e {resultado['acoes_inseridas']} ações inseridas.")



==================================================
ARQUIVO: .\main.py
==================================================

import os
import json
import re
import secrets
import mysql.connector
import requests
import io
import csv
from flask import Flask, render_template, redirect, url_for, session, request, jsonify, Response
from datetime import datetime, date, timedelta
from dotenv import load_dotenv
from werkzeug.security import generate_password_hash, check_password_hash
from flask_cors import CORS

# Assumindo que o ficheiro da IA continua a chamar-se agente_reputacao.py
from agente_reputacao import analisar_avaliacao_gemini
from servico_email import enviar_email_boas_vindas, enviar_notificacao_admin, enviar_email_redefinicao_senha

load_dotenv()

GOOGLE_CLIENT_ID = os.environ.get("GOOGLE_CLIENT_ID")
GOOGLE_CLIENT_SECRET = "******"
MICROSOFT_CLIENT_ID = os.environ.get("MICROSOFT_CLIENT_ID")
FACEBOOK_APP_ID = os.environ.get("FACEBOOK_APP_ID") or os.environ.get("META_APP_ID")
META_APP_ID = FACEBOOK_APP_ID
META_APP_SECRET = os.environ.get("META_APP_SECRET")
META_VERIFY_TOKEN = os.environ.get("META_VERIFY_TOKEN", "frameia_reaction_2026")

app = Flask(__name__)

@app.context_processor
def inject_oauth_credentials():
    return dict(
        google_client_id=GOOGLE_CLIENT_ID or '',
        facebook_app_id=FACEBOOK_APP_ID or '',
        microsoft_client_id=MICROSOFT_CLIENT_ID or ''
    )

# Chave de sessão estável para evitar desconexões quando o servidor reinicia
app.secret_key = os.environ.get("FLASK_SECRET_KEY", "chave_super_secreta_reaction_2026")

# CORS adaptativo para permitir desenvolvimento local e domínio oficial de produção
cors_origins = os.environ.get("CORS_ORIGINS", "https://reaction.frameia.com.br").split(",")
if app.debug or os.environ.get("FLASK_ENV") == "development":
    # Permite localhost e 127.0.0.1 nas portas comuns
    cors_origins.extend([
        "http://localhost:5000", "http://127.0.0.1:5000",
        "http://localhost:5001", "http://127.0.0.1:5001"
    ])
CORS(app, resources={r"/*": {"origins": cors_origins}})

DB_HOST = os.environ.get("DB_HOST", "localhost")
DB_USER = os.environ.get("DB_USER", "root")
DB_PASSWORD = os.environ.get("DB_PASS", "")
DB_NAME = os.environ.get("DB_NAME", "reaction_db")

# ================= BANCO DE DADOS =================
def get_db_connection():
    try:
        conn = mysql.connector.connect(
            host=DB_HOST,
            user=DB_USER,
            password=DB_PASSWORD,
            database=DB_NAME
        )
        return conn
    except mysql.connector.Error as err:
        if err.errno == mysql.connector.errorcode.ER_BAD_DB_ERROR:
            try:
                conn = mysql.connector.connect(
                    host=DB_HOST,
                    user=DB_USER,
                    password=DB_PASSWORD
                )
                cursor = conn.cursor()
                cursor.execute(f"CREATE DATABASE {DB_NAME}")
                conn.database = DB_NAME
                cursor.close()
                return conn
            except mysql.connector.Error as e:
                print(f"Erro ao tentar criar base de dados: {e}")
                return None
        else:
            print(f"Erro ao conectar à base de dados: {err}")
            return None

def criar_tabelas_se_nao_existirem():
    conn = get_db_connection()
    if not conn:
        print("Aviso: Falha ao obter conexão para criar tabelas.")
        return
        
    try:
        cursor = conn.cursor()
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS empresas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome_empresa VARCHAR(255) NOT NULL,
                plano_assinatura VARCHAR(50) DEFAULT 'basico',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                nome VARCHAR(255) NOT NULL,
                email VARCHAR(255) NOT NULL UNIQUE,
                senha VARCHAR(255) NOT NULL,
                role ENUM('dono', 'admin', 'operador') DEFAULT 'operador',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS integracoes_api (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                token_acesso TEXT,
                token_refresh TEXT,
                plataforma_user_id VARCHAR(255) NULL,
                data_expiracao DATETIME,
                status ENUM('ativo', 'inativo', 'erro') DEFAULT 'inativo',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Migração automática de colunas para integracoes_api se a tabela já existia
        colunas_integracoes = {
            'token_refresh': 'TEXT NULL AFTER token_acesso',
            'plataforma_user_id': 'VARCHAR(255) NULL AFTER token_acesso',
            'data_expiracao': 'DATETIME NULL AFTER status'
        }
        for col_nome, col_def in colunas_integracoes.items():
            try:
                cursor.execute(f"SHOW COLUMNS FROM integracoes_api LIKE '{col_nome}'")
                if not cursor.fetchone():
                    cursor.execute(f"ALTER TABLE integracoes_api ADD COLUMN {col_nome} {col_def}")
                    print(f"Coluna {col_nome} adicionada com sucesso na tabela integracoes_api.")
            except Exception as col_err:
                print(f"Erro ao verificar/adicionar coluna {col_nome} em integracoes_api: {col_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS avaliacoes_feed (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                plataforma_origem ENUM('google', 'instagram', 'whatsapp', 'ifood') NOT NULL,
                nome_cliente VARCHAR(255),
                nota INT CHECK (nota >= 1 AND nota <= 5),
                comentario TEXT,
                sentimento VARCHAR(50),
                rascunho_resposta TEXT,
                tags TEXT,
                status ENUM('pendente', 'respondido_ia', 'alerta_crise') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)
        
        # Garantir que a coluna 'tags' existe em 'avaliacoes_feed' se a tabela já existia
        try:
            cursor.execute("SHOW COLUMNS FROM avaliacoes_feed LIKE 'tags'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE avaliacoes_feed ADD COLUMN tags TEXT AFTER rascunho_resposta")
                print("Coluna 'tags' adicionada com sucesso na tabela avaliacoes_feed.")
        except Exception as tags_err:
            print(f"Erro ao verificar/adicionar coluna tags em avaliacoes_feed: {tags_err}")

        # Migração da coluna foto_perfil na tabela usuarios
        try:
            cursor.execute("SHOW COLUMNS FROM usuarios LIKE 'foto_perfil'")
            if not cursor.fetchone():
                cursor.execute("ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(500) DEFAULT NULL")
                print("Coluna 'foto_perfil' adicionada com sucesso na tabela usuarios.")
        except Exception as foto_err:
            print(f"Erro ao adicionar coluna foto_perfil em usuarios: {foto_err}")
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS acoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL,
                criado_por INT,
                titulo VARCHAR(255) NOT NULL,
                prioridade ENUM('normal', 'critical') DEFAULT 'normal',
                prazo DATE,
                status ENUM('pendente', 'concluido') DEFAULT 'pendente',
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE,
                FOREIGN KEY (criado_por) REFERENCES usuarios(id) ON DELETE SET NULL
            )
        """)
        
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS configuracoes_ia (
                id INT AUTO_INCREMENT PRIMARY KEY,
                empresa_id INT NOT NULL UNIQUE,
                tom_voz VARCHAR(255) DEFAULT 'Profissional e empático',
                regras_ouro TEXT,
                telefone_whatsapp VARCHAR(50),
                FOREIGN KEY (empresa_id) REFERENCES empresas(id) ON DELETE CASCADE
            )
        """)

        cursor.execute("INSERT IGNORE INTO empresas (id, nome_empresa) VALUES (1, 'Empresa Teste Reaction')")
        senha_hash = generate_password_hash('123')
        cursor.execute("""
            INSERT INTO usuarios (id, empresa_id, nome, email, senha, role) 
            VALUES (1, 1, 'Jadson', 'admin@teste.com', %s, 'dono')
            ON DUPLICATE KEY UPDATE senha=%s
        """, (senha_hash, senha_hash))

        # Auto-seed massa de dados se a tabela avaliacoes_feed estiver vazia
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = 1")
            row_count = cursor.fetchone()[0]
            if row_count == 0:
                print("Nenhuma avaliação encontrada. Gerando massa de dados demonstrativa automaticamente...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(1)
                print("Massa de dados auto-gerada com sucesso!")
        except Exception as seed_err:
            print(f"Erro ao verificar/auto-gerar massa de dados: {seed_err}")

        conn.commit()
        cursor.close()
        conn.close()
        
    except Exception as e:
        print(f"Erro fatal ao criar tabelas: {e}")

try:
    criar_tabelas_se_nao_existirem()
except Exception as e:
    print(f"Ignorando criação de tabelas: {e}")

# ================= MIDDLEWARES & HELPERS =================

def analisar_qualidade_comentario(comentario, nota):
    """Firewall de Spam embutido para poupar tokens do Gemini"""
    if not comentario or not str(comentario).strip():
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pela avaliação!", "sentimento": "positivo"}
        return {"valido": False, "status": "alerta_crise", "rascunho": "Lamentamos. Fale connosco.", "sentimento": "negativo"}

    texto = str(comentario).lower().strip()
    
    if re.search(r'(http|https)://', texto) or re.search(r'www\.', texto):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM - LINK DETETADO]", "sentimento": "neutro"}
        
    palavras_spam = ['ganhe dinheiro', 'bitcoin', 'jogo do tigrinho', 'fortune tiger', 'seguidores']
    if any(spam in texto for spam in palavras_spam):
        return {"valido": False, "status": "pendente", "rascunho": "[SPAM DETETADO]", "sentimento": "neutro"}

    if len(texto) < 15:
        if int(nota) >= 4:
            return {"valido": False, "status": "respondido_ia", "rascunho": "Obrigado pelo feedback!", "sentimento": "positivo"}
        elif int(nota) <= 3:
            return {"valido": False, "status": "alerta_crise", "rascunho": "Sentimos muito. Chame no canal de atendimento.", "sentimento": "negativo"}

    return {"valido": True}

def get_usuario_logado():
    """Função utilitária para pegar os dados do utilizador logado em todas as rotas"""
    nome = session.get('nome', 'Usuário')
    iniciais = nome[0].upper() if nome else 'U'
    foto_perfil = session.get('foto_perfil', None)
    return {'nome': nome, 'iniciais': iniciais, 'foto_perfil': foto_perfil}

def garantir_massa_dados_empresa():
    """Garante que a empresa logada possui dados demonstrativos populados automaticamente"""
    empresa_id = session.get('empresa_id')
    if not empresa_id: return
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT COUNT(*) FROM avaliacoes_feed WHERE empresa_id = %s", (empresa_id,))
            res = cursor.fetchone()
            total = res[0] if res else 0
            if total == 0:
                print(f"Empresa {empresa_id} sem avaliações. Gerando massa de dados demonstrativa...")
                from gerar_massa_dados import popular_banco_dados
                popular_banco_dados(empresa_id)
        except Exception as e:
            print(f"Erro ao verificar/garantir massa de dados: {e}")
        finally:
            cursor.close()
            conn.close()

# ================= ROTAS DE VIEWS (TELAS DO SAAS) =================

@app.route('/')
@app.route('/index.html')
def index():
    # Rota raiz sempre serve a Landing Page diretamente com headers no-cache
    response = app.make_response(render_template('index.html'))
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response

@app.route('/login', methods=['GET', 'POST'])
def login():
    # Se o usuário já estiver logado, redireciona de conveniência direto para o cockpit
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    if request.method == 'POST':
        email = request.form.get('email', '').strip().lower()
        senha = request.form.get('senha', '')
        
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
            user = cursor.fetchone()
        finally:
            cursor.close()
            conn.close()

        if user and check_password_hash(user['senha'], senha):
            session['usuario_id'] = user['id']
            session['empresa_id'] = user['empresa_id']
            session['nome'] = user['nome']
            return redirect(url_for('app_dashboard'))
        else:
            return render_template('login.html', erro="E-mail ou senha incorretos.")
    
    return render_template('login.html')

@app.route('/cadastro', methods=['GET', 'POST'])
@app.route('/registo', methods=['GET', 'POST'])
def registo():
    if request.method == 'GET':
        return render_template('login.html', mostrar_registo=True)

    nome_empresa = request.form.get('nome_empresa', '').strip()
    nome_usuario = request.form.get('nome_usuario', '').strip()
    email = request.form.get('email', '').strip().lower()
    senha = request.form.get('senha', '')

    if not all([nome_empresa, nome_usuario, email, senha]):
        return render_template('login.html', erro_registo="Todos os campos são obrigatórios.", mostrar_registo=True)

    senha_hash = generate_password_hash(senha)
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro_registo="Erro no servidor.", mostrar_registo=True)

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT id FROM usuarios WHERE email = %s", (email,))
        if cursor.fetchone():
            return render_template('login.html', erro_registo="Este e-mail já está em uso.", mostrar_registo=True)

        novo_usuario_id = None
        empresa_id = None

        cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
        empresa_id = cursor.lastrowid
        cursor.execute("INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')", 
                       (empresa_id, nome_usuario, email, senha_hash))
        novo_usuario_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return render_template('login.html', erro_registo="Erro interno ao criar conta.", mostrar_registo=True)
    finally:
        cursor.close()
        conn.close()

    # Disparar e-mail de boas-vindas ao cliente e notificar os 3 administradores
    try:
        enviar_email_boas_vindas(email, nome_usuario, nome_empresa)
        enviar_notificacao_admin("novo_usuario", {
            "nome_usuario": nome_usuario,
            "email": email,
            "nome_empresa": nome_empresa,
            "plano": "Shield Start (Freemium)",
            "ciclo": "Mensal",
            "valor": "R$ 0,00"
        })
    except Exception as err_mail:
        print(f"Aviso ao enviar e-mails de registo: {err_mail}")

    # Auto-login automático após o registo (Fricção Zero)
    session['usuario_id'] = novo_usuario_id
    session['empresa_id'] = empresa_id
    session['nome'] = nome_usuario
    garantir_massa_dados_empresa()
    return redirect(url_for('app_dashboard'))

@app.route('/login/google', methods=['GET', 'POST'])
@app.route('/api/auth/google', methods=['POST'])
def login_google():
    """Rota de Autenticação / Cadastro com a Conta do Google (Google SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))
        
    dados = request.get_json(silent=True) or request.values or {}
    google_token = dados.get('credential') or dados.get('g_token') or dados.get('access_token')
    google_email = dados.get('email')
    google_nome = dados.get('nome') or dados.get('name')
    google_sub = dados.get('sub') or dados.get('google_id')
    
    if google_token:
        try:
            import urllib.request
            import json
            req_url = f"https://oauth2.googleapis.com/tokeninfo?id_token={google_token}"
            with urllib.request.urlopen(req_url, timeout=5) as resp:
                token_data = json.loads(resp.read().decode('utf-8'))
                google_email = token_data.get('email')
                google_nome = token_data.get('name') or token_data.get('given_name')
                google_sub = token_data.get('sub')
                google_picture = token_data.get('picture') or dados.get('picture')
        except Exception as err_tkn:
            print(f"Aviso ao validar token Google: {err_tkn}")

    if not google_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Google não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Google requer credenciais válidas do provedor. Por favor, tente novamente.")

    google_email = google_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com o banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT google_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN google_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (google_id IS NOT NULL AND google_id = %s)", (google_email, google_sub))
        user = cursor.fetchone()
        
        if not user:
            nome_empresa = f"Empresa de {google_nome or google_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"GoogleSSO_{os.urandom(8).hex()}")
            
            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid
            
            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, google_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria, google_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, google_nome or google_email.split('@')[0], google_email, senha_aleatoria)
                )
                
            usuario_id = cursor.lastrowid
            conn.commit()
            
            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': google_nome or google_email.split('@')[0],
                'email': google_email
            }
            
            try:
                enviar_email_boas_vindas(google_email, google_nome or google_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": google_nome or google_email.split('@')[0],
                    "email": google_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Google SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Google SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Google SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar autenticação com o Google.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Google.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Google.")

@app.route('/login/facebook', methods=['GET', 'POST'])
@app.route('/api/auth/facebook', methods=['POST'])
def login_facebook():
    """Rota de Autenticação / Cadastro com a Conta do Facebook (Facebook SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    fb_email = dados.get('email')
    fb_nome = dados.get('nome') or dados.get('name')
    fb_sub = dados.get('sub') or dados.get('facebook_id')

    if access_token and not fb_email:
        try:
            import requests as req
            res = req.get(f'https://graph.facebook.com/me?fields=id,name,email,picture.type(large)&access_token={access_token}', timeout=5)
            if res.status_code == 200:
                info = res.json()
                fb_id = info.get('id')
                if info.get('email'):
                    fb_email = info.get('email')
                elif fb_id:
                    fb_email = f"fb_{fb_id}@facebook.user"
                if info.get('name'):
                    fb_nome = info.get('name')
                fb_sub = fb_id
                fb_picture = info.get('picture', {}).get('data', {}).get('url') if isinstance(info.get('picture'), dict) else None
        except Exception as e:
            print(f"Erro ao verificar token Facebook: {e}")

    if not fb_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail do Facebook não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com o Facebook requer credenciais válidas do provedor. Por favor, tente novamente.")

    fb_email = fb_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT facebook_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN facebook_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (facebook_id IS NOT NULL AND facebook_id = %s)", (fb_email, fb_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {fb_nome or fb_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"FacebookSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, facebook_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria, fb_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, fb_nome or fb_email.split('@')[0], fb_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': fb_nome or fb_email.split('@')[0],
                'email': fb_email
            }

            try:
                enviar_email_boas_vindas(fb_email, fb_nome or fb_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": fb_nome or fb_email.split('@')[0],
                    "email": fb_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Facebook SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Facebook SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Facebook SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Facebook SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com o Facebook.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        fb_picture = locals().get('fb_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if fb_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (fb_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = fb_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Facebook SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com o Facebook.")

@app.route('/login/microsoft', methods=['GET', 'POST'])
@app.route('/api/auth/microsoft', methods=['POST'])
def login_microsoft():
    """Rota de Autenticação / Cadastro com a Conta da Microsoft (Microsoft SSO)"""
    if 'usuario_id' in session and not request.path.startswith('/api/'):
        return redirect(url_for('app_dashboard'))

    dados = request.get_json(silent=True) or request.values or {}
    access_token = dados.get('access_token') or dados.get('credential') or dados.get('token')
    ms_email = dados.get('email')
    ms_nome = dados.get('nome') or dados.get('name')
    ms_sub = dados.get('sub') or dados.get('microsoft_id')

    if access_token and not ms_email:
        try:
            import requests as req
            headers = {'Authorization': f'Bearer {access_token}'}
            res = req.get('https://graph.microsoft.com/v1.0/me', headers=headers, timeout=5)
            if res.status_code == 200:
                info = res.json()
                ms_email = info.get('mail') or info.get('userPrincipalName')
                ms_nome = info.get('displayName') or (ms_email.split('@')[0] if ms_email else None)
                ms_sub = info.get('id')
                ms_picture = f'https://unavatar.io/{ms_email}' if ms_email else None
        except Exception as e:
            print(f"Erro no Graph API Microsoft: {e}")

    if not ms_email:
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': False, 'erro': 'E-mail da Microsoft não identificado.'}), 400
        return render_template('login.html', erro="Autenticação com a Microsoft requer credenciais válidas do provedor. Por favor, tente novamente.")

    ms_email = ms_email.lower().strip()
    conn = get_db_connection()
    if not conn:
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro de conexão com banco de dados.'}), 500
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT microsoft_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN microsoft_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (microsoft_id IS NOT NULL AND microsoft_id = %s)", (ms_email, ms_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {ms_nome or ms_email.split('@')[0]}"
            senha_aleatoria = generate_password_hash(f"MicrosoftSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, microsoft_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria, ms_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, ms_nome or ms_email.split('@')[0], ms_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': ms_nome or ms_email.split('@')[0],
                'email': ms_email
            }

            try:
                enviar_email_boas_vindas(ms_email, ms_nome or ms_email.split('@')[0], nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": ms_nome or ms_email.split('@')[0],
                    "email": ms_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Microsoft SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Microsoft SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Microsoft SSO: {err_db}")
        if conn: conn.rollback()
        if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Erro ao processar Microsoft SSO.'}), 500
        return render_template('login.html', erro="Erro ao processar a autenticação com a Microsoft.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        ms_picture = locals().get('ms_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if ms_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (ms_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = ms_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Microsoft SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        if request.path.startswith('/api/'):
            return jsonify({'sucesso': True, 'redirect': '/app'})
        return redirect(url_for('app_dashboard'))

    if request.path.startswith('/api/'): return jsonify({'sucesso': False, 'erro': 'Falha ao autenticar.'}), 400
    return render_template('login.html', erro="Falha ao autenticar com a Microsoft.")

@app.route('/login/apple', methods=['GET', 'POST'])
def login_apple():
    """Rota de Autenticação / Cadastro com a Conta da Apple (Apple SSO)"""
    if 'usuario_id' in session:
        return redirect(url_for('app_dashboard'))

    apple_email = request.values.get('email')
    apple_nome = request.values.get('nome') or request.values.get('name')
    apple_sub = request.values.get('sub') or request.values.get('apple_id')

    if not apple_email:
        return render_template('login.html', erro="Autenticação com a Apple requer credenciais válidas do provedor. Por favor, tente novamente.")

    conn = get_db_connection()
    if not conn:
        return render_template('login.html', erro="Erro temporário de conexão com o banco de dados.")

    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT apple_id FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN apple_id VARCHAR(255) DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s OR (apple_id IS NOT NULL AND apple_id = %s)", (apple_email, apple_sub))
        user = cursor.fetchone()

        if not user:
            nome_empresa = f"Empresa de {apple_nome}"
            senha_aleatoria = generate_password_hash(f"AppleSSO_{os.urandom(8).hex()}")

            cursor.execute("INSERT INTO empresas (nome_empresa) VALUES (%s)", (nome_empresa,))
            empresa_id = cursor.lastrowid

            try:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role, apple_id) VALUES (%s, %s, %s, %s, 'dono', %s)",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria, apple_sub)
                )
            except Exception:
                cursor.execute(
                    "INSERT INTO usuarios (empresa_id, nome, email, senha, role) VALUES (%s, %s, %s, %s, 'dono')",
                    (empresa_id, apple_nome, apple_email, senha_aleatoria)
                )

            usuario_id = cursor.lastrowid
            conn.commit()

            user = {
                'id': usuario_id,
                'empresa_id': empresa_id,
                'nome': apple_nome,
                'email': apple_email
            }

            try:
                enviar_email_boas_vindas(apple_email, apple_nome, nome_empresa)
                enviar_notificacao_admin("novo_usuario", {
                    "nome_usuario": apple_nome,
                    "email": apple_email,
                    "nome_empresa": nome_empresa,
                    "plano": "Shield Start (Apple SSO)",
                    "ciclo": "Mensal",
                    "valor": "R$ 0,00"
                })
            except Exception as err_m:
                print(f"Erro no envio de e-mails Apple SSO: {err_m}")

    except Exception as err_db:
        print(f"Erro no processamento do Apple SSO: {err_db}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar a autenticação com a Apple.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

    if user:
        # Atualizar foto do perfil se vier do SSO e usuário não tiver foto salva
        google_picture = locals().get('google_picture') or (dados.get('picture') if isinstance(dados, dict) else None)
        if google_picture and not user.get('foto_perfil'):
            try:
                conn_img = get_db_connection()
                if conn_img:
                    cur_img = conn_img.cursor()
                    cur_img.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (google_picture, user['id']))
                    conn_img.commit()
                    cur_img.close()
                    conn_img.close()
                    user['foto_perfil'] = google_picture
            except Exception as img_err:
                print(f"Erro ao salvar foto Google SSO: {img_err}")

        session['usuario_id'] = user['id']
        session['empresa_id'] = user['empresa_id']
        session['nome'] = user['nome']
        session['foto_perfil'] = user.get('foto_perfil')
        garantir_massa_dados_empresa()
        return redirect(url_for('app_dashboard'))

    return render_template('login.html', erro="Falha ao autenticar com a Apple.")

@app.route('/esqueci_senha', methods=['POST'])
def esqueci_senha():
    email = request.form.get('email', '').strip().lower()
    if not email:
        return render_template('login.html', erro="Por favor, informe o seu e-mail cadastrado.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    user = None
    try:
        try:
            cursor.execute("SELECT reset_token FROM usuarios LIMIT 1")
            cursor.fetchall()
        except Exception:
            try:
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_token VARCHAR(255) DEFAULT NULL")
                cursor.execute("ALTER TABLE usuarios ADD COLUMN reset_expires DATETIME DEFAULT NULL")
                conn.commit()
            except Exception:
                pass

        cursor.execute("SELECT * FROM usuarios WHERE email = %s", (email,))
        user = cursor.fetchone()
        
        if user:
            token = secrets.token_urlsafe(32)
            expiracao_str = (datetime.now() + timedelta(minutes=30)).strftime('%Y-%m-%d %H:%M:%S')
            cursor.execute("UPDATE usuarios SET reset_token = %s, reset_expires = %s WHERE id = %s", (token, expiracao_str, user['id']))
            conn.commit()
            
            reset_link = f"https://reaction.frameia.com.br/redefinir_senha?token={token}"
            enviar_email_redefinicao_senha(email, user['nome'], reset_link)
            msg_sucesso = f"Instruções e token enviados para <strong>{email}</strong>! <a href='/redefinir_senha?token={token}' class='underline font-bold hover:text-emerald-800'>Clique aqui para cadastrar a nova senha</a>."
            return render_template('login.html', sucesso=msg_sucesso)
        else:
            return render_template('login.html', erro="Nenhuma conta encontrada com este e-mail.")
    except Exception as e:
        print(f"Erro no esqueci_senha: {e}")
        if conn: conn.rollback()
        return render_template('login.html', erro="Erro ao processar solicitação de redefinição.")
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/redefinir_senha', methods=['GET', 'POST'])
def redefinir_senha():
    agora_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    if request.method == 'POST':
        token = request.form.get('token', '').strip()
        nova_senha = request.form.get('senha', '')
        
        if not token or not nova_senha:
            return render_template('login.html', erro="Token e nova palavra-passe são obrigatórios.")
            
        conn = get_db_connection()
        if not conn: return render_template('login.html', erro="Erro no servidor.")
            
        cursor = conn.cursor(dictionary=True)
        try:
            cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
            user = cursor.fetchone()
            
            if not user:
                return render_template('login.html', erro="O token de redefinição é inválido ou já expirou. Solicite um novo link.")
                
            senha_hash = generate_password_hash(nova_senha)
            cursor.execute("UPDATE usuarios SET senha = %s, reset_token = NULL, reset_expires = NULL WHERE id = %s", (senha_hash, user['id']))
            conn.commit()
            return render_template('login.html', sucesso="Sua palavra-passe foi redefinida com sucesso! Faça login abaixo.")
        except Exception as e:
            if conn: conn.rollback()
            return render_template('login.html', erro="Erro ao atualizar palavra-passe.")
        finally:
            if cursor: cursor.close()
            if conn: conn.close()

    token = request.args.get('token', '').strip()
    if not token:
        return render_template('login.html', erro="Acesso direto negado. É necessário um token válido enviado por e-mail para redefinir a senha.")
        
    conn = get_db_connection()
    if not conn: return render_template('login.html', erro="Erro temporário no servidor.")
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT * FROM usuarios WHERE reset_token = %s AND (reset_expires IS NULL OR reset_expires > %s)", (token, agora_str))
        user = cursor.fetchone()
        if not user:
            return render_template('login.html', erro="O link de redefinição é inválido ou expirou. Solicite um novo link.")
        return render_template('login.html', redefinir_token=token)
    finally:
        if cursor: cursor.close()
        if conn: conn.close()

@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('login'))

# ----- Telas Principais do SaaS -----

@app.route('/app')
def app_dashboard():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('dashboard.html', usuario=get_usuario_logado())

@app.route('/reputacao')
def reputacao():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('reputacao.html', usuario=get_usuario_logado())

@app.route('/minhas_acoes')
def minhas_acoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('minhas_acoes.html', usuario=get_usuario_logado())

@app.route('/relatorios')
def relatorios():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('relatorios.html', usuario=get_usuario_logado())

@app.route('/sala_maquinas')
def sala_maquinas():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('sala_maquinas.html', usuario=get_usuario_logado())

@app.route('/integracoes')
def integracoes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    garantir_massa_dados_empresa()
    return render_template('integracoes.html', usuario=get_usuario_logado())

@app.route('/ajustes')
def ajustes():
    if 'usuario_id' not in session: return redirect(url_for('login'))
    return render_template('ajustes.html', usuario=get_usuario_logado())

@app.route('/landing')
def landing():
    return render_template('index.html')

# ================= APIs REST (O Backend que alimenta o Frontend) =================

@app.route('/api/acoes', methods=['GET'])
def get_acoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id as __backendId, titulo as title, 
                   status = 'concluido' as completed, prioridade as priority, prazo as due_date
            FROM acoes WHERE empresa_id = %s ORDER BY prioridade DESC, prazo ASC
        """, (session.get('empresa_id'),))
        acoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
        
    for acao in acoes:
        if acao['due_date']: acao['due_date'] = acao['due_date'].isoformat()
        acao['completed'] = bool(acao['completed'])
            
    return jsonify(acoes)

@app.route('/api/acoes', methods=['POST'])
def create_acao():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    titulo = data.get('title')
    prioridade = data.get('priority', 'normal')
    prazo = data.get('due_date') or None
    
    if not titulo: return jsonify({"error": "Title required"}), 400
        
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("""
            INSERT INTO acoes (empresa_id, criado_por, titulo, prioridade, prazo, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (session.get('empresa_id'), session.get('usuario_id'), titulo, prioridade, prazo))
        new_id = cursor.lastrowid
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database write error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"__backendId": new_id}), 201

@app.route('/api/acoes/<int:acao_id>', methods=['PUT'])
def toggle_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        acao = cursor.fetchone()
        
        if not acao:
            return jsonify({"error": "Not found"}), 404
            
        new_status = 'pendente' if acao[0] == 'concluido' else 'concluido'
        cursor.execute("UPDATE acoes SET status = %s WHERE id = %s", (new_status, acao_id))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database update error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
        
    return jsonify({"success": True, "status": new_status})

@app.route('/api/acoes/<int:acao_id>', methods=['DELETE'])
def delete_acao(acao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor()
    try:
        cursor.execute("DELETE FROM acoes WHERE id = %s AND empresa_id = %s", (acao_id, session.get('empresa_id')))
        conn.commit()
        deleted = cursor.rowcount > 0
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database delete error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()
    
    if deleted: return jsonify({"success": True})
    return jsonify({"error": "Not found"}), 404

@app.route('/api/configuracoes', methods=['GET', 'POST'])
def api_configuracoes():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        if request.method == 'GET':
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config = cursor.fetchone()
            if not config:
                return jsonify({"tom_voz": "Profissional e empático", "regras_ouro": "", "telefone_whatsapp": ""})
            return jsonify(config)

        if request.method == 'POST':
            data = request.json
            tom_voz = data.get('tom_voz', 'Profissional e empático')
            regras_ouro = data.get('regras_ouro', '')
            telefone = data.get('telefone_whatsapp', '')

            cursor.execute("""
                INSERT INTO configuracoes_ia (empresa_id, tom_voz, regras_ouro, telefone_whatsapp) 
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE 
                tom_voz = VALUES(tom_voz), 
                regras_ouro = VALUES(regras_ouro), 
                telefone_whatsapp = VALUES(telefone_whatsapp)
            """, (empresa_id, tom_voz, regras_ouro, telefone))

            conn.commit()
            return jsonify({"success": True})
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database config error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

@app.route('/api/avaliacoes', methods=['GET'])
def get_avaliacoes():
    if 'usuario_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Database error"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("""
            SELECT id, nome_cliente as cliente, plataforma_origem as plataforma, 
                   nota, comentario, rascunho_resposta as rascunho, status
            FROM avaliacoes_feed WHERE empresa_id = %s ORDER BY id DESC
        """, (session.get('empresa_id'),))
        avaliacoes = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    for av in avaliacoes:
        av['tempo'] = "Recente" 
        av['plataforma'] = av['plataforma'].capitalize()
        if not av['rascunho']: av['rascunho'] = "Aguardando análise da IA..."
            
    return jsonify(avaliacoes)

@app.route('/api/avaliacoes/<int:avaliacao_id>/aprovar', methods=['PUT'])
def aprovar_avaliacao(avaliacao_id):
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de base de dados"}), 500

    cursor = conn.cursor()
    try:
        cursor.execute("SELECT status FROM avaliacoes_feed WHERE id = %s AND empresa_id = %s", (avaliacao_id, session.get('empresa_id')))
        av = cursor.fetchone()

        if not av:
            return jsonify({"error": "Avaliação não encontrada"}), 404

        cursor.execute("UPDATE avaliacoes_feed SET status = 'respondido_ia' WHERE id = %s", (avaliacao_id,))
        conn.commit()
    except Exception as e:
        conn.rollback()
        return jsonify({"error": f"Database error: {e}"}), 500
    finally:
        cursor.close()
        conn.close()

    return jsonify({"success": True, "message": "Avaliação aprovada e movida com sucesso."})

@app.route('/api/integracoes/status', methods=['GET'])
def integracoes_status():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    cursor = conn.cursor(dictionary=True)
    try:
        cursor.execute("SELECT plataforma, status FROM integracoes_api WHERE empresa_id = %s", (session.get('empresa_id'),))
        status_db = cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
    
    return jsonify(status_db)

@app.route('/api/analytics/resumo', methods=['GET'])
def get_analytics_resumo():
    if 'usuario_id' not in session: return jsonify({"error": "Não autorizado"}), 401
        
    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn: return jsonify({"error": "Erro de DB"}), 500
        
    try:
        cursor = conn.cursor(dictionary=True)
        # Sentimentos
        cursor.execute("""
            SELECT sentimento, COUNT(*) as qtd FROM avaliacoes_feed 
            WHERE empresa_id = %s AND sentimento IS NOT NULL GROUP BY sentimento
        """, (empresa_id,))
        dados_sentimento = cursor.fetchall()
        
        sentimentos_dict = {"positivo": 0, "neutro": 0, "negativo": 0}
        for linha in dados_sentimento:
            if linha['sentimento'] in sentimentos_dict:
                sentimentos_dict[linha['sentimento']] = linha['qtd']
                
        # Tags (Top Motivos)
        cursor.execute("SELECT tags FROM avaliacoes_feed WHERE empresa_id = %s AND tags IS NOT NULL", (empresa_id,))
        linhas_tags = cursor.fetchall()
        
        contagem_tags = {}
        for linha in linhas_tags:
            try:
                lista_tags = json.loads(linha['tags'])
                for tag in lista_tags:
                    if tag: contagem_tags[tag] = contagem_tags.get(tag, 0) + 1
            except: continue
                
        tags_ordenadas = sorted(contagem_tags.items(), key=lambda x: x[1], reverse=True)[:5]
        
        return jsonify({
            "sentimentos": sentimentos_dict,
            "top_tags": {"labels": [item[0] for item in tags_ordenadas], "valores": [item[1] for item in tags_ordenadas]}
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
    finally:
        cursor.close(); conn.close()

# ================= ROTAS DE INTEGRAÇÃO OAUTH =================

@app.route('/api/auth/<plataforma>/login')
def auth_login(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if plataforma == 'google':
        auth_url = (f"https://accounts.google.com/o/oauth2/v2/auth?client_id={GOOGLE_CLIENT_ID}&"
                    f"redirect_uri={redirect_uri}&response_type=code&"
                    f"scope=https://www.googleapis.com/auth/business.manage&access_type=offline&prompt=consent")
        return redirect(auth_url)
        
    elif plataforma == 'instagram':
        auth_url = (f"https://www.facebook.com/v18.0/dialog/oauth?client_id={META_APP_ID}&"
                    f"redirect_uri={redirect_uri}&config_id=SEU_CONFIG_ID_AQUI")
        return redirect(auth_url)

    elif plataforma in ['ifood', 'whatsapp', 'ze_delivery', '99food']:
        empresa_id = session.get('empresa_id')
        conn = get_db_connection()
        if conn:
            cursor = conn.cursor()
            try:
                cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                if cursor.fetchone():
                    cursor.execute("UPDATE integracoes_api SET status = 'ativo' WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
                else:
                    cursor.execute("INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, status) VALUES (%s, %s, 'demo_token', 'ativo')", (empresa_id, plataforma))
                conn.commit()
            except Exception as e:
                conn.rollback()
                print(f"Erro ao integrar {plataforma}: {e}")
            finally:
                cursor.close(); conn.close()
        return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

    return "Plataforma inválida", 400

@app.route('/api/auth/<plataforma>/callback')
def auth_callback(plataforma):
    if 'usuario_id' not in session: return redirect(url_for('login'))
    empresa_id = session.get('empresa_id')
    code = request.args.get('code')
    redirect_uri = url_for('auth_callback', plataforma=plataforma, _external=True)
    
    if not code: return "Erro: Código não recebido ou acesso negado pelo cliente.", 400

    token_acesso, token_refresh = "", ""
    page_id = None

    try:
        if plataforma == 'google':
            res = requests.post("https://oauth2.googleapis.com/token", data={
                "code": code, "client_id": GOOGLE_CLIENT_ID, "client_secret": GOOGLE_CLIENT_SECRET,
                "redirect_uri": redirect_uri, "grant_type": "authorization_code"
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = res.get("refresh_token", "N/A")

        elif plataforma == 'instagram':
            res = requests.get("https://graph.facebook.com/v18.0/oauth/access_token", params={
                "client_id": META_APP_ID, "redirect_uri": redirect_uri, "client_secret": META_APP_SECRET, "code": code
            }).json()
            token_acesso = res.get("access_token")
            token_refresh = "N/A" 
            
            if token_acesso:
                pages_res = requests.get(f"https://graph.facebook.com/v18.0/me/accounts?access_token={token_acesso}").json()
                if 'data' in pages_res and len(pages_res['data']) > 0:
                    page_id = pages_res['data'][0]['id']
                    page_token = pages_res['data'][0]['access_token']
                    requests.post(f"https://graph.facebook.com/v18.0/{page_id}/subscribed_apps", 
                                  data={"subscribed_fields": "feed, messages, comments", "access_token": page_token})
                    token_acesso = page_token 

        if not token_acesso: return f"Erro ao obter token real da plataforma.", 400

    except Exception as e:
        return f"Erro de comunicação com a plataforma: {str(e)}", 500
    
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("SELECT id FROM integracoes_api WHERE empresa_id = %s AND plataforma = %s", (empresa_id, plataforma))
            if cursor.fetchone():
                cursor.execute("""
                    UPDATE integracoes_api 
                    SET token_acesso = %s, token_refresh = %s, plataforma_user_id = %s, status = 'ativo' 
                    WHERE empresa_id = %s AND plataforma = %s
                """, (token_acesso, token_refresh, page_id, empresa_id, plataforma))
            else:
                cursor.execute("""
                    INSERT INTO integracoes_api (empresa_id, plataforma, token_acesso, token_refresh, plataforma_user_id, status) 
                    VALUES (%s, %s, %s, %s, %s, 'ativo')
                """, (empresa_id, plataforma, token_acesso, token_refresh, page_id))
            conn.commit()
        except Exception as e:
            conn.rollback()
            print(f"Erro ao salvar integração no banco: {e}")
        finally:
            cursor.close()
            conn.close()

    return redirect(url_for('integracoes', sucesso="true", plat=plataforma))

# ================= WEBHOOKS (Escuta Ativa) =================

@app.route('/api/webhooks/ingest', methods=['GET', 'POST'])
def webhook_ingest():
    if request.method == 'GET':
        if request.args.get('hub.mode') == 'subscribe' and request.args.get('hub.verify_token') == META_VERIFY_TOKEN:
            return request.args.get('hub.challenge'), 200
        return "Acesso negado", 403

    data = request.json
    if not data: return jsonify({"error": "Payload inválido"}), 400

    conn = get_db_connection()
    if not conn: return jsonify({"error": "Falha no DB"}), 500

    try:
        cursor = conn.cursor()
        
        # PARSE ADAPTATIVO
        if 'entry' in data:
            try:
                plataforma = 'instagram'
                
                # Mapeamento dinâmico multi-tenancy a partir do ID da página enviado pelo webhook da Meta
                meta_page_id = str(data['entry'][0].get('id', ''))
                cursor.execute("""
                    SELECT empresa_id FROM integracoes_api 
                    WHERE plataforma = 'instagram' AND plataforma_user_id = %s AND status = 'ativo'
                """, (meta_page_id,))
                row = cursor.fetchone()
                empresa_id = row[0] if row else 1 # Fallback seguro
                
                alteracao = data['entry'][0]['changes'][0]['value']
                nome_cliente = alteracao.get('from', {}).get('username', 'Utilizador')
                comentario = alteracao.get('text', '')
                nota = 3 
            except (KeyError, IndexError):
                return jsonify({"error": "Formato Meta inválido"}), 400
        else:
            empresa_id = data.get('empresa_id')
            plataforma = data.get('plataforma')
            nome_cliente = data.get('nome_cliente', 'Cliente')
            nota = data.get('nota')
            comentario = data.get('comentario', '')

        if not empresa_id or not plataforma or nota is None:
            return jsonify({"error": "Dados ausentes"}), 400

        cursor.execute("""
            INSERT INTO avaliacoes_feed (empresa_id, plataforma_origem, nome_cliente, nota, comentario, status)
            VALUES (%s, %s, %s, %s, %s, 'pendente')
        """, (empresa_id, plataforma, nome_cliente, nota, comentario))
        avaliacao_id = cursor.lastrowid
        
        filtro = analisar_qualidade_comentario(comentario, nota)
        
        if filtro["valido"]:
            cursor.execute("SELECT tom_voz, regras_ouro, telefone_whatsapp FROM configuracoes_ia WHERE empresa_id = %s", (empresa_id,))
            config_ia = cursor.fetchone()
            regras_reais = f"Tom: {config_ia[0]}. Regras: {config_ia[1]}" if config_ia else "Tom profissional."
            telefone_real = config_ia[2] if config_ia and config_ia[2] else "Não configurado"

            resultado_ia = analisar_avaliacao_gemini(
                nome_cliente=nome_cliente, nota=nota, comentario=comentario,
                regras_tom_voz=regras_reais, telefone_empresa=telefone_real
            )
            sentimento = resultado_ia.get("sentimento", "neutro")
            rascunho = resultado_ia.get("sugestao_resposta", "")
            
            # Tags estruturadas retornadas pelo Gemini 2.5
            tags_json = json.dumps(resultado_ia.get("tags", []))
            
            novo_status = 'respondido_ia' if nota >= 4 else 'alerta_crise' if nota <= 3 else 'pendente'
        else:
            sentimento = filtro["sentimento"]
            rascunho = filtro["rascunho"]
            tags_json = json.dumps(["Filtro Spam Ativo"])
            novo_status = filtro["status"]

        if novo_status == 'alerta_crise':
            titulo_crise = f"Contenção: Avaliação {nota}★ de {nome_cliente}"
            cursor.execute("INSERT INTO acoes (empresa_id, titulo, prioridade, prazo, status) VALUES (%s, %s, 'critical', %s, 'pendente')", 
                           (empresa_id, titulo_crise, date.today().strftime('%Y-%m-%d')))

        cursor.execute("""
            UPDATE avaliacoes_feed SET sentimento = %s, rascunho_resposta = %s, tags = %s, status = %s WHERE id = %s
        """, (sentimento, rascunho, tags_json, novo_status, avaliacao_id))

        conn.commit()
        return jsonify({"success": True, "message": "Avaliação processada", "status": novo_status}), 201

    except Exception as e:
        if conn: conn.rollback()
        return jsonify({"error": f"Erro interno: {str(e)}"}), 500
    finally:
        if 'cursor' in locals(): cursor.close()
        if conn: conn.close()

# ================= GERADOR DE MASSA DE DADOS (DEMO) =================

@app.route('/api/seed', methods=['GET', 'POST'])
def api_seed():
    """Endpoint para gerar massa de dados demonstrativa no app"""
    try:
        from gerar_massa_dados import popular_banco_dados
        res = popular_banco_dados()
        return jsonify({"success": True, "message": "Massa de dados gerada com sucesso!", "detalhes": res}), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500

# ================= INTEGRACAO MERCADO PAGO =================
MERCADOPAGO_ACCESS_TOKEN = os.environ.get("MERCADOPAGO_ACCESS_TOKEN", "APP_USR-DEMO-TOKEN")

@app.route('/api/checkout/<plano>/<ciclo>', methods=['GET', 'POST'])
def api_checkout(plano, ciclo):
    """Gera preferência de checkout no Mercado Pago (Checkout Pro / PIX)"""
    if 'usuario_id' not in session:
        return redirect(url_for('login', registo="true"))
    
    precos = {
        "pro": {"mensal": 79.90, "anual": 718.80},
        "premium": {"mensal": 197.90, "anual": 1774.80},
        "setup_vip": {"avista": 300.00, "mensal": 300.00, "anual": 300.00}
    }
    
    if plano == "setup_vip":
        ciclo = "avista"
        
    if plano not in precos or ciclo not in ["mensal", "anual", "avista"]:
        return jsonify({"error": "Plano ou ciclo inválido"}), 400
        
    valor = precos[plano][ciclo]
    titulo = "ReAction - Setup VIP (2 Sessões ao Vivo + 3 Meses Grátis)" if plano == "setup_vip" else f"ReAction - Plano {plano.capitalize()} ({ciclo.capitalize()})"
    
    try:
        import mercadopago
        sdk = mercadopago.SDK(MERCADOPAGO_ACCESS_TOKEN)
        
        preference_data = {
            "items": [
                {
                    "title": titulo,
                    "quantity": 1,
                    "unit_price": valor,
                    "currency_id": "BRL"
                }
            ],
            "payer": {
                "email": session.get('usuario_email', 'cliente@reaction.com.br')
            },
            "back_urls": {
                "success": url_for('app_dashboard', status="sucesso_pagamento", _external=True),
                "failure": url_for('app_dashboard', status="falha_pagamento", _external=True),
                "pending": url_for('app_dashboard', status="pendente_pagamento", _external=True)
            },
            "auto_return": "approved",
            "external_reference": f"empresa_{session.get('empresa_id')}_{plano}_{ciclo}"
        }
        
        preference_response = sdk.preference().create(preference_data)
        preference = preference_response.get("response", {})
        
        # Notificar os 3 administradores sobre o upgrade/contratação
        try:
            enviar_notificacao_admin("upgrade", {
                "nome_usuario": session.get('nome', 'Cliente'),
                "email": session.get('usuario_email', 'cliente@reaction.com.br'),
                "nome_empresa": f"Empresa ID #{session.get('empresa_id')}",
                "plano": f"Plano {plano.capitalize()}",
                "ciclo": ciclo.capitalize(),
                "valor": f"R$ {valor:.2f}"
            })
        except Exception as e_adm:
            print(f"Aviso envio e-mail admin checkout: {e_adm}")

        init_point = preference.get("init_point") or preference.get("sandbox_init_point")
        if init_point:
            return redirect(init_point)
        else:
            return jsonify({"success": True, "checkout_data": preference, "message": "Simulação de Checkout Mercado Pago iniciada"}), 200
            
    except Exception as mp_err:
        print(f"Erro no checkout Mercado Pago: {mp_err}")
        return jsonify({"error": f"Erro no Checkout Mercado Pago: {str(mp_err)}"}), 500

@app.route('/api/webhooks/mercadopago', methods=['POST'])
def webhook_mercadopago():
    """Webhook do Mercado Pago para escutar alterações e aprovações de pagamento"""
    data = request.json or {}
    print(f"Webhook Mercado Pago recebido: {data}")
    return jsonify({"status": "received"}), 200

# ================= ROTAS DE EXPORTAÇÃO EXCEL NATIVO (.XLSX MULTI-ABAS) =================

@app.route('/api/exportar/relatorio_excel')
@app.route('/api/exportar/avaliacoes')
@app.route('/api/exportar/acoes')
def exportar_relatorio_excel():
    """Gera um relatório completo em formato nativo Excel (.xlsx) com 4 abas profissionais estilizadas"""
    if 'usuario_id' not in session:
        return redirect(url_for('login'))

    empresa_id = session.get('empresa_id')
    conn = get_db_connection()
    if not conn:
        return "Erro de conexão com o banco de dados", 500

    cursor = conn.cursor(dictionary=True)
    avaliacoes = []
    acoes = []
    integracoes = []
    empresa_nome = f"Empresa #{empresa_id}"
    
    try:
        cursor.execute("SELECT nome_empresa FROM empresas WHERE id = %s", (empresa_id,))
        emp_res = cursor.fetchone()
        if emp_res and emp_res.get('nome_empresa'):
            empresa_nome = emp_res['nome_empresa']

        cursor.execute("""
            SELECT id, plataforma_origem, nome_cliente, nota, comentario, sentimento, status, rascunho_resposta
            FROM avaliacoes_feed
            WHERE empresa_id = %s
        """, (empresa_id,))
        avaliacoes = cursor.fetchall()

        cursor.execute("""
            SELECT id, titulo, prioridade, prazo, status
            FROM acoes
            WHERE empresa_id = %s
        """, (empresa_id,))
        acoes = cursor.fetchall()

        cursor.execute("""
            SELECT plataforma, status
            FROM integracoes_api
            WHERE empresa_id = %s
        """, (empresa_id,))
        integracoes = cursor.fetchall()

    except Exception as e:
        print(f"Erro ao buscar dados para relatório Excel: {e}")
    finally:
        cursor.close()
        conn.close()

    # Criar o Workbook openpyxl
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter

    wb = openpyxl.Workbook()

    fill_header = PatternFill(start_color="3A3A3A", end_color="3A3A3A", fill_type="solid")
    fill_brand = PatternFill(start_color="FF6B35", end_color="FF6B35", fill_type="solid")
    fill_zebra = PatternFill(start_color="F9FAFB", end_color="F9FAFB", fill_type="solid")

    font_title = Font(name="Calibri", size=14, bold=True, color="FFFFFF")
    font_header = Font(name="Calibri", size=11, bold=True, color="FFFFFF")
    font_bold = Font(name="Calibri", size=11, bold=True, color="1F2937")
    font_normal = Font(name="Calibri", size=11, color="374151")
    
    align_center = Alignment(horizontal="center", vertical="center")
    align_left = Alignment(horizontal="left", vertical="center")

    thin_border = Border(
        left=Side(style='thin', color='E5E7EB'),
        right=Side(style='thin', color='E5E7EB'),
        top=Side(style='thin', color='E5E7EB'),
        bottom=Side(style='thin', color='E5E7EB')
    )

    # ── ABA 1: RESUMO EXECUTIVO ──
    ws1 = wb.active
    ws1.title = "Resumo Executivo"
    ws1.views.sheetView[0].showGridLines = True

    ws1.merge_cells("A1:E2")
    cell_title = ws1["A1"]
    cell_title.value = f"REACTION | RELATÓRIO EXECUTIVO DE REPUTAÇÃO - {empresa_nome.upper()}"
    cell_title.font = font_title
    cell_title.fill = fill_brand
    cell_title.alignment = align_center

    total_av = len(avaliacoes)
    soma_notas = sum(r.get('nota', 0) for r in avaliacoes)
    nota_media = round(soma_notas / total_av, 2) if total_av > 0 else 5.0
    positivas = sum(1 for r in avaliacoes if r.get('sentimento') == 'positivo' or r.get('nota', 0) >= 4)
    neutras = sum(1 for r in avaliacoes if r.get('sentimento') == 'neutro' or r.get('nota', 0) == 3)
    negativas = sum(1 for r in avaliacoes if r.get('sentimento') == 'negativo' or r.get('nota', 0) <= 2)
    saude_pct = round((positivas / total_av) * 100, 1) if total_av > 0 else 100.0

    kpis = [
        ("Nota Média da Marca", f"{nota_media} / 5.0"),
        ("Total de Avaliações Cadastradas", total_av),
        ("Índice de Saúde da Reputação", f"{saude_pct}%"),
        ("Avaliações Positivas (4-5★)", positivas),
        ("Avaliações Neutras (3★)", neutras),
        ("Avaliações Críticas (1-2★)", negativas),
        ("Total de Ações de Contenção", len(acoes))
    ]

    ws1.cell(row=4, column=1, value="MÉTRICAS CHAVE DE PERFORMANCE").font = Font(name="Calibri", size=12, bold=True, color="FF6B35")

    ws1.cell(row=5, column=1, value="Indicador Executivo").font = font_header
    ws1.cell(row=5, column=1).fill = fill_header
    ws1.cell(row=5, column=2, value="Resultado").font = font_header
    ws1.cell(row=5, column=2).fill = fill_header

    for idx, (k, v) in enumerate(kpis, start=6):
        c1 = ws1.cell(row=idx, column=1, value=k)
        c2 = ws1.cell(row=idx, column=2, value=v)
        c1.font = font_bold
        c2.font = font_bold
        c1.border = thin_border
        c2.border = thin_border
        c2.alignment = align_center

    # ── ABA 2: AVALIAÇÕES ──
    ws2 = wb.create_sheet(title="Avaliações & Feedbacks")
    ws2.views.sheetView[0].showGridLines = True

    headers_av = ['ID', 'Cliente', 'Canal / Origem', 'Nota (Estrelas)', 'Comentário do Cliente', 'Sentimento', 'Status da Resposta', 'Resposta Gerada / IA']
    for col_num, h in enumerate(headers_av, 1):
        cell = ws2.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_left if col_num in [2, 5, 8] else align_center

    for r_idx, r in enumerate(avaliacoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('nome_cliente', ''),
            r.get('plataforma_origem', '').upper(),
            r.get('nota', ''),
            r.get('comentario', ''),
            r.get('sentimento', '').capitalize(),
            r.get('status', ''),
            r.get('rascunho_resposta', '')
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws2.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # ── ABA 3: PLANO DE AÇÕES ──
    ws3 = wb.create_sheet(title="Plano de Ações")
    ws3.views.sheetView[0].showGridLines = True

    headers_ac = ['ID', 'Título da Ação de Contenção', 'Prioridade', 'Prazo Final', 'Status']
    for col_num, h in enumerate(headers_ac, 1):
        cell = ws3.cell(row=1, column=col_num, value=h)
        cell.font = font_header
        cell.fill = fill_header
        cell.alignment = align_center

    for r_idx, r in enumerate(acoes, start=2):
        row_data = [
            r.get('id', ''),
            r.get('titulo', ''),
            r.get('prioridade', 'normal').capitalize(),
            str(r.get('prazo') or 'Sem prazo'),
            r.get('status', '').capitalize()
        ]
        for c_idx, val in enumerate(row_data, start=1):
            cell = ws3.cell(row=r_idx, column=c_idx, value=val)
            cell.font = font_normal
            cell.border = thin_border
            if r_idx % 2 == 0: cell.fill = fill_zebra

    # Auto-ajuste de largura de colunas em todas as abas
    for ws in [ws1, ws2, ws3]:
        for col in ws.columns:
            max_len = 0
            col_letter = get_column_letter(col[0].column)
            for cell in col:
                val_str = str(cell.value or '')
                if len(val_str) > max_len:
                    max_len = len(val_str)
            ws.column_dimensions[col_letter].width = min(max(max_len + 4, 12), 65)

    output = io.BytesIO()
    wb.save(output)
    output.seek(0)

    filename = f"reaction_relatorio_executivo_{empresa_id}.xlsx"
    return Response(
        output.getvalue(),
        mimetype="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f"attachment; filename={filename}"}
    )

@app.route('/api/usuario/foto', methods=['POST'])
def atualizar_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
    
    usuario_id = session['usuario_id']
    foto_url = None
    
    try:
        if 'foto' in request.files:
            file = request.files['foto']
            if file and file.filename != '':
                rnd_id = os.urandom(4).hex()
                filename = f"avatar_{usuario_id}_{rnd_id}.png"
                upload_dir = os.path.join(app.static_folder, 'uploads', 'avatars')
                os.makedirs(upload_dir, exist_ok=True)
                filepath = os.path.join(upload_dir, filename)
                file.save(filepath)
                foto_url = f"/static/uploads/avatars/{filename}"
        elif request.is_json and request.json.get('foto_url'):
            foto_url = request.json.get('foto_url')
            
        if foto_url:
            conn = get_db_connection()
            if conn:
                cursor = conn.cursor()
                try:
                    cursor.execute("UPDATE usuarios SET foto_perfil = %s WHERE id = %s", (foto_url, usuario_id))
                    conn.commit()
                    session['foto_perfil'] = foto_url
                    return jsonify({'sucesso': True, 'foto_url': foto_url})
                finally:
                    cursor.close()
                    conn.close()
    except Exception as err:
        print(f"Erro ao salvar foto de perfil: {err}")
        return jsonify({'sucesso': False, 'erro': f'Erro ao processar imagem: {str(err)}'}), 500
                
    return jsonify({'sucesso': False, 'erro': 'Nenhuma imagem fornecida'}), 400

@app.route('/api/usuario/foto/remover', methods=['POST'])
def remover_foto_perfil():
    if 'usuario_id' not in session:
        return jsonify({'sucesso': False, 'erro': 'Não autenticado'}), 401
        
    usuario_id = session['usuario_id']
    conn = get_db_connection()
    if conn:
        cursor = conn.cursor()
        try:
            cursor.execute("UPDATE usuarios SET foto_perfil = NULL WHERE id = %s", (usuario_id,))
            conn.commit()
            session['foto_perfil'] = None
            return jsonify({'sucesso': True})
        finally:
            cursor.close()
            conn.close()
    return jsonify({'sucesso': False, 'erro': 'Erro ao atualizar banco'}), 500

if __name__ == '__main__':
    flask_debug = os.environ.get('FLASK_DEBUG', 'False').lower() in ['true', '1']
    flask_host = os.environ.get('FLASK_HOST', '127.0.0.1')
    app.run(host=flask_host, port=5001, debug=flask_debug)



==================================================
ARQUIVO: .\massa_dados_producao.sql
==================================================

-- Dump de Massa de Dados para Produção ReAction
USE `u716503964_reacao`;

INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (101, 1, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (102, 1, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (103, 1, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (104, 1, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (105, 1, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (106, 1, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (107, 1, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (108, 1, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (109, 1, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (110, 1, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (111, 1, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (112, 1, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (113, 2, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (114, 2, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (115, 2, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (116, 2, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (117, 2, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (118, 2, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (119, 2, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (120, 2, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (121, 2, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (122, 2, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (123, 2, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (124, 2, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (125, 3, 'google', 'Mariana Souza', 5, 'Atendimento espetacular e a comida chegou super quente! Com certeza virei cliente fiel.', 'respondido_ia', 'positivo', 'Olá Mariana! Ficamos extremamente felizes com o seu carinho. Nossa equipe trabalha diariamente para entregar o melhor serviço!', '["atendimento excelente", "comida quente", "fideliza\u00e7\u00e3o"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (126, 3, 'ifood', 'Carlos Eduardo', 1, 'O pedido demorou mais de 1h30 para entregar e chegou completamente frio. Decepcionante!', 'alerta_crise', 'negativo', 'Olá Carlos! Pedimos imensas desculpas pelo atraso e pela temperatura do pedido. Esta não é a nossa norma. Queremos resolver isso agora mesmo com você no WhatsApp: wa.me/5585999999999', '["atraso na entrega", "comida fria", "crise"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (127, 3, 'instagram', '@beatriz_lima', 5, 'Amei a experiência de ontem no restaurante! O ambiente é lindo e os pratos são maravilhosos 😍✨', 'respondido_ia', 'positivo', 'Muito obrigado pelo carinho, Beatriz! Adoramos ter você conosco e já estamos ansiosos pela sua próxima visita!', '["ambiente agrad\u00e1vel", "prato delicioso", "instagram"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (128, 3, '', 'Rodrigo Fonseca', 5, 'Cerveja trincando de gelada e a entrega levou apenas 12 minutos! Perfeito para o futebol.', 'respondido_ia', 'positivo', 'Valeu Rodrigo! Cerveja trincando e entrega rápida é a nossa marca registrada. Bom jogo!', '["cerveja gelada", "entrega ultra r\u00e1pida", "z\u00e9 delivery"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (129, 3, '', 'Vanessa Castro', 2, 'O entregador do 99Food deixou a bebida vazar na sacola do lanche.', 'alerta_crise', 'negativo', 'Olá Vanessa! Lamentamos imensamente pelo incidente com a embalagem. Por favor nos chame no WhatsApp wa.me/5585999999999 para enviarmos um combo novinho imediatamente.', '["bebida vazou", "embalagem danificada", "99food"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (130, 3, 'ifood', 'Fernanda Ribeiro', 2, 'Veio faltando um item do meu combo. Tentei ligar mas ninguém atendeu.', 'alerta_crise', 'negativo', 'Olá Fernanda, sentimos muito pelo item faltante e pela falha na comunicação. Por favor, nos chame no WhatsApp wa.me/5585999999999 para fazer o reembolso imediato ou envio do item.', '["item faltante", "falha no suporte"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (131, 3, 'google', 'Roberto Alves', 4, 'Comida excelente, apenas a sobremesa que achei um pouco doce demais. No geral muito bom!', 'respondido_ia', 'positivo', 'Olá Roberto! Agradecemos o feedback e a avaliação. Vamos repassar sua observação sobre a sobremesa para o nosso chef!', '["comida excelente", "feedback sobremesa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (132, 3, 'whatsapp', 'Juliana Mendes', 5, 'Gostaria de parabenizar a equipe pelo jantar de aniversário da minha mãe! Foi impecável.', 'respondido_ia', 'positivo', 'Olá Juliana! Ficamos honrados em fazer parte desse momento tão especial da sua família. Parabéns à sua mãe!', '["anivers\u00e1rio", "atendimento impec\u00e1vel"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (133, 3, 'ifood', 'Lucas Pinheiro', 3, 'A comida é boa mas a embalagem veio meio amassada.', 'alerta_crise', 'neutro', 'Olá Lucas! Agradecemos a observação. Já estamos alinhando com a equipe de entregas para reforçar as embalagens.', '["embalagem amassada", "comida saborosa"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (134, 3, 'instagram', '@thiago_gastronomia', 5, 'Um dos melhores pontos de carne da cidade! Recomendadíssimo.', 'respondido_ia', 'positivo', 'Valeu demais pelo apoio, Thiago! O segredo é o carinho no preparo. Até a próxima!', '["ponto da carne", "recomendado"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (135, 3, 'google', 'Camila Martins', 5, 'Preço justo, porção bem servida e atendimento rápido.', 'respondido_ia', 'positivo', 'Obrigado Camila! É sempre um prazer servir bem e com qualidade!', '["pre\u00e7o justo", "por\u00e7\u00e3o servida", "r\u00e1pido"]');
INSERT INTO avaliacoes_feed (`id`, `empresa_id`, `plataforma_origem`, `nome_cliente`, `nota`, `comentario`, `status`, `sentimento`, `rascunho_resposta`, `tags`) VALUES (136, 3, 'whatsapp', 'Gabriel Torres', 1, 'Cobraram um valor diferente no cartão do que estava no cardápio.', 'alerta_crise', 'negativo', 'Gabriel, pedimos sinceras desculpas pelo erro de cobrança. Por favor nos envie o comprovante no wa.me/5585999999999 para ajustarmos imediatamente.', '["erro de cobran\u00e7a", "crise urgente"]');



==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction - Gestão de Reputação com IA",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#050505",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/static/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}



==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.19">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.19">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.19">
    <link rel="manifest" href="/static/manifest.json?v=0.1.19">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-brand transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4 text-brand"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
  <link rel="alternate icon" href="/static/favicon.svg">
  <link rel="apple-touch-icon" href="/static/favicon.svg">
  <link rel="manifest" href="/static/manifest.json?v=0.1.19">
  <meta name="theme-color" content="#ff6b35">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>
</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

      <!-- LOGO AJUSTADA CONFORME A LANDING PAGE -->
      <div class="p-6 flex items-center gap-3">
            <a href="/app" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
            </a>
        </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
          {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
          <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
          <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
      </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.19">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.19">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.19">
    <link rel="manifest" href="/static/manifest.json?v=0.1.19">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            max-width: 100vw !important;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: -1;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white">

    <div class="glow-effect w-[500px] h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
    <div class="glow-effect w-[600px] h-[600px] bg-[#ffb347]/10 top-[20%] right-[-200px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.19">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.19">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.19">
    <link rel="manifest" href="/static/manifest.json?v=0.1.19">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-blue-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-blue-100/50">
                            <svg class="w-8 h-8" viewBox="0 0 24 24">
                                <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                                <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                                <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                                <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-pink-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-pink-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <defs>
                                    <linearGradient id="ig-grad" x1="2" y1="22" x2="22" y2="2">
                                        <stop offset="0%" stop-color="#fdf497"/>
                                        <stop offset="25%" stop-color="#fdf497"/>
                                        <stop offset="50%" stop-color="#fd5949"/>
                                        <stop offset="75%" stop-color="#d6249f"/>
                                        <stop offset="100%" stop-color="#285AEB"/>
                                    </linearGradient>
                                </defs>
                                <rect x="2" y="2" width="20" height="20" rx="5" ry="5" fill="url(#ig-grad)"/>
                                <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z" stroke="white" stroke-width="1.8" fill="none"/>
                                <line x1="17.5" y1="6.5" x2="17.51" y2="6.5" stroke="white" stroke-width="2.2" stroke-linecap="round"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-red-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-red-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#EA1D2C"/>
                                <path d="M5 15c2.5 3 11.5 3 14 0" stroke="white" stroke-width="2.2" stroke-linecap="round"/>
                                <circle cx="8.5" cy="10" r="1.5" fill="white"/>
                                <circle cx="15.5" cy="10" r="1.5" fill="white"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-green-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-green-100/50">
                            <svg class="w-8 h-8" viewBox="0 0 24 24" fill="none">
                                <path fill="#25D366" d="M12 2a10 10 0 0 0-8.6 15.1L2 22l5.1-1.3A10 10 0 1 0 12 2z"/>
                                <path fill="white" d="M16.7 14.3c-.3-.1-1.6-.8-1.9-.9-.3-.1-.5.1-.7.3-.2.3-.7.9-.9 1.1-.1.2-.3.2-.6.1-.3-.1-1.3-.5-2.5-1.5-.9-.8-1.5-1.8-1.7-2.1-.2-.3 0-.5.1-.6.1-.1.3-.4.4-.5.1-.2.2-.3.2-.5 0-.2-.1-.4-.2-.5s-.7-1.7-1-2.3c-.3-.6-.5-.5-.7-.5h-.6c-.2 0-.6.1-.9.4-.3.3-1.1 1.1-1.1 2.7s1.1 3.1 1.3 3.3c.2.2 2.2 3.4 5.4 4.8.8.3 1.4.5 1.8.7.8.2 1.5.2 2.1.1.7-.1 2.2-.9 2.5-1.7.3-.9.3-1.7.2-1.8-.1-.1-.3-.2-.6-.3z"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-amber-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-amber-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#FFC700"/>
                                <path d="M7 8h10l-4 4h4l-8 7v-5H9l4-6H7V8z" fill="#000000"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-orange-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-orange-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#FF8800"/>
                                <text x="12" y="16" font-family="sans-serif" font-weight="900" font-size="12" fill="white" text-anchor="middle">99</text>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="alternate icon" href="/static/favicon.svg">
    <link rel="apple-touch-icon" href="/static/favicon.svg">
    <link rel="manifest" href="/static/manifest.json?v=0.1.19">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        function iniciarFacebookLogin() {
            if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.open(fbAuthUrl, 'FBAuth', 'width=500,height=600');
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', null, null, tokenUser);
                        }
                    })
                    .catch(err => {
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.19">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.19">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.19">
    <link rel="manifest" href="/static/manifest.json?v=0.1.19">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.19">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.19">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.19">
    <link rel="manifest" href="/static/manifest.json?v=0.1.19">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.19">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.19">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.19">
    <link rel="manifest" href="/static/manifest.json?v=0.1.19">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name"
                    title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <button onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" aria-label="Sair"
                    title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function applyTheme(theme) {
            if (theme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
            localStorage.setItem('reaction_theme', theme);
        }

        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            applyTheme(isDark ? 'light' : 'dark');
        }

        applyTheme(localStorage.getItem('reaction_theme') || 'light');

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-gray-100 shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.19">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.19">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.19">
    <link rel="manifest" href="/static/manifest.json?v=0.1.19">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair"
                    aria-label="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction - Gestão de Reputação com IA",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#050505",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/static/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}



==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-brand transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4 text-brand"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
  <link rel="alternate icon" href="/static/favicon.svg">
  <link rel="apple-touch-icon" href="/static/favicon.svg">
  <link rel="manifest" href="/static/manifest.json?v=0.2.0">
  <meta name="theme-color" content="#ff6b35">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>
</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

      <!-- LOGO AJUSTADA CONFORME A LANDING PAGE -->
      <div class="p-6 flex items-center gap-3">
            <a href="/app" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
            </a>
        </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
          {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
          <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
          <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
      </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            max-width: 100vw !important;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: -1;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white">

    <div class="glow-effect w-[500px] h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
    <div class="glow-effect w-[600px] h-[600px] bg-[#ffb347]/10 top-[20%] right-[-200px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-blue-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-blue-100/50">
                            <svg class="w-8 h-8" viewBox="0 0 24 24">
                                <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                                <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                                <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                                <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-pink-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-pink-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <defs>
                                    <linearGradient id="ig-grad" x1="2" y1="22" x2="22" y2="2">
                                        <stop offset="0%" stop-color="#fdf497"/>
                                        <stop offset="25%" stop-color="#fdf497"/>
                                        <stop offset="50%" stop-color="#fd5949"/>
                                        <stop offset="75%" stop-color="#d6249f"/>
                                        <stop offset="100%" stop-color="#285AEB"/>
                                    </linearGradient>
                                </defs>
                                <rect x="2" y="2" width="20" height="20" rx="5" ry="5" fill="url(#ig-grad)"/>
                                <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z" stroke="white" stroke-width="1.8" fill="none"/>
                                <line x1="17.5" y1="6.5" x2="17.51" y2="6.5" stroke="white" stroke-width="2.2" stroke-linecap="round"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-red-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-red-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#EA1D2C"/>
                                <path d="M5 15c2.5 3 11.5 3 14 0" stroke="white" stroke-width="2.2" stroke-linecap="round"/>
                                <circle cx="8.5" cy="10" r="1.5" fill="white"/>
                                <circle cx="15.5" cy="10" r="1.5" fill="white"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-green-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-green-100/50">
                            <svg class="w-8 h-8" viewBox="0 0 24 24" fill="none">
                                <path fill="#25D366" d="M12 2a10 10 0 0 0-8.6 15.1L2 22l5.1-1.3A10 10 0 1 0 12 2z"/>
                                <path fill="white" d="M16.7 14.3c-.3-.1-1.6-.8-1.9-.9-.3-.1-.5.1-.7.3-.2.3-.7.9-.9 1.1-.1.2-.3.2-.6.1-.3-.1-1.3-.5-2.5-1.5-.9-.8-1.5-1.8-1.7-2.1-.2-.3 0-.5.1-.6.1-.1.3-.4.4-.5.1-.2.2-.3.2-.5 0-.2-.1-.4-.2-.5s-.7-1.7-1-2.3c-.3-.6-.5-.5-.7-.5h-.6c-.2 0-.6.1-.9.4-.3.3-1.1 1.1-1.1 2.7s1.1 3.1 1.3 3.3c.2.2 2.2 3.4 5.4 4.8.8.3 1.4.5 1.8.7.8.2 1.5.2 2.1.1.7-.1 2.2-.9 2.5-1.7.3-.9.3-1.7.2-1.8-.1-.1-.3-.2-.6-.3z"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-amber-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-amber-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#FFC700"/>
                                <path d="M7 8h10l-4 4h4l-8 7v-5H9l4-6H7V8z" fill="#000000"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-orange-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-orange-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#FF8800"/>
                                <text x="12" y="16" font-family="sans-serif" font-weight="900" font-size="12" fill="white" text-anchor="middle">99</text>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="alternate icon" href="/static/favicon.svg">
    <link rel="apple-touch-icon" href="/static/favicon.svg">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', null, null, tokenUser);
                        }
                    })
                    .catch(err => {
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name"
                    title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <button onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" aria-label="Sair"
                    title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function applyTheme(theme) {
            if (theme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
            localStorage.setItem('reaction_theme', theme);
        }

        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            applyTheme(isDark ? 'light' : 'dark');
        }

        applyTheme(localStorage.getItem('reaction_theme') || 'light');

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-gray-100 shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair"
                    aria-label="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction - Gestão de Reputação com IA",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#050505",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/static/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}



==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.20">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.20">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.20">
    <link rel="manifest" href="/static/manifest.json?v=0.1.20">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-brand transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4 text-brand"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
  <link rel="alternate icon" href="/static/favicon.svg">
  <link rel="apple-touch-icon" href="/static/favicon.svg">
  <link rel="manifest" href="/static/manifest.json?v=0.1.20">
  <meta name="theme-color" content="#ff6b35">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>
</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

      <!-- LOGO AJUSTADA CONFORME A LANDING PAGE -->
      <div class="p-6 flex items-center gap-3">
            <a href="/app" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
            </a>
        </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
          {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
          <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
          <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
      </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.20">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.20">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.20">
    <link rel="manifest" href="/static/manifest.json?v=0.1.20">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            max-width: 100vw !important;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: -1;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white">

    <div class="glow-effect w-[500px] h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
    <div class="glow-effect w-[600px] h-[600px] bg-[#ffb347]/10 top-[20%] right-[-200px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.20">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.20">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.20">
    <link rel="manifest" href="/static/manifest.json?v=0.1.20">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-blue-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-blue-100/50">
                            <svg class="w-8 h-8" viewBox="0 0 24 24">
                                <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                                <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                                <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                                <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-pink-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-pink-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <defs>
                                    <linearGradient id="ig-grad" x1="2" y1="22" x2="22" y2="2">
                                        <stop offset="0%" stop-color="#fdf497"/>
                                        <stop offset="25%" stop-color="#fdf497"/>
                                        <stop offset="50%" stop-color="#fd5949"/>
                                        <stop offset="75%" stop-color="#d6249f"/>
                                        <stop offset="100%" stop-color="#285AEB"/>
                                    </linearGradient>
                                </defs>
                                <rect x="2" y="2" width="20" height="20" rx="5" ry="5" fill="url(#ig-grad)"/>
                                <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z" stroke="white" stroke-width="1.8" fill="none"/>
                                <line x1="17.5" y1="6.5" x2="17.51" y2="6.5" stroke="white" stroke-width="2.2" stroke-linecap="round"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-red-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-red-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#EA1D2C"/>
                                <path d="M5 15c2.5 3 11.5 3 14 0" stroke="white" stroke-width="2.2" stroke-linecap="round"/>
                                <circle cx="8.5" cy="10" r="1.5" fill="white"/>
                                <circle cx="15.5" cy="10" r="1.5" fill="white"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-green-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-green-100/50">
                            <svg class="w-8 h-8" viewBox="0 0 24 24" fill="none">
                                <path fill="#25D366" d="M12 2a10 10 0 0 0-8.6 15.1L2 22l5.1-1.3A10 10 0 1 0 12 2z"/>
                                <path fill="white" d="M16.7 14.3c-.3-.1-1.6-.8-1.9-.9-.3-.1-.5.1-.7.3-.2.3-.7.9-.9 1.1-.1.2-.3.2-.6.1-.3-.1-1.3-.5-2.5-1.5-.9-.8-1.5-1.8-1.7-2.1-.2-.3 0-.5.1-.6.1-.1.3-.4.4-.5.1-.2.2-.3.2-.5 0-.2-.1-.4-.2-.5s-.7-1.7-1-2.3c-.3-.6-.5-.5-.7-.5h-.6c-.2 0-.6.1-.9.4-.3.3-1.1 1.1-1.1 2.7s1.1 3.1 1.3 3.3c.2.2 2.2 3.4 5.4 4.8.8.3 1.4.5 1.8.7.8.2 1.5.2 2.1.1.7-.1 2.2-.9 2.5-1.7.3-.9.3-1.7.2-1.8-.1-.1-.3-.2-.6-.3z"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-amber-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-amber-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#FFC700"/>
                                <path d="M7 8h10l-4 4h4l-8 7v-5H9l4-6H7V8z" fill="#000000"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-orange-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-orange-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#FF8800"/>
                                <text x="12" y="16" font-family="sans-serif" font-weight="900" font-size="12" fill="white" text-anchor="middle">99</text>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="alternate icon" href="/static/favicon.svg">
    <link rel="apple-touch-icon" href="/static/favicon.svg">
    <link rel="manifest" href="/static/manifest.json?v=0.1.20">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', null, null, tokenUser);
                        }
                    })
                    .catch(err => {
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.20">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.20">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.20">
    <link rel="manifest" href="/static/manifest.json?v=0.1.20">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.20">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.20">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.20">
    <link rel="manifest" href="/static/manifest.json?v=0.1.20">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.20">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.20">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.20">
    <link rel="manifest" href="/static/manifest.json?v=0.1.20">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name"
                    title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <button onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" aria-label="Sair"
                    title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function applyTheme(theme) {
            if (theme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
            localStorage.setItem('reaction_theme', theme);
        }

        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            applyTheme(isDark ? 'light' : 'dark');
        }

        applyTheme(localStorage.getItem('reaction_theme') || 'light');

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-gray-100 shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.20">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.20">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.20">
    <link rel="manifest" href="/static/manifest.json?v=0.1.20">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair"
                    aria-label="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction - Gestão de Reputação com IA",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#050505",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/static/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}



==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-brand transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4 text-brand"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
  <link rel="alternate icon" href="/static/favicon.svg">
  <link rel="apple-touch-icon" href="/static/favicon.svg">
  <link rel="manifest" href="/static/manifest.json?v=0.1.21">
  <meta name="theme-color" content="#ff6b35">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>
</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

      <!-- LOGO AJUSTADA CONFORME A LANDING PAGE -->
      <div class="p-6 flex items-center gap-3">
            <a href="/app" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
            </a>
        </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
          {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
          <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
          <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
      </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            max-width: 100vw !important;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: -1;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white">

    <div class="glow-effect w-[500px] h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
    <div class="glow-effect w-[600px] h-[600px] bg-[#ffb347]/10 top-[20%] right-[-200px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-blue-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-blue-100/50">
                            <svg class="w-8 h-8" viewBox="0 0 24 24">
                                <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                                <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                                <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                                <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-pink-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-pink-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <defs>
                                    <linearGradient id="ig-grad" x1="2" y1="22" x2="22" y2="2">
                                        <stop offset="0%" stop-color="#fdf497"/>
                                        <stop offset="25%" stop-color="#fdf497"/>
                                        <stop offset="50%" stop-color="#fd5949"/>
                                        <stop offset="75%" stop-color="#d6249f"/>
                                        <stop offset="100%" stop-color="#285AEB"/>
                                    </linearGradient>
                                </defs>
                                <rect x="2" y="2" width="20" height="20" rx="5" ry="5" fill="url(#ig-grad)"/>
                                <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z" stroke="white" stroke-width="1.8" fill="none"/>
                                <line x1="17.5" y1="6.5" x2="17.51" y2="6.5" stroke="white" stroke-width="2.2" stroke-linecap="round"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-red-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-red-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#EA1D2C"/>
                                <path d="M5 15c2.5 3 11.5 3 14 0" stroke="white" stroke-width="2.2" stroke-linecap="round"/>
                                <circle cx="8.5" cy="10" r="1.5" fill="white"/>
                                <circle cx="15.5" cy="10" r="1.5" fill="white"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-green-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-green-100/50">
                            <svg class="w-8 h-8" viewBox="0 0 24 24" fill="none">
                                <path fill="#25D366" d="M12 2a10 10 0 0 0-8.6 15.1L2 22l5.1-1.3A10 10 0 1 0 12 2z"/>
                                <path fill="white" d="M16.7 14.3c-.3-.1-1.6-.8-1.9-.9-.3-.1-.5.1-.7.3-.2.3-.7.9-.9 1.1-.1.2-.3.2-.6.1-.3-.1-1.3-.5-2.5-1.5-.9-.8-1.5-1.8-1.7-2.1-.2-.3 0-.5.1-.6.1-.1.3-.4.4-.5.1-.2.2-.3.2-.5 0-.2-.1-.4-.2-.5s-.7-1.7-1-2.3c-.3-.6-.5-.5-.7-.5h-.6c-.2 0-.6.1-.9.4-.3.3-1.1 1.1-1.1 2.7s1.1 3.1 1.3 3.3c.2.2 2.2 3.4 5.4 4.8.8.3 1.4.5 1.8.7.8.2 1.5.2 2.1.1.7-.1 2.2-.9 2.5-1.7.3-.9.3-1.7.2-1.8-.1-.1-.3-.2-.6-.3z"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-amber-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-amber-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#FFC700"/>
                                <path d="M7 8h10l-4 4h4l-8 7v-5H9l4-6H7V8z" fill="#000000"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-orange-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-orange-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#FF8800"/>
                                <text x="12" y="16" font-family="sans-serif" font-weight="900" font-size="12" fill="white" text-anchor="middle">99</text>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="alternate icon" href="/static/favicon.svg">
    <link rel="apple-touch-icon" href="/static/favicon.svg">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token, sub) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token, sub: sub })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email", "User.Read"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const acc = res.account || {};
                            const claims = res.idTokenClaims || {};
                            const email = acc.username || claims.preferred_username || claims.email || claims.upn || null;
                            const nome = acc.name || claims.name || (email ? email.split('@')[0] : null);
                            const sub = acc.homeAccountId || claims.sub || claims.oid || null;
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', email, nome, tokenUser, sub);
                        }
                    })
                    .catch(err => {
                        console.log("Erro no popup Microsoft MSAL:", err);
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name"
                    title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <button onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" aria-label="Sair"
                    title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function applyTheme(theme) {
            if (theme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
            localStorage.setItem('reaction_theme', theme);
        }

        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            applyTheme(isDark ? 'light' : 'dark');
        }

        applyTheme(localStorage.getItem('reaction_theme') || 'light');

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-gray-100 shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair"
                    aria-label="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction - Gestão de Reputação com IA",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#050505",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/static/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}



==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-brand transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4 text-brand"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
  <link rel="alternate icon" href="/static/favicon.svg">
  <link rel="apple-touch-icon" href="/static/favicon.svg">
  <link rel="manifest" href="/static/manifest.json?v=0.1.21">
  <meta name="theme-color" content="#ff6b35">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>
</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

      <!-- LOGO AJUSTADA CONFORME A LANDING PAGE -->
      <div class="p-6 flex items-center gap-3">
            <a href="/app" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
            </a>
        </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
          {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
          <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
          <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
      </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            width: 100% !important;
            max-width: 100% !important;
            margin: 0;
            padding: 0;
        }

        *, ::before, ::after {
            box-sizing: border-box;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
            overflow-wrap: break-word;
            word-break: break-word;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: 0;
            pointer-events: none;
            max-width: 100vw;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white bg-[#050505] text-white w-full overflow-x-hidden">

    <div class="relative w-full overflow-hidden min-h-screen">

        <div class="glow-effect w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
        <div class="glow-effect w-[300px] md:w-[600px] h-[300px] md:h-[600px] bg-[#ffb347]/10 top-[20%] right-[-100px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
    </div>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-blue-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-blue-100/50">
                            <svg class="w-8 h-8" viewBox="0 0 24 24">
                                <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                                <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                                <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                                <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-pink-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-pink-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <defs>
                                    <linearGradient id="ig-grad" x1="2" y1="22" x2="22" y2="2">
                                        <stop offset="0%" stop-color="#fdf497"/>
                                        <stop offset="25%" stop-color="#fdf497"/>
                                        <stop offset="50%" stop-color="#fd5949"/>
                                        <stop offset="75%" stop-color="#d6249f"/>
                                        <stop offset="100%" stop-color="#285AEB"/>
                                    </linearGradient>
                                </defs>
                                <rect x="2" y="2" width="20" height="20" rx="5" ry="5" fill="url(#ig-grad)"/>
                                <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z" stroke="white" stroke-width="1.8" fill="none"/>
                                <line x1="17.5" y1="6.5" x2="17.51" y2="6.5" stroke="white" stroke-width="2.2" stroke-linecap="round"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-red-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-red-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#EA1D2C"/>
                                <path d="M5 15c2.5 3 11.5 3 14 0" stroke="white" stroke-width="2.2" stroke-linecap="round"/>
                                <circle cx="8.5" cy="10" r="1.5" fill="white"/>
                                <circle cx="15.5" cy="10" r="1.5" fill="white"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-green-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-green-100/50">
                            <svg class="w-8 h-8" viewBox="0 0 24 24" fill="none">
                                <path fill="#25D366" d="M12 2a10 10 0 0 0-8.6 15.1L2 22l5.1-1.3A10 10 0 1 0 12 2z"/>
                                <path fill="white" d="M16.7 14.3c-.3-.1-1.6-.8-1.9-.9-.3-.1-.5.1-.7.3-.2.3-.7.9-.9 1.1-.1.2-.3.2-.6.1-.3-.1-1.3-.5-2.5-1.5-.9-.8-1.5-1.8-1.7-2.1-.2-.3 0-.5.1-.6.1-.1.3-.4.4-.5.1-.2.2-.3.2-.5 0-.2-.1-.4-.2-.5s-.7-1.7-1-2.3c-.3-.6-.5-.5-.7-.5h-.6c-.2 0-.6.1-.9.4-.3.3-1.1 1.1-1.1 2.7s1.1 3.1 1.3 3.3c.2.2 2.2 3.4 5.4 4.8.8.3 1.4.5 1.8.7.8.2 1.5.2 2.1.1.7-.1 2.2-.9 2.5-1.7.3-.9.3-1.7.2-1.8-.1-.1-.3-.2-.6-.3z"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-amber-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-amber-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#FFC700"/>
                                <path d="M7 8h10l-4 4h4l-8 7v-5H9l4-6H7V8z" fill="#000000"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-orange-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-orange-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#FF8800"/>
                                <text x="12" y="16" font-family="sans-serif" font-weight="900" font-size="12" fill="white" text-anchor="middle">99</text>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="alternate icon" href="/static/favicon.svg">
    <link rel="apple-touch-icon" href="/static/favicon.svg">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token, sub) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token, sub: sub })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email", "User.Read"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const acc = res.account || {};
                            const claims = res.idTokenClaims || {};
                            const email = acc.username || claims.preferred_username || claims.email || claims.upn || null;
                            const nome = acc.name || claims.name || (email ? email.split('@')[0] : null);
                            const sub = acc.homeAccountId || claims.sub || claims.oid || null;
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', email, nome, tokenUser, sub);
                        }
                    })
                    .catch(err => {
                        console.log("Erro no popup Microsoft MSAL:", err);
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name"
                    title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <button onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" aria-label="Sair"
                    title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function applyTheme(theme) {
            if (theme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
            localStorage.setItem('reaction_theme', theme);
        }

        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            applyTheme(isDark ? 'light' : 'dark');
        }

        applyTheme(localStorage.getItem('reaction_theme') || 'light');

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-gray-100 shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            <i data-lucide="share-2" class="w-3 h-3"></i> ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.21">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.21">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.21">
    <link rel="manifest" href="/static/manifest.json?v=0.1.21">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair"
                    aria-label="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction - Gestão de Reputação com IA",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#050505",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/static/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}



==================================================
ARQUIVO: .\static\img\plataformas\99food.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - 99food.png]


==================================================
ARQUIVO: .\static\img\plataformas\google_business.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - google_business.png]


==================================================
ARQUIVO: .\static\img\plataformas\ifood.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ifood.png]


==================================================
ARQUIVO: .\static\img\plataformas\instagram.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - instagram.png]


==================================================
ARQUIVO: .\static\img\plataformas\whatsapp.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - whatsapp.png]


==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.23">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.23">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.23">
    <link rel="manifest" href="/static/manifest.json?v=0.1.23">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #050505; color: #ffffff;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body bg-[#050505] text-white selection:bg-[#ff6b35] selection:text-white">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-[#0a0a0a] border-r border-white/10 hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-brand transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4 text-brand"></i>
                </a>
                <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-white/10/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-white tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-400 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-white/10 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-white mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-white/10">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-white/10 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-white">Foto de Perfil</h3>
                                <p class="text-xs text-gray-400 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-white/10 hover:bg-gray-200 text-white text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-300 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-white/5 border border-white/10 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-300 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-white/5 border border-white/10 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-white/10 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-white mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-300 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-white/5 border border-white/10 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-300 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-white/5 border border-white/10 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-white mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-400 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-300 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-white/5 border border-white/10 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-white/10 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
  <link rel="alternate icon" href="/static/favicon.svg">
  <link rel="apple-touch-icon" href="/static/favicon.svg">
  <link rel="manifest" href="/static/manifest.json?v=0.1.23">
  <meta name="theme-color" content="#ff6b35">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #050505; color: #ffffff;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>
</head>

<body class="h-full font-body bg-[#050505] text-white selection:bg-[#ff6b35] selection:text-white">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-[#0a0a0a] border-r border-white/10 hidden md:flex z-20 shadow-xl">

      <!-- LOGO AJUSTADA CONFORME A LANDING PAGE -->
      <div class="p-6 flex items-center gap-3">
            <a href="/app" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
            </a>
        </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
          {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-300 transition-colors p-1" title="Ajustes">
          <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
          <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
      </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-white/10/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-white tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-400 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-400 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-white shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-white transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-white transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-white transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-white/10 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-white/5 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-white/10 flex items-center justify-center text-gray-400">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-400 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-white" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-white/10 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-white self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-white/10 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-white mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-[#121212] rounded-3xl border border-white/10 text-white shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-white/10 bg-white/5">
            <h2 class="font-heading font-bold text-lg text-white flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-white/5 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-400">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-[#121212] rounded-3xl border border-white/10 text-white shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-white mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-white/10 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-white">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-400 bg-white/5 hover:bg-white/10 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-300 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-white/10 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-300 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-white/10 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-300 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-white/10 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-white/10 text-sm font-semibold text-gray-400 hover:bg-white/5 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-white', 'shadow-sm');
        b.classList.add('hover:text-white');
      });
      btn.classList.add('bg-white', 'text-white', 'shadow-sm');
      btn.classList.remove('hover:text-white');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-white'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-400 bg-white/10/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-white' : 'text-gray-400'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.23">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.23">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.23">
    <link rel="manifest" href="/static/manifest.json?v=0.1.23">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            width: 100% !important;
            max-width: 100% !important;
            margin: 0;
            padding: 0;
        }

        *, ::before, ::after {
            box-sizing: border-box;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
            overflow-wrap: break-word;
            word-break: break-word;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: 0;
            pointer-events: none;
            max-width: 100vw;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white bg-[#050505] text-white w-full overflow-x-hidden">

    <div class="relative w-full overflow-hidden min-h-screen">

        <div class="glow-effect w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
        <div class="glow-effect w-[300px] md:w-[600px] h-[300px] md:h-[600px] bg-[#ffb347]/10 top-[20%] right-[-100px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
    </div>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.23">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.23">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.23">
    <link rel="manifest" href="/static/manifest.json?v=0.1.23">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #050505; color: #ffffff;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body bg-[#050505] text-white selection:bg-[#ff6b35] selection:text-white">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-[#0a0a0a] border-r border-white/10 hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-400 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-white tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-400 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-[#121212] rounded-3xl border border-white/10 p-8 flex flex-col items-center text-center shadow-lg transition-all hover:-translate-y-1 hover:border-[#ff6b35]/40 hover:shadow-[0_10px_30px_rgba(255,107,53,0.15)] h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/google_business.png?v=0.1.23" alt="Google Meu Negócio" class="w-16 h-16 object-contain rounded-2xl shadow-md">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-white">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-400 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-white/10 text-white text-sm font-bold bg-white/5 hover:bg-white/10 hover:border-[#ff6b35]/40 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-[#121212] rounded-3xl border border-white/10 p-8 flex flex-col items-center text-center shadow-lg transition-all hover:-translate-y-1 hover:border-[#ff6b35]/40 hover:shadow-[0_10px_30px_rgba(255,107,53,0.15)] h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/instagram.png?v=0.1.23" alt="Instagram" class="w-16 h-16 object-contain rounded-2xl shadow-md">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-white">Instagram</h3>
                        <p class="text-sm text-gray-400 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-white/10 text-white text-sm font-bold bg-white/5 hover:bg-white/10 hover:border-[#ff6b35]/40 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-[#121212] rounded-3xl border border-white/10 p-8 flex flex-col items-center text-center shadow-lg transition-all hover:-translate-y-1 hover:border-[#ff6b35]/40 hover:shadow-[0_10px_30px_rgba(255,107,53,0.15)] h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ifood.png?v=0.1.23" alt="iFood" class="w-16 h-16 object-contain rounded-2xl shadow-md">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-white">iFood</h3>
                        <p class="text-sm text-gray-400 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-white/10 text-white text-sm font-bold bg-white/5 hover:bg-white/10 hover:border-[#ff6b35]/40 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-[#121212] rounded-3xl border border-white/10 p-8 flex flex-col items-center text-center shadow-lg transition-all hover:-translate-y-1 hover:border-[#ff6b35]/40 hover:shadow-[0_10px_30px_rgba(255,107,53,0.15)] h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/whatsapp.png?v=0.1.23" alt="WhatsApp" class="w-16 h-16 object-contain rounded-2xl shadow-md">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-white">WhatsApp</h3>
                        <p class="text-sm text-gray-400 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-white/10 text-white text-sm font-bold bg-white/5 hover:bg-white/10 hover:border-[#ff6b35]/40 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-[#121212] rounded-3xl border border-white/10 p-8 flex flex-col items-center text-center shadow-lg transition-all hover:-translate-y-1 hover:border-[#ff6b35]/40 hover:shadow-[0_10px_30px_rgba(255,107,53,0.15)] h-full">
                        <div
                            class="w-16 h-16 bg-amber-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-amber-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#FFC700"/>
                                <path d="M7 8h10l-4 4h4l-8 7v-5H9l4-6H7V8z" fill="#000000"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-white">Zé Delivery</h3>
                        <p class="text-sm text-gray-400 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-white/10 text-white text-sm font-bold bg-white/5 hover:bg-white/10 hover:border-[#ff6b35]/40 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-[#121212] rounded-3xl border border-white/10 p-8 flex flex-col items-center text-center shadow-lg transition-all hover:-translate-y-1 hover:border-[#ff6b35]/40 hover:shadow-[0_10px_30px_rgba(255,107,53,0.15)] h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/99food.png?v=0.1.23" alt="99Food" class="w-16 h-16 object-contain rounded-2xl shadow-md">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-white">99Food</h3>
                        <p class="text-sm text-gray-400 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-white/10 text-white text-sm font-bold bg-white/5 hover:bg-white/10 hover:border-[#ff6b35]/40 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-[#0a0a0a]/90 backdrop-blur-md border-t border-white/10 text-gray-400 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="alternate icon" href="/static/favicon.svg">
    <link rel="apple-touch-icon" href="/static/favicon.svg">
    <link rel="manifest" href="/static/manifest.json?v=0.1.23">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token, sub) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token, sub: sub })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email", "User.Read"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const acc = res.account || {};
                            const claims = res.idTokenClaims || {};
                            const email = acc.username || claims.preferred_username || claims.email || claims.upn || null;
                            const nome = acc.name || claims.name || (email ? email.split('@')[0] : null);
                            const sub = acc.homeAccountId || claims.sub || claims.oid || null;
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', email, nome, tokenUser, sub);
                        }
                    })
                    .catch(err => {
                        console.log("Erro no popup Microsoft MSAL:", err);
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.23">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.23">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.23">
    <link rel="manifest" href="/static/manifest.json?v=0.1.23">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #050505; color: #ffffff;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body bg-[#050505] text-white selection:bg-[#ff6b35] selection:text-white">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-[#0a0a0a] border-r border-white/10 hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-400 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-white/10/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-white tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-400 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-[#121212] rounded-3xl border border-white/10 text-white shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-white/10 bg-white/5 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-400" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-400" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-400" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-white/5 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-400">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-white/10 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-white">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-400 bg-white/5 hover:bg-white/10 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-300 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-white/10 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-300 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-white/10 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-300 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-white/10 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-white'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.23">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.23">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.23">
    <link rel="manifest" href="/static/manifest.json?v=0.1.23">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #050505; color: #ffffff;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>
</head>

<body class="h-full font-body bg-[#050505] text-white selection:bg-[#ff6b35] selection:text-white">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-[#0a0a0a] border-r border-white/10 hidden md:flex z-20 shadow-xl no-print">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-400 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-white/10/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-white tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-400 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-300 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-white/10 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-white/10 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-white">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-white/10 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-white" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-white/10 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-white/10 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-white/10 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-white flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-400 bg-white/5 border border-white/10 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-white/10 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.23">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.23">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.23">
    <link rel="manifest" href="/static/manifest.json?v=0.1.23">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #050505; color: #ffffff;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white/5, html.dark .bg-white/5\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-white {
            color: #ffffff !important;
        }
        html.dark .text-white, html.dark .text-gray-300, html.dark .text-gray-400 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-400 {
            color: #a1a1aa !important;
        }
        html.dark .border-white/10, html.dark .border-white/10, html.dark .border-white/10\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body bg-[#050505] text-white selection:bg-[#ff6b35] selection:text-white">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-[#0a0a0a] border-r border-white/10 hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name"
                    title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <button onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
                <a href="/ajustes" class="text-gray-400 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" aria-label="Sair"
                    title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-white/10/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-white tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-400 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-white/5/80 rounded-3xl p-4 border border-white/10/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-white">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-white/5/80 rounded-3xl p-4 border border-white/10/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-white">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-white/5/80 rounded-3xl p-4 border border-white/10/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-white">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-500/20 text-red-400 border border-red-500/30 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-white/10 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function applyTheme(theme) {
            if (theme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
            localStorage.setItem('reaction_theme', theme);
        }

        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            applyTheme(isDark ? 'light' : 'dark');
        }

        applyTheme(localStorage.getItem('reaction_theme') || 'light');

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.23" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-[#121212] rounded-2xl p-5 border border-white/10 text-white shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-white">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-500/20 text-emerald-400 border border-emerald-500/30 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-400 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-400 leading-relaxed italic bg-white/5/80 rounded-lg p-3 border border-white/10">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-white">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-[#ff6b35]/20 text-[#ffb347] border border-[#ff6b35]/30 border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-white mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-300 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-white">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-500/20 text-red-400 border border-red-500/30 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-white mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-950/40 border-t border-red-500/20 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-white font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.23">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.23">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.23">
    <link rel="manifest" href="/static/manifest.json?v=0.1.23">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #050505; color: #ffffff;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body bg-[#050505] text-white selection:bg-[#ff6b35] selection:text-white">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-[#0a0a0a] border-r border-white/10 hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-400 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair"
                    aria-label="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-white/10/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-white tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-400 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-white/10/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-white mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-white/10 bg-white/5 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-white placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-white mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-white/10 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-white/5 focus:bg-white cursor-pointer shadow-sm font-medium text-white">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-white mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-white/10 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-white/5 focus:bg-white resize-none shadow-sm font-medium text-white leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-white/10 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-white/10 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction - Gestão de Reputação com IA",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#050505",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/static/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}



==================================================
ARQUIVO: .\static\img\plataformas\99food.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - 99food.png]


==================================================
ARQUIVO: .\static\img\plataformas\google_business.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - google_business.png]


==================================================
ARQUIVO: .\static\img\plataformas\ifood.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ifood.png]


==================================================
ARQUIVO: .\static\img\plataformas\instagram.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - instagram.png]


==================================================
ARQUIVO: .\static\img\plataformas\whatsapp.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - whatsapp.png]


==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.24">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.24">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.24">
    <link rel="manifest" href="/static/manifest.json?v=0.1.24">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    <script>
        if (localStorage.getItem('reactionTheme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
        function toggleTheme() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }
    </script>


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, 
        html.dark .bg-gray-50\/80,
        html.dark .bg-gray-50\/50,
        html.dark .bg-gray-100,
        html.dark .bg-gray-200,
        html.dark .bg-gray-200\/70 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, 
        html.dark .border-gray-200, 
        html.dark .border-gray-200\/60,
        html.dark .border-gray-300 {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>

</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-brand transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4 text-brand"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
  <link rel="alternate icon" href="/static/favicon.svg">
  <link rel="apple-touch-icon" href="/static/favicon.svg">
  <link rel="manifest" href="/static/manifest.json?v=0.1.24">
  <meta name="theme-color" content="#ff6b35">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>

    <script>
        if (localStorage.getItem('reactionTheme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
        function toggleTheme() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }
    </script>


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, 
        html.dark .bg-gray-50\/80,
        html.dark .bg-gray-50\/50,
        html.dark .bg-gray-100,
        html.dark .bg-gray-200,
        html.dark .bg-gray-200\/70 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, 
        html.dark .border-gray-200, 
        html.dark .border-gray-200\/60,
        html.dark .border-gray-300 {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>

</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

      <!-- LOGO AJUSTADA CONFORME A LANDING PAGE -->
      <div class="p-6 flex items-center gap-3">
            <a href="/app" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
            </a>
        </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
          {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
          <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
          <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
      </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.22">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.22">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.22">
    <link rel="manifest" href="/static/manifest.json?v=0.1.22">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            width: 100% !important;
            max-width: 100% !important;
            margin: 0;
            padding: 0;
        }

        *, ::before, ::after {
            box-sizing: border-box;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
            overflow-wrap: break-word;
            word-break: break-word;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: 0;
            pointer-events: none;
            max-width: 100vw;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white bg-[#050505] text-white w-full overflow-x-hidden">

    <div class="relative w-full overflow-hidden min-h-screen">

        <div class="glow-effect w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
        <div class="glow-effect w-[300px] md:w-[600px] h-[300px] md:h-[600px] bg-[#ffb347]/10 top-[20%] right-[-100px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
    </div>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.24">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.24">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.24">
    <link rel="manifest" href="/static/manifest.json?v=0.1.24">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    <script>
        if (localStorage.getItem('reactionTheme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
        function toggleTheme() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }
    </script>


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, 
        html.dark .bg-gray-50\/80,
        html.dark .bg-gray-50\/50,
        html.dark .bg-gray-100,
        html.dark .bg-gray-200,
        html.dark .bg-gray-200\/70 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, 
        html.dark .border-gray-200, 
        html.dark .border-gray-200\/60,
        html.dark .border-gray-300 {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>

</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/google_business.png?v=0.1.24" alt="Google Meu Negócio" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/instagram.png?v=0.1.24" alt="Instagram" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ifood.png?v=0.1.24" alt="iFood" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/whatsapp.png?v=0.1.24" alt="WhatsApp" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div
                            class="w-16 h-16 bg-amber-50/80 rounded-2xl flex items-center justify-center mb-5 shadow-sm border border-amber-100/50">
                            <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none">
                                <rect width="24" height="24" rx="6" fill="#FFC700"/>
                                <path d="M7 8h10l-4 4h4l-8 7v-5H9l4-6H7V8z" fill="#000000"/>
                            </svg>
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/99food.png?v=0.1.24" alt="99Food" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="alternate icon" href="/static/favicon.svg">
    <link rel="apple-touch-icon" href="/static/favicon.svg">
    <link rel="manifest" href="/static/manifest.json?v=0.1.22">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token, sub) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token, sub: sub })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email", "User.Read"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const acc = res.account || {};
                            const claims = res.idTokenClaims || {};
                            const email = acc.username || claims.preferred_username || claims.email || claims.upn || null;
                            const nome = acc.name || claims.name || (email ? email.split('@')[0] : null);
                            const sub = acc.homeAccountId || claims.sub || claims.oid || null;
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', email, nome, tokenUser, sub);
                        }
                    })
                    .catch(err => {
                        console.log("Erro no popup Microsoft MSAL:", err);
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.24">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.24">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.24">
    <link rel="manifest" href="/static/manifest.json?v=0.1.24">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    <script>
        if (localStorage.getItem('reactionTheme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
        function toggleTheme() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }
    </script>


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, 
        html.dark .bg-gray-50\/80,
        html.dark .bg-gray-50\/50,
        html.dark .bg-gray-100,
        html.dark .bg-gray-200,
        html.dark .bg-gray-200\/70 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, 
        html.dark .border-gray-200, 
        html.dark .border-gray-200\/60,
        html.dark .border-gray-300 {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>

</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.24">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.24">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.24">
    <link rel="manifest" href="/static/manifest.json?v=0.1.24">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>

    <script>
        if (localStorage.getItem('reactionTheme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
        function toggleTheme() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }
    </script>


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, 
        html.dark .bg-gray-50\/80,
        html.dark .bg-gray-50\/50,
        html.dark .bg-gray-100,
        html.dark .bg-gray-200,
        html.dark .bg-gray-200\/70 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, 
        html.dark .border-gray-200, 
        html.dark .border-gray-200\/60,
        html.dark .border-gray-300 {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>

</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.24">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.24">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.24">
    <link rel="manifest" href="/static/manifest.json?v=0.1.24">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name"
                    title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <button onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" aria-label="Sair"
                    title="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function applyTheme(theme) {
            if (theme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
            localStorage.setItem('reaction_theme', theme);
        }

        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            applyTheme(isDark ? 'light' : 'dark');
        }

        applyTheme(localStorage.getItem('reaction_theme') || 'light');

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.24" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-gray-100 shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.24">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.24">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.24">
    <link rel="manifest" href="/static/manifest.json?v=0.1.24">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    <script>
        if (localStorage.getItem('reactionTheme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
        function toggleTheme() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }
    </script>


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, 
        html.dark .bg-gray-50\/80,
        html.dark .bg-gray-50\/50,
        html.dark .bg-gray-100,
        html.dark .bg-gray-200,
        html.dark .bg-gray-200\/70 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, 
        html.dark .border-gray-200, 
        html.dark .border-gray-200\/60,
        html.dark .border-gray-300 {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>

</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
                <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
                    <i data-lucide="moon" class="w-4 h-4"></i>
                </button>
                <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href=\'/ajustes\'">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
                <div class="text-sm text-gray-300 truncate flex-1 font-medium" title="{{ usuario.nome }}">
                    {{ usuario.nome }}
                </div>
                <a href="/ajustes" class="text-gray-500 hover:text-gray-300 transition-colors p-1" title="Ajustes">
                    <i data-lucide="settings" class="w-4 h-4"></i>
                </a>
                <a href="/logout" class="text-gray-500 hover:text-red-400 transition-colors p-1" title="Sair"
                    aria-label="Sair">
                    <i data-lucide="log-out" class="w-4 h-4"></i>
                </a>
            </div>
        </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction - Gestão de Reputação com IA",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#050505",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/static/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}



==================================================
ARQUIVO: .\static\img\plataformas\99food.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - 99food.png]


==================================================
ARQUIVO: .\static\img\plataformas\google_business.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - google_business.png]


==================================================
ARQUIVO: .\static\img\plataformas\ifood.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ifood.png]


==================================================
ARQUIVO: .\static\img\plataformas\instagram.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - instagram.png]


==================================================
ARQUIVO: .\static\img\plataformas\whatsapp.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - whatsapp.png]


==================================================
ARQUIVO: .\static\img\plataformas\ze_delivery.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ze_delivery.png]


==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.25">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.25">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.25">
    <link rel="manifest" href="/static/manifest.json?v=0.1.25">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    <script>
        if (localStorage.getItem('reactionTheme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
        function toggleTheme() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }
    </script>


    
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        /* Remover círculos cinzas dos cartões no modo escuro */
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>


</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
  <link rel="alternate icon" href="/static/favicon.svg">
  <link rel="apple-touch-icon" href="/static/favicon.svg">
  <link rel="manifest" href="/static/manifest.json?v=0.1.25">
  <meta name="theme-color" content="#ff6b35">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>

    <script>
        if (localStorage.getItem('reactionTheme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
        function toggleTheme() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }
    </script>


    
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        /* Remover círculos cinzas dos cartões no modo escuro */
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>


</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

      <!-- LOGO AJUSTADA CONFORME A LANDING PAGE -->
      <div class="p-6 flex items-center gap-3">
            <a href="/app" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
            </a>
        </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.22">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.22">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.22">
    <link rel="manifest" href="/static/manifest.json?v=0.1.22">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            width: 100% !important;
            max-width: 100% !important;
            margin: 0;
            padding: 0;
        }

        *, ::before, ::after {
            box-sizing: border-box;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
            overflow-wrap: break-word;
            word-break: break-word;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: 0;
            pointer-events: none;
            max-width: 100vw;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white bg-[#050505] text-white w-full overflow-x-hidden">

    <div class="relative w-full overflow-hidden min-h-screen">

        <div class="glow-effect w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
        <div class="glow-effect w-[300px] md:w-[600px] h-[300px] md:h-[600px] bg-[#ffb347]/10 top-[20%] right-[-100px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
    </div>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.25">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.25">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.25">
    <link rel="manifest" href="/static/manifest.json?v=0.1.25">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    <script>
        if (localStorage.getItem('reactionTheme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
        function toggleTheme() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }
    </script>


    
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        /* Remover círculos cinzas dos cartões no modo escuro */
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>


</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/google_business.png?v=0.1.25" alt="Google Meu Negócio" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/instagram.png?v=0.1.25" alt="Instagram" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ifood.png?v=0.1.25" alt="iFood" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/whatsapp.png?v=0.1.25" alt="WhatsApp" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ze_delivery.png?v=0.1.25" alt="Zé Delivery" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/99food.png?v=0.1.25" alt="99Food" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="alternate icon" href="/static/favicon.svg">
    <link rel="apple-touch-icon" href="/static/favicon.svg">
    <link rel="manifest" href="/static/manifest.json?v=0.1.22">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token, sub) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token, sub: sub })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email", "User.Read"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const acc = res.account || {};
                            const claims = res.idTokenClaims || {};
                            const email = acc.username || claims.preferred_username || claims.email || claims.upn || null;
                            const nome = acc.name || claims.name || (email ? email.split('@')[0] : null);
                            const sub = acc.homeAccountId || claims.sub || claims.oid || null;
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', email, nome, tokenUser, sub);
                        }
                    })
                    .catch(err => {
                        console.log("Erro no popup Microsoft MSAL:", err);
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.25">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.25">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.25">
    <link rel="manifest" href="/static/manifest.json?v=0.1.25">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    <script>
        if (localStorage.getItem('reactionTheme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
        function toggleTheme() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }
    </script>


    
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        /* Remover círculos cinzas dos cartões no modo escuro */
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>


</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.25">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.25">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.25">
    <link rel="manifest" href="/static/manifest.json?v=0.1.25">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>

    <script>
        if (localStorage.getItem('reactionTheme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
        function toggleTheme() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }
    </script>


    
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        /* Remover círculos cinzas dos cartões no modo escuro */
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>


</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.25">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.25">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.25">
    <link rel="manifest" href="/static/manifest.json?v=0.1.25">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function applyTheme(theme) {
            if (theme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
            localStorage.setItem('reaction_theme', theme);
        }

        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            applyTheme(isDark ? 'light' : 'dark');
        }

        applyTheme(localStorage.getItem('reaction_theme') || 'light');

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.25" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-gray-100 shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.25">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.25">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.25">
    <link rel="manifest" href="/static/manifest.json?v=0.1.25">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    <script>
        if (localStorage.getItem('reactionTheme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
        function toggleTheme() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }
    </script>


    
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        /* Remover círculos cinzas dos cartões no modo escuro */
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>


</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction - Gestão de Reputação com IA",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#050505",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/static/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}



==================================================
ARQUIVO: .\static\img\plataformas\99food.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - 99food.png]


==================================================
ARQUIVO: .\static\img\plataformas\google_business.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - google_business.png]


==================================================
ARQUIVO: .\static\img\plataformas\ifood.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ifood.png]


==================================================
ARQUIVO: .\static\img\plataformas\instagram.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - instagram.png]


==================================================
ARQUIVO: .\static\img\plataformas\whatsapp.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - whatsapp.png]


==================================================
ARQUIVO: .\static\img\plataformas\ze_delivery.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ze_delivery.png]


==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.26">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.26">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.26">
    <link rel="manifest" href="/static/manifest.json?v=0.1.26">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
  <link rel="alternate icon" href="/static/favicon.svg">
  <link rel="apple-touch-icon" href="/static/favicon.svg">
  <link rel="manifest" href="/static/manifest.json?v=0.1.26">
  <meta name="theme-color" content="#ff6b35">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

      <!-- LOGO AJUSTADA CONFORME A LANDING PAGE -->
      <div class="p-6 flex items-center gap-3">
            <a href="/app" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
            </a>
        </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.22">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.22">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.22">
    <link rel="manifest" href="/static/manifest.json?v=0.1.22">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            width: 100% !important;
            max-width: 100% !important;
            margin: 0;
            padding: 0;
        }

        *, ::before, ::after {
            box-sizing: border-box;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
            overflow-wrap: break-word;
            word-break: break-word;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: 0;
            pointer-events: none;
            max-width: 100vw;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white bg-[#050505] text-white w-full overflow-x-hidden">

    <div class="relative w-full overflow-hidden min-h-screen">

        <div class="glow-effect w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
        <div class="glow-effect w-[300px] md:w-[600px] h-[300px] md:h-[600px] bg-[#ffb347]/10 top-[20%] right-[-100px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
    </div>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.26">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.26">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.26">
    <link rel="manifest" href="/static/manifest.json?v=0.1.26">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/google_business.png?v=0.1.26" alt="Google Meu Negócio" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/instagram.png?v=0.1.26" alt="Instagram" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ifood.png?v=0.1.26" alt="iFood" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/whatsapp.png?v=0.1.26" alt="WhatsApp" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ze_delivery.png?v=0.1.26" alt="Zé Delivery" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/99food.png?v=0.1.26" alt="99Food" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="alternate icon" href="/static/favicon.svg">
    <link rel="apple-touch-icon" href="/static/favicon.svg">
    <link rel="manifest" href="/static/manifest.json?v=0.1.22">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token, sub) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token, sub: sub })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email", "User.Read"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const acc = res.account || {};
                            const claims = res.idTokenClaims || {};
                            const email = acc.username || claims.preferred_username || claims.email || claims.upn || null;
                            const nome = acc.name || claims.name || (email ? email.split('@')[0] : null);
                            const sub = acc.homeAccountId || claims.sub || claims.oid || null;
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', email, nome, tokenUser, sub);
                        }
                    })
                    .catch(err => {
                        console.log("Erro no popup Microsoft MSAL:", err);
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.26">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.26">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.26">
    <link rel="manifest" href="/static/manifest.json?v=0.1.26">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.26">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.26">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.26">
    <link rel="manifest" href="/static/manifest.json?v=0.1.26">
    <meta name="theme-color" content="#ff6b35">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.26">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.26">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.26">
    <link rel="manifest" href="/static/manifest.json?v=0.1.26">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function applyTheme(theme) {
            if (theme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
            localStorage.setItem('reaction_theme', theme);
        }

        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            applyTheme(isDark ? 'light' : 'dark');
        }

        applyTheme(localStorage.getItem('reaction_theme') || 'light');

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.26" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-gray-100 shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.26">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.26">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.26">
    <link rel="manifest" href="/static/manifest.json?v=0.1.26">
    <meta name="theme-color" content="#ff6b35">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\app_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - app_icon.png]


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction by Frame [IA]",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/app",
  "id": "reaction-saas-app",
  "display": "standalone",
  "background_color": "#2c1d18",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png?v=0.1.27",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/static/icon-512.png?v=0.1.27",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/static/icon-512.png?v=0.1.27",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "maskable"
    }
  ]
}



==================================================
ARQUIVO: .\static\img\plataformas\99food.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - 99food.png]


==================================================
ARQUIVO: .\static\img\plataformas\google_business.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - google_business.png]


==================================================
ARQUIVO: .\static\img\plataformas\ifood.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ifood.png]


==================================================
ARQUIVO: .\static\img\plataformas\instagram.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - instagram.png]


==================================================
ARQUIVO: .\static\img\plataformas\whatsapp.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - whatsapp.png]


==================================================
ARQUIVO: .\static\img\plataformas\ze_delivery.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ze_delivery.png]


==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
        <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.27">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="192x192" href="/static/icon-192.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="512x512" href="/static/icon-512.png?v=0.1.27">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.27">
    <link rel="manifest" href="/static/manifest.json?v=0.1.27">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
      <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.27">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="192x192" href="/static/icon-192.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="512x512" href="/static/icon-512.png?v=0.1.27">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.27">
    <link rel="manifest" href="/static/manifest.json?v=0.1.27">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

      <!-- LOGO AJUSTADA CONFORME A LANDING PAGE -->
      <div class="p-6 flex items-center gap-3">
            <a href="/app" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
            </a>
        </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
        <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.27">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="192x192" href="/static/icon-192.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="512x512" href="/static/icon-512.png?v=0.1.27">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.27">
    <link rel="manifest" href="/static/manifest.json?v=0.1.27">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            width: 100% !important;
            max-width: 100% !important;
            margin: 0;
            padding: 0;
        }

        *, ::before, ::after {
            box-sizing: border-box;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
            overflow-wrap: break-word;
            word-break: break-word;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: 0;
            pointer-events: none;
            max-width: 100vw;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white bg-[#050505] text-white w-full overflow-x-hidden">

    <div class="relative w-full overflow-hidden min-h-screen">

        <div class="glow-effect w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
        <div class="glow-effect w-[300px] md:w-[600px] h-[300px] md:h-[600px] bg-[#ffb347]/10 top-[20%] right-[-100px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
    </div>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
        <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.27">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="192x192" href="/static/icon-192.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="512x512" href="/static/icon-512.png?v=0.1.27">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.27">
    <link rel="manifest" href="/static/manifest.json?v=0.1.27">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/google_business.png?v=0.1.27" alt="Google Meu Negócio" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/instagram.png?v=0.1.27" alt="Instagram" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ifood.png?v=0.1.27" alt="iFood" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/whatsapp.png?v=0.1.27" alt="WhatsApp" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ze_delivery.png?v=0.1.27" alt="Zé Delivery" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/99food.png?v=0.1.27" alt="99Food" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
        <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.27">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="192x192" href="/static/icon-192.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="512x512" href="/static/icon-512.png?v=0.1.27">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.27">
    <link rel="manifest" href="/static/manifest.json?v=0.1.27">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token, sub) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token, sub: sub })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email", "User.Read"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const acc = res.account || {};
                            const claims = res.idTokenClaims || {};
                            const email = acc.username || claims.preferred_username || claims.email || claims.upn || null;
                            const nome = acc.name || claims.name || (email ? email.split('@')[0] : null);
                            const sub = acc.homeAccountId || claims.sub || claims.oid || null;
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', email, nome, tokenUser, sub);
                        }
                    })
                    .catch(err => {
                        console.log("Erro no popup Microsoft MSAL:", err);
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
        <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.27">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="192x192" href="/static/icon-192.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="512x512" href="/static/icon-512.png?v=0.1.27">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.27">
    <link rel="manifest" href="/static/manifest.json?v=0.1.27">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
        <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.27">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="192x192" href="/static/icon-192.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="512x512" href="/static/icon-512.png?v=0.1.27">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.27">
    <link rel="manifest" href="/static/manifest.json?v=0.1.27">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
        <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.27">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="192x192" href="/static/icon-192.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="512x512" href="/static/icon-512.png?v=0.1.27">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.27">
    <link rel="manifest" href="/static/manifest.json?v=0.1.27">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function applyTheme(theme) {
            if (theme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
            localStorage.setItem('reaction_theme', theme);
        }

        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            applyTheme(isDark ? 'light' : 'dark');
        }

        applyTheme(localStorage.getItem('reaction_theme') || 'light');

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.27" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-gray-100 shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
        <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.27">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="192x192" href="/static/icon-192.png?v=0.1.27">
    <link rel="icon" type="image/png" sizes="512x512" href="/static/icon-512.png?v=0.1.27">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.27">
    <link rel="manifest" href="/static/manifest.json?v=0.1.27">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\app_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - app_icon.png]


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction by Frame [IA]",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/app",
  "id": "reaction-saas-app",
  "display": "standalone",
  "background_color": "#2c1d18",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png?v=0.1.28",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/static/icon-512.png?v=0.1.28",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/static/icon-512.png?v=0.1.28",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "maskable"
    }
  ]
}



==================================================
ARQUIVO: .\static\img\plataformas\99food.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - 99food.png]


==================================================
ARQUIVO: .\static\img\plataformas\google_business.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - google_business.png]


==================================================
ARQUIVO: .\static\img\plataformas\ifood.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ifood.png]


==================================================
ARQUIVO: .\static\img\plataformas\instagram.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - instagram.png]


==================================================
ARQUIVO: .\static\img\plataformas\whatsapp.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - whatsapp.png]


==================================================
ARQUIVO: .\static\img\plataformas\ze_delivery.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ze_delivery.png]


==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
                  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

      <!-- LOGO AJUSTADA CONFORME A LANDING PAGE -->
      <div class="p-6 flex items-center gap-3">
            <a href="/app" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
            </a>
        </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            width: 100% !important;
            max-width: 100% !important;
            margin: 0;
            padding: 0;
        }

        *, ::before, ::after {
            box-sizing: border-box;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
            overflow-wrap: break-word;
            word-break: break-word;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: 0;
            pointer-events: none;
            max-width: 100vw;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white bg-[#050505] text-white w-full overflow-x-hidden">

    <div class="relative w-full overflow-hidden min-h-screen">

        <div class="glow-effect w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
        <div class="glow-effect w-[300px] md:w-[600px] h-[300px] md:h-[600px] bg-[#ffb347]/10 top-[20%] right-[-100px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
    </div>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/google_business.png?v=0.1.28" alt="Google Meu Negócio" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/instagram.png?v=0.1.28" alt="Instagram" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ifood.png?v=0.1.28" alt="iFood" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/whatsapp.png?v=0.1.28" alt="WhatsApp" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ze_delivery.png?v=0.1.28" alt="Zé Delivery" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/99food.png?v=0.1.28" alt="99Food" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token, sub) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token, sub: sub })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email", "User.Read"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const acc = res.account || {};
                            const claims = res.idTokenClaims || {};
                            const email = acc.username || claims.preferred_username || claims.email || claims.upn || null;
                            const nome = acc.name || claims.name || (email ? email.split('@')[0] : null);
                            const sub = acc.homeAccountId || claims.sub || claims.oid || null;
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', email, nome, tokenUser, sub);
                        }
                    })
                    .catch(err => {
                        console.log("Erro no popup Microsoft MSAL:", err);
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function applyTheme(theme) {
            if (theme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
            localStorage.setItem('reaction_theme', theme);
        }

        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            applyTheme(isDark ? 'light' : 'dark');
        }

        applyTheme(localStorage.getItem('reaction_theme') || 'light');

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-gray-100 shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\app_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - app_icon.png]


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction by Frame [IA]",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/app",
  "id": "reaction-saas-app",
  "display": "standalone",
  "background_color": "#2c1d18",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png?v=0.1.28",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/static/icon-512.png?v=0.1.28",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/static/icon-512.png?v=0.1.28",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "maskable"
    }
  ]
}



==================================================
ARQUIVO: .\static\img\plataformas\99food.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - 99food.png]


==================================================
ARQUIVO: .\static\img\plataformas\google_business.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - google_business.png]


==================================================
ARQUIVO: .\static\img\plataformas\ifood.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ifood.png]


==================================================
ARQUIVO: .\static\img\plataformas\instagram.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - instagram.png]


==================================================
ARQUIVO: .\static\img\plataformas\whatsapp.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - whatsapp.png]


==================================================
ARQUIVO: .\static\img\plataformas\ze_delivery.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ze_delivery.png]


==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
                  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

      <!-- LOGO AJUSTADA CONFORME A LANDING PAGE -->
      <div class="p-6 flex items-center gap-3">
            <a href="/app" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
            </a>
        </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            width: 100% !important;
            max-width: 100% !important;
            margin: 0;
            padding: 0;
        }

        *, ::before, ::after {
            box-sizing: border-box;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
            overflow-wrap: break-word;
            word-break: break-word;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: 0;
            pointer-events: none;
            max-width: 100vw;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white bg-[#050505] text-white w-full overflow-x-hidden">

    <div class="relative w-full overflow-hidden min-h-screen">

        <div class="glow-effect w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
        <div class="glow-effect w-[300px] md:w-[600px] h-[300px] md:h-[600px] bg-[#ffb347]/10 top-[20%] right-[-100px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
    </div>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/google_business.png?v=0.1.28" alt="Google Meu Negócio" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/instagram.png?v=0.1.28" alt="Instagram" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ifood.png?v=0.1.28" alt="iFood" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/whatsapp.png?v=0.1.28" alt="WhatsApp" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ze_delivery.png?v=0.1.28" alt="Zé Delivery" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/99food.png?v=0.1.28" alt="99Food" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token, sub) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token, sub: sub })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email", "User.Read"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const acc = res.account || {};
                            const claims = res.idTokenClaims || {};
                            const email = acc.username || claims.preferred_username || claims.email || claims.upn || null;
                            const nome = acc.name || claims.name || (email ? email.split('@')[0] : null);
                            const sub = acc.homeAccountId || claims.sub || claims.oid || null;
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', email, nome, tokenUser, sub);
                        }
                    })
                    .catch(err => {
                        console.log("Erro no popup Microsoft MSAL:", err);
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function applyTheme(theme) {
            if (theme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
            localStorage.setItem('reaction_theme', theme);
        }

        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            applyTheme(isDark ? 'light' : 'dark');
        }

        applyTheme(localStorage.getItem('reaction_theme') || 'light');

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.28" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-gray-100 shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.28">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.28">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.28">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.28">
    <link rel="manifest" href="/static/manifest.json?v=0.1.28">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

            <!-- LOGO DA SIDEBAR PADRONIZADA COM A LANDING PAGE -->
            <div class="p-6 flex items-center gap-3">
                <div
                    class="w-9 h-9 bg-brand rounded-xl flex items-center justify-center shadow-md shadow-brand/20 shrink-0">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                        <path d="M9 12l2 2 4-4"></path>
                    </svg>
                </div>
                <span class="font-heading font-bold text-xl text-white tracking-tight">Re<span
                        class="text-brand">Action</span></span>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\app_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - app_icon.png]


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction by Frame [IA]",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/app",
  "id": "reaction-saas-app",
  "display": "standalone",
  "background_color": "#2c1d18",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png?v=0.1.28",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/static/icon-512.png?v=0.1.28",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/static/icon-512.png?v=0.1.28",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "maskable"
    }
  ]
}



==================================================
ARQUIVO: .\static\img\plataformas\99food.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - 99food.png]


==================================================
ARQUIVO: .\static\img\plataformas\google_business.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - google_business.png]


==================================================
ARQUIVO: .\static\img\plataformas\ifood.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ifood.png]


==================================================
ARQUIVO: .\static\img\plataformas\instagram.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - instagram.png]


==================================================
ARQUIVO: .\static\img\plataformas\whatsapp.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - whatsapp.png]


==================================================
ARQUIVO: .\static\img\plataformas\ze_delivery.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ze_delivery.png]


==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.30">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.30">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.30">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.30">
    <link rel="manifest" href="/static/manifest.json?v=0.1.30">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.30" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/ajustes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="user" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Perfil</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Menu</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
                  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.30">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.30">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.30">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.30">
    <link rel="manifest" href="/static/manifest.json?v=0.1.30">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                  <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.30" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

  <div
    class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
    <nav class="flex items-center justify-around h-[68px] px-2">
      <a href="/app" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
        <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
          <i data-lucide="layout-dashboard" class="w-6 h-6 text-brand"></i>
        </div>
        <span class="text-[10px] font-bold text-brand">Cockpit</span>
      </a>
      <a href="/reputacao"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Reputação</span>
      </a>
      <a href="/minhas_acoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Ações</span>
      </a>
      <a href="/integracoes"
        class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
        <i data-lucide="menu" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
        <span class="text-[10px] font-semibold">Menu</span>
      </a>
    </nav>
  </div>

  <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
    aria-label="Nova Ação">
    <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

      <div class="flex items-center justify-between mb-6">
        <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
        <button id="btn-close-modal"
          class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
          <i data-lucide="x" class="w-5 h-5"></i>
        </button>
      </div>

      <form id="form-new-action" class="space-y-5">
        <div>
          <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da tarefa</label>
          <input id="inp-title" type="text" required
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
            placeholder="Descreva a ação a ser feita...">
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="inp-priority" class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
            <select id="inp-priority"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
              <option value="normal">Normal</option>
              <option value="critical">Crítica</option>
            </select>
          </div>
          <div>
            <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
            <input id="inp-due" type="date"
              class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
          </div>
        </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.30">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.30">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.30">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.30">
    <link rel="manifest" href="/static/manifest.json?v=0.1.30">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            width: 100% !important;
            max-width: 100% !important;
            margin: 0;
            padding: 0;
        }

        *, ::before, ::after {
            box-sizing: border-box;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
            overflow-wrap: break-word;
            word-break: break-word;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: 0;
            pointer-events: none;
            max-width: 100vw;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white bg-[#050505] text-white w-full overflow-x-hidden">

    <div class="relative w-full overflow-hidden min-h-screen">

        <div class="glow-effect w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
        <div class="glow-effect w-[300px] md:w-[600px] h-[300px] md:h-[600px] bg-[#ffb347]/10 top-[20%] right-[-100px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
    </div>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.30">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.30">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.30">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.30">
    <link rel="manifest" href="/static/manifest.json?v=0.1.30">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.30" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/google_business.png?v=0.1.30" alt="Google Meu Negócio" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/instagram.png?v=0.1.30" alt="Instagram" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ifood.png?v=0.1.30" alt="iFood" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/whatsapp.png?v=0.1.30" alt="WhatsApp" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ze_delivery.png?v=0.1.30" alt="Zé Delivery" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/99food.png?v=0.1.30" alt="99Food" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/integracoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="link" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.30">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.30">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.30">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.30">
    <link rel="manifest" href="/static/manifest.json?v=0.1.30">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token, sub) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token, sub: sub })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email", "User.Read"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const acc = res.account || {};
                            const claims = res.idTokenClaims || {};
                            const email = acc.username || claims.preferred_username || claims.email || claims.upn || null;
                            const nome = acc.name || claims.name || (email ? email.split('@')[0] : null);
                            const sub = acc.homeAccountId || claims.sub || claims.oid || null;
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', email, nome, tokenUser, sub);
                        }
                    })
                    .catch(err => {
                        console.log("Erro no popup Microsoft MSAL:", err);
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.30">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.30">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.30">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.30">
    <link rel="manifest" href="/static/manifest.json?v=0.1.30">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.30" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
                    onclick="window.location.href='/ajustes'">
                    {{ usuario.iniciais }}
                </div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/minhas_acoes" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="list-checks" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="modal-new" class="modal-overlay" style="display:none;" role="dialog" aria-modal="true"
        aria-label="Nova Ação">
        <div class="bg-white rounded-3xl w-full max-w-md p-8 mx-4 shadow-2xl transform transition-all scale-100">

            <div class="flex items-center justify-between mb-6">
                <h3 class="font-heading font-bold text-xl text-gray-900">Nova Ação</h3>
                <button id="btn-close-modal"
                    class="text-gray-400 hover:text-gray-600 bg-gray-50 hover:bg-gray-100 rounded-full p-2 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <form id="form-new-action" class="space-y-5">
                <div>
                    <label for="inp-title" class="block text-sm font-semibold text-gray-700 mb-2">Título da
                        tarefa</label>
                    <input id="inp-title" type="text" required
                        class="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all"
                        placeholder="O que precisa ser feito?">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="inp-priority"
                            class="block text-sm font-semibold text-gray-700 mb-2">Prioridade</label>
                        <select id="inp-priority"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand bg-white cursor-pointer transition-all">
                            <option value="normal">Normal</option>
                            <option value="critical">Crítica</option>
                        </select>
                    </div>
                    <div>
                        <label for="inp-due" class="block text-sm font-semibold text-gray-700 mb-2">Prazo Final</label>
                        <input id="inp-due" type="date"
                            class="w-full border border-gray-200 rounded-xl px-3 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                    </div>
                </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.30">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.30">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.30">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.30">
    <link rel="manifest" href="/static/manifest.json?v=0.1.30">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.30" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60 no-print">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <div
        class="mobile-nav fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/90 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] no-print">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="pie-chart" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Relatórios</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.30">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.30">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.30">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.30">
    <link rel="manifest" href="/static/manifest.json?v=0.1.30">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.30" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-2">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-5 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="message-square" class="w-6 h-6 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Reputação</span>
            </a>
            <a href="/minhas_acoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="list-checks" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Ações</span>
            </a>
            <a href="/integracoes"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="link" class="w-6 h-6 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Canais</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        // Tema sincronizado globalmente no <head>

        // Helpers
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg; // Permite ícones no toast se necessário
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.30" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function renderStars(nota) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                stars += `<span class="text-[15px] ${i <= nota ? 'text-amber-400' : 'text-gray-200'}">★</span>`;
            }
            return stars;
        }

        // Empty State Template
        function renderEmptyState(mensagem, icone) {
            return `
                <div class="flex flex-col items-center justify-center h-40 text-center px-4 opacity-70">
                    <i data-lucide="${icone}" class="w-8 h-8 text-gray-300 mb-2"></i>
                    <p class="text-sm font-medium text-gray-400">${mensagem}</p>
                </div>
            `;
        }

        function createCardHTML(av) {
            const c_cliente = escapeHTML(av.cliente);
            const c_plataforma = escapeHTML(av.plataforma);
            const c_tempo = escapeHTML(av.tempo || "Recente");
            const c_comentario = escapeHTML(av.comentario);
            const c_rascunho = escapeHTML(av.rascunho);

            if (av.status === 'respondido_ia') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-gray-100 shadow-sm hover:border-emerald-200/50">
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.30">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.30">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.30">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.30">
    <link rel="manifest" href="/static/manifest.json?v=0.1.30">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.30" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div
                class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-30 border-b border-gray-200/60">
                <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>
                <div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer">{% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}</div>
            </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

    <div
        class="fixed bottom-0 left-0 right-0 z-50 md:hidden pb-safe bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-[0_-4px_20px_rgba(0,0,0,0.05)]">
        <nav class="flex items-center justify-around h-[68px] px-1">
            <a href="/app"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="layout-dashboard" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Cockpit</span>
            </a>
            <a href="/reputacao"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="message-square" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Reputação</span>
            </a>
            <a href="/relatorios"
                class="flex flex-col items-center justify-center w-full h-full gap-1 group text-gray-400 hover:text-brand transition-colors">
                <i data-lucide="pie-chart" class="w-5 h-5 transition-transform group-hover:scale-110"></i>
                <span class="text-[10px] font-semibold">Relatórios</span>
            </a>
            <a href="/sala_maquinas" class="flex flex-col items-center justify-center w-full h-full gap-1 active group">
                <div class="px-4 py-1 rounded-full bg-brand/10 transition-colors">
                    <i data-lucide="settings" class="w-5 h-5 text-brand"></i>
                </div>
                <span class="text-[10px] font-bold text-brand">Ajustes</span>
            </a>
        </nav>
    </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>




==================================================
ARQUIVO: .\requirements.txt
==================================================

Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0



==================================================
ARQUIVO: .\servico_email.py
==================================================

import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)



==================================================
ARQUIVO: .\test_webhook.py
==================================================

import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))


==================================================
ARQUIVO: .\static\app_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - app_icon.png]


==================================================
ARQUIVO: .\static\apple-touch-icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - apple-touch-icon.png]


==================================================
ARQUIVO: .\static\favicon.ico
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.ico]


==================================================
ARQUIVO: .\static\favicon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - favicon.png]


==================================================
ARQUIVO: .\static\favicon.svg
==================================================

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="100%" height="100%">
  <defs>
    <linearGradient id="brand-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ff6b35" />
      <stop offset="100%" stop-color="#e05524" />
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="128" fill="url(#brand-grad)" />
  <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M200 256l40 40 80-80" fill="none" stroke="#ffffff" stroke-width="36" stroke-linecap="round" stroke-linejoin="round"/>
</svg>



==================================================
ARQUIVO: .\static\icon-192.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-192.png]


==================================================
ARQUIVO: .\static\icon-512.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - icon-512.png]


==================================================
ARQUIVO: .\static\logo_icon.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_icon.png]


==================================================
ARQUIVO: .\static\logo_reaction_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_light.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_dark.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_dark.png]


==================================================
ARQUIVO: .\static\logo_reaction_transparent_light.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - logo_reaction_transparent_light.png]


==================================================
ARQUIVO: .\static\manifest.json
==================================================

{
  "name": "ReAction by Frame [IA]",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/app",
  "id": "reaction-saas-app",
  "display": "standalone",
  "background_color": "#2c1d18",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png?v=0.1.28",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/static/icon-512.png?v=0.1.28",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/static/icon-512.png?v=0.1.28",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "maskable"
    }
  ]
}



==================================================
ARQUIVO: .\static\img\plataformas\99food.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - 99food.png]


==================================================
ARQUIVO: .\static\img\plataformas\google_business.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - google_business.png]


==================================================
ARQUIVO: .\static\img\plataformas\ifood.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ifood.png]


==================================================
ARQUIVO: .\static\img\plataformas\instagram.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - instagram.png]


==================================================
ARQUIVO: .\static\img\plataformas\whatsapp.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - whatsapp.png]


==================================================
ARQUIVO: .\static\img\plataformas\ze_delivery.png
==================================================

[ARQUIVO BINÁRIO ILUSTRAMAX - ze_delivery.png]


==================================================
ARQUIVO: .\templates\ajustes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.31">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.31">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.31">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.31">
    <link rel="manifest" href="/static/manifest.json?v=0.1.31">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.31" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div class="md:hidden flex items-center justify-between px-5 py-3.5 bg-white/85 dark:bg-[#141517]/85 backdrop-blur-xl sticky top-0 z-30 border-b border-gray-200/70 dark:border-white/10 transition-colors">
        <!-- LOGO MOBILE OFICIAL REACTION BY FRAME [IA] (FUNDO CLARO) -->
        <a href="/app" class="flex items-center gap-2">
          <img src="/static/logo_reaction_transparent_light.png?v=0.1.31" alt="ReAction Logo" class="h-10 w-auto object-contain transition-all">
        </a>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
      </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>3 meses grátis de sistema (90 dias isento de mensalidade)</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

      <!-- DOCK FLUTUANTE MOBILE ULTRA-PREMIUM (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto flex items-center justify-around gap-1 sm:gap-2 px-3 py-2 rounded-full bg-white/92 dark:bg-[#16171a]/92 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.14)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] max-w-sm w-full transition-all duration-300">
      <a href="/app" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span>Cockpit</span>
      </a>
      <a href="/reputacao" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span>Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span>Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex items-center gap-1.5 px-3.5 py-1.5 rounded-full bg-brand text-white font-bold text-xs shadow-md shadow-brand/25 transition-all">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span>Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\dashboard.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
                  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.31">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.31">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.31">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.31">
    <link rel="manifest" href="/static/manifest.json?v=0.1.31">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                  <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.31" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições e <strong>3 meses grátis de sistema</strong> (90 dias isento de mensalidade).
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

    <!-- DOCK FLUTUANTE MOBILE ULTRA-PREMIUM (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto flex items-center justify-around gap-1 sm:gap-2 px-3 py-2 rounded-full bg-white/92 dark:bg-[#16171a]/92 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.14)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] max-w-sm w-full transition-all duration-300">
      <a href="/app" class="flex items-center gap-1.5 px-3.5 py-1.5 rounded-full bg-brand text-white font-bold text-xs shadow-md shadow-brand/25 transition-all">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span>Cockpit</span>
      </a>
      <a href="/reputacao" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span>Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span>Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span>Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.31" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.31" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.31" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.31" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.31" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.31" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\index.html
==================================================

<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.31">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.31">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.31">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.31">
    <link rel="manifest" href="/static/manifest.json?v=0.1.31">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            width: 100% !important;
            max-width: 100% !important;
            margin: 0;
            padding: 0;
        }

        *, ::before, ::after {
            box-sizing: border-box;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
            overflow-wrap: break-word;
            word-break: break-word;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: 0;
            pointer-events: none;
            max-width: 100vw;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white bg-[#050505] text-white w-full overflow-x-hidden">

    <div class="relative w-full overflow-hidden min-h-screen">

        <div class="glow-effect w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
        <div class="glow-effect w-[300px] md:w-[600px] h-[300px] md:h-[600px] bg-[#ffb347]/10 top-[20%] right-[-100px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>3 meses grátis de sistema</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="#" class="hover:text-white transition">Termos de Uso</a>
                <a href="#" class="hover:text-white transition">Política de Privacidade</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-white transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-600">
            &copy; 2026 ReAction - Desenvolvido por Frame [IA]
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
    </div>
</body>

</html>


==================================================
ARQUIVO: .\templates\integracoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.31">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.31">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.31">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.31">
    <link rel="manifest" href="/static/manifest.json?v=0.1.31">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.31" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div class="md:hidden flex items-center justify-between px-5 py-3.5 bg-white/85 dark:bg-[#141517]/85 backdrop-blur-xl sticky top-0 z-30 border-b border-gray-200/70 dark:border-white/10 transition-colors">
        <!-- LOGO MOBILE OFICIAL REACTION BY FRAME [IA] (FUNDO CLARO) -->
        <a href="/app" class="flex items-center gap-2">
          <img src="/static/logo_reaction_transparent_light.png?v=0.1.31" alt="ReAction Logo" class="h-10 w-auto object-contain transition-all">
        </a>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
      </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/google_business.png?v=0.1.31" alt="Google Meu Negócio" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/instagram.png?v=0.1.31" alt="Instagram" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ifood.png?v=0.1.31" alt="iFood" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/whatsapp.png?v=0.1.31" alt="WhatsApp" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ze_delivery.png?v=0.1.31" alt="Zé Delivery" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/99food.png?v=0.1.31" alt="99Food" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

      <!-- DOCK FLUTUANTE MOBILE ULTRA-PREMIUM (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto flex items-center justify-around gap-1 sm:gap-2 px-3 py-2 rounded-full bg-white/92 dark:bg-[#16171a]/92 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.14)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] max-w-sm w-full transition-all duration-300">
      <a href="/app" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span>Cockpit</span>
      </a>
      <a href="/reputacao" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span>Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span>Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex items-center gap-1.5 px-3.5 py-1.5 rounded-full bg-brand text-white font-bold text-xs shadow-md shadow-brand/25 transition-all">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span>Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\login.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.31">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.31">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.31">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.31">
    <link rel="manifest" href="/static/manifest.json?v=0.1.31">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token, sub) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token, sub: sub })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email", "User.Read"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const acc = res.account || {};
                            const claims = res.idTokenClaims || {};
                            const email = acc.username || claims.preferred_username || claims.email || claims.upn || null;
                            const nome = acc.name || claims.name || (email ? email.split('@')[0] : null);
                            const sub = acc.homeAccountId || claims.sub || claims.oid || null;
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', email, nome, tokenUser, sub);
                        }
                    })
                    .catch(err => {
                        console.log("Erro no popup Microsoft MSAL:", err);
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\minhas_acoes.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.31">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.31">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.31">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.31">
    <link rel="manifest" href="/static/manifest.json?v=0.1.31">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.31" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div class="md:hidden flex items-center justify-between px-5 py-3.5 bg-white/85 dark:bg-[#141517]/85 backdrop-blur-xl sticky top-0 z-30 border-b border-gray-200/70 dark:border-white/10 transition-colors">
        <!-- LOGO MOBILE OFICIAL REACTION BY FRAME [IA] (FUNDO CLARO) -->
        <a href="/app" class="flex items-center gap-2">
          <img src="/static/logo_reaction_transparent_light.png?v=0.1.31" alt="ReAction Logo" class="h-10 w-auto object-contain transition-all">
        </a>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
      </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

      <!-- DOCK FLUTUANTE MOBILE ULTRA-PREMIUM (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto flex items-center justify-around gap-1 sm:gap-2 px-3 py-2 rounded-full bg-white/92 dark:bg-[#16171a]/92 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.14)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] max-w-sm w-full transition-all duration-300">
      <a href="/app" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span>Cockpit</span>
      </a>
      <a href="/reputacao" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span>Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex items-center gap-1.5 px-3.5 py-1.5 rounded-full bg-brand text-white font-bold text-xs shadow-md shadow-brand/25 transition-all">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span>Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span>Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.1.31" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.1.31" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.1.31" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.1.31" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.1.31" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.1.31" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\relatorios.html
==================================================

<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.31">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.31">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.31">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.31">
    <link rel="manifest" href="/static/manifest.json?v=0.1.31">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.31" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div class="md:hidden flex items-center justify-between px-5 py-3.5 bg-white/85 dark:bg-[#141517]/85 backdrop-blur-xl sticky top-0 z-30 border-b border-gray-200/70 dark:border-white/10 transition-colors">
        <!-- LOGO MOBILE OFICIAL REACTION BY FRAME [IA] (FUNDO CLARO) -->
        <a href="/app" class="flex items-center gap-2">
          <img src="/static/logo_reaction_transparent_light.png?v=0.1.31" alt="ReAction Logo" class="h-10 w-auto object-contain transition-all">
        </a>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
      </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

      <!-- DOCK FLUTUANTE MOBILE ULTRA-PREMIUM (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto flex items-center justify-around gap-1 sm:gap-2 px-3 py-2 rounded-full bg-white/92 dark:bg-[#16171a]/92 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.14)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] max-w-sm w-full transition-all duration-300">
      <a href="/app" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span>Cockpit</span>
      </a>
      <a href="/reputacao" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span>Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span>Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex items-center gap-1.5 px-3.5 py-1.5 rounded-full bg-brand text-white font-bold text-xs shadow-md shadow-brand/25 transition-all">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span>Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\reputacao.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.31">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.31">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.31">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.31">
    <link rel="manifest" href="/static/manifest.json?v=0.1.31">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.31" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div class="md:hidden flex items-center justify-between px-5 py-3.5 bg-white/85 dark:bg-[#141517]/85 backdrop-blur-xl sticky top-0 z-30 border-b border-gray-200/70 dark:border-white/10 transition-colors">
        <!-- LOGO MOBILE OFICIAL REACTION BY FRAME [IA] (FUNDO CLARO) -->
        <a href="/app" class="flex items-center gap-2">
          <img src="/static/logo_reaction_transparent_light.png?v=0.1.31" alt="ReAction Logo" class="h-10 w-auto object-contain transition-all">
        </a>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
      </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

      <!-- DOCK FLUTUANTE MOBILE ULTRA-PREMIUM (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto flex items-center justify-around gap-1 sm:gap-2 px-3 py-2 rounded-full bg-white/92 dark:bg-[#16171a]/92 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.14)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] max-w-sm w-full transition-all duration-300">
      <a href="/app" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span>Cockpit</span>
      </a>
      <a href="/reputacao" class="flex items-center gap-1.5 px-3.5 py-1.5 rounded-full bg-brand text-white font-bold text-xs shadow-md shadow-brand/25 transition-all">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span>Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span>Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span>Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>
</body>

</html>


==================================================
ARQUIVO: .\templates\sala_maquinas.html
==================================================

<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.1.31">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.1.31">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.1.31">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.1.31">
    <link rel="manifest" href="/static/manifest.json?v=0.1.31">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.1.31" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div class="md:hidden flex items-center justify-between px-5 py-3.5 bg-white/85 dark:bg-[#141517]/85 backdrop-blur-xl sticky top-0 z-30 border-b border-gray-200/70 dark:border-white/10 transition-colors">
        <!-- LOGO MOBILE OFICIAL REACTION BY FRAME [IA] (FUNDO CLARO) -->
        <a href="/app" class="flex items-center gap-2">
          <img src="/static/logo_reaction_transparent_light.png?v=0.1.31" alt="ReAction Logo" class="h-10 w-auto object-contain transition-all">
        </a>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
      </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

      <!-- DOCK FLUTUANTE MOBILE ULTRA-PREMIUM (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto flex items-center justify-around gap-1 sm:gap-2 px-3 py-2 rounded-full bg-white/92 dark:bg-[#16171a]/92 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.14)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] max-w-sm w-full transition-all duration-300">
      <a href="/app" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span>Cockpit</span>
      </a>
      <a href="/reputacao" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span>Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex items-center gap-1 px-3 py-1.5 rounded-full text-gray-600 dark:text-gray-400 hover:text-brand dark:hover:text-brand font-medium text-xs transition-all hover:bg-gray-100/60 dark:hover:bg-white/5">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span>Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex items-center gap-1.5 px-3.5 py-1.5 rounded-full bg-brand text-white font-bold text-xs shadow-md shadow-brand/25 transition-all">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span>Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>
</body>

</html>


================================================================================
FILE: requirements.txt
================================================================================
Flask==3.0.3
flask-cors==4.0.1
mysql-connector-python==8.4.0
requests==2.32.3
python-dotenv==1.0.1
mercadopago==2.2.3
resvg-py==0.2.2
gunicorn==22.0.0

================================================================================
FILE: servico_email.py
================================================================================
import os
import smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from dotenv import load_dotenv

load_dotenv()

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.hostinger.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", 587))
SMTP_USER = os.environ.get("SMTP_USER", "contato@frameia.com.br")
SMTP_PASS = os.environ.get("SMTP_PASS", "")

ADMIN_EMAILS = [
    "contato@frameia.com.br",
    "jadson@mjsv.com.br",
    "mara@mjsv.com.br"
]

def _enviar_email_html(destinatarios, assunto, html_content):
    """Função utilitária interna para disparar e-mail em HTML via SMTP"""
    if isinstance(destinatarios, str):
        destinatarios = [destinatarios]

    print(f"\n[SERVICO DE E-MAIL] Disparando e-mail para: {', '.join(destinatarios)}")
    
    if not SMTP_PASS or SMTP_PASS == "sua_senha_smtp_aqui":
        print("[SISTEMA E-MAIL] Configuração SMTP não preenchida no .env. E-mail simulado com sucesso!")
        return True

    try:
        msg = MIMEMultipart('alternative')
        msg['Subject'] = assunto
        msg['From'] = f"ReAction IA <{SMTP_USER}>"
        msg['To'] = ", ".join(destinatarios)

        part_html = MIMEText(html_content, 'html', 'utf-8')
        msg.attach(part_html)

        server = smtplib.SMTP(SMTP_HOST, SMTP_PORT, timeout=10)
        server.starttls()
        server.login(SMTP_USER, SMTP_PASS)
        server.sendmail(SMTP_USER, destinatarios, msg.as_string())
        server.quit()
        print("[SERVIÇO DE E-MAIL] E-mail enviado via SMTP com sucesso!")
        return True
    except Exception as e:
        print(f"[ERRO E-MAIL] Falha ao enviar e-mail via SMTP: {e}")
        return False

def enviar_email_boas_vindas(user_email, nome_usuario, empresa_nome):
    """Envia um e-mail de boas-vindas elegante para o usuário recém-cadastrado"""
    assunto = f"Bem-vindo ao ReAction, {nome_usuario}! Sua marca está protegida 🛡️"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.5); }}
            .header {{ background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); padding: 40px 30px; text-align: center; color: #ffffff; }}
            .header h1 {{ margin: 0; font-size: 28px; font-weight: 800; tracking-tight: -0.5px; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .card {{ background-color: #1f2023; border-radius: 16px; border: 1px solid #27272a; padding: 20px; margin: 25px 0; }}
            .card-item {{ display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #2d2f34; font-size: 14px; }}
            .card-item:last-child {{ border-bottom: none; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; box-shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div style="font-size: 40px; margin-bottom: 10px;">🛡️</div>
                <h1>ReAction</h1>
                <p style="margin: 5px 0 0 0; opacity: 0.9; font-size: 14px;">Gestão de Reputação e Contenção de Crises com IA</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 20px; margin-top: 0;">Olá, {nome_usuario}!</h2>
                <p>É com enorme satisfação que recebemos a <strong>{empresa_nome}</strong> no ReAction. A partir de agora, a sua Inteligência Artificial cuidará das suas avaliações 24/7, blindando a sua marca contra crises e engajando clientes satisfeitos.</p>

                <div class="card">
                    <h3 style="color: #ff6b35; margin-top: 0; font-size: 15px; text-transform: uppercase;">Resumo da sua Conta:</h3>
                    <div class="card-item"><span style="color: #a1a1aa;">Empresa:</span> <strong style="color: #ffffff;">{empresa_nome}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">E-mail de Acesso:</span> <strong style="color: #ffffff;">{user_email}</strong></div>
                    <div class="card-item"><span style="color: #a1a1aa;">Plano Ativo:</span> <strong style="color: #4ade80;">Shield Start (Freemium)</strong></div>
                </div>

                <div style="text-align: center;">
                    <a href="https://reaction.frameia.com.br/login" class="btn">Acessar Meu Painel ReAction →</a>
                </div>

                <p style="font-size: 13px; color: #a1a1aa; margin-top: 25px;">Dica: Acesse a <strong>Sala de Máquinas</strong> no seu painel para calibrar o tom de voz da IA e cadastrar as suas regras de ouro de atendimento.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Inteligência Artificial para Reputação Online.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

def enviar_notificacao_admin(tipo_evento, dados):
    """Envia uma notificação em HTML de alta fidelidade para os 3 administradores sobre novos usuários ou mudanças de plano"""
    
    titulos = {
        "novo_usuario": "🎉 Novo Cadastro Realizado no ReAction",
        "upgrade": "🚀 Upgrade de Plano Confirmado",
        "downgrade": "⚠️ Alteração de Plano Registrada"
    }
    
    assunto = titulos.get(tipo_evento, f"🔔 Notificação Administrativa ReAction - {tipo_evento}")
    
    nome_cliente = dados.get('nome_usuario', 'Cliente')
    email_cliente = dados.get('email', '-')
    empresa = dados.get('nome_empresa', '-')
    plano = dados.get('plano', 'Freemium')
    ciclo = dados.get('ciclo', 'Mensal')
    valor = dados.get('valor', 'R$ 0,00')

    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #09090b; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #121316; border-radius: 20px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: #1f2023; border-bottom: 2px solid #ff6b35; padding: 25px 30px; text-align: left; }}
            .badge {{ display: inline-block; background-color: #ff6b35; color: #ffffff; padding: 4px 12px; border-radius: 99px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }}
            .content {{ padding: 30px; }}
            .table-box {{ width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }}
            .table-box td {{ padding: 12px 15px; border-bottom: 1px solid #27272a; color: #d4d4d8; }}
            .table-box td:first-child {{ color: #a1a1aa; font-weight: 600; width: 35%; }}
            .footer {{ background-color: #09090b; padding: 20px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <div class="badge">Painel Administrativo</div>
                <h2 style="color: #ffffff; margin: 0; font-size: 20px;">{assunto}</h2>
            </div>
            <div class="content">
                <p style="margin-top: 0; color: #a1a1aa;">Atenção equipe, um novo evento de conta foi registrado na plataforma ReAction:</p>

                <table class="table-box">
                    <tr><td>Cliente / Responsável:</td><td><strong style="color: #ffffff;">{nome_cliente}</strong></td></tr>
                    <tr><td>E-mail:</td><td><a href="mailto:{email_cliente}" style="color: #ff6b35; text-decoration: none;">{email_cliente}</a></td></tr>
                    <tr><td>Nome da Empresa:</td><td><strong style="color: #ffffff;">{empresa}</strong></td></tr>
                    <tr><td>Plano Contratado:</td><td><strong style="color: #4ade80;">{plano} ({ciclo})</strong></td></tr>
                    <tr><td>Valor Estimado:</td><td><strong style="color: #ffffff;">{valor}</strong></td></tr>
                </table>

                <div style="margin-top: 30px; padding: 15px; background-color: #1f2023; border-radius: 12px; font-size: 13px; color: #a1a1aa; text-align: center;">
                    Notificação automática enviada para: <strong>contato@frameia.com.br</strong>, <strong>jadson@mjsv.com.br</strong> e <strong>mara@mjsv.com.br</strong>.
                </div>
            </div>
            <div class="footer">
                &copy; 2026 ReAction Admin Monitor • Frame [IA]
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(ADMIN_EMAILS, assunto, html)

def enviar_email_redefinicao_senha(user_email, nome_usuario, token_link):
    """Envia um e-mail elegante para solicitação de redefinição de senha"""
    assunto = "Redefinição de Senha — ReAction"
    
    html = f"""
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="utf-8">
        <style>
            body {{ font-family: 'Segoe UI', Helvetica, Arial, sans-serif; background-color: #0c0d0e; color: #e4e4e7; margin: 0; padding: 0; }}
            .container {{ max-width: 600px; margin: 30px auto; background-color: #141517; border-radius: 24px; border: 1px solid #27272a; overflow: hidden; }}
            .header {{ background: linear-gradient(135deg, #1f2023 0%, #0c0d0e 100%); padding: 35px 30px; text-align: center; border-bottom: 2px solid #ff6b35; }}
            .content {{ padding: 35px 30px; line-height: 1.6; color: #d4d4d8; }}
            .btn {{ display: inline-block; background: linear-gradient(135deg, #ff6b35 0%, #e05524 100%); color: #ffffff !important; text-decoration: none; padding: 16px 36px; border-radius: 14px; font-weight: 700; font-size: 16px; margin: 25px 0; text-align: center; shadow: 0 10px 20px rgba(255,107,53,0.3); }}
            .footer {{ background-color: #0c0d0e; padding: 25px; text-align: center; font-size: 12px; color: #71717a; border-top: 1px solid #1f2023; }}
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1 style="color: #ff6b35; margin: 0; font-size: 24px;">ReAction</h1>
                <p style="color: #a1a1aa; margin: 5px 0 0 0; font-size: 13px;">Redefinição de Palavra-passe / Senha</p>
            </div>
            <div class="content">
                <h2 style="color: #ffffff; font-size: 18px; margin-top: 0;">Olá, {nome_usuario}.</h2>
                <p>Recebemos um pedido para redefinir a senha da sua conta no ReAction. Se você fez essa solicitação, clique no botão abaixo para escolher uma nova palavra-passe:</p>

                <div style="text-align: center;">
                    <a href="{token_link}" class="btn">Redefinir Minha Senha Agora →</a>
                </div>

                <p style="font-size: 12px; color: #71717a; margin-top: 30px;">Se você não solicitou a redefinição de senha, ignore este e-mail com segurança. Sua senha atual permanecerá inalterada.</p>
            </div>
            <div class="footer">
                &copy; 2026 ReAction — Suporte e Segurança.<br>
                Desenvolvido por <strong>Frame [IA]</strong>
            </div>
        </div>
    </body>
    </html>
    """
    return _enviar_email_html(user_email, assunto, html)

================================================================================
FILE: test_webhook.py
================================================================================
import requests
import json

url = "http://localhost:5001/api/webhooks/ingest"

payload = {
    "empresa_id": 1,
    "plataforma": "google",
    "nome_cliente": "Cliente Fúria",
    "nota": 1,
    "comentario": "Comida horrível e atendimento péssimo. Nunca mais volto!"
}

headers = {
    "Content-Type": "application/json"
}

print("A enviar avaliação simulada para o Webhook...")
response = requests.post(url, data=json.dumps(payload), headers=headers)

print(f"Status Code: {response.status_code}")
print("Resposta do Servidor:")
print(json.dumps(response.json(), indent=2, ensure_ascii=False))
================================================================================
FILE: static\manifest.json
================================================================================
{
  "name": "ReAction by Frame [IA]",
  "short_name": "ReAction",
  "description": "Domine a sua Reputação Online com Inteligência Artificial",
  "start_url": "/app",
  "id": "reaction-saas-app",
  "display": "standalone",
  "background_color": "#2c1d18",
  "theme_color": "#ff6b35",
  "icons": [
    {
      "src": "/static/icon-192.png?v=0.1.28",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/static/icon-512.png?v=0.1.28",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/static/icon-512.png?v=0.1.28",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "maskable"
    }
  ]
}

================================================================================
FILE: templates\ajustes.html
================================================================================
<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Ajustes</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.2.0">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell" id="app-shell">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.2.0" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <main class="main-scroll relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div class="md:hidden flex items-center justify-between px-5 py-3.5 bg-white/85 dark:bg-[#141517]/85 backdrop-blur-xl sticky top-0 z-30 border-b border-gray-200/70 dark:border-white/10 transition-colors">
        <!-- LOGO MOBILE OFICIAL REACTION BY FRAME [IA] (FUNDO CLARO) -->
        <a href="/app" class="flex items-center gap-2">
          <img src="/static/logo_reaction_transparent_light.png?v=0.2.0" alt="ReAction Logo" class="h-10 w-auto object-contain transition-all">
        </a>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
      </div>

            <header class="px-6 md:px-10 py-8">
                <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Perfil & Ajustes</h1>
                <p class="text-gray-500 text-sm mt-2 font-medium">Gerencie as suas informações de acesso e da conta.</p>
            </header>

            <section class="px-6 md:px-10 pb-28 md:pb-20 max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

                <!-- COLUNA DA ESQUERDA: PERFIL & SEGURANÇA -->
                <div class="space-y-8">
                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="text-brand w-5 h-5"></i> Informações Pessoais
                        </h2>
                        <!-- FOTO DE PERFIL / AVATAR DA CONTA -->
                        <div class="flex items-center gap-5 mb-6 pb-6 border-b border-gray-100">
                            <div class="relative w-16 h-16 rounded-full overflow-hidden shrink-0 border-2 border-brand/30 shadow-md bg-gray-100 flex items-center justify-center">
                                {% if usuario.foto_perfil %}
                                <img id="avatar-preview-img" src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">
                                {% else %}
                                <div id="avatar-preview-initials" class="w-full h-full flex items-center justify-center text-white text-xl font-bold bg-brand">
                                    {{ usuario.iniciais }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="space-y-1.5">
                                <h3 class="font-heading font-bold text-sm text-gray-900">Foto de Perfil</h3>
                                <p class="text-xs text-gray-500 font-medium">Vinculada do Google / Microsoft ou carregue uma imagem personalizada.</p>
                                <div class="flex items-center gap-2 pt-1">
                                    <input type="file" id="input-foto-avatar" accept="image/*" class="hidden" onchange="enviarFotoPerfil(this)">
                                    <button type="button" onclick="document.getElementById('input-foto-avatar').click()"
                                        class="px-3 py-1.5 rounded-lg bg-gray-100 hover:bg-gray-200 text-gray-800 text-xs font-bold transition-all flex items-center gap-1.5 shadow-sm">
                                        <i data-lucide="camera" class="w-3.5 h-3.5 text-brand"></i> Alterar Foto
                                    </button>
                                    <button type="button" onclick="removerFotoPerfil()"
                                        class="px-3 py-1.5 rounded-lg bg-red-50 hover:bg-red-100 text-red-600 text-xs font-bold transition-all">
                                        Remover
                                    </button>
                                </div>
                            </div>
                        </div>

                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nome Completo</label>
                                <input type="text" value="{{ usuario.nome }}"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Endereço de E-mail</label>
                                <input type="email" value="jadson@frameia.com.br"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
                            </div>
                            
          <div>
            <label for="inp-telefone" class="block text-xs font-bold text-gray-700 dark:text-gray-300 uppercase tracking-wider mb-2">WhatsApp para Alertas & IA Bot</label>
            <div class="relative">
              <input id="inp-telefone" type="tel" value="{{ usuario.telefone or '' }}"
                placeholder="(85) 99999-9999"
                class="w-full border border-gray-200 dark:border-gray-800 bg-white dark:bg-[#18191c] text-gray-900 dark:text-white rounded-xl px-4 py-3 pl-11 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
              <i data-lucide="message-circle" class="w-5 h-5 text-emerald-500 absolute left-3.5 top-3.5"></i>
            </div>
            <p class="text-xs text-gray-500 dark:text-gray-400 mt-1.5">Receba avisos imediatos de avaliações de crise (1 e 2 estrelas) e consulte status pelo WhatsApp.</p>
          </div>
          <div class="pt-2">
                                <button type="submit"
                                    class="bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-brand/20 hover:brightness-110 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Guardar Alterações
                                </button>
                            </div>
                        </form>
                    </div>

                    <div
                        class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="shield-check" class="text-brand w-5 h-5"></i> Segurança
                        </h2>
                        <form class="space-y-5 settings-form">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Palavra-passe Atual</label>
                                <input type="password" placeholder="••••••••"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Nova Palavra-passe</label>
                                <input type="password" placeholder="Escreva a nova palavra-passe"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-gray-300 focus:border-gray-400 transition-all">
                            </div>
                            
          <div>
            <label for="inp-telefone" class="block text-xs font-bold text-gray-700 dark:text-gray-300 uppercase tracking-wider mb-2">WhatsApp para Alertas & IA Bot</label>
            <div class="relative">
              <input id="inp-telefone" type="tel" value="{{ usuario.telefone or '' }}"
                placeholder="(85) 99999-9999"
                class="w-full border border-gray-200 dark:border-gray-800 bg-white dark:bg-[#18191c] text-gray-900 dark:text-white rounded-xl px-4 py-3 pl-11 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
              <i data-lucide="message-circle" class="w-5 h-5 text-emerald-500 absolute left-3.5 top-3.5"></i>
            </div>
            <p class="text-xs text-gray-500 dark:text-gray-400 mt-1.5">Receba avisos imediatos de avaliações de crise (1 e 2 estrelas) e consulte status pelo WhatsApp.</p>
          </div>
          <div class="pt-2">
                                <button type="submit"
                                    class="bg-gray-800 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md hover:bg-gray-900 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Atualizar Palavra-passe
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- COLUNA DA DIREITA: WHATSAPP DE CRISES & SETUP VIP -->
                <div class="space-y-8">
                    <!-- NOTIFICAÇÃO & PONTE PRIVADA DE CRISES WHATSAPP -->
                    <div class="bg-white p-6 md:p-8 rounded-3xl border border-emerald-100 shadow-sm transition-all hover:shadow-md">
                        <h2 class="font-heading font-bold text-lg text-gray-900 mb-2 flex items-center gap-2">
                            <i data-lucide="message-circle" class="text-emerald-600 w-5 h-5"></i> WhatsApp do Gerente / Vendedor (Alertas de Crise)
                        </h2>
                        <p class="text-xs text-gray-500 mb-6">Cadastre o número que receberá os alertas de crise (avaliações 1-2 estrelas) com link direto para o atendimento privado via WhatsApp.</p>
                        <form class="space-y-5 settings-form" id="form-whatsapp">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Número WhatsApp com DDD</label>
                                <input type="text" id="whatsapp-number" value="+55 (85) 99999-9999" placeholder="+55 (85) 99999-9999"
                                    class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-400 focus:border-emerald-500 transition-all font-mono">
                            </div>
                            <div class="flex items-center gap-3 bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <input type="checkbox" id="whatsapp-alert-active" checked class="w-4 h-4 text-emerald-600 rounded focus:ring-emerald-500">
                                <label for="whatsapp-alert-active" class="text-xs font-semibold text-emerald-900">
                                    Receber alertas instantâneos de contenção de crise no WhatsApp quando entrar review de 1 ou 2 estrelas.
                                </label>
                            </div>
                            
          <div>
            <label for="inp-telefone" class="block text-xs font-bold text-gray-700 dark:text-gray-300 uppercase tracking-wider mb-2">WhatsApp para Alertas & IA Bot</label>
            <div class="relative">
              <input id="inp-telefone" type="tel" value="{{ usuario.telefone or '' }}"
                placeholder="(85) 99999-9999"
                class="w-full border border-gray-200 dark:border-gray-800 bg-white dark:bg-[#18191c] text-gray-900 dark:text-white rounded-xl px-4 py-3 pl-11 text-sm focus:outline-none focus:ring-2 focus:ring-brand/40 focus:border-brand transition-all">
              <i data-lucide="message-circle" class="w-5 h-5 text-emerald-500 absolute left-3.5 top-3.5"></i>
            </div>
            <p class="text-xs text-gray-500 dark:text-gray-400 mt-1.5">Receba avisos imediatos de avaliações de crise (1 e 2 estrelas) e consulte status pelo WhatsApp.</p>
          </div>
          <div class="pt-2">
                                <button type="submit"
                                    class="bg-emerald-600 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-emerald-600/20 hover:bg-emerald-700 transition-all flex items-center justify-center gap-2 w-full sm:w-auto">
                                    Salvar WhatsApp de Alertas
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- CARD FIXO PERMANENTE SETUP VIP REACTION -->
                    <div class="bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl relative overflow-hidden">
                        <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
                        <div class="flex items-start gap-4 relative z-10 mb-4">
                            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
                                <i data-lucide="sparkles" class="w-6 h-6"></i>
                            </div>
                            <div>
                                <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                                    Experiência 360º • Setup VIP
                                </div>
                                <h2 class="font-heading font-bold text-xl text-white">Setup VIP ReAction (R$ 300,00 à vista)</h2>
                                <p class="text-xs md:text-sm text-gray-300 mt-2 leading-relaxed">
                                    Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa? O Setup VIP inclui:
                                </p>
                                <ul class="mt-3 space-y-1.5 text-xs text-gray-300">
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> 2 Sessões ao vivo de 60min com nossos especialistas</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> Pasta privada no Google Drive com gravações e transcrições</li>
                                    <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-brand"></i> <strong>Acompanhamento consultivo e suporte prioritário</strong></li>
                                </ul>
                            </div>
                        </div>
                        <div class="pt-4 border-t border-gray-800 flex flex-col sm:flex-row items-center justify-between gap-4 relative z-10">
                            <span class="text-xs text-gray-400">Contratação única com onboarding consultivo.</span>
                            <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20solicitar%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="py-3 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full sm:w-auto">
                                Solicitar Setup VIP (R$ 300) →
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

        <!-- DOCK FLUTUANTE MOBILE SIMÉTRICO E CENTRALIZADO (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto max-w-[360px] w-full mx-auto grid grid-cols-4 gap-1 p-1.5 rounded-full bg-white/95 dark:bg-[#16171a]/95 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.16)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] transition-all duration-300">
      <a href="/app" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Cockpit</span>
      </a>
      <a href="/reputacao" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full bg-brand text-white shadow-sm shadow-brand/30 transition-all duration-200">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex flex-col gap-2">
      <div class="flex items-center justify-between gap-3">
        <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
          <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
          <span>Modo Escuro/Claro</span>
        </button>
        <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
          <i data-lucide="log-out" class="w-4 h-4"></i>
          <span>Sair</span>
        </a>
      </div>
      <div class="flex items-center justify-center gap-4 pt-2 text-[11px] text-gray-400 dark:text-gray-500">
        <a href="/termos" target="_blank" class="hover:text-brand">Termos de Uso</a>
        <span>•</span>
        <a href="/privacidade" target="_blank" class="hover:text-brand">Privacidade & LGPD</a>
      </div>
    </div>
  </div><button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa os ícones do Lucide
        lucide.createIcons();

        // Sistema de Toasts (Notificações)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        function enviarFotoPerfil(input) {
            if (!input.files || !input.files[0]) return;
            const file = input.files[0];
            const formData = new FormData();
            formData.append('foto', file);

            toast('⏳ Enviando imagem de perfil...');

            fetch('/api/usuario/foto', {
                method: 'POST',
                body: formData
            })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto de perfil atualizada com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    toast('❌ ' + (data.erro || 'Falha ao atualizar foto de perfil.'));
                }
            })
            .catch(err => {
                console.error('Erro ao enviar foto:', err);
                toast('❌ Erro de conexão ao enviar foto.');
            });
        }

        function removerFotoPerfil() {
            if (!confirm('Deseja remover sua foto de perfil?')) return;
            fetch('/api/usuario/foto/remover', { method: 'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.sucesso) {
                    toast('✅ Foto removida com sucesso!');
                    setTimeout(() => window.location.reload(), 600);
                }
            });
        }

        // Simulação visual de salvamento nos formulários
        document.querySelectorAll('.settings-form').forEach(form => {
            form.addEventListener('submit', (e) => {
                e.preventDefault(); // Evita recarregar a página

                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerHTML;

                // Estado de "A Carregar"
                btn.disabled = true;
                btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
                lucide.createIcons({ root: btn });

                // Simula tempo de requisição à API
                setTimeout(() => {
                    btn.disabled = false;
                    btn.innerHTML = originalText;
                    lucide.createIcons({ root: btn });
                    toast('✅ Alterações guardadas com sucesso!');

                    // Se for o formulário de senha, limpa os campos
                    if (form.querySelector('input[type="password"]')) {
                        form.reset();
                    }
                }, 800);
            });
        });
    </script>

{% if usuario.termos_pendentes %}
<!-- MODAL DE ACEITE OBRIGATÓRIO DE TERMOS E PRIVACIDADE (LGPD) -->
<div id="modal-termos-obrigatorio" class="fixed inset-0 z-[100] bg-black/80 backdrop-blur-md flex items-center justify-center p-4">
    <div class="bg-white dark:bg-[#141517] rounded-3xl p-6 sm:p-8 max-w-md w-full border border-gray-200 dark:border-gray-800 shadow-2xl space-y-5 text-center">
        
        <!-- LOGO OFICIAL REACTION COM ESCUDO VIBRANTE -->
        <div class="flex justify-center items-center">
            <div class="w-16 h-16 rounded-2xl bg-gradient-to-br from-[#ff6b35] to-[#e05524] p-3 shadow-lg shadow-brand/30 flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" class="w-10 h-10 text-white fill-none stroke-current" stroke-width="36" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" />
                    <path d="M200 256l40 40 80-80" />
                </svg>
            </div>
        </div>

        <div>
            <h3 class="font-heading font-bold text-xl text-gray-900 dark:text-white">Atualização dos Termos & LGPD</h3>
            <p class="text-xs sm:text-sm text-gray-600 dark:text-gray-400 mt-2 leading-relaxed">
                Atualizamos nossos <a href="/termos" target="_blank" class="text-brand font-bold underline">Termos de Uso</a> e <a href="/privacidade" target="_blank" class="text-brand font-bold underline">Política de Privacidade</a> com os dados da holding e da <strong>Frame [IA]</strong>. Para continuar utilizando o ReAction, confirme a sua concordância.
            </p>
        </div>

        <div class="p-4 rounded-2xl bg-gray-50 dark:bg-[#18191c] border border-gray-200/60 dark:border-gray-800 text-left text-xs text-gray-600 dark:text-gray-400 space-y-1.5">
            <p><strong>Holding Controladora:</strong> MJSV TECNOLOGIA E SOLUCOES DIGITAIS (I.S.)</p>
            <p><strong>Operação & IA:</strong> Frame [IA] (Divisão de Inteligência Artificial)</p>
            <p><strong>CNPJ:</strong> 68.614.850/0001-13</p>
            <p><strong>Versão:</strong> 0.2.0 (16/08/2026)</p>
        </div>

        <button type="button" id="btn-aceitar-termos" onclick="confirmarAceiteTermos()" class="w-full py-3.5 rounded-xl bg-brand text-white font-bold text-sm hover:brightness-110 shadow-lg shadow-brand/25 transition-all flex items-center justify-center gap-2">
            <span>Li e Concordo com os Termos</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M5 13l4 4L19 7"></path></svg>
        </button>
    </div>
</div>
<script>
function confirmarAceiteTermos() {
    const btn = document.getElementById('btn-aceitar-termos');
    if (btn) { btn.disabled = true; btn.innerHTML = '<span>Gravando consentimento...</span>'; }
    fetch('/api/aceitar_termos', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'}
    })
    .then(r => r.json())
    .then(d => {
        if (d.success) {
            const modal = document.getElementById('modal-termos-obrigatorio');
            if (modal) modal.remove();
        } else {
            alert('Erro ao gravar aceite: ' + (d.error || 'Tente novamente'));
            if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
        }
    })
    .catch(err => {
        console.error(err);
        alert('Erro ao conectar ao servidor.');
        if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
    });
}
</script>
{% endif %}

</body>

</html>
================================================================================
FILE: templates\dashboard.html
================================================================================
<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReAction by Frame [IA] | Cockpit</title>
                  <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.2.0">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            heading: ['Poppins', 'sans-serif'],
            body: ['DM Sans', 'sans-serif']
          },
          colors: {
            brand: '#ff6b35',
            sidebar: '#3a3a3a',
            bglight: '#f4f4f5'
          }
        }
      }
    };

    if (localStorage.getItem('hideSetupVipReAction') === 'true') {
      document.documentElement.classList.add('hide-setup-vip');
    }
  </script>

  <style>
    .hide-setup-vip #banner-setup-vip {
      display: none !important;
    }

    /* Reset e Estrutura Base */
    html,
    body {
      height: 100%;
      margin: 0;
      box-sizing: border-box;
      background-color: #f4f4f5;
    }

    .app-shell {
      height: 100%;
      display: flex;
      overflow: hidden;
    }

    .main-scroll {
      flex: 1;
      overflow-y: auto;
      scroll-behavior: smooth;
    }

    /* Scrollbar minimalista */
    ::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: #d4d4d8;
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #a1a1aa;
    }

    /* Animação do Botão Principal */
    .action-btn {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    @keyframes pulse-glow {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
      }

      50% {
        box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
      }
    }

    /* Animações de Entrada (Fade In + Slide Up) */
    @keyframes slide-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .anim-in {
      animation: slide-up 0.5s ease-out both;
    }

    .anim-d1 {
      animation-delay: 0.05s;
    }

    .anim-d2 {
      animation-delay: 0.1s;
    }

    .anim-d3 {
      animation-delay: 0.15s;
    }

    .anim-d4 {
      animation-delay: 0.2s;
    }

    /* Checkbox de Tarefas Personalizado */
    .check-box {
      width: 24px;
      height: 24px;
      border: 2px solid #d4d4d8;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      flex-shrink: 0;
    }

    .check-box.finalizado {
      background: #ff6b35;
      border-color: #ff6b35;
      transform: scale(1.05);
    }

    .check-box:hover:not(.finalizado) {
      border-color: #ff6b35;
      background: #fff5f2;
    }

    /* Navegação Lateral */
    .nav-item {
      transition: all 0.2s ease;
    }

    .nav-item:hover,
    .nav-item.active {
      background: rgba(255, 107, 53, 0.1);
    }

    .nav-item.active {
      border-left: 3px solid #ff6b35;
      color: white;
    }

    /* Cartões KPI */
    .kpi-card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .kpi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
    }

    /* Linhas de Tarefa */
    .task-row {
      transition: all 0.2s ease;
    }

    .task-row:hover {
      background: #fafafa;
      transform: translateX(2px);
    }

    /* Notificações (Toasts) */
    .toast-msg {
      position: fixed;
      bottom: 90px;
      /* Ajustado para ficar acima da barra mobile */
      left: 50%;
      transform: translateX(-50%);
      background: #27272a;
      color: #fff;
      padding: 12px 24px;
      border-radius: 99px;
      font-size: 14px;
      font-weight: 500;
      z-index: 1000;
      animation: slide-up 0.3s ease-out;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
    }

    /* Modais */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 100;
      animation: fade-in 0.2s;
    }

    @keyframes fade-in {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Área Segura Mobile (Notch iPhones) */
    .pb-safe {
      padding-bottom: env(safe-area-inset-bottom);
    }
  </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
  <div class="app-shell" id="app-shell">

    <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                  <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.2.0" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

      <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation" aria-label="Navegação principal">
        <button
          class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
          onclick="window.location.href='/app'">
          <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/reputacao'">
          <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/minhas_acoes'">
          <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/relatorios'">
          <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/sala_maquinas'">
          <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
        </button>
        <button
          class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
          onclick="window.location.href='/integracoes'">
          <i data-lucide="link" class="w-5 h-5"></i> Integrações
        </button>
      </nav>

      <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

    <div class="main-scroll relative" id="main-area">

      <div
        class="md:hidden flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur-md sticky top-0 z-10 border-b border-gray-200/60">

        <!-- LOGO MOBILE AJUSTADA CONFORME A LANDING PAGE -->
        <a href="/app" class="flex items-center gap-2">
            <img src="/static/logo_reaction_transparent_light.png" alt="ReAction Logo" class="h-11 w-auto object-contain transition-all">
        </a>

        <div
          class="w-8 h-8 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm"
          onclick="window.location.href='/ajustes'">
          {{ usuario.iniciais }}
        </div>
      </div>

      <header class="flex flex-col md:flex-row md:items-center justify-between px-6 md:px-10 py-8 gap-4">
        <div>
          <div class="flex flex-wrap items-center gap-3">
            <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight" id="greeting-text">Bom dia, {{ usuario.nome }}.</h1>
            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-50 text-emerald-600 border border-emerald-200/60 shadow-sm">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span> 98.4% Saúde da Marca (+3.2%)
            </span>
          </div>
          <p class="text-sm text-gray-500 mt-2 font-medium" id="greeting-sub">O que vamos resolver hoje na sua reputação?</p>
        </div>

        <div class="flex items-center gap-3 shrink-0">
          <!-- PILLS DE FILTRO DE PERÍODO (V0.1.0) -->
          <div class="bg-gray-200/70 p-1 rounded-xl flex items-center gap-1 text-xs font-bold text-gray-600 shadow-inner">
            <button type="button" onclick="filtrarPeriodo(this, 'hoje')" class="btn-periodo px-3 py-1.5 rounded-lg bg-white text-gray-900 shadow-sm transition-all">Hoje</button>
            <button type="button" onclick="filtrarPeriodo(this, '7d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">7 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, '30d')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">30 Dias</button>
            <button type="button" onclick="filtrarPeriodo(this, 'mes')" class="btn-periodo px-3 py-1.5 rounded-lg hover:text-gray-900 transition-all">Este Mês</button>
          </div>

          <button id="btn-new-action"
            class="action-btn flex items-center justify-center gap-2 px-5 py-2 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20">
            <i data-lucide="plus" class="w-4 h-4"></i> Nova Ação
          </button>
        </div>
      </header>

      <main class="px-6 md:px-10 pb-28 md:pb-10 space-y-8 max-w-7xl mx-auto" role="main">

        <section class="grid grid-cols-1 sm:grid-cols-3 gap-5" aria-label="Indicadores">
          <div
            class="kpi-card anim-in anim-d1 bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-gray-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600">
                <i data-lucide="clock" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Pendentes</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-pending">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d2 bg-white rounded-3xl p-6 border border-red-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-red-50 rounded-full opacity-50"></div>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                  <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                </div>
                <span class="text-xs font-semibold text-red-500 uppercase tracking-wider">Crítico / Hoje</span>
              </div>
              <span class="flex h-2.5 w-2.5 relative" title="Atenção necessária em tempo real">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
              </span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-red-600" id="kpi-critical">0</div>
          </div>

          <div
            class="kpi-card anim-in anim-d3 bg-white rounded-3xl p-6 border border-emerald-100 shadow-sm flex flex-col justify-between h-32 relative overflow-hidden">
            <div class="absolute -right-4 -top-4 w-24 h-24 bg-emerald-50 rounded-full opacity-50"></div>
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                <i data-lucide="check-circle" class="w-5 h-5"></i>
              </div>
              <span class="text-xs font-semibold text-emerald-600 uppercase tracking-wider">Concluídas / Resolvidas</span>
            </div>
            <div class="font-heading font-extrabold text-4xl text-emerald-600" id="kpi-concluido">0</div>
          </div>
        </section>

        <!-- BANNER DISMISSÍVEL SETUP VIP REACTION -->
        <section id="banner-setup-vip" class="anim-in anim-d2 bg-gradient-to-r from-gray-900 via-gray-800 to-black text-white p-6 md:p-8 rounded-3xl border border-gray-700/60 shadow-xl flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden">
          <button onclick="dismissSetupVip()" class="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-full transition-all z-20" title="Fechar anúncio">
            <i data-lucide="x" class="w-4 h-4"></i>
          </button>
          <div class="absolute -right-10 -bottom-10 w-48 h-48 bg-brand/20 rounded-full blur-2xl"></div>
          <div class="flex items-start gap-4 relative z-10">
            <div class="w-12 h-12 rounded-2xl bg-brand flex items-center justify-center text-white shrink-0 shadow-lg shadow-brand/30">
              <i data-lucide="sparkles" class="w-6 h-6"></i>
            </div>
            <div>
              <div class="inline-block px-3 py-0.5 rounded-full bg-brand/20 text-[#ffb347] text-[11px] font-bold uppercase tracking-wider mb-1.5 border border-brand/30">
                Experiência 360º • Onboarding VIP
              </div>
              <h3 class="font-heading font-bold text-xl text-white">Quer que nossa equipe configure e calibre a IA do ReAction para o tom de voz exato da sua empresa?</h3>
              <p class="text-xs md:text-sm text-gray-300 mt-1 max-w-2xl leading-relaxed">
                Contrate o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, pasta no Google Drive com gravações/transcrições com calibração completa e suporte prioritário.
              </p>
            </div>
          </div>
          <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Sou%20cliente%20ReAction%20e%20gostaria%20de%20contratar%20o%20Setup%20VIP%20por%20R%24%20300" target="_blank" rel="noopener noreferrer" class="relative z-10 py-3.5 px-6 rounded-xl bg-brand text-white font-bold text-xs md:text-sm hover:brightness-110 transition-all shadow-lg shadow-brand/30 whitespace-nowrap text-center w-full md:w-auto shrink-0">
            Garantir Setup VIP (R$ 300) →
          </a>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-3 gap-6 anim-in anim-d2">

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm flex flex-col items-center">
            <h3 class="font-heading font-bold text-sm text-gray-800 self-start mb-6 flex items-center gap-2">
              <i data-lucide="pie-chart" class="w-4 h-4 text-brand"></i> Saúde da Marca
            </h3>
            <div class="w-full max-w-[200px] flex-1 flex items-center justify-center relative">
              <canvas id="chart-sentimento"></canvas>
            </div>
          </div>

          <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm md:col-span-2 flex flex-col">
            <h3 class="font-heading font-bold text-sm text-gray-800 mb-6 flex items-center gap-2">
              <i data-lucide="bar-chart-3" class="w-4 h-4 text-brand"></i> Principais Motivos de Interação
            </h3>
            <div class="w-full flex-1 min-h-[220px]">
              <canvas id="chart-tags"></canvas>
            </div>
          </div>

        </section>

        <section
          class="anim-in anim-d3 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden flex flex-col">
          <div class="flex items-center justify-between px-8 py-5 border-b border-gray-100 bg-gray-50/50">
            <h2 class="font-heading font-bold text-lg text-gray-900 flex items-center gap-2">
              <i data-lucide="target" class="w-5 h-5 text-brand"></i> Foco Agora
            </h2>
            <span class="text-xs font-bold text-brand bg-orange-100 px-3 py-1 rounded-full shadow-sm" id="focus-count">0
              ações</span>
          </div>

          <div id="task-list" class="divide-y divide-gray-100 min-h-[160px] flex-1">
            <div id="empty-state" class="flex flex-col items-center justify-center py-16 text-gray-400">
              <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                <i data-lucide="inbox" class="w-8 h-8 text-gray-300"></i>
              </div>
              <p class="text-sm font-medium text-gray-500">Tudo limpo por aqui.</p>
              <p class="text-xs mt-1 text-gray-400">Nenhuma ação pendente na sua fila.</p>
            </div>
          </div>
        </section>

        <section class="anim-in anim-d4 bg-white rounded-3xl border border-gray-100 shadow-sm p-8">
          <h2 class="font-heading font-bold text-lg text-gray-900 mb-6 flex items-center gap-2">
            <i data-lucide="calendar" class="w-5 h-5 text-brand"></i> Radar de Prazos
          </h2>
          <div id="timeline" class="grid grid-cols-7 gap-2 md:gap-4 text-center"></div>
        </section>

      </main>
    </div>
  </div>

      <!-- DOCK FLUTUANTE MOBILE SIMÉTRICO E CENTRALIZADO (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto max-w-[360px] w-full mx-auto grid grid-cols-4 gap-1 p-1.5 rounded-full bg-white/95 dark:bg-[#16171a]/95 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.16)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] transition-all duration-300">
      <a href="/app" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full bg-brand text-white shadow-sm shadow-brand/30 transition-all duration-200">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Cockpit</span>
      </a>
      <a href="/reputacao" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex flex-col gap-2">
      <div class="flex items-center justify-between gap-3">
        <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
          <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
          <span>Modo Escuro/Claro</span>
        </button>
        <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
          <i data-lucide="log-out" class="w-4 h-4"></i>
          <span>Sair</span>
        </a>
      </div>
      <div class="flex items-center justify-center gap-4 pt-2 text-[11px] text-gray-400 dark:text-gray-500">
        <a href="/termos" target="_blank" class="hover:text-brand">Termos de Uso</a>
        <span>•</span>
        <a href="/privacidade" target="_blank" class="hover:text-brand">Privacidade & LGPD</a>
      </div>
    </div>
  </div><button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>

        <div class="pt-6 mt-2 flex gap-3">
          <button type="button" id="btn-cancel"
            class="flex-1 py-3 rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
            Cancelar
          </button>
          <button type="submit" id="btn-salvar"
            class="flex-1 py-3 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
            Criar Ação
          </button>
        </div>
      </form>

    </div>
  </div>

  <div id="toast-container"></div>

  <script>
    // ── Estado Global ──
    let allActions = [];
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // ── Funções Utilitárias ──
    
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.2.0" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.2.0" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.2.0" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.2.0" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.2.0" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.2.0" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
      if (!str) return '';
      return str.toString().replace(/[&<>'"]/g, tag => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
      }[tag] || tag));
    }

    function getGreetingTime() {
      const h = new Date().getHours();
      if (h < 12) return 'Bom dia';
      if (h < 18) return 'Boa tarde';
      return 'Boa noite';
    }

    function isToday(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      d.setHours(0, 0, 0, 0);
      return d.getTime() === today.getTime();
    }

    function isThisWeek(dateStr) {
      if (!dateStr) return false;
      const d = new Date(dateStr);
      const start = new Date(today);
      start.setDate(today.getDate() - today.getDay());
      const end = new Date(start);
      end.setDate(start.getDate() + 7);
      return d >= start && d < end;
    }

    function filtrarPeriodo(btn, periodo) {
      document.querySelectorAll('.btn-periodo').forEach(b => {
        b.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
        b.classList.add('hover:text-gray-900');
      });
      btn.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      btn.classList.remove('hover:text-gray-900');

      let label = "Hoje";
      if (periodo === '7d') label = "Últimos 7 Dias";
      if (periodo === '30d') label = "Últimos 30 Dias";
      if (periodo === 'mes') label = "Este Mês";
      
      toast(`Filtro aplicado: ${label}`);
    }

    function toast(msg) {
      const el = document.createElement('div');
      el.className = 'toast-msg font-medium';
      el.textContent = msg;
      document.getElementById('toast-container').appendChild(el);

      // Auto-remover após 3 segundos com fade out
      setTimeout(() => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.3s ease-out';
        setTimeout(() => el.remove(), 300);
      }, 3000);
    }

    // ── Renderização de Gráficos (Chart.js) ──
    async function renderizarGraficos() {
      try {
        const res = await fetch('/api/analytics/resumo');
        if (!res.ok) return;
        const data = await res.json();

        // Gráfico de Sentimento (Rosca)
        const ctxS = document.getElementById('chart-sentimento').getContext('2d');
        new Chart(ctxS, {
          type: 'doughnut',
          data: {
            labels: ['Positivo', 'Neutro', 'Negativo'],
            datasets: [{
              data: [data.sentimentos.positivo, data.sentimentos.neutro, data.sentimentos.negativo],
              backgroundColor: ['#10b981', '#f59e0b', '#ef4444'], // Cores modernas
              borderWidth: 0,
              hoverOffset: 4
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '70%',
            plugins: {
              legend: {
                display: true,
                position: 'bottom',
                labels: { usePointStyle: true, padding: 20, font: { family: 'DM Sans', size: 12, weight: '500' } }
              }
            }
          }
        });

        // Gráfico de Assuntos/Tags (Barras)
        const ctxT = document.getElementById('chart-tags').getContext('2d');
        new Chart(ctxT, {
          type: 'bar',
          data: {
            labels: data.top_tags.labels.length ? data.top_tags.labels : ['Sem dados'],
            datasets: [{
              label: 'Ocorrências',
              data: data.top_tags.valores.length ? data.top_tags.valores : [0],
              backgroundColor: '#ff6b35', // Laranja da Marca
              hoverBackgroundColor: '#ff8559', // Feedback visual melhorado ao passar o rato
              borderRadius: 8,
              barPercentage: 0.6
            }]
          },
          options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
              x: { grid: { display: false, drawBorder: false }, ticks: { display: false } },
              y: { grid: { display: false, drawBorder: false }, ticks: { font: { family: 'DM Sans', weight: '600', size: 12 }, color: '#3f3f46' } }
            }
          }
        });

      } catch (err) {
        console.error("Erro ao renderizar gráficos:", err);
      }
    }

    // ── Lógica de Interface (KPIs e Tarefas) ──
    function updateKPIs() {
      const pending = allActions.filter(a => !a.completed).length;
      const critical = allActions.filter(a => !a.completed && (a.priority === 'critical' || isToday(a.due_date))).length;
      const finishedWeek = allActions.filter(a => a.completed && isThisWeek(a.created_at)).length;

      document.getElementById('kpi-pending').textContent = pending;
      document.getElementById('kpi-critical').textContent = critical;
      document.getElementById('kpi-concluido').textContent = finishedWeek;
    }

    function renderTasks() {
      const container = document.getElementById('task-list');
      const emptyState = document.getElementById('empty-state');

      // Ordena tarefas
      const sorted = [...allActions].sort((a, b) => {
        if (a.completed !== b.completed) return a.completed ? 1 : -1;
        if (a.priority === 'critical' && b.priority !== 'critical') return -1;
        if (b.priority === 'critical' && a.priority !== 'critical') return 1;
        return 0;
      });

      // Gestão otimizada do DOM (reutiliza nós existentes se possível)
      const existing = new Map([...container.querySelectorAll('.task-row')].map(el => [el.dataset.id, el]));

      sorted.forEach(action => {
        const id = action.__backendId;
        if (existing.has(id)) {
          updateTaskRowInner(existing.get(id), action);
          existing.delete(id);
        } else {
          container.appendChild(createTaskRow(action));
        }
      });

      // Remove os que já não existem
      existing.forEach(el => el.remove());

      // Esconde ou mostra o estado vazio
      if (emptyState) {
        emptyState.style.display = sorted.length ? 'none' : 'flex';
      }

      // Atualiza o contador de foco
      document.getElementById('focus-count').textContent = `${allActions.filter(a => !a.completed).length} ações`;
    }

    function createTaskRow(action) {
      const row = document.createElement('div');
      row.className = 'task-row flex items-center gap-4 px-8 py-4';
      row.dataset.id = action.__backendId;
      updateTaskRowInner(row, action);
      return row;
    }

    function updateTaskRowInner(row, action) {
      const isFinished = action.completed;
      const isCrit = action.priority === 'critical';
      const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

      // Html Seguro para a linha
      row.innerHTML = ` 
        <div class="check-box ${isFinished ? 'finalizado' : ''}"> 
          ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''} 
        </div> 
        <span class="flex-1 text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-800'}">${escapeHTML(action.title)}</span> 
        ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100">Crítica</span>' : ''} 
        ${dueStr ? '<span class="text-[11px] font-bold text-gray-500 bg-gray-100/80 px-2.5 py-1 rounded-md">' + dueStr + '</span>' : ''} 
        <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-1.5 rounded-lg hover:bg-red-50" title="Remover Tarefa">
          <i data-lucide="trash-2" class="w-4 h-4"></i>
        </button> 
      `;
      lucide.createIcons({ root: row });

      // Evento de Concluir Tarefa (Checkbox)
      const checkBtn = row.querySelector('.check-box');
      checkBtn.onclick = async () => {
        checkBtn.style.opacity = '0.5';
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
          if (!res.ok) throw new Error('Erro na API');

          const index = allActions.findIndex(a => a.__backendId === action.__backendId);
          if (index > -1) {
            allActions[index].completed = !allActions[index].completed;
            updateKPIs();
            renderTasks();
            renderTimeline();
            toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
          }
        } catch (err) {
          checkBtn.style.opacity = '1';
          toast('Erro ao atualizar ação no servidor.');
        }
      };

      // Evento de Excluir Tarefa
      const delBtn = row.querySelector('.del-btn');
      delBtn.onclick = async () => {
        delBtn.disabled = true;
        try {
          const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
          if (!res.ok) throw new Error('Erro na API');

          allActions = allActions.filter(a => a.__backendId !== action.__backendId);
          updateKPIs();
          renderTasks();
          renderTimeline();
          toast('🗑️ Ação removida com sucesso.');
        } catch (err) {
          delBtn.disabled = false;
          toast('Erro ao remover a ação.');
        }
      };
    }

    function renderTimeline() {
      const container = document.getElementById('timeline');
      container.innerHTML = '';

      const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
      const startOfWeek = new Date(today);
      startOfWeek.setDate(today.getDate() - today.getDay());

      for (let i = 0; i < 7; i++) {
        const d = new Date(startOfWeek);
        d.setDate(startOfWeek.getDate() + i);

        const dateStr = d.toISOString().split('T')[0];
        const count = allActions.filter(a => a.due_date === dateStr && !a.completed).length;
        const isT = d.getTime() === today.getTime();

        const col = document.createElement('div');
        col.className = `flex flex-col items-center gap-1.5 p-2 rounded-xl transition-colors ${isT ? 'bg-orange-50/50' : ''}`;
        col.innerHTML = ` 
          <span class="text-[11px] font-bold uppercase tracking-wider ${isT ? 'text-brand' : 'text-gray-400'}">${days[i]}</span> 
          <span class="text-sm font-semibold ${isT ? 'text-gray-900' : 'text-gray-500'}">${d.getDate()}</span> 
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold ${count > 0 ? 'text-white' : 'text-gray-400'}" style="${count > 0 ? (isT ? 'background:#ff6b35; box-shadow: 0 4px 10px rgba(255,107,53,0.3);' : 'background:#3f3f46;') : 'background:#f4f4f5;'}">${count || '-'}</div> 
        `;
        container.appendChild(col);
      }
    }

    // ── Eventos do Modal (Nova Ação) ──
    const modal = document.getElementById('modal-new');
    const formNewAction = document.getElementById('form-new-action');

    const openModal = () => {
      modal.style.display = 'flex';
      document.getElementById('inp-title').focus();
    };

    const closeModal = () => {
      modal.style.display = 'none';
      formNewAction.reset();
    };

    document.getElementById('btn-new-action').onclick = openModal;
    document.getElementById('btn-close-modal').onclick = closeModal;
    document.getElementById('btn-cancel').onclick = closeModal;

    modal.onclick = (e) => {
      if (e.target === modal) { closeModal(); }
    };

    formNewAction.onsubmit = async (e) => {
      e.preventDefault();

      const titleInput = document.getElementById('inp-title').value.trim();
      if (!titleInput) {
        toast('O título da ação não pode estar vazio.');
        return;
      }

      // Bloquear botão para evitar multi-submits
      const btn = document.getElementById('btn-salvar');
      const originalBtnText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Criando...';
      lucide.createIcons();

      const payload = {
        title: titleInput,
        priority: document.getElementById('inp-priority').value,
        due_date: document.getElementById('inp-due').value || ''
      };

      try {
        const res = await fetch('/api/acoes', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });

        if (!res.ok) throw new Error('Erro na API');
        const data = await res.json();

        // Adiciona a nova tarefa localmente
        allActions.push({
          __backendId: data.__backendId,
          title: payload.title,
          completed: false,
          priority: payload.priority,
          due_date: payload.due_date,
          created_at: new Date().toISOString()
        });

        closeModal();
        updateKPIs();
        renderTasks();
        renderTimeline();
        toast('✨ Ação adicionada com sucesso!');

      } catch (err) {
        toast('Erro ao comunicar com o servidor.');
      } finally {
        // Restaura botão
        btn.disabled = false;
        btn.innerHTML = originalBtnText;
        lucide.createIcons();
      }
    };

    // ── Inicialização (Carregamento da Página) ──
    (async () => {
      // Configurar a Saudação inicial
      document.getElementById('greeting-text').textContent = `${getGreetingTime()}, {{ usuario.nome }}.`;

      // Iniciar a busca de ações do backend
      try {
        const res = await fetch('/api/acoes');
        if (res.ok) {
          allActions = await res.json();
        }
      } catch (err) {
        console.error('Falha ao carregar as ações iniciais:', err);
      }

      // Verificar se o usuário ocultou o banner de Setup VIP
      if (localStorage.getItem('hideSetupVipReAction') === 'true') {
        const banner = document.getElementById('banner-setup-vip');
        if (banner) banner.style.display = 'none';
      }

      // Renderizar o ecrã
      updateKPIs();
      renderTasks();
      renderTimeline();

      // Carregar Gráficos e Ícones
      await renderizarGraficos();
      lucide.createIcons();
    })(); 

    function dismissSetupVip() {
      const banner = document.getElementById('banner-setup-vip');
      if (banner) banner.style.display = 'none';
      localStorage.setItem('hideSetupVipReAction', 'true');
    } 
  </script>

{% if usuario.termos_pendentes %}
<!-- MODAL DE ACEITE OBRIGATÓRIO DE TERMOS E PRIVACIDADE (LGPD) -->
<div id="modal-termos-obrigatorio" class="fixed inset-0 z-[100] bg-black/80 backdrop-blur-md flex items-center justify-center p-4">
    <div class="bg-white dark:bg-[#141517] rounded-3xl p-6 sm:p-8 max-w-md w-full border border-gray-200 dark:border-gray-800 shadow-2xl space-y-5 text-center">
        
        <!-- LOGO OFICIAL REACTION COM ESCUDO VIBRANTE -->
        <div class="flex justify-center items-center">
            <div class="w-16 h-16 rounded-2xl bg-gradient-to-br from-[#ff6b35] to-[#e05524] p-3 shadow-lg shadow-brand/30 flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" class="w-10 h-10 text-white fill-none stroke-current" stroke-width="36" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" />
                    <path d="M200 256l40 40 80-80" />
                </svg>
            </div>
        </div>

        <div>
            <h3 class="font-heading font-bold text-xl text-gray-900 dark:text-white">Atualização dos Termos & LGPD</h3>
            <p class="text-xs sm:text-sm text-gray-600 dark:text-gray-400 mt-2 leading-relaxed">
                Atualizamos nossos <a href="/termos" target="_blank" class="text-brand font-bold underline">Termos de Uso</a> e <a href="/privacidade" target="_blank" class="text-brand font-bold underline">Política de Privacidade</a> com os dados da holding e da <strong>Frame [IA]</strong>. Para continuar utilizando o ReAction, confirme a sua concordância.
            </p>
        </div>

        <div class="p-4 rounded-2xl bg-gray-50 dark:bg-[#18191c] border border-gray-200/60 dark:border-gray-800 text-left text-xs text-gray-600 dark:text-gray-400 space-y-1.5">
            <p><strong>Holding Controladora:</strong> MJSV TECNOLOGIA E SOLUCOES DIGITAIS (I.S.)</p>
            <p><strong>Operação & IA:</strong> Frame [IA] (Divisão de Inteligência Artificial)</p>
            <p><strong>CNPJ:</strong> 68.614.850/0001-13</p>
            <p><strong>Versão:</strong> 0.2.0 (16/08/2026)</p>
        </div>

        <button type="button" id="btn-aceitar-termos" onclick="confirmarAceiteTermos()" class="w-full py-3.5 rounded-xl bg-brand text-white font-bold text-sm hover:brightness-110 shadow-lg shadow-brand/25 transition-all flex items-center justify-center gap-2">
            <span>Li e Concordo com os Termos</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M5 13l4 4L19 7"></path></svg>
        </button>
    </div>
</div>
<script>
function confirmarAceiteTermos() {
    const btn = document.getElementById('btn-aceitar-termos');
    if (btn) { btn.disabled = true; btn.innerHTML = '<span>Gravando consentimento...</span>'; }
    fetch('/api/aceitar_termos', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'}
    })
    .then(r => r.json())
    .then(d => {
        if (d.success) {
            const modal = document.getElementById('modal-termos-obrigatorio');
            if (modal) modal.remove();
        } else {
            alert('Erro ao gravar aceite: ' + (d.error || 'Tente novamente'));
            if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
        }
    })
    .catch(err => {
        console.error(err);
        alert('Erro ao conectar ao servidor.');
        if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
    });
}
</script>
{% endif %}

</body>

</html>
================================================================================
FILE: templates\index.html
================================================================================
<!doctype html>
<html lang="pt-BR" class="scroll-smooth scroll-pt-24">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Domine a sua Reputação Online com IA</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.2.0">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700;900&family=Inter:wght@400;500;600;800&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            font-family: 'Inter', sans-serif;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden !important;
            width: 100% !important;
            max-width: 100% !important;
            margin: 0;
            padding: 0;
        }

        *, ::before, ::after {
            box-sizing: border-box;
        }

        h1,
        h2,
        h3,
        .font-heading {
            font-family: 'Poppins', sans-serif;
            overflow-wrap: break-word;
            word-break: break-word;
        }

        /* Efeitos Premium */
        .text-gradient {
            background: linear-gradient(135deg, #ff6b35, #ffb347);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-gradient-brand {
            background: linear-gradient(135deg, #ff6b35, #e05524);
        }

        .glow-effect {
            position: absolute;
            border-radius: 50%;
            filter: blur(90px);
            z-index: 0;
            pointer-events: none;
            max-width: 100vw;
        }

        /* Glassmorphism e Bento Grid */
        .glass-card {
            background: rgba(25, 25, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .bento-hover {
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .bento-hover:hover {
            transform: translateY(-5px);
            border-color: rgba(255, 107, 53, 0.4);
            box-shadow: 0 15px 30px -10px rgba(255, 107, 53, 0.15);
        }

        /* Animação Flutuante */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
    </style>
</head>

<body class="antialiased selection:bg-[#ff6b35] selection:text-white bg-[#050505] text-white w-full overflow-x-hidden">

    <div class="relative w-full overflow-hidden min-h-screen">

        <div class="glow-effect w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-[#ff6b35]/20 top-[-200px] left-[-100px]"></div>
        <div class="glow-effect w-[300px] md:w-[600px] h-[300px] md:h-[600px] bg-[#ffb347]/10 top-[20%] right-[-100px]"></div>

    <nav class="fixed top-0 w-full z-50 glass-card border-b-0 border-white/10 transition-all duration-300" id="navbar">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-3">
                <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-14 md:h-16 w-auto object-contain transition-all">
            </a>

            <div class="hidden md:flex items-center gap-8 text-sm font-semibold text-gray-300 ml-auto mr-8">
                <a href="#solucao" class="hover:text-white transition-colors">A Solução</a>
                <a href="#funcionalidades" class="hover:text-white transition-colors">Funcionalidades</a>
                <a href="#como-funciona" class="hover:text-white transition-colors">Como Funciona</a>
                <a href="#planos" class="hover:text-white transition-colors">Planos</a>
                <a href="/login" class="hover:text-white transition-colors">Entrar</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login?registo=true"
                    class="hidden sm:inline-block px-5 py-2.5 rounded-xl bg-white text-black font-bold text-sm hover:scale-105 hover:bg-gray-100 transition-all shadow-lg">
                    Começar Grátis
                </a>

                <!-- BOTÃO HAMBÚRGUER MOBILE (ESTILO CASH) -->
                <button type="button" id="btn-mobile-menu" onclick="toggleMobileMenu()"
                    class="md:hidden text-white p-2 rounded-xl bg-white/10 hover:bg-white/20 transition-all focus:outline-none"
                    aria-label="Abrir menu móvel">
                    <i data-lucide="menu" class="w-6 h-6" id="icon-mobile-menu"></i>
                </button>
            </div>
        </div>

        <!-- PAINEL DE NAVEGAÇÃO MOBILE (ESTILO CASH) -->
        <div id="mobile-menu-drawer" class="hidden md:hidden glass-card border-t border-white/10 px-6 py-6 space-y-4">
            <a href="#solucao" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">A Solução</a>
            <a href="#funcionalidades" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Funcionalidades</a>
            <a href="#como-funciona" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Como Funciona</a>
            <a href="#planos" onclick="toggleMobileMenu()" class="block text-base font-semibold text-gray-200 hover:text-white transition-colors">Planos</a>
            <div class="pt-4 border-t border-white/10 flex flex-col gap-3">
                <a href="/login" class="w-full py-3 rounded-xl border border-white/20 text-center font-bold text-sm text-white hover:bg-white/10 transition-all">Entrar na Conta</a>
                <a href="/login?registo=true" class="w-full py-3 rounded-xl bg-gradient-brand text-center font-bold text-sm text-white transition-all shadow-lg">Começar Grátis</a>
            </div>
        </div>
    </nav>

    <section class="relative pt-28 pb-16 md:pt-32 md:pb-24 px-6 overflow-hidden min-h-[85vh] flex items-center">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center w-full">

            <div class="z-10 text-center lg:text-left">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-card border-white/10 text-xs font-bold tracking-widest text-[#ffb347] mb-8 uppercase">
                    <span class="w-2 h-2 rounded-full bg-[#ff6b35] animate-pulse"></span>
                    IA de Gestão de Reputação
                </div>

                <h1 class="text-5xl md:text-7xl font-heading font-black mb-6 leading-[1.1] tracking-tight">
                    A sua marca, <br><span class="text-gradient">à prova de crises.</span>
                </h1>

                <p class="text-lg md:text-xl text-gray-400 mb-10 max-w-2xl mx-auto lg:mx-0 leading-relaxed font-medium">
                    O ReAction monitora o Google, Instagram e WhatsApp 24/7. Responde elogios com a sua voz e isola
                    clientes insatisfeitos antes que virem um escândalo público.
                </p>

                <div class="flex flex-col sm:flex-row items-center gap-4 justify-center lg:justify-start">
                    <a href="/login?registo=true"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl bg-gradient-brand text-white font-bold text-lg hover:scale-105 transition-all shadow-[0_10px_30px_rgba(255,107,53,0.4)] flex items-center justify-center gap-2">
                        Proteger Minha Marca <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </a>
                    <a href="#como-funciona"
                        class="w-full sm:w-auto px-8 py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                        <i data-lucide="play-circle" class="w-5 h-5 text-gray-400"></i> Ver como funciona
                    </a>
                </div>
            </div>

            <div class="relative z-10 hidden lg:block animate-float">
                <div
                    class="absolute inset-0 bg-gradient-to-tr from-[#ff6b35]/20 to-transparent rounded-[2.5rem] blur-2xl">
                </div>
                <div class="relative glass-card p-6 rounded-[2.5rem] border-white/20 shadow-2xl">
                    <div class="flex items-center gap-3 mb-6 border-b border-white/10 pb-4">
                        <div class="w-10 h-10 bg-red-500/20 rounded-full flex items-center justify-center">
                            <i data-lucide="alert-triangle" class="text-red-500 w-5 h-5"></i>
                        </div>
                        <div>
                            <p class="font-bold text-sm">Alerta de Crise Detetado</p>
                            <p class="text-xs text-gray-400">Google Business • Há 2 min</p>
                        </div>
                    </div>
                    <p class="text-sm text-gray-300 italic mb-4">"Péssimo atendimento, comida fria e demorou 2 horas.
                        Não recomendo a ninguém!"</p>
                    <div class="p-4 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30">
                        <p
                            class="text-xs font-bold text-[#ffb347] mb-2 uppercase tracking-wider flex items-center gap-1">
                            <i data-lucide="zap" class="w-3 h-3"></i> Ação da IA
                        </p>
                        <p class="text-sm text-white/90">"Olá! Pedimos imensas desculpas. Esta não é a nossa norma. A
                            nossa gerência quer resolver isto agora mesmo consigo no privado: wa.me/55999999999"</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="border-y border-white/5 bg-white/[0.02] py-10">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-widest mb-6">Projetado para empresas que
                valorizam cada cliente</p>
            <div
                class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="utensils"
                        class="w-6 h-6"></i> Gastronomia</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="building"
                        class="w-6 h-6"></i> Clínicas</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="shopping-bag"
                        class="w-6 h-6"></i> E-commerce</div>
                <div class="flex items-center gap-2 text-xl font-black font-heading"><i data-lucide="car"
                        class="w-6 h-6"></i> Concessionárias</div>
            </div>
        </div>
    </section>

    <section id="solucao" class="py-24 md:py-32 px-6 relative">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-16 items-center">
            <div>
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6 leading-tight">O silêncio digital está
                    <br><span class="text-red-500">custando caro.</span></h2>
                <p class="text-gray-400 text-lg mb-8 leading-relaxed">Cada avaliação negativa não respondida afasta
                    dezenas de novos clientes. Fazer a gestão disso manualmente consome horas que você não tem.</p>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Perda de Rank no Google</h4>
                            <p class="text-gray-500 text-sm">Empresas que não respondem avaliações perdem visibilidade
                                nas buscas locais.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center shrink-0 mt-1">
                            <i data-lucide="x" class="w-4 h-4 text-red-500"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-lg">Crises Escaladas</h4>
                            <p class="text-gray-500 text-sm">Um comentário ruim no Instagram pode inflamar outros
                                clientes se não for contido rápido.</p>
                        </div>
                    </li>
                </ul>
            </div>

            <div class="glass-card p-10 rounded-[2rem] border-l-4 border-l-[#ff6b35] relative overflow-hidden">
                <div class="absolute right-4 -top-6 text-[10rem] text-white/5 font-serif font-black select-none pointer-events-none font-sans">“</div>
                <h3 class="text-2xl font-heading font-bold mb-6 text-white">Com o ReAction:</h3>
                <p class="text-gray-300 text-lg mb-8 leading-relaxed">A Inteligência Artificial atua como o seu Relações
                    Públicas senior. Ela entende o contexto de cada mensagem, aproxima os fãs e esfria os detratores,
                    levando o problema para o WhatsApp privado antes que o mundo veja.</p>

                <div class="flex items-center gap-4 pt-6 border-t border-white/10">
                    <div class="flex -space-x-4">
                        <div class="w-10 h-10 rounded-full bg-gray-700 border-2 border-black"></div>
                        <div class="w-10 h-10 rounded-full bg-gray-600 border-2 border-black"></div>
                        <div
                            class="w-10 h-10 rounded-full bg-[#ff6b35] border-2 border-black flex items-center justify-center text-xs font-bold">
                            +99</div>
                    </div>
                    <p class="text-sm font-bold text-gray-400">Marcas salvas diariamente</p>
                </div>
            </div>
        </div>
    </section>

    <section id="funcionalidades" class="py-24 px-6 bg-black">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-4xl md:text-5xl font-heading font-black mb-6">Tudo que você precisa num só <span
                        class="text-[#ff6b35]">Cockpit.</span></h2>
                <p class="text-gray-400 text-lg">Não precisa abrir 5 abas diferentes. Centralize o atendimento e deixe a
                    máquina trabalhar por si.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex flex-col justify-center relative overflow-hidden group">
                    <div
                        class="absolute top-0 right-0 w-64 h-64 bg-[#ff6b35]/10 rounded-full blur-3xl group-hover:bg-[#ff6b35]/20 transition-all">
                    </div>
                    <i data-lucide="bot"
                        class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-2xl font-bold mb-4 font-heading">Clone do seu Tom de Voz</h3>
                    <p class="text-gray-400 max-w-md">Na "Sala de Máquinas", você ensina à IA as suas regras de ouro.
                        Ela não responde como um robô genérico, ela responde como VOCÊ, usando os seus jargões e
                        políticas de devolução.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="shield-alert"
                        class="w-12 h-12 text-red-400 mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Alerta Vermelho</h3>
                    <p class="text-gray-400 text-sm">Críticas nota 1 a 3 acionam um alerta crítico no seu painel. A IA
                        tenta isolar o cliente no privado imediatamente.</p>
                </div>

                <div class="glass-card p-8 rounded-[2rem] bento-hover group">
                    <i data-lucide="link-2"
                        class="w-12 h-12 text-[#ffb347] mb-6 transform group-hover:scale-110 transition-transform"></i>
                    <h3 class="text-xl font-bold mb-4 font-heading">Integração Nativa</h3>
                    <p class="text-gray-400 text-sm">Ligue o seu Google Meu Negócio e Instagram com 2 cliques. Nós
                        cuidamos das chaves e das APIs.</p>
                </div>

                <div
                    class="glass-card p-8 rounded-[2rem] bento-hover md:col-span-2 flex items-center justify-between overflow-hidden group">
                    <div class="z-10 max-w-sm">
                        <i data-lucide="pie-chart"
                            class="w-12 h-12 text-[#ff6b35] mb-6 transform group-hover:scale-110 transition-transform"></i>
                        <h3 class="text-2xl font-bold mb-4 font-heading">Analytics Avançado</h3>
                        <p class="text-gray-400">Descubra as palavras mais usadas pelos seus clientes e a evolução da
                            sua nota média. Exporte tudo num PDF bonito para a sua reunião de resultados.</p>
                    </div>
                    <i data-lucide="bar-chart-4"
                        class="w-48 h-48 text-white/5 -mr-10 transform group-hover:scale-105 transition-transform"></i>
                </div>
            </div>
        </div>
    </section>

    <section id="como-funciona" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-heading font-black text-center mb-16">Instale em <span class="text-[#ff6b35]">3
                    passos simples</span></h2>

            <div class="grid md:grid-cols-3 gap-8 relative">
                <div
                    class="hidden md:block absolute top-12 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-gray-800 via-[#ff6b35] to-gray-800 -z-10">
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        1</div>
                    <h3 class="text-xl font-bold mb-3">Conecte os Canais</h3>
                    <p class="text-gray-400 text-sm">Faça login com o Google ou Meta. O sistema puxa o histórico
                        automaticamente.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border-2 border-[#ff6b35] rounded-full flex items-center justify-center text-3xl font-black font-heading text-[#ff6b35] mb-6 shadow-[0_0_30px_rgba(255,107,53,0.3)]">
                        2</div>
                    <h3 class="text-xl font-bold mb-3">Calibre a IA</h3>
                    <p class="text-gray-400 text-sm">Acesse a Sala de Máquinas, defina o tom de voz e o link do seu
                        WhatsApp de contenção.</p>
                </div>

                <div class="text-center relative">
                    <div
                        class="w-24 h-24 mx-auto bg-black border border-gray-800 rounded-full flex items-center justify-center text-3xl font-black font-heading text-gray-500 mb-6 shadow-[0_0_20px_rgba(0,0,0,0.5)]">
                        3</div>
                    <h3 class="text-xl font-bold mb-3">Durma Tranquilo</h3>
                    <p class="text-gray-400 text-sm">Acompanhe o Cockpit. O sistema agradece os fãs e alerta-o apenas
                        quando houver fogo real.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SEÇÃO DE PLANOS E PREÇOS COM TOGGLE -->
    <section id="planos" class="py-24 px-6 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="text-3xl md:text-5xl font-heading font-black mb-6">Planos que cabem no seu negócio.</h2>
                <p class="text-lg text-gray-400 mb-8 font-medium">Proteja a sua marca com uma Inteligência Artificial que trabalha 24 horas por dia por um fração do custo de um especialista.</p>
                
                <!-- Toggle Mensal / Anual -->
                <div class="inline-flex items-center gap-3 p-1.5 rounded-2xl glass-card border-white/10">
                    <button id="btn-mensal" onclick="toggleBilling('mensal')" class="px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all">
                        Mensal
                    </button>
                    <button id="btn-anual" onclick="toggleBilling('anual')" class="px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2">
                        Anual <span class="px-2 py-0.5 rounded-full bg-white/20 text-[10px] uppercase font-black tracking-wide">25% OFF</span>
                    </button>
                </div>
            </div>

            <!-- Cards dos Planos -->
            <div class="grid md:grid-cols-3 gap-8 items-stretch mb-20">
                
                <!-- Card 1: Freemium -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Shield Start (Freemium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Degustação ativa do poder de resposta automática da IA.</p>
                        
                        <div class="mb-8">
                            <span class="text-4xl font-black font-heading text-white">R$ 0</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 1 Canal Conectado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Até 20 avaliações/mês por IA</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Respostas Automáticas básicas</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> 5 Rascunhos IA / mês</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta de Crise por E-mail</li>
                            <li class="flex items-center gap-3 text-gray-500"><i data-lucide="x" class="w-4 h-4"></i> Ponte Privada WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Começar Grátis
                    </a>
                </div>

                <!-- Card 2: Pro (Escudo Ativo) - MAIS PROCURADO -->
                <div class="glass-card p-8 rounded-[2.5rem] border-2 border-[#ff6b35] flex flex-col justify-between relative shadow-[0_0_50px_rgba(255,107,53,0.25)]">
                    <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-brand text-white text-xs font-black uppercase tracking-wider shadow-md">
                        🔥 Mais Procurado
                    </div>

                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Escudo Ativo (Pro)</h3>
                        <p class="text-sm text-gray-400 mb-6">Automação completa do dia a dia e calibração da IA.</p>
                        
                        <div class="mb-2">
                            <span id="price-pro" class="text-5xl font-black font-heading text-white">R$ 59,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-pro" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)</p>

                        <div class="p-3 bg-[#ff6b35]/10 rounded-xl border border-[#ff6b35]/30 mb-6 text-xs text-gray-300">
                            💡 <em>"Isso dá apenas R$ 1,96 por dia na opção anual para ter uma IA cuidando de todas as suas avaliações 24 horas por dia."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 3 Canais Conectados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 250 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Sala de Máquinas completa</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Aprovação ilimitada de rascunhos</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Ponte Privada WhatsApp (Crises)</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Relatórios Executivos PDF (90 dias)</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl bg-gradient-brand text-white font-bold text-center hover:scale-105 transition-all shadow-lg">
                        Garantir Plano Pro
                    </a>
                </div>

                <!-- Card 3: Premium (Blindagem Total) -->
                <div class="glass-card p-8 rounded-[2.5rem] border-white/10 flex flex-col justify-between relative">
                    <div>
                        <h3 class="text-xl font-heading font-bold text-white mb-2">Blindagem Total (Premium)</h3>
                        <p class="text-sm text-gray-400 mb-6">Proteção em escala com prioridade e motor Gemini Avançado.</p>
                        
                        <div class="mb-2">
                            <span id="price-premium" class="text-5xl font-black font-heading text-white">R$ 147,90</span>
                            <span class="text-gray-400 text-sm font-medium">/ mês</span>
                        </div>
                        <p id="sub-premium" class="text-xs text-[#ffb347] font-semibold mb-6">Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)</p>

                        <div class="p-3 bg-white/5 rounded-xl border border-white/10 mb-6 text-xs text-gray-300">
                            💡 <em>"Menos de R$ 4,90 por dia para blindar totalmente sua marca, responder ilimitado e direcionar todas as crises direto para o seu WhatsApp."</em>
                        </div>

                        <ul class="space-y-4 text-sm text-gray-300 font-medium mb-8">
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Canais Ilimitados</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> <strong>Até 1.500 avaliações/mês</strong></li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Motor Gemini IA Avançado</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Alerta em tempo real via WhatsApp</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Multi-usuários e Gestão de Equipe</li>
                            <li class="flex items-center gap-3"><i data-lucide="check" class="w-4 h-4 text-[#ff6b35]"></i> Suporte Prioritário WhatsApp</li>
                        </ul>
                    </div>

                    <a href="/login?registo=true" class="w-full py-4 rounded-xl glass-card font-bold text-white hover:bg-white/10 transition-all text-center">
                        Contratar Premium
                    </a>
                </div>

            </div>

            <!-- TABELA COMPARATIVA DETALHADA -->
            <div class="glass-card rounded-[2.5rem] p-8 border-white/10 overflow-x-auto">
                <h3 class="text-2xl font-heading font-bold text-white mb-6 text-center">Tabela Comparativa de Recursos</h3>
                <table class="w-full text-left text-sm text-gray-300">
                    <thead>
                        <tr class="border-b border-white/10 text-white font-heading">
                            <th class="py-4 px-4 font-bold">Recurso / Benefício</th>
                            <th class="py-4 px-4 text-center font-bold">Freemium</th>
                            <th class="py-4 px-4 text-center font-bold text-[#ff6b35]">Pro (Escudo Ativo)</th>
                            <th class="py-4 px-4 text-center font-bold">Premium (Blindagem Total)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Mensal</td>
                            <td class="py-4 px-4 text-center">R$ 0 / mês</td>
                            <td class="py-4 px-4 text-center text-[#ffb347] font-semibold">R$ 79,90 / mês</td>
                            <td class="py-4 px-4 text-center">R$ 197,90 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Plano Anual (Com Economia)</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 59,90 / mês (R$ 718,80/ano)</td>
                            <td class="py-4 px-4 text-center font-bold text-white">12x de R$ 147,90 / mês (R$ 1.774,80/ano)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Economia no Anual</td>
                            <td class="py-4 px-4 text-center">—</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 240 / ano (~25% OFF)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-semibold">Economiza R$ 600 / ano (~25% OFF)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Canais Conectados</td>
                            <td class="py-4 px-4 text-center">1 Canal</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Até 3 Canais</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Canais Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Volume de Interações</td>
                            <td class="py-4 px-4 text-center">Até 20 / mês</td>
                            <td class="py-4 px-4 text-center">Até 250 / mês</td>
                            <td class="py-4 px-4 text-center">Até 1.500 / mês</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Respostas Automáticas (Elogios)</td>
                            <td class="py-4 px-4 text-center">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                            <td class="py-4 px-4 text-center text-green-400">Sim</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Rascunhos com IA (Dúvidas)</td>
                            <td class="py-4 px-4 text-center">5 / mês</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Ilimitados</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Contenção de Crises (Link WhatsApp)</td>
                            <td class="py-4 px-4 text-center text-gray-500">❌</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Ponte Privada)</td>
                            <td class="py-4 px-4 text-center text-green-400 font-medium">Sim (Com Alerta de Crise)</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Calibragem da IA (Tom de Voz)</td>
                            <td class="py-4 px-4 text-center">Básico</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total (Sala de Máquinas)</td>
                            <td class="py-4 px-4 text-center font-semibold text-white">Total + Treinamento Avançado</td>
                        </tr>
                        <tr>
                            <td class="py-4 px-4 font-medium">Relatórios Executivos</td>
                            <td class="py-4 px-4 text-center">7 dias</td>
                            <td class="py-4 px-4 text-center">90 dias + Exportar PDF</td>
                            <td class="py-4 px-4 text-center font-bold text-white">Histórico Ilimitado + PDF</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- CHAMADA ESTRATÉGICA SETUP VIP REACTION -->
            <div class="mt-12 glass-card rounded-[2.5rem] p-8 md:p-10 border-[#ff6b35]/30 bg-gradient-to-r from-[#ff6b35]/10 via-black to-[#e05524]/10 flex flex-col md:flex-row items-center justify-between gap-6 shadow-[0_10px_30px_rgba(255,107,53,0.15)]">
                <div class="flex items-start gap-4">
                    <div class="w-12 h-12 rounded-2xl bg-gradient-brand flex items-center justify-center text-white shrink-0 shadow-md">
                        <i data-lucide="sparkles" class="w-6 h-6"></i>
                    </div>
                    <div>
                        <div class="inline-block px-3 py-1 rounded-full bg-[#ff6b35]/20 text-[#ffb347] text-xs font-bold uppercase tracking-wider mb-2 border border-[#ff6b35]/30">
                            Experiência 360º • Setup VIP
                        </div>
                        <h4 class="text-xl font-heading font-bold text-white mb-2">Prefere receber a estrutura pronta, personalizada e treinada para sua equipe?</h4>
                        <p class="text-sm text-gray-300 max-w-3xl leading-relaxed">
                            Conheça o <strong>Setup VIP ReAction (R$ 300,00 à vista)</strong>: Inclui 2 sessões ao vivo de 60min com nossos especialistas, criação de repositório exclusivo no Google Drive com gravações/transcrições, parametrização da IA no tom de voz da sua marca e <strong>Suporte e calibração completa</strong> (90 dias isento).
                        </p>
                    </div>
                </div>
                <a href="https://wa.me/5585999999999?text=Ol%C3%A1!%20Gostaria%20de%20saber%20mais%20sobre%20o%20Setup%20VIP%20ReAction%20por%20R%24%20300%2C00" target="_blank" rel="noopener noreferrer" class="py-4 px-8 rounded-xl bg-gradient-brand text-white font-bold text-sm whitespace-nowrap hover:scale-105 transition-all shadow-lg text-center shrink-0">
                    Contratar Setup VIP (R$ 300) →
                </a>
            </div>

        </div>
    </section>

    <section class="py-24 px-6">
        <div
            class="max-w-5xl mx-auto rounded-[3rem] bg-gradient-brand p-12 md:p-24 text-center relative overflow-hidden shadow-[0_20px_50px_rgba(255,107,53,0.4)]">
            <div
                class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4yKSIvPjwvc3ZnPg==')] opacity-30">
            </div>

            <h2 class="text-4xl md:text-6xl font-heading font-black mb-8 relative z-10">Tome as rédeas da sua reputação
                online.</h2>
            <p class="text-lg md:text-xl text-white/90 mb-10 max-w-2xl mx-auto relative z-10 font-medium">Junte-se a
                dezenas de empresas que pararam de perder clientes por falta de resposta e gestão de crise.</p>

            <a href="/login?registo=true"
                class="inline-block relative z-10 px-10 py-5 bg-white text-black rounded-2xl font-bold text-lg hover:scale-105 hover:bg-gray-100 transition-all shadow-2xl">
                Criar Minha Conta Grátis
            </a>

            <p class="mt-6 text-sm text-white/70 relative z-10 font-semibold">
                <i data-lucide="check" class="inline w-4 h-4 mr-1"></i> Setup em menos de 5 minutos.
            </p>
        </div>
    </section>

    <footer class="border-t border-white/10 bg-black pt-16 pb-8 px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-2">
                <i data-lucide="shield-check" class="text-[#ff6b35] w-6 h-6"></i>
                <span class="text-xl font-heading font-black tracking-tight">Re<span
                        class="text-[#ff6b35]">Action</span></span>
            </div>
            <div class="flex flex-wrap justify-center gap-6 text-sm font-semibold text-gray-400">
                <a href="/termos" class="hover:text-brand transition">Termos de Uso</a>
                <a href="/privacidade" class="hover:text-brand transition">Política de Privacidade & LGPD</a>
                <a href="mailto:contato@frameia.com.br" class="hover:text-brand transition">Suporte</a>
            </div>
        </div>
        <div class="max-w-7xl mx-auto text-center mt-12 text-xs font-medium text-gray-500 space-y-1">
            <p>&copy; 2026 MJSV TECNOLOGIA E SOLUCOES DIGITAIS INOVA SIMPLES (I.S.) • CNPJ: 68.614.850/0001-13</p>
            <p class="text-gray-600">ReAction by Frame [IA] • Todos os direitos reservados.</p>
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Alternância do Toggle Mensal / Anual na Seção de Preços
        function toggleBilling(ciclo) {
            const btnMensal = document.getElementById('btn-mensal');
            const btnAnual = document.getElementById('btn-anual');
            const pricePro = document.getElementById('price-pro');
            const subPro = document.getElementById('sub-pro');
            const pricePremium = document.getElementById('price-premium');
            const subPremium = document.getElementById('sub-premium');

            if (ciclo === 'anual') {
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all flex items-center gap-2';
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all';
                pricePro.textContent = 'R$ 59,90';
                subPro.textContent = 'Faturado 12x de R$ 59,90/mês (R$ 718,80/ano no PIX ou Cartão)';
                pricePremium.textContent = 'R$ 147,90';
                subPremium.textContent = 'Faturado 12x de R$ 147,90/mês (R$ 1.774,80/ano - Economiza R$ 600/ano)';
            } else {
                btnMensal.className = 'px-6 py-2.5 rounded-xl font-bold text-sm bg-gradient-brand text-white shadow-lg transition-all';
                btnAnual.className = 'px-6 py-2.5 rounded-xl font-bold text-sm text-gray-400 transition-all flex items-center gap-2';
                pricePro.textContent = 'R$ 79,90';
                subPro.textContent = 'Pagamento mensal recorrente';
                pricePremium.textContent = 'R$ 197,90';
                subPremium.textContent = 'Pagamento mensal recorrente';
            }
        }

        function toggleMobileMenu() {
            const drawer = document.getElementById('mobile-menu-drawer');
            const icon = document.getElementById('icon-mobile-menu');
            if (drawer.classList.contains('hidden')) {
                drawer.classList.remove('hidden');
                icon.setAttribute('data-lucide', 'x');
            } else {
                drawer.classList.add('hidden');
                icon.setAttribute('data-lucide', 'menu');
            }
            if (window.lucide) lucide.createIcons();
        }

        // Efeito de background na navbar ao dar scroll
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 20) {
                nav.classList.add('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.remove('border-b-0');
            } else {
                nav.classList.remove('bg-black/80', 'shadow-lg', 'backdrop-blur-lg');
                nav.classList.add('border-b-0');
            }
        });
    </script>
    </div>
</body>

</html>
================================================================================
FILE: templates\integracoes.html
================================================================================
<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Canais e Integrações</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.2.0">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* Reset e Estrutura Base */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Navegação Lateral */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* Notificações (Toasts) */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
            animation: slide-up 0.3s ease-out;
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* Área Segura Mobile (Notch iPhones) */
        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.2.0" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div class="md:hidden flex items-center justify-between px-5 py-3.5 bg-white/85 dark:bg-[#141517]/85 backdrop-blur-xl sticky top-0 z-30 border-b border-gray-200/70 dark:border-white/10 transition-colors">
        <!-- LOGO MOBILE OFICIAL REACTION BY FRAME [IA] (FUNDO CLARO) -->
        <a href="/app" class="flex items-center gap-2">
          <img src="/static/logo_reaction_transparent_light.png?v=0.2.0" alt="ReAction Logo" class="h-10 w-auto object-contain transition-all">
        </a>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
      </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Conectar Canais</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Ligue as suas contas para que a IA do ReAction
                        possa atuar de forma automática.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full">

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">

                    <!-- GOOGLE MEU NEGÓCIO -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/google_business.png?v=0.2.0" alt="Google Meu Negócio" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Google Meu Negócio</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações
                            do Google Meu Negócio e deixe a IA responder e reter clientes.</p>

                        <div id="status-google" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/google/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- INSTAGRAM -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/instagram.png?v=0.2.0" alt="Instagram" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Instagram</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Proteja a caixa de
                            comentários e transforme interações do Instagram em defensores da marca.</p>

                        <div id="status-instagram" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/instagram/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- IFOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ifood.png?v=0.2.0" alt="iFood" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">iFood</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte a sua loja no iFood para gerenciar avaliações de entrega e evitar cancelamentos.</p>

                        <div id="status-ifood" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ifood/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- WHATSAPP -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/whatsapp.png?v=0.2.0" alt="WhatsApp" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">WhatsApp</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Conecte o bot de atendimento e receba alertas de contenção de crise instantâneos no seu celular.</p>

                        <div id="status-whatsapp" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/whatsapp/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- ZÉ DELIVERY -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/ze_delivery.png?v=0.2.0" alt="Zé Delivery" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">Zé Delivery</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Gerencie a reputação de pedidos de bebidas e entregas rápidas com respostas automáticas da IA.</p>

                        <div id="status-ze_delivery" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/ze_delivery/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                    <!-- 99FOOD -->
                    <div
                        class="bg-white rounded-3xl border border-gray-100 p-8 flex flex-col items-center text-center shadow-sm transition-all hover:-translate-y-1 hover:shadow-md h-full">
                        <div class="w-16 h-16 mb-5 flex items-center justify-center">
                            <img src="/static/img/plataformas/99food.png?v=0.2.0" alt="99Food" class="w-16 h-16 object-contain rounded-2xl shadow-sm">
                        </div>
                        <h3 class="font-heading font-bold text-lg text-gray-900">99Food</h3>
                        <p class="text-sm text-gray-500 mt-2 mb-8 font-medium leading-relaxed">Sincronize as avaliações do 99Food e responda automaticamente a entregas e pedidos.</p>

                        <div id="status-99food" class="mt-auto w-full">
                            <button onclick="conectarCanal(this, '/api/auth/99food/login')"
                                class="w-full py-3.5 rounded-xl border border-gray-200 text-gray-700 text-sm font-bold hover:bg-gray-50 hover:border-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-200 shadow-sm flex items-center justify-center gap-2">
                                Conectar Conta
                            </button>
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

        <!-- DOCK FLUTUANTE MOBILE SIMÉTRICO E CENTRALIZADO (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto max-w-[360px] w-full mx-auto grid grid-cols-4 gap-1 p-1.5 rounded-full bg-white/95 dark:bg-[#16171a]/95 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.16)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] transition-all duration-300">
      <a href="/app" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Cockpit</span>
      </a>
      <a href="/reputacao" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full bg-brand text-white shadow-sm shadow-brand/30 transition-all duration-200">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex flex-col gap-2">
      <div class="flex items-center justify-between gap-3">
        <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
          <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
          <span>Modo Escuro/Claro</span>
        </button>
        <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
          <i data-lucide="log-out" class="w-4 h-4"></i>
          <span>Sair</span>
        </a>
      </div>
      <div class="flex items-center justify-center gap-4 pt-2 text-[11px] text-gray-400 dark:text-gray-500">
        <a href="/termos" target="_blank" class="hover:text-brand">Termos de Uso</a>
        <span>•</span>
        <a href="/privacidade" target="_blank" class="hover:text-brand">Privacidade & LGPD</a>
      </div>
    </div>
  </div><button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>

    <div id="toast-container"></div>

    <script>
        // Inicializa Ícones
        lucide.createIcons();

        // Helpers de Segurança e UI
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // Função UX para conectar com efeito de loading
        function conectarCanal(btn, url) {
            // Desabilita o botão para evitar cliques duplicados
            btn.disabled = true;
            btn.classList.add('opacity-70', 'cursor-not-allowed');

            // Troca o texto por um spinner
            btn.innerHTML = `<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Conectando...`;
            lucide.createIcons({ root: btn });

            // Redireciona com um pequeno atraso para o utilizador ver o feedback
            setTimeout(() => {
                window.location.href = url;
            }, 400);
        }

        // Verifica se voltou de um redirecionamento com sucesso (OAuth)
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('sucesso') === 'true') {
            const plataformaRaw = urlParams.get('plat');
            const plataformaSegura = escapeHTML(plataformaRaw);

            // Capitalizar a primeira letra para ficar bonito no toast
            const plataformaCapitalizada = plataformaSegura.charAt(0).toUpperCase() + plataformaSegura.slice(1);

            toast(`✨ Conta do ${plataformaCapitalizada} conectada com sucesso!`);

            // Limpa a URL para não exibir a notificação de novo ao atualizar a página
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        // Carrega o status da base de dados para pintar de verde o que já está ligado
        async function carregarStatus() {
            try {
                const res = await fetch('/api/integracoes/status');
                if (res.ok) {
                    const integracoes = await res.json();
                    integracoes.forEach(intg => {
                        if (intg.status === 'ativo') {
                            const platSegura = escapeHTML(intg.plataforma);
                            const container = document.getElementById(`status-${platSegura}`);

                            if (container) {
                                container.innerHTML = `
                                <div class="w-full py-3.5 rounded-xl bg-emerald-50 text-emerald-700 text-sm font-bold flex items-center justify-center gap-2 border border-emerald-200/60 shadow-sm">
                                    <i data-lucide="check-circle-2" class="w-5 h-5"></i> Conectado
                                </div>
                                `;
                            }
                        }
                    });
                    lucide.createIcons();
                }
            } catch (e) {
                console.error("Erro ao ler integrações", e);
            }
        }

        // Inicializa ao carregar
        carregarStatus();
    </script>

{% if usuario.termos_pendentes %}
<!-- MODAL DE ACEITE OBRIGATÓRIO DE TERMOS E PRIVACIDADE (LGPD) -->
<div id="modal-termos-obrigatorio" class="fixed inset-0 z-[100] bg-black/80 backdrop-blur-md flex items-center justify-center p-4">
    <div class="bg-white dark:bg-[#141517] rounded-3xl p-6 sm:p-8 max-w-md w-full border border-gray-200 dark:border-gray-800 shadow-2xl space-y-5 text-center">
        
        <!-- LOGO OFICIAL REACTION COM ESCUDO VIBRANTE -->
        <div class="flex justify-center items-center">
            <div class="w-16 h-16 rounded-2xl bg-gradient-to-br from-[#ff6b35] to-[#e05524] p-3 shadow-lg shadow-brand/30 flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" class="w-10 h-10 text-white fill-none stroke-current" stroke-width="36" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" />
                    <path d="M200 256l40 40 80-80" />
                </svg>
            </div>
        </div>

        <div>
            <h3 class="font-heading font-bold text-xl text-gray-900 dark:text-white">Atualização dos Termos & LGPD</h3>
            <p class="text-xs sm:text-sm text-gray-600 dark:text-gray-400 mt-2 leading-relaxed">
                Atualizamos nossos <a href="/termos" target="_blank" class="text-brand font-bold underline">Termos de Uso</a> e <a href="/privacidade" target="_blank" class="text-brand font-bold underline">Política de Privacidade</a> com os dados da holding e da <strong>Frame [IA]</strong>. Para continuar utilizando o ReAction, confirme a sua concordância.
            </p>
        </div>

        <div class="p-4 rounded-2xl bg-gray-50 dark:bg-[#18191c] border border-gray-200/60 dark:border-gray-800 text-left text-xs text-gray-600 dark:text-gray-400 space-y-1.5">
            <p><strong>Holding Controladora:</strong> MJSV TECNOLOGIA E SOLUCOES DIGITAIS (I.S.)</p>
            <p><strong>Operação & IA:</strong> Frame [IA] (Divisão de Inteligência Artificial)</p>
            <p><strong>CNPJ:</strong> 68.614.850/0001-13</p>
            <p><strong>Versão:</strong> 0.2.0 (16/08/2026)</p>
        </div>

        <button type="button" id="btn-aceitar-termos" onclick="confirmarAceiteTermos()" class="w-full py-3.5 rounded-xl bg-brand text-white font-bold text-sm hover:brightness-110 shadow-lg shadow-brand/25 transition-all flex items-center justify-center gap-2">
            <span>Li e Concordo com os Termos</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M5 13l4 4L19 7"></path></svg>
        </button>
    </div>
</div>
<script>
function confirmarAceiteTermos() {
    const btn = document.getElementById('btn-aceitar-termos');
    if (btn) { btn.disabled = true; btn.innerHTML = '<span>Gravando consentimento...</span>'; }
    fetch('/api/aceitar_termos', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'}
    })
    .then(r => r.json())
    .then(d => {
        if (d.success) {
            const modal = document.getElementById('modal-termos-obrigatorio');
            if (modal) modal.remove();
        } else {
            alert('Erro ao gravar aceite: ' + (d.error || 'Tente novamente'));
            if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
        }
    })
    .catch(err => {
        console.error(err);
        alert('Erro ao conectar ao servidor.');
        if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
    });
}
</script>
{% endif %}

</body>

</html>
================================================================================
FILE: templates\login.html
================================================================================
<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Gestor de Reputação</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.2.0">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="https://alcdn.msauth.net/browser/2.35.0/js/msal-browser.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { heading: ['Poppins', 'sans-serif'], body: ['DM Sans', 'sans-serif'] },
                    colors: { brand: '#ff6b35', sidebar: '#3a3a3a', bglight: '#f4f4f5' }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f4f4f5;
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Suaviza o autocomplete dos navegadores */
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px white inset !important;
            -webkit-text-fill-color: #1f2937 !important;
        }
    </style>
</head>

<body class="min-h-screen font-body flex items-center justify-center p-4">

    <div
        class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden fade-in border border-gray-100">

        <!-- CABEÇALHO DO LOGIN COM A LOGO DO ESCUDO PADRONIZADA -->
        <div class="bg-sidebar p-8 text-center flex flex-col items-center border-b border-gray-800">
            <img src="/static/logo_reaction_transparent_dark.png" alt="ReAction Logo" class="h-16 w-auto object-contain mb-3 transition-all">
            <p class="text-gray-400 text-sm mt-1.5 font-medium tracking-wide">O seu gestor de reputação inteligente</p>
        </div>

        <div class="p-8 md:p-10">

            {% if erro %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro }}
            </div>
            {% endif %}

            {% if erro_registo %}
            <div
                class="mb-6 p-4 bg-red-50 border border-red-100 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 animate-pulse shadow-sm">
                <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i> {{ erro_registo }}
            </div>
            {% endif %}

            <!-- ALERTA DE SUCESSO -->
            {% if sucesso %}
            <div
                class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 shadow-sm">
                <i data-lucide="check-circle-2" class="w-5 h-5 flex-shrink-0"></i> <div>{{ sucesso|safe }}</div>
            </div>
            {% endif %}

            {% if redefinir_token %}
            <!-- FORMULÁRIO DE NOVA SENHA COM TOKEN SEGURO -->
            <form id="form-redefinir" action="/redefinir_senha" method="POST" class="space-y-5" onsubmit="bloquearBotao(this)">
                <input type="hidden" name="token" value="{{ redefinir_token }}">
                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Cadastrar Nova Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Informe a sua nova senha para redefinir o seu acesso com segurança.</p>

                    <label for="redefinir-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Nova Senha</label>
                    <input id="redefinir-senha" type="password" name="senha" required minlength="6" autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Atualizar Senha</span> <i data-lucide="shield-check" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>
            </form>
            {% else %}
            <!-- GRADE DE AUTENTICAÇÃO SOCIAL (GOOGLE, FACEBOOK, APPLE) -->
            <div class="mb-6 space-y-3 {% if mostrar_registo %}hidden{% endif %}" id="container-google-login">
                <!-- Botão Google -->
                <button type="button" onclick="iniciarGoogleLogin()"
                    class="w-full py-3 px-4 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-sm flex items-center justify-center gap-3 transition-all shadow-sm hover:shadow hover:border-gray-300">
                    <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Continuar com o Google</span>
                </button>

                <div class="grid grid-cols-2 gap-3">
                    <!-- Botão Facebook -->
                    <button type="button" onclick="iniciarFacebookLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-blue-600 bg-[#1877F2] hover:bg-[#166fe5] text-white font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow">
                        <svg class="w-4 h-4 shrink-0 fill-current" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        <span>Facebook</span>
                    </button>

                    <!-- Botão Microsoft -->
                    <button type="button" onclick="iniciarMicrosoftLogin()"
                        class="w-full py-2.5 px-3 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 text-gray-700 font-bold text-xs flex items-center justify-center gap-2 transition-all shadow-sm hover:shadow hover:border-gray-300">
                        <svg class="w-4 h-4 shrink-0" viewBox="0 0 23 23">
                            <rect x="1" y="1" width="10" height="10" fill="#f25022"/>
                            <rect x="12" y="1" width="10" height="10" fill="#7fba00"/>
                            <rect x="1" y="12" width="10" height="10" fill="#00a4ef"/>
                            <rect x="12" y="12" width="10" height="10" fill="#ffb900"/>
                        </svg>
                        <span>Microsoft</span>
                    </button>
                </div>

                <div class="relative my-4 flex items-center justify-center">
                    <div class="border-t border-gray-200 w-full"></div>
                    <span class="bg-white px-3 text-[10px] font-bold text-gray-400 uppercase tracking-wider absolute">OU COM E-MAIL E SENHA</span>
                </div>
            </div>

            <form id="form-login" action="/login" method="POST"
                class="space-y-5 {% if mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="login-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail de
                        Acesso</label>
                    <input id="login-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="login-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Senha</label>
                    <input id="login-senha" type="password" name="senha" required autocomplete="current-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="••••••••">
                    <div class="flex justify-end text-xs mt-1.5">
                        <a href="#" onclick="mostrarEsqueciSenha(event)" class="text-gray-500 font-semibold hover:text-brand transition-colors hover:underline">Esqueceu a sua senha?</a>
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Entrar na Plataforma</span> <i data-lucide="arrow-right" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Não tem uma conta? <a href="#" onclick="toggleForms(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Criar conta
                        grátis</a>
                </p>
            </form>

            <form id="form-esqueci" action="/esqueci_senha" method="POST"
                class="space-y-5 hidden" onsubmit="bloquearBotao(this)">

                <div>
                    <h3 class="font-heading font-bold text-lg text-gray-900 mb-1">Recuperar Senha</h3>
                    <p class="text-xs text-gray-500 mb-4 font-medium">Insira o seu e-mail cadastrado para receber as instruções de redefinição.</p>

                    <label for="esqueci-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Cadastrado</label>
                    <input id="esqueci-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 hover:-translate-y-0.5 transition-all shadow-lg shadow-brand/30">
                        <span>Enviar Link de Recuperação</span> <i data-lucide="mail" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Lembrou da senha? <a href="#" onclick="voltarParaLogin(event)"
                        class="text-brand font-bold hover:text-orange-600 transition-colors hover:underline">Voltar ao Login</a>
                </p>
            </form>

            <form id="form-registo" action="/registo" method="POST"
                class="space-y-5 {% if not mostrar_registo %}hidden{% endif %}" onsubmit="bloquearBotao(this)">

                <div>
                    <label for="reg-empresa" class="block text-sm font-bold text-gray-800 mb-1.5">Nome da
                        Empresa</label>
                    <input id="reg-empresa" type="text" name="nome_empresa" required autocomplete="organization"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="A sua marca">
                </div>

                <div>
                    <label for="reg-nome" class="block text-sm font-bold text-gray-800 mb-1.5">O seu Nome</label>
                    <input id="reg-nome" type="text" name="nome_usuario" required autocomplete="name"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Como gosta de ser chamado">
                </div>

                <div>
                    <label for="reg-email" class="block text-sm font-bold text-gray-800 mb-1.5">E-mail Comercial</label>
                    <input id="reg-email" type="email" name="email" required autocomplete="email"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="seu@email.com">
                </div>

                <div>
                    <label for="reg-senha" class="block text-sm font-bold text-gray-800 mb-1.5">Criar
                        Palavra-passe</label>
                    <input id="reg-senha" type="password" name="senha" required minlength="6"
                        autocomplete="new-password"
                        class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-sidebar focus:ring-2 focus:ring-gray-300 transition-all bg-gray-50 focus:bg-white font-medium"
                        placeholder="Mínimo de 6 caracteres">
                </div>

                                <div class="flex items-start gap-2.5 pt-1">
                    <input id="reg-termos" type="checkbox" required name="concordo_termos" value="1"
                        class="mt-1 w-4 h-4 text-brand rounded border-gray-300 focus:ring-brand cursor-pointer">
                    <label for="reg-termos" class="text-xs text-gray-600 leading-relaxed cursor-pointer">
                        Declaro que li e concordo com os <a href="/termos" target="_blank" class="text-brand font-bold underline hover:text-orange-600">Termos de Uso</a> e a <a href="/privacidade" target="_blank" class="text-brand font-bold underline hover:text-orange-600">Política de Privacidade</a> (LGPD).
                    </label>
                </div>

                <div class="pt-2">
                    <button type="submit"
                        class="w-full py-3.5 rounded-xl text-white font-bold flex items-center justify-center gap-2 bg-sidebar hover:bg-black hover:-translate-y-0.5 transition-all shadow-lg shadow-gray-900/20">
                        <span>Criar Nova Conta</span> <i data-lucide="user-plus" class="w-4 h-4 icone-normal"></i>
                    </button>
                </div>

                <p class="text-center text-sm font-medium text-gray-500 mt-6">
                    Já é cliente? <a href="#" onclick="toggleForms(event)"
                        class="text-sidebar font-bold hover:text-black transition-colors hover:underline">Iniciar
                        sessão</a>
                </p>
            </form>
            {% endif %}

                    <div class="mt-8 pt-4 border-t border-gray-100 flex items-center justify-center gap-4 text-xs text-gray-400">
                <a href="/termos" target="_blank" class="hover:text-brand transition-colors">Termos de Uso</a>
                <span>•</span>
                <a href="/privacidade" target="_blank" class="hover:text-brand transition-colors">Privacidade & LGPD</a>
            </div>
        </div>
    </div>

    <script>
        // Inicializa ícones
        lucide.createIcons();

        // Alternar entre formulários de Login e Registo
        function toggleForms(e) {
            if (e) e.preventDefault();
            const formLogin = document.getElementById('form-login');
            const formRegisto = document.getElementById('form-registo');
            const formEsqueci = document.getElementById('form-esqueci');
            const containerGoogle = document.getElementById('container-google-login');

            if (formLogin.classList.contains('hidden')) {
                formLogin.classList.remove('hidden');
                formRegisto.classList.add('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            } else {
                formLogin.classList.add('hidden');
                formRegisto.classList.remove('hidden');
                formEsqueci.classList.add('hidden');
                if (containerGoogle) containerGoogle.classList.remove('hidden');
            }
        }

        const GOOGLE_CLIENT_ID = "{{ google_client_id if google_client_id else '' }}";
        const FACEBOOK_APP_ID = "{{ facebook_app_id if facebook_app_id else '' }}";
        const MICROSOFT_CLIENT_ID = "{{ microsoft_client_id if microsoft_client_id else '' }}";

        let msalInstance = null;
        if (typeof msal !== 'undefined' && MICROSOFT_CLIENT_ID) {
            try {
                msalInstance = new msal.PublicClientApplication({
                    auth: {
                        clientId: MICROSOFT_CLIENT_ID,
                        authority: "https://login.microsoftonline.com/common",
                        redirectUri: window.location.origin + window.location.pathname
                    },
                    cache: { cacheLocation: "sessionStorage", storeAuthStateInCookie: false }
                });
                msalInstance.initialize().catch(err => console.log("MSAL Init Error:", err));
            } catch(e) {
                console.log("MSAL init info:", e);
            }
        }

        function autenticarContaSocialDireta(endpoint, email, nome, token, sub) {
            fetch(endpoint, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: email, nome: nome, access_token: token, credential: token, sub: sub })
            })
            .then(res => res.json())
            .then(data => {
                if (data.sucesso) {
                    window.location.href = data.redirect || '/app';
                } else {
                    alert(data.erro || 'Erro ao conectar com a conta social.');
                }
            })
            .catch(err => {
                window.location.href = endpoint;
            });
        }

        function iniciarGoogleLogin() {
            if (window.google && google.accounts && google.accounts.id && GOOGLE_CLIENT_ID) {
                google.accounts.id.initialize({
                    client_id: GOOGLE_CLIENT_ID,
                    callback: function(response) {
                        if (response && response.credential) {
                            autenticarContaSocialDireta('/api/auth/google', null, null, response.credential);
                        }
                    }
                });
                google.accounts.id.prompt((notification) => {
                    if (notification.isNotDisplayed() || notification.isSkippedMoment()) {
                        window.location.href = '/login/google';
                    }
                });
            } else {
                window.location.href = '/login/google';
            }
        }

        window.fbAsyncInit = function() {
            if (FACEBOOK_APP_ID && typeof FB !== 'undefined') {
                FB.init({
                    appId      : FACEBOOK_APP_ID,
                    cookie     : true,
                    xfbml      : true,
                    version    : 'v18.0'
                });
            }
        };

        function iniciarFacebookLogin() {
            if (typeof FB !== 'undefined' && FACEBOOK_APP_ID) {
                FB.login(function(response) {
                    if (response && response.authResponse) {
                        const tokenUser = response.authResponse.accessToken;
                        autenticarContaSocialDireta('/api/auth/facebook', null, null, tokenUser);
                    } else {
                        window.location.href = '/login/facebook';
                    }
                }, { scope: 'public_profile,email' });
            } else if (FACEBOOK_APP_ID) {
                const redirectUri = encodeURIComponent(window.location.origin + '/login/facebook');
                const fbAuthUrl = `https://www.facebook.com/v18.0/dialog/oauth?client_id=${FACEBOOK_APP_ID}&redirect_uri=${redirectUri}&scope=public_profile,email&response_type=token`;
                window.location.href = fbAuthUrl;
            } else {
                window.location.href = '/login/facebook';
            }
        }

        function iniciarMicrosoftLogin() {
            if (msalInstance) {
                msalInstance.loginPopup({ scopes: ["openid", "profile", "email", "User.Read"], prompt: "select_account" })
                    .then(res => {
                        if (res) {
                            const acc = res.account || {};
                            const claims = res.idTokenClaims || {};
                            const email = acc.username || claims.preferred_username || claims.email || claims.upn || null;
                            const nome = acc.name || claims.name || (email ? email.split('@')[0] : null);
                            const sub = acc.homeAccountId || claims.sub || claims.oid || null;
                            const tokenUser = res.idToken || res.accessToken;
                            autenticarContaSocialDireta('/api/auth/microsoft', email, nome, tokenUser, sub);
                        }
                    })
                    .catch(err => {
                        console.log("Erro no popup Microsoft MSAL:", err);
                        window.location.href = '/login/microsoft';
                    });
            } else {
                window.location.href = '/login/microsoft';
            }
        }

        function iniciarAppleLogin() {
            window.location.href = '/login/apple';
        }

        function mostrarEsqueciSenha(e) {
            if (e) e.preventDefault();
            document.getElementById('form-login').classList.add('hidden');
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('container-google-login').classList.add('hidden');
            document.getElementById('form-esqueci').classList.remove('hidden');
        }

        function voltarParaLogin(e) {
            if (e) e.preventDefault();
            document.getElementById('form-registo').classList.add('hidden');
            document.getElementById('form-esqueci').classList.add('hidden');
            document.getElementById('form-login').classList.remove('hidden');
        }

        // Se o usuário veio pelo botão "Começar Grátis", abrir a aba de Registo diretamente
        window.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('registo') === 'true' || urlParams.get('register') === 'true') {
                const formLogin = document.getElementById('form-login');
                const formRegisto = document.getElementById('form-registo');
                if (formLogin && formRegisto) {
                    formLogin.classList.add('hidden');
                    formRegisto.classList.remove('hidden');
                }
            }
        });

        // UX: Animação e bloqueio de botão no envio para evitar envios duplicados
        function bloquearBotao(form) {
            const btn = form.querySelector('button[type="submit"]');

            // Impede cliques duplos
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');

            // Modifica o conteúdo do botão para mostrar estado de carregamento
            btn.innerHTML = `<i data-lucide="loader" class="w-5 h-5 animate-spin"></i> <span>A processar...</span>`;

            // Re-renderiza os ícones do Lucide no botão modificado
            lucide.createIcons({ root: btn });
        }
    </script>
</body>

</html>
================================================================================
FILE: templates\minhas_acoes.html
================================================================================
<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Minhas Ações</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.2.0">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        /* ================= ANIMAÇÕES DE BOTÕES ================= */
        .action-btn {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.4);
            }

            50% {
                box-shadow: 0 0 15px 4px rgba(255, 107, 53, 0.2);
            }
        }

        /* ================= CHECKBOX CUSTOMIZADO ================= */
        .check-box {
            width: 24px;
            height: 24px;
            border: 2px solid #d4d4d8;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }

        .check-box.finalizado {
            background: #10b981;
            border-color: #10b981;
            transform: scale(1.05);
        }

        .check-box:hover:not(.finalizado) {
            border-color: #10b981;
            background: #ecfdf5;
        }

        /* ================= COMPONENTES DA LISTA ================= */
        .task-row {
            transition: all 0.2s ease;
        }

        .task-row:hover {
            background: #fafafa;
        }

        .tab-btn {
            transition: all 0.2s;
            border-bottom: 2px solid transparent;
        }

        .tab-btn.active {
            border-bottom-color: #ff6b35;
            color: #ff6b35;
            font-weight: 700;
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        /* ================= MODAL OVERLAY ================= */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            animation: fade-in 0.2s;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.2.0" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div class="md:hidden flex items-center justify-between px-5 py-3.5 bg-white/85 dark:bg-[#141517]/85 backdrop-blur-xl sticky top-0 z-30 border-b border-gray-200/70 dark:border-white/10 transition-colors">
        <!-- LOGO MOBILE OFICIAL REACTION BY FRAME [IA] (FUNDO CLARO) -->
        <a href="/app" class="flex items-center gap-2">
          <img src="/static/logo_reaction_transparent_light.png?v=0.2.0" alt="ReAction Logo" class="h-10 w-auto object-contain transition-all">
        </a>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
      </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Minhas Ações</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Controlo total sobre as tarefas e resoluções de crises.</p>
                </div>
                <button id="btn-new-action"
                    class="action-btn flex items-center justify-center gap-2 px-6 py-3 rounded-xl text-white font-heading font-semibold text-sm hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 w-full sm:w-auto">
                    <i data-lucide="plus" class="w-5 h-5"></i> Nova Ação
                </button>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-5xl mx-auto w-full">

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm flex flex-col flex-1 overflow-hidden">

                    <div class="flex items-center gap-6 px-8 border-b border-gray-100 bg-gray-50/50 pt-2">
                        <button class="tab-btn active px-2 py-4 text-sm font-semibold text-gray-500" data-filter="all">
                            Todas as Ações
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="pending">
                            Pendentes
                            <span class="bg-orange-100 text-brand px-2 py-0.5 rounded-md text-[10px] ml-1 font-bold"
                                id="badge-pending">0</span>
                        </button>
                        <button class="tab-btn px-2 py-4 text-sm font-semibold text-gray-500" data-filter="completed">
                            Concluídas
                        </button>
                    </div>

                    <div id="full-task-list" class="flex-1 overflow-y-auto divide-y divide-gray-100 p-2">
                        <div id="empty-state"
                            class="flex flex-col items-center justify-center py-20 text-gray-400 hidden">
                            <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                <i data-lucide="check-circle-2" class="w-8 h-8 text-gray-300"></i>
                            </div>
                            <p class="text-sm font-medium text-gray-500">Nenhuma ação encontrada.</p>
                            <p class="text-xs mt-1 text-gray-400">O seu backlog está limpo e organizado.</p>
                        </div>
                    </div>

                </div>

            </main>
        </div>
    </div>

        <!-- DOCK FLUTUANTE MOBILE SIMÉTRICO E CENTRALIZADO (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto max-w-[360px] w-full mx-auto grid grid-cols-4 gap-1 p-1.5 rounded-full bg-white/95 dark:bg-[#16171a]/95 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.16)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] transition-all duration-300">
      <a href="/app" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Cockpit</span>
      </a>
      <a href="/reputacao" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full bg-brand text-white shadow-sm shadow-brand/30 transition-all duration-200">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex flex-col gap-2">
      <div class="flex items-center justify-between gap-3">
        <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
          <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
          <span>Modo Escuro/Claro</span>
        </button>
        <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
          <i data-lucide="log-out" class="w-4 h-4"></i>
          <span>Sair</span>
        </a>
      </div>
      <div class="flex items-center justify-center gap-4 pt-2 text-[11px] text-gray-400 dark:text-gray-500">
        <a href="/termos" target="_blank" class="hover:text-brand">Termos de Uso</a>
        <span>•</span>
        <a href="/privacidade" target="_blank" class="hover:text-brand">Privacidade & LGPD</a>
      </div>
    </div>
  </div><button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>

                <div class="pt-6 mt-2">
                    <button type="submit" id="btn-salvar"
                        class="w-full py-3.5 rounded-xl text-white text-sm font-bold hover:brightness-110 transition-all bg-brand shadow-lg shadow-brand/20 flex items-center justify-center gap-2">
                        <i data-lucide="check" class="w-4 h-4"></i> Guardar Ação
                    </button>
                </div>
            </form>

        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // ================= ESTADO GLOBAL =================
        lucide.createIcons();
        let allActions = [];
        let currentFilter = 'all'; // 'all', 'pending', 'completed'

        // ================= FUNÇÕES UTILITÁRIAS =================
        
        function getPlatformIconHTML(plat) {
            const p = (plat || '').toLowerCase();
            if (p.includes('google')) return '<img src="/static/img/plataformas/google_business.png?v=0.2.0" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Google">';
            if (p.includes('instagram')) return '<img src="/static/img/plataformas/instagram.png?v=0.2.0" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Instagram">';
            if (p.includes('ifood')) return '<img src="/static/img/plataformas/ifood.png?v=0.2.0" class="w-4 h-4 object-contain inline-block rounded-sm" alt="iFood">';
            if (p.includes('whatsapp')) return '<img src="/static/img/plataformas/whatsapp.png?v=0.2.0" class="w-4 h-4 object-contain inline-block rounded-sm" alt="WhatsApp">';
            if (p.includes('ze') || p.includes('zé')) return '<img src="/static/img/plataformas/ze_delivery.png?v=0.2.0" class="w-4 h-4 object-contain inline-block rounded-sm" alt="Zé Delivery">';
            if (p.includes('99')) return '<img src="/static/img/plataformas/99food.png?v=0.2.0" class="w-4 h-4 object-contain inline-block rounded-sm" alt="99Food">';
            return '<i data-lucide="share-2" class="w-3 h-3"></i>';
        }
    
        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                "'": '&#39;',
                '"': '&quot;'
            }[tag] || tag));
        }

        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg font-medium';
            el.textContent = msg;
            document.getElementById('toast-container').appendChild(el);

            setTimeout(() => {
                el.style.opacity = '0';
                el.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => el.remove(), 300);
            }, 3000);
        }

        // ================= GESTÃO DAS TABS =================
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                currentFilter = e.target.dataset.filter;
                renderTasks();
            });
        });

        function updateBadges() {
            const pendingCount = allActions.filter(a => !a.completed).length;
            document.getElementById('badge-pending').textContent = pendingCount;
        }

        // ================= RENDERIZAÇÃO DA LISTA =================
        function renderTasks() {
            const container = document.getElementById('full-task-list');
            const emptyState = document.getElementById('empty-state');

            // Limpa tudo menos o empty-state
            Array.from(container.children).forEach(child => {
                if (child.id !== 'empty-state') child.remove();
            });

            // Aplica os filtros
            let filtered = allActions;
            if (currentFilter === 'pending') {
                filtered = allActions.filter(a => !a.completed);
            }
            if (currentFilter === 'completed') {
                filtered = allActions.filter(a => a.completed);
            }

            // Ordena as ações
            filtered.sort((a, b) => {
                if (a.completed !== b.completed) return a.completed ? 1 : -1;
                if (a.priority === 'critical' && b.priority !== 'critical') return -1;
                if (b.priority === 'critical' && a.priority !== 'critical') return 1;
                return 0;
            });

            // Lida com o estado vazio
            if (filtered.length === 0) {
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                filtered.forEach(action => {
                    container.appendChild(createTaskRow(action));
                });
            }
            updateBadges();
        }

        // ================= CRIAÇÃO DAS LINHAS (DOM) =================
        function createTaskRow(action) {
            const isFinished = action.completed;
            const isCrit = action.priority === 'critical';
            const dueStr = action.due_date ? new Date(action.due_date).toLocaleDateString('pt-BR', { timeZone: 'UTC', day: '2-digit', month: 'short' }) : '';

            const row = document.createElement('div');
            row.className = `task-row flex items-center gap-4 px-6 py-4 mx-2 rounded-2xl ${isFinished ? 'opacity-70' : ''}`;

            row.innerHTML = `
                <div class="check-box ${isFinished ? 'finalizado' : ''}">
                    ${isFinished ? '<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7l3 3 5-5" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>' : ''}
                </div>
                <div class="flex-1 flex flex-col">
                    <span class="text-sm font-semibold ${isFinished ? 'line-through text-gray-400' : 'text-gray-900'}">
                        ${escapeHTML(action.title)}
                    </span>
                    ${dueStr ? `<span class="text-[11px] font-medium text-gray-400 mt-0.5 flex items-center gap-1"><i data-lucide="calendar" class="w-3 h-3"></i> ${dueStr}</span>` : ''}
                </div>
                ${isCrit && !isFinished ? '<span class="text-[10px] px-2.5 py-1 rounded-md font-bold tracking-wider uppercase bg-red-50 text-red-600 border border-red-100 shrink-0">Crítica</span>' : ''}
                <button class="del-btn text-gray-300 hover:text-red-500 transition-colors p-2 rounded-lg hover:bg-red-50 shrink-0" title="Remover Tarefa">
                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                </button>
            `;

            // Ação de Toggle (Concluir/Reabrir)
            row.querySelector('.check-box').onclick = async () => {
                const checkBtn = row.querySelector('.check-box');
                checkBtn.style.opacity = '0.5';
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'PUT' });
                    if (!res.ok) throw new Error('Erro na API');

                    const index = allActions.findIndex(a => a.__backendId === action.__backendId);
                    if (index > -1) {
                        allActions[index].completed = !allActions[index].completed;
                        renderTasks();
                        toast(allActions[index].completed ? '✅ Ação concluída!' : '🔄 Ação reaberta');
                    }
                } catch (err) {
                    checkBtn.style.opacity = '1';
                    toast('Erro ao atualizar a ação no servidor.');
                }
            };

            // Ação de Excluir (Delete)
            row.querySelector('.del-btn').onclick = async () => {
                const delBtn = row.querySelector('.del-btn');
                delBtn.disabled = true;
                try {
                    const res = await fetch(`/api/acoes/${action.__backendId}`, { method: 'DELETE' });
                    if (!res.ok) throw new Error('Erro na API');

                    allActions = allActions.filter(a => a.__backendId !== action.__backendId);
                    renderTasks();
                    toast('🗑️ Ação removida com sucesso.');
                } catch (err) {
                    delBtn.disabled = false;
                    toast('Erro ao remover a ação.');
                }
            };

            lucide.createIcons({ root: row });
            return row;
        }

        // ================= GESTÃO DO MODAL DE NOVA AÇÃO =================
        const modal = document.getElementById('modal-new');
        const formNewAction = document.getElementById('form-new-action');

        document.getElementById('btn-new-action').onclick = () => {
            modal.style.display = 'flex';
            document.getElementById('inp-title').focus();
        };

        document.getElementById('btn-close-modal').onclick = () => {
            modal.style.display = 'none';
            formNewAction.reset();
        };

        modal.onclick = (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
                formNewAction.reset();
            }
        };

        formNewAction.onsubmit = async (e) => {
            e.preventDefault();
            const titleInput = document.getElementById('inp-title').value.trim();
            if (!titleInput) return;

            const btn = document.getElementById('btn-salvar');
            btn.disabled = true;
            btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i> Guardando...';
            lucide.createIcons();

            const payload = {
                title: titleInput,
                priority: document.getElementById('inp-priority').value,
                due_date: document.getElementById('inp-due').value || ''
            };

            try {
                const res = await fetch('/api/acoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!res.ok) throw new Error('Erro na API');
                const data = await res.json();

                allActions.push({
                    __backendId: data.__backendId,
                    title: payload.title,
                    completed: false,
                    priority: payload.priority,
                    due_date: payload.due_date,
                    created_at: new Date().toISOString()
                });

                modal.style.display = 'none';
                formNewAction.reset();
                renderTasks();
                toast('✨ Ação adicionada com sucesso!');

            } catch (err) {
                toast('Erro ao criar ação no servidor.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i data-lucide="check" class="w-4 h-4"></i> Guardar Ação';
                lucide.createIcons();
            }
        };

        // ================= INICIALIZAÇÃO (ON LOAD) =================
        (async () => {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    allActions = await res.json();
                }
            } catch (err) {
                console.error('Falha ao carregar ações:', err);
                toast('Falha ao conectar com o banco de dados.');
            }
            renderTasks();
            lucide.createIcons();
        })();
    </script>

{% if usuario.termos_pendentes %}
<!-- MODAL DE ACEITE OBRIGATÓRIO DE TERMOS E PRIVACIDADE (LGPD) -->
<div id="modal-termos-obrigatorio" class="fixed inset-0 z-[100] bg-black/80 backdrop-blur-md flex items-center justify-center p-4">
    <div class="bg-white dark:bg-[#141517] rounded-3xl p-6 sm:p-8 max-w-md w-full border border-gray-200 dark:border-gray-800 shadow-2xl space-y-5 text-center">
        
        <!-- LOGO OFICIAL REACTION COM ESCUDO VIBRANTE -->
        <div class="flex justify-center items-center">
            <div class="w-16 h-16 rounded-2xl bg-gradient-to-br from-[#ff6b35] to-[#e05524] p-3 shadow-lg shadow-brand/30 flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" class="w-10 h-10 text-white fill-none stroke-current" stroke-width="36" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" />
                    <path d="M200 256l40 40 80-80" />
                </svg>
            </div>
        </div>

        <div>
            <h3 class="font-heading font-bold text-xl text-gray-900 dark:text-white">Atualização dos Termos & LGPD</h3>
            <p class="text-xs sm:text-sm text-gray-600 dark:text-gray-400 mt-2 leading-relaxed">
                Atualizamos nossos <a href="/termos" target="_blank" class="text-brand font-bold underline">Termos de Uso</a> e <a href="/privacidade" target="_blank" class="text-brand font-bold underline">Política de Privacidade</a> com os dados da holding e da <strong>Frame [IA]</strong>. Para continuar utilizando o ReAction, confirme a sua concordância.
            </p>
        </div>

        <div class="p-4 rounded-2xl bg-gray-50 dark:bg-[#18191c] border border-gray-200/60 dark:border-gray-800 text-left text-xs text-gray-600 dark:text-gray-400 space-y-1.5">
            <p><strong>Holding Controladora:</strong> MJSV TECNOLOGIA E SOLUCOES DIGITAIS (I.S.)</p>
            <p><strong>Operação & IA:</strong> Frame [IA] (Divisão de Inteligência Artificial)</p>
            <p><strong>CNPJ:</strong> 68.614.850/0001-13</p>
            <p><strong>Versão:</strong> 0.2.0 (16/08/2026)</p>
        </div>

        <button type="button" id="btn-aceitar-termos" onclick="confirmarAceiteTermos()" class="w-full py-3.5 rounded-xl bg-brand text-white font-bold text-sm hover:brightness-110 shadow-lg shadow-brand/25 transition-all flex items-center justify-center gap-2">
            <span>Li e Concordo com os Termos</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M5 13l4 4L19 7"></path></svg>
        </button>
    </div>
</div>
<script>
function confirmarAceiteTermos() {
    const btn = document.getElementById('btn-aceitar-termos');
    if (btn) { btn.disabled = true; btn.innerHTML = '<span>Gravando consentimento...</span>'; }
    fetch('/api/aceitar_termos', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'}
    })
    .then(r => r.json())
    .then(d => {
        if (d.success) {
            const modal = document.getElementById('modal-termos-obrigatorio');
            if (modal) modal.remove();
        } else {
            alert('Erro ao gravar aceite: ' + (d.error || 'Tente novamente'));
            if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
        }
    })
    .catch(err => {
        console.error(err);
        alert('Erro ao conectar ao servidor.');
        if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
    });
}
</script>
{% endif %}

</body>

</html>
================================================================================
FILE: templates\privacidade.html
================================================================================
<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Política de Privacidade & LGPD | ReAction by Frame [IA]</title>
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.2.0">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a'
                    }
                }
            }
        }
    </script>
    <style>
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #d4d4d8 !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="min-h-full font-body text-gray-800 bg-gray-50 antialiased flex flex-col">
    <!-- Top Header -->
    <header class="bg-white border-b border-gray-200 sticky top-0 z-30 shadow-sm transition-colors">
        <div class="max-w-5xl mx-auto px-4 sm:px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_light.png?v=0.2.0" alt="ReAction Logo" class="h-10 w-auto object-contain dark:hidden">
                <img src="/static/logo_reaction_transparent_dark.png?v=0.2.0" alt="ReAction Logo" class="h-10 w-auto object-contain hidden dark:block">
            </a>
            <div class="flex items-center gap-3">
                <a href="/login" class="px-4 py-2 text-xs sm:text-sm font-semibold rounded-xl bg-gray-100 hover:bg-gray-200 dark:bg-[#1f2024] dark:hover:bg-gray-700 text-gray-700 dark:text-gray-300 transition-colors">
                    Entrar no App
                </a>
                <a href="/" class="px-4 py-2 text-xs sm:text-sm font-semibold rounded-xl bg-brand text-white hover:brightness-110 shadow-sm shadow-brand/20 transition-all flex items-center gap-1.5">
                    <i data-lucide="arrow-left" class="w-4 h-4"></i> Voltar ao Início
                </a>
            </div>
        </div>
    </header>

    <!-- Main Privacy Content -->
    <main class="flex-1 max-w-4xl w-full mx-auto px-4 sm:px-6 py-10 sm:py-14">
        <div class="bg-white rounded-3xl p-6 sm:p-10 border border-gray-200/80 shadow-sm space-y-8">
            <div class="border-b border-gray-100 dark:border-gray-800 pb-6">
                <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-emerald-50 dark:bg-emerald-500/10 text-emerald-600 text-xs font-bold uppercase tracking-wider mb-3">
                    <i data-lucide="lock" class="w-4 h-4"></i> Conformidade LGPD
                </div>
                <h1 class="font-heading font-bold text-2xl sm:text-3xl text-gray-900 tracking-tight">Política de Privacidade e Proteção de Dados</h1>
                <p class="text-sm text-gray-500 dark:text-gray-400 mt-2">Versão oficial 0.2.0 • Em conformidade com a Lei Federal nº 13.709/2018 (LGPD)</p>
            </div>

                        <!-- Dados da Controladora e Frame [IA] -->
            <div class="p-5 rounded-2xl bg-gray-50 dark:bg-[#18191c] border border-gray-200/60 dark:border-gray-800 space-y-3">
                <div class="flex items-center justify-between">
                    <h3 class="font-heading font-bold text-sm text-gray-900 dark:text-white uppercase tracking-wider">Identificação da Controladora & Operação de IA</h3>
                    <span class="text-[11px] font-bold px-2.5 py-0.5 rounded-full bg-emerald-500/10 text-emerald-500">LGPD / DPO</span>
                </div>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 text-xs text-gray-600 dark:text-gray-400">
                    <p><strong>Holding Controladora:</strong> MJSV TECNOLOGIA E SOLUCOES DIGITAIS (I.S.)</p>
                    <p><strong>Operação & IA:</strong> Frame [IA]</p>
                    <p><strong>CNPJ:</strong> 68.614.850/0001-13</p>
                    <p><strong>Encarregado pelo Tratamento (DPO):</strong> Jadson Sousa Vieira</p>
                    <p><strong>Sede:</strong> Eusébio - CE, Brasil (Região Metropolitana de Fortaleza)</p>
                    <p><strong>E-mail de Privacidade / DPO:</strong> contato@frameia.com.br / jadson@mjsv.com.br</p>
                    <p><strong>Telefone Comercial:</strong> (85) 9662-4278 / (85) 8731-8995</p>
                    <p><strong>Plataforma:</strong> ReAction by Frame [IA]</p>
                </div>
                <p class="text-[11px] text-gray-500 dark:text-gray-400 border-t border-gray-200/60 dark:border-gray-800 pt-2 leading-relaxed">
                    <em>Transparência LGPD:</em> A <strong>MJSV TECNOLOGIA</strong> figura como controladora legal dos dados, enquanto a divisão <strong>Frame [IA]</strong> executa os serviços de tecnologia e tratamento automatizado seguro de dados para a plataforma <strong>ReAction</strong>.
                </p>
            </div>

            <!-- Seções da Política -->
            <section class="space-y-4 text-sm leading-relaxed text-gray-700 dark:text-gray-300">
                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">1. Princípios e Compromisso de Privacidade</h2>
                <p>
                    A <strong>MJSV TECNOLOGIA E SOLUCOES DIGITAIS INOVA SIMPLES (I.S.)</strong> e a sua divisão de inovação <strong>Frame [IA]</strong> valorizam a segurança, confidencialidade e privacidade de seus usuários e clientes. Esta Política explica de forma transparente como os dados pessoais são coletados, utilizados, armazenados, protegidos e compartilhados ao utilizar a plataforma <strong>ReAction</strong>.
                </p>

                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">2. Dados Coletados</h2>
                <p>Coletamos exclusivamente os dados estritamente necessários para a prestação dos serviços do SaaS:</p>
                <ul class="list-disc pl-5 space-y-1.5 text-xs sm:text-sm">
                    <li><strong>Dados de Cadastro e Acesso:</strong> Nome completo, endereço de e-mail corporativo, senha criptografada (hash seguro), foto de perfil e telefone WhatsApp para alertas operacionais.</li>
                    <li><strong>Dados das Avaliações e Canais:</strong> Nomes públicos de autores de reviews, notas atribuídas e comentários públicos postados nas plataformas conectadas (Google Meu Negócio, Instagram, iFood, 99Food, WhatsApp e Zé Delivery).</li>
                    <li><strong>Logs e Auditoria de Segurança:</strong> Endereço IP de acesso, registro de data/hora do aceite dos termos e logs técnicos de requisições.</li>
                </ul>

                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">3. Finalidade do Tratamento de Dados</h2>
                <p>Os dados tratados possuem as seguintes finalidades legítimas:</p>
                <ul class="list-disc pl-5 space-y-1.5 text-xs sm:text-sm">
                    <li>Execução do contrato SaaS e disponibilização do painel Cockpit e Central de Reputação.</li>
                    <li>Processamento inteligente de sentimentos e sugestões de respostas via Inteligência Artificial.</li>
                    <li>Envio de alertas de crise imediatos via WhatsApp e e-mail quando avaliações negativas de alto impacto são detectadas.</li>
                    <li>Cumprimento de obrigações legais, regulatórias e proteção aos direitos da empresa.</li>
                </ul>

                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">4. Armazenamento, Segurança e Criptografia</h2>
                <p>
                    Todos os dados trafegam exclusivamente sob protocolo seguro criptografado SSL/TLS (HTTPS). As senhas são armazenadas com algoritmos de derivação de chave de alta entropia. O acesso ao banco de dados MySQL é estritamente isolado por inquilino (*Multi-Tenant Architecture*), impedindo qualquer cruzamento ou vazamento de informações entre empresas clientes.
                </p>

                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">5. Compartilhamento de Dados</h2>
                <p>
                    A MJSV Tecnologia <strong>não comercializa, não aluga e não transfere</strong> seus dados pessoais para terceiros para fins de marketing. O compartilhamento ocorre exclusivamente com provedores de infraestrutura estritamente necessários:
                </p>
                <ul class="list-disc pl-5 space-y-1.5 text-xs sm:text-sm">
                    <li>Servidores em nuvem de alta confiabilidade (Hostinger VPS / Certificados SSL).</li>
                    <li>Provedores de pagamento (Mercado Pago) para liquidação de transações.</li>
                    <li>Provedores oficiais de mensageria (Meta Cloud API / WhatsApp) para entrega de notificações solicitadas.</li>
                </ul>

                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">6. Direitos do Titular (Art. 18 da LGPD)</h2>
                <p>
                    Como titular dos dados, você pode a qualquer momento exercer seus direitos garantidos pela LGPD, incluindo: confirmação da existência de tratamento, acesso aos dados, correção de dados incompletos ou inexatos, anonimização ou eliminação de dados desnecessários, e revogação do consentimento.
                </p>
                <p>
                    Para exercer seus direitos, basta enviar uma mensagem direta ao nosso canal de DPO no e-mail <strong>contato@frameia.com.br</strong> ou <strong>jadson@mjsv.com.br</strong>.
                </p>

                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">7. Atualizações desta Política</h2>
                <p>
                    Esta Política pode ser atualizada periodicamente para refletir melhorias no sistema ou exigências legais. Quaisquer alterações relevantes serão notificadas através do painel do ReAction.
                </p>
            </section>
        </div>
    </main>

    <footer class="bg-white dark:bg-[#0c0d0f] border-t border-gray-200 dark:border-gray-800 py-6 text-center text-xs text-gray-500 transition-colors">
        <div class="max-w-5xl mx-auto px-4 flex flex-col sm:flex-row items-center justify-between gap-3">
            <p>© 2026 MJSV TECNOLOGIA E SOLUCOES DIGITAIS INOVA SIMPLES (I.S.) • CNPJ: 68.614.850/0001-13</p>
            <div class="flex items-center gap-4">
                <a href="/termos" class="hover:text-brand">Termos de Uso</a>
                <a href="/privacidade" class="hover:text-brand font-bold text-brand">Privacidade & LGPD</a>
            </div>
        </div>
    </footer>

    <script>
        lucide.createIcons();
    </script>
</body>
</html>

================================================================================
FILE: templates\relatorios.html
================================================================================
<!doctype html>
<html lang="pt-PT" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Relatórios Executivos</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.2.0">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>

    <style>
        /* ================= RESET & ESTRUTURA BASE ================= */
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* ================= SCROLLBAR MINIMALISTA ================= */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* ================= NAVEGAÇÃO & MENUS ================= */
        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover:not(.active) {
            background: rgba(255, 107, 53, 0.05);
            color: #d1d5db;
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
            background: rgba(255, 107, 53, 0.1);
        }

        /* ================= CARTÕES DE KPI ================= */
        .kpi-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .kpi-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.1);
        }

        /* ================= TOASTS (NOTIFICAÇÕES) ================= */
        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s ease-out;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 16px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }

        /* ================= ESTILOS DE IMPRESSÃO (EXPORTAR PDF) ================= */
        @media print {
            @page {
                size: A4 portrait;
                margin: 1.2cm;
            }

            #sidebar,
            .md\:hidden,
            .no-print,
            header button,
            header a {
                display: none !important;
            }

            html, body, .app-shell, #main-area, .main-scroll {
                height: auto !important;
                max-height: none !important;
                overflow: visible !important;
                background-color: #ffffff !important;
                color: #000000 !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            .kpi-card,
            .bg-white,
            section,
            canvas {
                box-shadow: none !important;
                border: 1px solid #e4e4e7 !important;
                break-inside: avoid !important;
                page-break-inside: avoid !important;
            }

            header {
                padding-top: 0 !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar"
            class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl no-print">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.2.0" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div class="md:hidden flex items-center justify-between px-5 py-3.5 bg-white/85 dark:bg-[#141517]/85 backdrop-blur-xl sticky top-0 z-30 border-b border-gray-200/70 dark:border-white/10 transition-colors">
        <!-- LOGO MOBILE OFICIAL REACTION BY FRAME [IA] (FUNDO CLARO) -->
        <a href="/app" class="flex items-center gap-2">
          <img src="/static/logo_reaction_transparent_light.png?v=0.2.0" alt="ReAction Logo" class="h-10 w-auto object-contain transition-all">
        </a>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
      </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Relatório Executivo</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Análise de sentimento e performance da marca nos
                        últimos 30 dias.</p>
                </div>
                <div class="flex gap-3 no-print">
                    <a href="/api/exportar/relatorio_excel"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-heading font-bold text-sm transition-all shadow-md shadow-emerald-600/20 hover:-translate-y-0.5 w-full sm:w-auto">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> Exportar Relatório Excel
                    </a>
                    <button onclick="window.print()"
                        class="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl text-gray-700 font-heading font-semibold text-sm hover:bg-white transition-all bg-white/50 border border-gray-200 shadow-sm w-full sm:w-auto">
                        <i data-lucide="printer" class="w-4 h-4"></i> Exportar PDF
                    </button>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-6xl mx-auto w-full space-y-6">

                <section class="grid grid-cols-1 sm:grid-cols-3 gap-5">
                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Nota Média</span>
                            <span
                                class="text-xs font-bold text-emerald-600 bg-emerald-50 px-2 py-1 rounded-md">+0.4</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800">4.7</div>
                            <div class="text-amber-400 mb-1 flex"><i data-lucide="star"
                                    class="w-5 h-5 fill-current"></i></div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Crises
                                Evitadas</span>
                            <span class="text-xs font-bold text-brand bg-orange-50 px-2 py-1 rounded-md">Via IA</span>
                        </div>
                        <div class="flex items-end gap-2">
                            <div class="font-heading font-extrabold text-4xl text-gray-800" id="kpi-crises">0</div>
                            <div class="text-sm font-medium text-gray-400 mb-1.5">resolvidas</div>
                        </div>
                    </div>

                    <div
                        class="kpi-card bg-white rounded-3xl p-6 border border-gray-100 shadow-sm flex flex-col justify-between h-32">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Sentimento
                                Positivo</span>
                        </div>
                        <div class="flex items-center gap-4 mt-auto">
                            <div class="flex-1 bg-gray-100 rounded-full h-2.5 overflow-hidden">
                                <div class="bg-emerald-500 h-full rounded-full" style="width: 78%"></div>
                            </div>
                            <div class="font-heading font-extrabold text-2xl text-emerald-600">78%</div>
                        </div>
                    </div>
                </section>

                <section class="bg-white p-6 md:p-8 rounded-3xl border border-gray-100 shadow-sm w-full">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="font-heading font-bold text-base text-gray-900 flex items-center gap-2">
                            <i data-lucide="trending-up" class="w-5 h-5 text-brand"></i> Evolução da Reputação
                        </h3>
                        <select
                            class="text-xs font-semibold text-gray-500 bg-gray-50 border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none no-print">
                            <option>Últimos 7 Dias</option>
                            <option>Últimos 30 Dias</option>
                        </select>
                    </div>
                    <div class="w-full h-[280px]">
                        <canvas id="chart-evolucao"></canvas>
                    </div>
                </section>

            </main>
        </div>
    </div>

        <!-- DOCK FLUTUANTE MOBILE SIMÉTRICO E CENTRALIZADO (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto max-w-[360px] w-full mx-auto grid grid-cols-4 gap-1 p-1.5 rounded-full bg-white/95 dark:bg-[#16171a]/95 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.16)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] transition-all duration-300">
      <a href="/app" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Cockpit</span>
      </a>
      <a href="/reputacao" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full bg-brand text-white shadow-sm shadow-brand/30 transition-all duration-200">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex flex-col gap-2">
      <div class="flex items-center justify-between gap-3">
        <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
          <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
          <span>Modo Escuro/Claro</span>
        </button>
        <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
          <i data-lucide="log-out" class="w-4 h-4"></i>
          <span>Sair</span>
        </a>
      </div>
      <div class="flex items-center justify-center gap-4 pt-2 text-[11px] text-gray-400 dark:text-gray-500">
        <a href="/termos" target="_blank" class="hover:text-brand">Termos de Uso</a>
        <span>•</span>
        <a href="/privacidade" target="_blank" class="hover:text-brand">Privacidade & LGPD</a>
      </div>
    </div>
  </div><button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>

    <script>
        lucide.createIcons();

        // ================= GRÁFICO DE EVOLUÇÃO (CHART.JS) =================
        function renderEvolucaoChart() {
            const ctx = document.getElementById('chart-evolucao').getContext('2d');

            // Dados Mockados para o Pitch Comercial (Mostrando crescimento)
            const labels = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'];
            const dataElogios = [2, 3, 5, 4, 7, 8, 10];
            const dataReclamacoes = [4, 3, 2, 2, 1, 1, 0];

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Avaliações Positivas',
                            data: dataElogios,
                            borderColor: '#10b981', // Emerald 500
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#10b981',
                            pointBorderWidth: 2,
                            pointRadius: 4
                        },
                        {
                            label: 'Críticas / Reclamações',
                            data: dataReclamacoes,
                            borderColor: '#ef4444', // Red 500
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            borderDash: [5, 5],
                            tension: 0.4,
                            pointBackgroundColor: '#ffffff',
                            pointBorderColor: '#ef4444',
                            pointBorderWidth: 2,
                            pointRadius: 3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    plugins: {
                        legend: {
                            position: 'top',
                            align: 'end',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 8,
                                font: { family: 'DM Sans', size: 12 }
                            }
                        },
                        tooltip: {
                            backgroundColor: '#27272a',
                            titleFont: { family: 'Poppins' },
                            bodyFont: { family: 'DM Sans' },
                            padding: 12,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false, drawBorder: false },
                            ticks: { font: { family: 'DM Sans' }, color: '#a1a1aa' }
                        },
                        y: {
                            grid: { color: '#f4f4f5', drawBorder: false },
                            ticks: { stepSize: 2, font: { family: 'DM Sans' }, color: '#a1a1aa' },
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // ================= CONSUMO DA API PARA KPI DE CRISES =================
        // Puxa o número real de crises resolvidas (ações concluídas com prioridade crítica)
        async function loadRealData() {
            try {
                const res = await fetch('/api/acoes');
                if (res.ok) {
                    const acoes = await res.json();
                    const crisesResolvidas = acoes.filter(a => a.completed && a.priority === 'critical').length;
                    // Fallback para 12 no pitch se o DB estiver vazio
                    document.getElementById('kpi-crises').textContent = crisesResolvidas || 12;
                }
            } catch (e) {
                console.error("Erro ao puxar dados da API", e);
                document.getElementById('kpi-crises').textContent = 12;
            }
        }

        // Inicializar
        renderEvolucaoChart();
        loadRealData();
    </script>

{% if usuario.termos_pendentes %}
<!-- MODAL DE ACEITE OBRIGATÓRIO DE TERMOS E PRIVACIDADE (LGPD) -->
<div id="modal-termos-obrigatorio" class="fixed inset-0 z-[100] bg-black/80 backdrop-blur-md flex items-center justify-center p-4">
    <div class="bg-white dark:bg-[#141517] rounded-3xl p-6 sm:p-8 max-w-md w-full border border-gray-200 dark:border-gray-800 shadow-2xl space-y-5 text-center">
        
        <!-- LOGO OFICIAL REACTION COM ESCUDO VIBRANTE -->
        <div class="flex justify-center items-center">
            <div class="w-16 h-16 rounded-2xl bg-gradient-to-br from-[#ff6b35] to-[#e05524] p-3 shadow-lg shadow-brand/30 flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" class="w-10 h-10 text-white fill-none stroke-current" stroke-width="36" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" />
                    <path d="M200 256l40 40 80-80" />
                </svg>
            </div>
        </div>

        <div>
            <h3 class="font-heading font-bold text-xl text-gray-900 dark:text-white">Atualização dos Termos & LGPD</h3>
            <p class="text-xs sm:text-sm text-gray-600 dark:text-gray-400 mt-2 leading-relaxed">
                Atualizamos nossos <a href="/termos" target="_blank" class="text-brand font-bold underline">Termos de Uso</a> e <a href="/privacidade" target="_blank" class="text-brand font-bold underline">Política de Privacidade</a> com os dados da holding e da <strong>Frame [IA]</strong>. Para continuar utilizando o ReAction, confirme a sua concordância.
            </p>
        </div>

        <div class="p-4 rounded-2xl bg-gray-50 dark:bg-[#18191c] border border-gray-200/60 dark:border-gray-800 text-left text-xs text-gray-600 dark:text-gray-400 space-y-1.5">
            <p><strong>Holding Controladora:</strong> MJSV TECNOLOGIA E SOLUCOES DIGITAIS (I.S.)</p>
            <p><strong>Operação & IA:</strong> Frame [IA] (Divisão de Inteligência Artificial)</p>
            <p><strong>CNPJ:</strong> 68.614.850/0001-13</p>
            <p><strong>Versão:</strong> 0.2.0 (16/08/2026)</p>
        </div>

        <button type="button" id="btn-aceitar-termos" onclick="confirmarAceiteTermos()" class="w-full py-3.5 rounded-xl bg-brand text-white font-bold text-sm hover:brightness-110 shadow-lg shadow-brand/25 transition-all flex items-center justify-center gap-2">
            <span>Li e Concordo com os Termos</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M5 13l4 4L19 7"></path></svg>
        </button>
    </div>
</div>
<script>
function confirmarAceiteTermos() {
    const btn = document.getElementById('btn-aceitar-termos');
    if (btn) { btn.disabled = true; btn.innerHTML = '<span>Gravando consentimento...</span>'; }
    fetch('/api/aceitar_termos', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'}
    })
    .then(r => r.json())
    .then(d => {
        if (d.success) {
            const modal = document.getElementById('modal-termos-obrigatorio');
            if (modal) modal.remove();
        } else {
            alert('Erro ao gravar aceite: ' + (d.error || 'Tente novamente'));
            if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
        }
    })
    .catch(err => {
        console.error(err);
        alert('Erro ao conectar ao servidor.');
        if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
    });
}
</script>
{% endif %}

</body>

</html>
================================================================================
FILE: templates\reputacao.html
================================================================================
<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Central de Reputação</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.2.0">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-gray-50, html.dark .bg-gray-50\/80 {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark .border-gray-100, html.dark .border-gray-200, html.dark .border-gray-200\/60 {
            border-color: #27272a !important;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        /* Ocultar scrollbar nas colunas do Kanban para ficar mais limpo */
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }

        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .review-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }

        .review-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px -8px rgba(0, 0, 0, 0.08);
        }

        .pulse-alert {
            animation: pulse-red 2s infinite;
        }

        @keyframes pulse-red {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.2);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(220, 38, 38, 0);
            }
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.2.0" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex-1 flex flex-col h-screen overflow-y-auto relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div class="md:hidden flex items-center justify-between px-5 py-3.5 bg-white/85 dark:bg-[#141517]/85 backdrop-blur-xl sticky top-0 z-30 border-b border-gray-200/70 dark:border-white/10 transition-colors">
        <!-- LOGO MOBILE OFICIAL REACTION BY FRAME [IA] (FUNDO CLARO) -->
        <a href="/app" class="flex items-center gap-2">
          <img src="/static/logo_reaction_transparent_light.png?v=0.2.0" alt="ReAction Logo" class="h-10 w-auto object-contain transition-all">
        </a>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
      </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Central de Reputação</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Gestão inteligente e contenção de crises em tempo real.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col">
                <div class="flex-1 grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-sm shadow-emerald-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Respondido pela IA</h2>
                            <span
                                class="ml-auto text-[10px] px-2.5 py-1 rounded-md bg-emerald-100 text-emerald-700 font-bold tracking-wider uppercase border border-emerald-200/50">Automático</span>
                        </div>
                        <div id="col-respondidos" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-brand shadow-sm shadow-brand/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Dúvidas (Aprovação)</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-orange-100 text-brand border border-orange-200/50 transition-colors"
                                id="count-pendentes">0</span>
                        </div>
                        <div id="col-pendentes" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                    <div class="flex flex-col h-full bg-gray-50/80 rounded-3xl p-4 border border-gray-200/60 shadow-sm">
                        <div class="flex items-center gap-2 mb-5 px-2">
                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-sm shadow-red-500/50"></div>
                            <h2 class="font-heading font-bold text-sm text-gray-900">Alerta Vermelho</h2>
                            <span
                                class="ml-auto text-xs font-bold px-2.5 py-0.5 rounded-md bg-red-100 text-red-700 border border-red-200/50 transition-colors"
                                id="count-alertas">0</span>
                        </div>
                        <div id="col-alertas" class="flex-1 overflow-y-auto space-y-4 pb-4 pr-1 scrollbar-hide">
                        </div>
                    </div>

                </div>
            </main>
        </div>
    </div>

        <!-- DOCK FLUTUANTE MOBILE SIMÉTRICO E CENTRALIZADO (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto max-w-[360px] w-full mx-auto grid grid-cols-4 gap-1 p-1.5 rounded-full bg-white/95 dark:bg-[#16171a]/95 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.16)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] transition-all duration-300">
      <a href="/app" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Cockpit</span>
      </a>
      <a href="/reputacao" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full bg-brand text-white shadow-sm shadow-brand/30 transition-all duration-200">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex flex-col gap-2">
      <div class="flex items-center justify-between gap-3">
        <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
          <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
          <span>Modo Escuro/Claro</span>
        </button>
        <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
          <i data-lucide="log-out" class="w-4 h-4"></i>
          <span>Sair</span>
        </a>
      </div>
      <div class="flex items-center justify-center gap-4 pt-2 text-[11px] text-gray-400 dark:text-gray-500">
        <a href="/termos" target="_blank" class="hover:text-brand">Termos de Uso</a>
        <span>•</span>
        <a href="/privacidade" target="_blank" class="hover:text-brand">Privacidade & LGPD</a>
      </div>
    </div>
  </div><button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md border border-emerald-200/50 bg-emerald-50 text-emerald-700 flex items-center gap-1">
                        <i data-lucide="check-circle" class="w-3 h-3"></i> Respondido
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-600 mb-4 line-clamp-3 leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="bot" class="w-3 h-3"></i> Resposta da IA:
                      </div>
                      <p class="text-[13px] text-gray-500 leading-relaxed italic bg-gray-50/80 rounded-lg p-3 border border-gray-100">"${c_rascunho}"</p>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'pendente') {
                return `
                  <div class="review-card bg-white rounded-2xl p-5 border border-brand/20 shadow-sm relative overflow-hidden hover:border-brand/40">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-brand/5 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-orange-50 text-brand border border-orange-200/50 flex items-center gap-1">
                        <i data-lucide="clock" class="w-3 h-3"></i> Pendente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-800 mb-4 font-medium leading-relaxed">"${c_comentario}"</p>
                    <div class="pt-3 border-t border-gray-50">
                      <div class="text-[10px] font-bold mb-1.5 text-gray-400 uppercase tracking-wider flex items-center gap-1">
                        <i data-lucide="pen-tool" class="w-3 h-3"></i> Rascunho da IA:
                      </div>
                      <p class="text-[13px] text-gray-700 leading-relaxed mb-4 p-3 bg-orange-50/30 rounded-lg border border-brand/10">"${c_rascunho}"</p>
                      
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                        <i data-lucide="check" class="w-4 h-4"></i> Aprovar Rascunho
                      </button>
                    </div>
                  </div>
                `;
            }

            if (av.status === 'alerta_crise') {
                return `
                  <div class="review-card pulse-alert bg-white rounded-2xl p-5 border border-red-300 shadow-sm relative overflow-hidden hover:border-red-400">
                    <div class="absolute top-0 right-0 w-16 h-16 bg-red-50 rounded-bl-full -z-10"></div>
                    <div class="flex items-center justify-between mb-3">
                      <div>
                        <span class="text-sm font-bold text-gray-900">${c_cliente}</span>
                        <div class="text-[11px] font-medium text-gray-400 flex items-center gap-1">
                            ${getPlatformIconHTML(c_plataforma)} ${c_plataforma} · ${c_tempo}
                        </div>
                      </div>
                      <span class="text-[10px] font-bold px-2.5 py-1 rounded-md bg-red-100 text-red-700 flex items-center gap-1 border border-red-200/50">
                        <i data-lucide="alert-triangle" class="w-3 h-3"></i> Urgente
                      </span>
                    </div>
                    <div class="flex gap-0.5 mb-2">${renderStars(av.nota)}</div>
                    <p class="text-sm text-gray-900 mb-3 font-semibold leading-relaxed">"${c_comentario}"</p>
                    
                    <div class="pt-3 border-t border-red-100 bg-red-50/50 -mx-5 -mb-5 px-5 pb-5 rounded-b-2xl mt-4">
                      <div class="flex items-center gap-1.5 mb-2 pt-3">
                        <i data-lucide="shield-alert" class="w-4 h-4 text-red-600"></i>
                        <span class="text-xs font-bold text-red-700">Ação imediata necessária</span>
                      </div>
                      <div class="text-[11px] text-red-600/80 mb-4 font-medium">Sugestão de Contenção (Ponte Privada):<br>
                        <div class="text-[13px] text-gray-800 font-medium mt-1.5 p-3 bg-white rounded-lg border border-red-100 shadow-sm">"${c_rascunho}"</div>
                      </div>
                      <button onclick="aprovarAvaliacao(${av.id}, this)" class="w-full py-3 rounded-xl text-white text-xs font-bold flex items-center justify-center gap-2 bg-red-600 hover:bg-red-700 active:scale-[0.98] transition-all shadow-md shadow-red-600/20">
                        <i data-lucide="message-circle" class="w-4 h-4"></i> Publicar Contenção
                      </button>
                    </div>
                  </div>
                `;
            }
        }

        async function aprovarAvaliacao(id, btnElement) {
            const originalHTML = btnElement.innerHTML;
            btnElement.disabled = true;
            btnElement.classList.add('opacity-70', 'cursor-not-allowed');
            btnElement.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Processando...`;
            lucide.createIcons();

            try {
                const res = await fetch(`/api/avaliacoes/${id}/aprovar`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Resposta publicada com sucesso!');
                    initKanban();
                } else {
                    throw new Error('Falha na resposta do servidor');
                }
            } catch (err) {
                console.error(err);
                toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao aprovar a resposta.');
                btnElement.disabled = false;
                btnElement.classList.remove('opacity-70', 'cursor-not-allowed');
                btnElement.innerHTML = originalHTML;
                lucide.createIcons();
            }
        }

        async function initKanban() {
            try {
                const response = await fetch('/api/avaliacoes');
                if (!response.ok) throw new Error('Erro ao buscar dados da API');
                const data = await response.json();

                const colRes = document.getElementById('col-respondidos');
                const colPen = document.getElementById('col-pendentes');
                const colAle = document.getElementById('col-alertas');

                colRes.innerHTML = '';
                colPen.innerHTML = '';
                colAle.innerHTML = '';

                let cRes = 0, cPen = 0, cAle = 0;

                data.forEach(av => {
                    const html = createCardHTML(av);
                    if (av.status === 'respondido_ia') { colRes.insertAdjacentHTML('beforeend', html); cRes++; }
                    if (av.status === 'pendente') { colPen.insertAdjacentHTML('beforeend', html); cPen++; }
                    if (av.status === 'alerta_crise') { colAle.insertAdjacentHTML('beforeend', html); cAle++; }
                });

                // Tratamento de Empty States (Se a coluna estiver vazia)
                if (cRes === 0) colRes.innerHTML = renderEmptyState("Nenhuma resposta automática recente.", "check-circle");
                if (cPen === 0) colPen.innerHTML = renderEmptyState("Tudo limpo! Nenhuma aprovação pendente.", "inbox");
                if (cAle === 0) colAle.innerHTML = renderEmptyState("Excelente! Nenhum alerta de crise no radar.", "shield-check");

                // Atualizar Contadores
                document.getElementById('count-pendentes').innerText = cPen;
                document.getElementById('count-alertas').innerText = cAle;

                // Animação leve nos contadores se houver mudanças
                ['count-pendentes', 'count-alertas'].forEach(id => {
                    const el = document.getElementById(id);
                    el.classList.add('scale-110');
                    setTimeout(() => el.classList.remove('scale-110'), 200);
                });

                lucide.createIcons();
            } catch (error) {
                console.error("Falha ao carregar o Kanban:", error);
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha ao sincronizar avaliações.');
            }
        }

        // Inicializar
        document.addEventListener('DOMContentLoaded', initKanban);
    </script>

{% if usuario.termos_pendentes %}
<!-- MODAL DE ACEITE OBRIGATÓRIO DE TERMOS E PRIVACIDADE (LGPD) -->
<div id="modal-termos-obrigatorio" class="fixed inset-0 z-[100] bg-black/80 backdrop-blur-md flex items-center justify-center p-4">
    <div class="bg-white dark:bg-[#141517] rounded-3xl p-6 sm:p-8 max-w-md w-full border border-gray-200 dark:border-gray-800 shadow-2xl space-y-5 text-center">
        
        <!-- LOGO OFICIAL REACTION COM ESCUDO VIBRANTE -->
        <div class="flex justify-center items-center">
            <div class="w-16 h-16 rounded-2xl bg-gradient-to-br from-[#ff6b35] to-[#e05524] p-3 shadow-lg shadow-brand/30 flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" class="w-10 h-10 text-white fill-none stroke-current" stroke-width="36" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" />
                    <path d="M200 256l40 40 80-80" />
                </svg>
            </div>
        </div>

        <div>
            <h3 class="font-heading font-bold text-xl text-gray-900 dark:text-white">Atualização dos Termos & LGPD</h3>
            <p class="text-xs sm:text-sm text-gray-600 dark:text-gray-400 mt-2 leading-relaxed">
                Atualizamos nossos <a href="/termos" target="_blank" class="text-brand font-bold underline">Termos de Uso</a> e <a href="/privacidade" target="_blank" class="text-brand font-bold underline">Política de Privacidade</a> com os dados da holding e da <strong>Frame [IA]</strong>. Para continuar utilizando o ReAction, confirme a sua concordância.
            </p>
        </div>

        <div class="p-4 rounded-2xl bg-gray-50 dark:bg-[#18191c] border border-gray-200/60 dark:border-gray-800 text-left text-xs text-gray-600 dark:text-gray-400 space-y-1.5">
            <p><strong>Holding Controladora:</strong> MJSV TECNOLOGIA E SOLUCOES DIGITAIS (I.S.)</p>
            <p><strong>Operação & IA:</strong> Frame [IA] (Divisão de Inteligência Artificial)</p>
            <p><strong>CNPJ:</strong> 68.614.850/0001-13</p>
            <p><strong>Versão:</strong> 0.2.0 (16/08/2026)</p>
        </div>

        <button type="button" id="btn-aceitar-termos" onclick="confirmarAceiteTermos()" class="w-full py-3.5 rounded-xl bg-brand text-white font-bold text-sm hover:brightness-110 shadow-lg shadow-brand/25 transition-all flex items-center justify-center gap-2">
            <span>Li e Concordo com os Termos</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M5 13l4 4L19 7"></path></svg>
        </button>
    </div>
</div>
<script>
function confirmarAceiteTermos() {
    const btn = document.getElementById('btn-aceitar-termos');
    if (btn) { btn.disabled = true; btn.innerHTML = '<span>Gravando consentimento...</span>'; }
    fetch('/api/aceitar_termos', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'}
    })
    .then(r => r.json())
    .then(d => {
        if (d.success) {
            const modal = document.getElementById('modal-termos-obrigatorio');
            if (modal) modal.remove();
        } else {
            alert('Erro ao gravar aceite: ' + (d.error || 'Tente novamente'));
            if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
        }
    })
    .catch(err => {
        console.error(err);
        alert('Erro ao conectar ao servidor.');
        if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
    });
}
</script>
{% endif %}

</body>

</html>
================================================================================
FILE: templates\sala_maquinas.html
================================================================================
<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();

        function toggleTheme() {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('reactionTheme', isDark ? 'dark' : 'light');
        }

        window.addEventListener('storage', function(e) {
            if (e.key === 'reactionTheme') {
                if (e.newValue === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
            }
        });
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ReAction by Frame [IA] | Sala de Máquinas</title>
                    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.2.0">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <link rel="manifest" href="/static/manifest.json?v=0.2.0">
    <meta name="theme-color" content="#ff6b35">
    <meta name="application-name" content="ReAction">
    <meta name="apple-mobile-web-app-title" content="ReAction">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a',
                        bglight: '#f4f4f5'
                    }
                }
            }
        } 
    </script>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            box-sizing: border-box;
            background-color: #f4f4f5;
        }

        .app-shell {
            height: 100%;
            display: flex;
            overflow: hidden;
        }

        .main-scroll {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        /* Scrollbar minimalista */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: #d4d4d8;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1aa;
        }

        .nav-item {
            transition: all 0.2s ease;
        }

        .nav-item:hover,
        .nav-item.active {
            background: rgba(255, 107, 53, 0.1);
        }

        .nav-item.active {
            border-left: 3px solid #ff6b35;
            color: white;
        }

        .toast-msg {
            position: fixed;
            bottom: 90px;
            left: 50%;
            transform: translateX(-50%);
            background: #27272a;
            color: #fff;
            padding: 12px 24px;
            border-radius: 99px;
            font-size: 14px;
            font-weight: 500;
            z-index: 1000;
            animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        @keyframes slide-up {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>

    


    
    


    <style>
        /* TEMA ESCURO (DARK MODE OBSIDIAN PREMIUM OPCIONAL MULTI-TAB) */
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark [class*="bg-gray-50"], 
        html.dark [class*="bg-gray-100"],
        html.dark [class*="bg-gray-200"] {
            background-color: #18191c !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-800, 
        html.dark .text-gray-700, 
        html.dark .text-gray-600 {
            color: #e4e4e7 !important;
        }
        html.dark .text-gray-500 {
            color: #a1a1aa !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
        html.dark input, 
        html.dark select, 
        html.dark textarea {
            background-color: #18191c !important;
            color: #ffffff !important;
            border-color: #27272a !important;
        }
        html.dark .review-card, 
        html.dark .kpi-card {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #ffffff !important;
        }
        html.dark .kpi-card .rounded-full.blur-xl,
        html.dark .kpi-card .rounded-full,
        html.dark .review-card .rounded-bl-full {
            display: none !important;
        }
        html.dark .review-card p {
            color: #e4e4e7 !important;
        }
        html.dark .review-card [class*="bg-orange-50"],
        html.dark .review-card [class*="bg-gray-50"],
        html.dark .review-card .bg-white {
            background-color: #121315 !important;
            border-color: #27272a !important;
            color: #d4d4d8 !important;
        }
        html.dark .task-row:hover {
            background-color: #1c1d21 !important;
        }
        html.dark header, 
        html.dark .sticky {
            background-color: rgba(9, 9, 11, 0.85) !important;
            border-color: #27272a !important;
        }
        html.dark aside {
            background-color: #0c0d0f !important;
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="h-full font-body text-gray-800">
    <div class="app-shell w-full">

        <aside id="sidebar" class="w-64 flex-shrink-0 flex-col transition-all bg-sidebar hidden md:flex z-20 shadow-xl">

                        <!-- LOGO PADRONIZADA REACTION BY FRAME [IA] -->
            <div class="p-6 flex items-center gap-3">
                <a href="/app" class="flex items-center gap-2">
                    <img src="/static/logo_reaction_transparent_dark.png?v=0.2.0" alt="ReAction Logo" class="h-12 md:h-14 w-auto object-contain transition-all">
                </a>
            </div>

            <nav class="flex-1 mt-6 px-4 space-y-1.5" role="navigation">
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/app'">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/reputacao'">
                    <i data-lucide="message-square" class="w-5 h-5"></i> Reputação
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/minhas_acoes'">
                    <i data-lucide="list-checks" class="w-5 h-5"></i> Minhas Ações
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/relatorios'">
                    <i data-lucide="pie-chart" class="w-5 h-5"></i> Relatórios
                </button>
                <button
                    class="nav-item active w-full flex items-center gap-3 px-4 py-3 rounded-xl text-white text-sm font-semibold shadow-sm shadow-brand/10"
                    onclick="window.location.href='/sala_maquinas'">
                    <i data-lucide="settings" class="w-5 h-5"></i> Sala de Máquinas
                </button>
                <button
                    class="nav-item w-full flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-gray-200 text-sm font-medium"
                    onclick="window.location.href='/integracoes'">
                    <i data-lucide="link" class="w-5 h-5"></i> Integrações
                </button>
            </nav>

            <div class="p-5 border-t border-gray-700/50 flex items-center gap-3 bg-black/10 m-3 rounded-2xl">
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-inner overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
            {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div class="text-sm text-gray-300 truncate flex-1 font-medium" id="sidebar-name" title="{{ usuario.nome }}">
            {{ usuario.nome }}
        </div>
        <a href="/ajustes" class="text-gray-400 hover:text-gray-200 transition-colors p-1" title="Ajustes">
            <i data-lucide="settings" class="w-4 h-4"></i>
        </a>
        <button type="button" onclick="toggleTheme()" class="text-gray-400 hover:text-amber-400 transition-colors p-1" title="Alternar Modo Escuro/Claro">
            <i data-lucide="moon" class="w-4 h-4"></i>
        </button>
        <a href="/logout" class="text-gray-400 hover:text-red-400 transition-colors p-1" title="Sair">
            <i data-lucide="log-out" class="w-4 h-4"></i>
        </a>
    </div>
    </aside>

        <div class="main-scroll flex flex-col relative" id="main-area">

            <!-- LOGO MOBILE PADRONIZADA COM A LANDING PAGE -->
            <div class="md:hidden flex items-center justify-between px-5 py-3.5 bg-white/85 dark:bg-[#141517]/85 backdrop-blur-xl sticky top-0 z-30 border-b border-gray-200/70 dark:border-white/10 transition-colors">
        <!-- LOGO MOBILE OFICIAL REACTION BY FRAME [IA] (FUNDO CLARO) -->
        <a href="/app" class="flex items-center gap-2">
          <img src="/static/logo_reaction_transparent_light.png?v=0.2.0" alt="ReAction Logo" class="h-10 w-auto object-contain transition-all">
        </a>
        <div class="w-9 h-9 rounded-full flex items-center justify-center text-white text-xs font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
      </div>

            <header
                class="flex flex-col sm:flex-row sm:items-end justify-between px-6 md:px-10 py-8 bg-transparent shrink-0 gap-4">
                <div>
                    <h1 class="font-heading font-bold text-3xl text-gray-900 tracking-tight">Calibragem da IA</h1>
                    <p class="text-sm text-gray-500 mt-2 font-medium">Defina as regras de ouro e o tom de voz para o
                        agente de reputação atuar.</p>
                </div>
            </header>

            <main class="px-6 md:px-10 pb-28 md:pb-10 flex-1 flex flex-col max-w-4xl mx-auto w-full">

                <div
                    class="bg-white rounded-3xl border border-gray-200/60 p-8 md:p-10 shadow-sm transition-all hover:shadow-md">

                    <form id="form-ia" class="space-y-8">

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="phone" class="w-4 h-4 text-brand"></i> WhatsApp de Contenção <span
                                    class="text-red-500">*</span>
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Para onde a IA deve direcionar as crises e
                                clientes insatisfeitos?</p>

                            <div
                                class="flex items-center shadow-sm rounded-xl border border-gray-200 bg-gray-50 transition-all focus-within:border-brand focus-within:ring-2 focus-within:ring-brand/40 focus-within:bg-white group overflow-hidden">
                                <span
                                    class="pl-4 pr-2 py-3.5 text-gray-400 text-sm font-semibold tracking-wide">wa.me/</span>
                                <input type="tel" id="inp-whatsapp" placeholder="5585999999999" required
                                    class="flex-1 pr-4 py-3.5 text-sm bg-transparent focus:outline-none font-medium text-gray-800 placeholder-gray-300">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="mic" class="w-4 h-4 text-brand"></i> Tom de Voz da Marca
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Como a IA deve comunicar nas respostas
                                automáticas?</p>
                            <select id="inp-tom"
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white cursor-pointer shadow-sm font-medium text-gray-800">
                                <option value="Profissional e empático">Profissional e Empático (Recomendado)</option>
                                <option value="Descontraído, jovem e amigável">Descontraído e Jovem</option>
                                <option value="Formal, técnico e direto">Formal e Direto</option>
                                <option value="Divertido e cheio de emojis">Divertido (com Emojis)</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-800 mb-1.5 flex items-center gap-2">
                                <i data-lucide="book-open" class="w-4 h-4 text-brand"></i> Regras de Ouro
                            </label>
                            <p class="text-xs text-gray-400 mb-3 font-medium">Instruções que a IA nunca pode quebrar
                                (horários, políticas de devolução, saudações específicas).</p>
                            <textarea id="inp-regras" rows="5"
                                placeholder="Ex: Nunca prometa reembolsos. O horário de atendimento é das 8h às 18h. Chame os clientes de 'Tripulação'."
                                class="w-full border border-gray-200 rounded-xl px-4 py-3.5 text-sm focus:outline-none focus:border-brand focus:ring-2 focus:ring-brand/40 transition-all bg-gray-50 focus:bg-white resize-none shadow-sm font-medium text-gray-800 leading-relaxed placeholder-gray-300"></textarea>
                        </div>

                        <hr class="border-gray-100 my-8">

                        <div class="flex justify-end pt-2">
                            <button type="submit" id="btn-salvar"
                                class="w-full sm:w-auto px-8 py-3.5 rounded-xl text-white text-sm font-bold flex items-center justify-center gap-2 bg-brand hover:brightness-110 active:scale-[0.98] transition-all shadow-md shadow-brand/20">
                                <i data-lucide="save" class="w-4 h-4"></i> Guardar Calibragem
                            </button>
                        </div>

                    </form>
                </div>
            </main>
        </div>
    </div>

        <!-- DOCK FLUTUANTE MOBILE SIMÉTRICO E CENTRALIZADO (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div class="fixed bottom-4 inset-x-0 z-40 md:hidden flex justify-center items-center px-4 pointer-events-none pb-safe">
    <nav class="pointer-events-auto max-w-[360px] w-full mx-auto grid grid-cols-4 gap-1 p-1.5 rounded-full bg-white/95 dark:bg-[#16171a]/95 backdrop-blur-2xl border border-gray-200/80 dark:border-white/10 shadow-[0_12px_32px_rgba(0,0,0,0.16)] dark:shadow-[0_12px_32px_rgba(0,0,0,0.7)] transition-all duration-300">
      <a href="/app" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Cockpit</span>
      </a>
      <a href="/reputacao" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="message-square" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Reputação</span>
      </a>
      <a href="/minhas_acoes" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full text-gray-500 dark:text-gray-400 hover:text-brand dark:hover:text-brand transition-all duration-200 hover:bg-gray-100/50 dark:hover:bg-white/5">
        <i data-lucide="list-checks" class="w-4 h-4"></i>
        <span class="text-[10px] font-semibold tracking-tight mt-0.5">Ações</span>
      </a>
      <button type="button" onclick="openMobileMenuDrawer()" class="flex flex-col items-center justify-center py-1.5 px-1 rounded-full bg-brand text-white shadow-sm shadow-brand/30 transition-all duration-200">
        <i data-lucide="menu" class="w-4 h-4"></i>
        <span class="text-[10px] font-bold tracking-tight mt-0.5">Menu</span>
      </button>
    </nav>
  </div>

  <!-- DRAWER / MENU MOBILE SLIDE-UP (ESTILO NETFLIX & GOOGLE FOTOS) -->
  <div id="mobile-menu-drawer-backdrop" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm transition-opacity duration-300 hidden opacity-0" onclick="closeMobileMenuDrawer()"></div>
  <div id="mobile-menu-drawer" class="fixed inset-x-0 bottom-0 z-50 transform translate-y-full transition-transform duration-300 ease-out bg-white dark:bg-[#141517] rounded-t-3xl border-t border-gray-200/80 dark:border-white/10 shadow-2xl p-6 pb-safe max-w-lg mx-auto">
    <div class="w-12 h-1.5 bg-gray-300 dark:bg-gray-700 rounded-full mx-auto mb-5"></div>
    <div class="flex items-center justify-between pb-4 border-b border-gray-100 dark:border-gray-800">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full flex items-center justify-center text-white text-sm font-bold bg-brand shadow-sm overflow-hidden cursor-pointer" onclick="window.location.href='/ajustes'">
          {% if usuario.foto_perfil %}<img src="{{ usuario.foto_perfil }}" alt="{{ usuario.nome }}" class="w-full h-full object-cover">{% else %}{{ usuario.iniciais }}{% endif %}
        </div>
        <div>
          <p class="font-heading font-bold text-sm text-gray-900 dark:text-white">{{ usuario.nome }}</p>
          <p class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[200px]">{{ usuario.email }}</p>
        </div>
      </div>
      <button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex flex-col gap-2">
      <div class="flex items-center justify-between gap-3">
        <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
          <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
          <span>Modo Escuro/Claro</span>
        </button>
        <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
          <i data-lucide="log-out" class="w-4 h-4"></i>
          <span>Sair</span>
        </a>
      </div>
      <div class="flex items-center justify-center gap-4 pt-2 text-[11px] text-gray-400 dark:text-gray-500">
        <a href="/termos" target="_blank" class="hover:text-brand">Termos de Uso</a>
        <span>•</span>
        <a href="/privacidade" target="_blank" class="hover:text-brand">Privacidade & LGPD</a>
      </div>
    </div>
  </div><button type="button" onclick="closeMobileMenuDrawer()" class="p-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 rounded-full hover:bg-gray-100 dark:hover:bg-white/5 transition-colors">
        <i data-lucide="x" class="w-5 h-5"></i>
      </button>
    </div>
    <div class="grid grid-cols-2 gap-3 py-4">
      <a href="/relatorios" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-orange-500/10 text-brand"><i data-lucide="pie-chart" class="w-4 h-4"></i></div>
        <span>Relatórios</span>
      </a>
      <a href="/sala_maquinas" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-blue-500/10 text-blue-500"><i data-lucide="settings" class="w-4 h-4"></i></div>
        <span>Sala de Máquinas</span>
      </a>
      <a href="/integracoes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-emerald-500/10 text-emerald-500"><i data-lucide="link" class="w-4 h-4"></i></div>
        <span>Canais & APIs</span>
      </a>
      <a href="/ajustes" class="flex items-center gap-3 p-3 rounded-2xl bg-gray-50 dark:bg-[#1c1d21] border border-gray-200/60 dark:border-gray-800 hover:border-brand/40 text-gray-800 dark:text-gray-200 text-xs font-semibold transition-all">
        <div class="p-2 rounded-xl bg-purple-500/10 text-purple-500"><i data-lucide="user-cog" class="w-4 h-4"></i></div>
        <span>Meus Ajustes</span>
      </a>
    </div>
    <div class="pt-2 border-t border-gray-100 dark:border-gray-800 flex items-center justify-between gap-3">
      <button type="button" onclick="toggleTheme()" class="flex-1 flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-gray-100 dark:bg-[#1f2024] text-xs font-semibold text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
        <i data-lucide="moon" class="w-4 h-4 text-amber-500"></i>
        <span>Modo Escuro/Claro</span>
      </button>
      <a href="/logout" class="flex items-center justify-center gap-2 py-2.5 px-4 rounded-xl bg-red-50 dark:bg-red-500/10 text-xs font-semibold text-red-600 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-500/20 transition-colors">
        <i data-lucide="log-out" class="w-4 h-4"></i>
        <span>Sair</span>
      </a>
    </div>
  </div>

    <div id="toast-container"></div>

    <script>
        lucide.createIcons();

        function escapeHTML(str) {
            if (!str) return '';
            return str.toString().replace(/[&<>'"]/g, tag => ({
                '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
            }[tag] || tag));
        }

        // Toast atualizado para suportar HTML (ícones)
        function toast(msg) {
            const el = document.createElement('div');
            el.className = 'toast-msg';
            el.innerHTML = msg;
            document.getElementById('toast-container').appendChild(el);
            setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.remove(), 300); }, 3000);
        }

        async function carregarConfiguracoes() {
            try {
                const res = await fetch('/api/configuracoes');
                if (res.ok) {
                    const data = await res.json();
                    document.getElementById('inp-tom').value = escapeHTML(data.tom_voz) || 'Profissional e empático';
                    document.getElementById('inp-regras').value = escapeHTML(data.regras_ouro) || '';
                    document.getElementById('inp-whatsapp').value = escapeHTML(data.telefone_whatsapp) || '';
                }
            } catch (err) {
                console.error("Erro ao carregar configurações:", err);
            }
        }

        document.getElementById('form-ia').onsubmit = async (e) => {
            e.preventDefault();
            const btn = document.getElementById('btn-salvar');
            const originalText = btn.innerHTML;

            btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Guardando...';
            btn.disabled = true;
            btn.classList.add('opacity-80', 'cursor-not-allowed');
            lucide.createIcons();

            const payload = {
                tom_voz: document.getElementById('inp-tom').value.trim(),
                regras_ouro: document.getElementById('inp-regras').value.trim(),
                telefone_whatsapp: document.getElementById('inp-whatsapp').value.trim()
            };

            try {
                const res = await fetch('/api/configuracoes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (res.ok) {
                    toast('<i data-lucide="check-circle-2" class="w-4 h-4 inline-block mr-1 align-text-bottom"></i> Motor da IA calibrado com sucesso!');
                } else {
                    toast('<i data-lucide="alert-circle" class="w-4 h-4 inline-block mr-1 align-text-bottom text-red-400"></i> Erro ao guardar as configurações.');
                }
            } catch (err) {
                toast('<i data-lucide="wifi-off" class="w-4 h-4 inline-block mr-1 align-text-bottom text-brand"></i> Falha de comunicação com o servidor.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
                btn.classList.remove('opacity-80', 'cursor-not-allowed');
                lucide.createIcons();
            }
        };

        // Inicia carregando os dados se o backend estiver rodando
        carregarConfiguracoes();
    </script>

{% if usuario.termos_pendentes %}
<!-- MODAL DE ACEITE OBRIGATÓRIO DE TERMOS E PRIVACIDADE (LGPD) -->
<div id="modal-termos-obrigatorio" class="fixed inset-0 z-[100] bg-black/80 backdrop-blur-md flex items-center justify-center p-4">
    <div class="bg-white dark:bg-[#141517] rounded-3xl p-6 sm:p-8 max-w-md w-full border border-gray-200 dark:border-gray-800 shadow-2xl space-y-5 text-center">
        
        <!-- LOGO OFICIAL REACTION COM ESCUDO VIBRANTE -->
        <div class="flex justify-center items-center">
            <div class="w-16 h-16 rounded-2xl bg-gradient-to-br from-[#ff6b35] to-[#e05524] p-3 shadow-lg shadow-brand/30 flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" class="w-10 h-10 text-white fill-none stroke-current" stroke-width="36" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M256 426.667s149.333-74.667 149.333-186.667V128L256 74.667 106.667 128v112c0 112 149.333 186.667 149.333 186.667z" />
                    <path d="M200 256l40 40 80-80" />
                </svg>
            </div>
        </div>

        <div>
            <h3 class="font-heading font-bold text-xl text-gray-900 dark:text-white">Atualização dos Termos & LGPD</h3>
            <p class="text-xs sm:text-sm text-gray-600 dark:text-gray-400 mt-2 leading-relaxed">
                Atualizamos nossos <a href="/termos" target="_blank" class="text-brand font-bold underline">Termos de Uso</a> e <a href="/privacidade" target="_blank" class="text-brand font-bold underline">Política de Privacidade</a> com os dados da holding e da <strong>Frame [IA]</strong>. Para continuar utilizando o ReAction, confirme a sua concordância.
            </p>
        </div>

        <div class="p-4 rounded-2xl bg-gray-50 dark:bg-[#18191c] border border-gray-200/60 dark:border-gray-800 text-left text-xs text-gray-600 dark:text-gray-400 space-y-1.5">
            <p><strong>Holding Controladora:</strong> MJSV TECNOLOGIA E SOLUCOES DIGITAIS (I.S.)</p>
            <p><strong>Operação & IA:</strong> Frame [IA] (Divisão de Inteligência Artificial)</p>
            <p><strong>CNPJ:</strong> 68.614.850/0001-13</p>
            <p><strong>Versão:</strong> 0.2.0 (16/08/2026)</p>
        </div>

        <button type="button" id="btn-aceitar-termos" onclick="confirmarAceiteTermos()" class="w-full py-3.5 rounded-xl bg-brand text-white font-bold text-sm hover:brightness-110 shadow-lg shadow-brand/25 transition-all flex items-center justify-center gap-2">
            <span>Li e Concordo com os Termos</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M5 13l4 4L19 7"></path></svg>
        </button>
    </div>
</div>
<script>
function confirmarAceiteTermos() {
    const btn = document.getElementById('btn-aceitar-termos');
    if (btn) { btn.disabled = true; btn.innerHTML = '<span>Gravando consentimento...</span>'; }
    fetch('/api/aceitar_termos', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'}
    })
    .then(r => r.json())
    .then(d => {
        if (d.success) {
            const modal = document.getElementById('modal-termos-obrigatorio');
            if (modal) modal.remove();
        } else {
            alert('Erro ao gravar aceite: ' + (d.error || 'Tente novamente'));
            if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
        }
    })
    .catch(err => {
        console.error(err);
        alert('Erro ao conectar ao servidor.');
        if (btn) { btn.disabled = false; btn.innerHTML = '<span>Tentar Novamente</span>'; }
    });
}
</script>
{% endif %}

</body>

</html>
================================================================================
FILE: templates\termos.html
================================================================================
<!doctype html>
<html lang="pt-BR" class="h-full">

<head>
    <script>
        (function() {
            var currentTheme = localStorage.getItem('reactionTheme');
            if (currentTheme === 'dark') {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark');
            }
        })();
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Termos de Uso e Serviço | ReAction by Frame [IA]</title>
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg?v=0.2.0">
    <link rel="shortcut icon" href="/static/favicon.ico?v=0.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/static/favicon.png?v=0.2.0">
    <link rel="apple-touch-icon" sizes="180x180" href="/static/apple-touch-icon.png?v=0.2.0">
    <script src="https://cdn.tailwindcss.com/3.4.17"></script>
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&family=DM+Sans:wght@400;500;600&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        heading: ['Poppins', 'sans-serif'],
                        body: ['DM Sans', 'sans-serif']
                    },
                    colors: {
                        brand: '#ff6b35',
                        sidebar: '#3a3a3a'
                    }
                }
            }
        }
    </script>
    <style>
        html.dark, html.dark body {
            background-color: #09090b !important;
            color: #f4f4f5 !important;
        }
        html.dark .bg-white {
            background-color: #141517 !important;
            border-color: #27272a !important;
            color: #f4f4f5 !important;
        }
        html.dark .text-gray-900 {
            color: #ffffff !important;
        }
        html.dark .text-gray-700, html.dark .text-gray-600 {
            color: #d4d4d8 !important;
        }
        html.dark [class*="border-gray-"] {
            border-color: #27272a !important;
        }
    </style>
</head>

<body class="min-h-full font-body text-gray-800 bg-gray-50 antialiased flex flex-col">
    <!-- Top Header -->
    <header class="bg-white border-b border-gray-200 sticky top-0 z-30 shadow-sm transition-colors">
        <div class="max-w-5xl mx-auto px-4 sm:px-6 py-4 flex items-center justify-between">
            <a href="/" class="flex items-center gap-2">
                <img src="/static/logo_reaction_transparent_light.png?v=0.2.0" alt="ReAction Logo" class="h-10 w-auto object-contain dark:hidden">
                <img src="/static/logo_reaction_transparent_dark.png?v=0.2.0" alt="ReAction Logo" class="h-10 w-auto object-contain hidden dark:block">
            </a>
            <div class="flex items-center gap-3">
                <a href="/login" class="px-4 py-2 text-xs sm:text-sm font-semibold rounded-xl bg-gray-100 hover:bg-gray-200 dark:bg-[#1f2024] dark:hover:bg-gray-700 text-gray-700 dark:text-gray-300 transition-colors">
                    Entrar no App
                </a>
                <a href="/" class="px-4 py-2 text-xs sm:text-sm font-semibold rounded-xl bg-brand text-white hover:brightness-110 shadow-sm shadow-brand/20 transition-all flex items-center gap-1.5">
                    <i data-lucide="arrow-left" class="w-4 h-4"></i> Voltar ao Início
                </a>
            </div>
        </div>
    </header>

    <!-- Main Legal Content -->
    <main class="flex-1 max-w-4xl w-full mx-auto px-4 sm:px-6 py-10 sm:py-14">
        <div class="bg-white rounded-3xl p-6 sm:p-10 border border-gray-200/80 shadow-sm space-y-8">
            <div class="border-b border-gray-100 dark:border-gray-800 pb-6">
                <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-orange-50 dark:bg-orange-500/10 text-brand text-xs font-bold uppercase tracking-wider mb-3">
                    <i data-lucide="shield-check" class="w-4 h-4"></i> Documento Oficial
                </div>
                <h1 class="font-heading font-bold text-2xl sm:text-3xl text-gray-900 tracking-tight">Termos e Condições de Uso</h1>
                <p class="text-sm text-gray-500 dark:text-gray-400 mt-2">Versão oficial 0.2.0 • Atualizada em 16 de Agosto de 2026</p>
            </div>

                        <!-- Dados da Empresa e Holding / Frame [IA] -->
            <div class="p-5 rounded-2xl bg-gray-50 dark:bg-[#18191c] border border-gray-200/60 dark:border-gray-800 space-y-3">
                <div class="flex items-center justify-between">
                    <h3 class="font-heading font-bold text-sm text-gray-900 dark:text-white uppercase tracking-wider">Estrutura Corporativa & Titularidade</h3>
                    <span class="text-[11px] font-bold px-2.5 py-0.5 rounded-full bg-brand/10 text-brand">Holding & Divisão de IA</span>
                </div>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 text-xs text-gray-600 dark:text-gray-400">
                    <p><strong>Holding Controladora:</strong> MJSV TECNOLOGIA E SOLUCOES DIGITAIS INOVA SIMPLES (I.S.)</p>
                    <p><strong>Divisão de Tecnologia & IA:</strong> Frame [IA]</p>
                    <p><strong>CNPJ:</strong> 68.614.850/0001-13</p>
                    <p><strong>Natureza Jurídica:</strong> Empresa Simples de Inovação (234-8)</p>
                    <p><strong>Sede:</strong> Eusébio - CE, Brasil (Região Metropolitana de Fortaleza)</p>
                    <p><strong>E-mails de Contato:</strong> contato@frameia.com.br / jadson@mjsv.com.br</p>
                    <p><strong>Telefone Comercial:</strong> (85) 9662-4278 / (85) 8731-8995</p>
                    <p><strong>Plataforma SaaS:</strong> ReAction by Frame [IA]</p>
                </div>
                <p class="text-[11px] text-gray-500 dark:text-gray-400 border-t border-gray-200/60 dark:border-gray-800 pt-2 leading-relaxed">
                    <em>Nota Institucional:</em> A <strong>MJSV TECNOLOGIA</strong> é a entidade jurídica controladora (holding), titular do CNPJ oficial, enquanto a <strong>Frame [IA]</strong> constitui o braço operacional de tecnologia e inteligência artificial que concebeu, desenvolve e mantém a plataforma <strong>ReAction</strong>.
                </p>
            </div>

            <!-- Seções dos Termos -->
            <section class="space-y-4 text-sm leading-relaxed text-gray-700 dark:text-gray-300">
                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">1. Objeto e Aceitação dos Termos</h2>
                <p>
                    O presente instrumento regula o licenciamento de software sob a modalidade SaaS (Software as a Service) da plataforma <strong>ReAction</strong>, desenvolvida e operada pela <strong>Frame [IA]</strong> (vertical de inteligência artificial da <strong>MJSV TECNOLOGIA E SOLUCOES DIGITAIS INOVA SIMPLES (I.S.)</strong>). Ao cadastrar-se, acessar ou utilizar qualquer funcionalidade do ReAction, você (doravante "Usuário" ou "Contratante") declara ter lido, compreendido e concordado integralmente com estes Termos de Uso e com a nossa Política de Privacidade.
                </p>

                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">2. Serviços Prestados e Inteligência Artificial</h2>
                <p>
                    O <strong>ReAction</strong> é uma solução tecnológica voltada à gestão de reputação online, centralização de avaliações (reviews de Google Meu Negócio, Instagram, iFood, 99Food, WhatsApp e Zé Delivery), geração automatizada de respostas com IA, radar de prazos e contenção de crises em tempo real. O Usuário reconhece que as respostas geradas por IA são sugestões inteligentes calibradas e que a responsabilidade final pelo atendimento e posicionamento institucional perante seus clientes é exclusiva do Usuário/Empresa titular.
                </p>

                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">3. Cadastro, Senhas e Acesso Seguro</h2>
                <p>
                    O Usuário é responsável por manter a confidencialidade de suas credenciais de acesso (e-mail e senha). Qualquer atividade realizada com sua conta será de sua exclusiva responsabilidade. É expressamente proibido compartilhar credenciais, tentar burlar autenticações OAuth, realizar engenharia reversa ou acessar dados de outros inquilinos (tenants).
                </p>

                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">4. Planos, Pagamentos e Cancelamento</h2>
                <p>
                    O acesso aos recursos do ReAction é fornecido mediante assinatura nos planos contratados (Básico, Pro ou Enterprise) ou serviços consultivos (Setup VIP). Os pagamentos são processados com segurança via gateways certificados (Mercado Pago). O Usuário pode solicitar o cancelamento de sua assinatura a qualquer momento através do painel de ajustes ou pelo canal oficial de suporte, mantendo o acesso até o término do ciclo já pago.
                </p>

                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">5. Integração com WhatsApp e Canais Terceiros</h2>
                <p>
                    O ReAction oferece integração via API oficial da Meta (WhatsApp Business Cloud API) e conectores de terceiros. O Usuário autoriza o envio de alertas operacionais, notificações de crises e comandos remotos para o número de WhatsApp vinculado em seu perfil corporativo. O Usuário compromete-se a utilizar o serviço em conformidade com as Políticas Comerciais da Meta.
                </p>

                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">6. Disponibilidade (SLA) e Manutenção</h2>
                <p>
                    A MJSV Tecnologia emprega os mais altos padrões de segurança em nuvem e infraestrutura para garantir disponibilidade contínua da plataforma. Manutenções programadas serão comunicadas previamente aos clientes sempre que possível.
                </p>

                <h2 class="font-heading font-bold text-lg text-gray-900 dark:text-white">7. Foro e Legislação Aplicável</h2>
                <p>
                    Estes Termos são regidos pelas leis da República Federativa do Brasil. Para dirimir quaisquer controvérsias oriundas do presente contrato, fica eleito o Foro da Comarca de Eusébio/CE, com renúncia expressa a qualquer outro, por mais privilegiado que seja.
                </p>
            </section>
        </div>
    </main>

    <footer class="bg-white dark:bg-[#0c0d0f] border-t border-gray-200 dark:border-gray-800 py-6 text-center text-xs text-gray-500 transition-colors">
        <div class="max-w-5xl mx-auto px-4 flex flex-col sm:flex-row items-center justify-between gap-3">
            <p>© 2026 MJSV TECNOLOGIA E SOLUCOES DIGITAIS INOVA SIMPLES (I.S.) • CNPJ: 68.614.850/0001-13</p>
            <div class="flex items-center gap-4">
                <a href="/termos" class="hover:text-brand font-bold text-brand">Termos de Uso</a>
                <a href="/privacidade" class="hover:text-brand">Privacidade & LGPD</a>
            </div>
        </div>
    </footer>

    <script>
        lucide.createIcons();
    </script>
</body>
</html>
